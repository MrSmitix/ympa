#' Партнерский API Маркета
#'
#' API Яндекс Маркета помогает продавцам автоматизировать и упростить работу с маркетплейсом.  В числе возможностей интеграции:  * управление каталогом товаров и витриной,  * обработка заказов,  * изменение настроек магазина,  * получение отчетов. 
#'
#' The version of the OpenAPI document: LATEST
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title Stocks operations
#' @description StocksApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @section Methods:
#' \describe{
#' \strong{ GetStocks } \emph{ Информация об остатках и оборачиваемости }
#' Возвращает данные об остатках товаров (для всех моделей) и об [оборачиваемости](*turnover) товаров (для модели FBY).  {\% note info \"По умолчанию данные по оборачивамости не возращаются\" \%}  Чтобы они были в ответе, передавайте `true` в поле `withTurnover`.  {\% endnote \%}  |**⚙️ Лимит:** 100 000 товаров в минуту| |-|  [//]: <> (turnover: Среднее количество дней, за которое товар продается. Подробно об оборачиваемости рассказано в Справке Маркета для продавцов https://yandex.ru/support/marketplace/analytics/turnover.html.)
#'
#' \itemize{
#' \item \emph{ @param } campaign_id integer
#' \item \emph{ @param } page_token character
#' \item \emph{ @param } limit integer
#' \item \emph{ @param } get_warehouse_stocks_request \link{GetWarehouseStocksRequest}
#' \item \emph{ @returnType } \link{GetWarehouseStocksResponse} \cr
#'
#'
#' \item status code : 200 | Остатки товаров на складах.
#'
#' \item return type : GetWarehouseStocksResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 400 | Запрос содержит неправильные данные.
#'
#' \item return type : ApiClientDataErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 401 | В запросе не указаны данные для авторизации.
#'
#' \item return type : ApiUnauthorizedErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 403 | Данные для авторизации неверны или доступ к ресурсу запрещен.
#'
#' \item return type : ApiForbiddenErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 420 | Превышено ограничение на доступ к ресурсу.
#'
#' \item return type : ApiLimitErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 500 | Внутренняя ошибка сервера.
#'
#' \item return type : ApiServerErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ UpdateStocks } \emph{ Передача информации об остатках }
#' Передает данные об остатках товаров на витрине.  Обязательно указывайте SKU **в точности** так, как он указан в каталоге. Например, _557722_ и _0557722_ — это два разных SKU.  {\% note info \"Данные в каталоге обновляются не мгновенно\" \%}  Это занимает до нескольких минут.  {\% endnote \%}  |**⚙️ Лимит:** 100 000 товаров в минуту| |-|
#'
#' \itemize{
#' \item \emph{ @param } campaign_id integer
#' \item \emph{ @param } update_stocks_request \link{UpdateStocksRequest}
#' \item \emph{ @returnType } \link{EmptyApiResponse} \cr
#'
#'
#' \item status code : 200 | Пустой ответ.
#'
#' \item return type : EmptyApiResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 400 | Запрос содержит неправильные данные.
#'
#' \item return type : ApiClientDataErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 401 | В запросе не указаны данные для авторизации.
#'
#' \item return type : ApiUnauthorizedErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 403 | Данные для авторизации неверны или доступ к ресурсу запрещен.
#'
#' \item return type : ApiForbiddenErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 404 | Запрашиваемый ресурс не найден.
#'
#' \item return type : ApiNotFoundErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 420 | Превышено ограничение на доступ к ресурсу.
#'
#' \item return type : ApiLimitErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 500 | Внутренняя ошибка сервера.
#'
#' \item return type : ApiServerErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' }
#'
#'
#' @examples
#' \dontrun{
#' ####################  GetStocks  ####################
#'
#' library(ympa_r_client)
#' var_campaign_id <- 56 # integer | Идентификатор кампании в API и магазина в кабинете. Каждая кампания в API соответствует магазину в кабинете.  Чтобы узнать идентификаторы своих магазинов, воспользуйтесь запросом [GET campaigns](../../reference/campaigns/getCampaigns.md).  ℹ️ [Что такое кабинет и магазин на Маркете](https://yandex.ru/support/marketplace/account/introduction.html) 
#' var_page_token <- "eyBuZXh0SWQ6IDIzNDIgfQ==" # character | Идентификатор страницы c результатами.  Если параметр не указан, возвращается первая страница.  Рекомендуется передавать значение выходного параметра `nextPageToken`, полученное при последнем запросе.  Если задан `page_token` и в запросе есть параметры `offset`, `page_number` и `page_size`, они игнорируются.  (Optional)
#' var_limit <- 20 # integer | Количество значений на одной странице.  (Optional)
#' var_get_warehouse_stocks_request <- GetWarehouseStocksRequest$new("withTurnover_example", "archived_example", c("offerIds_example")) # GetWarehouseStocksRequest |  (Optional)
#'
#' #Информация об остатках и оборачиваемости
#' api_instance <- StocksApi$new()
#'
#' # Configure OAuth2 access token for authorization: OAuth
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetStocks(var_campaign_id, page_token = var_page_token, limit = var_limit, get_warehouse_stocks_request = var_get_warehouse_stocks_requestdata_file = "result.txt")
#' result <- api_instance$GetStocks(var_campaign_id, page_token = var_page_token, limit = var_limit, get_warehouse_stocks_request = var_get_warehouse_stocks_request)
#' dput(result)
#'
#'
#' ####################  UpdateStocks  ####################
#'
#' library(ympa_r_client)
#' var_campaign_id <- 56 # integer | Идентификатор кампании в API и магазина в кабинете. Каждая кампания в API соответствует магазину в кабинете.  Чтобы узнать идентификаторы своих магазинов, воспользуйтесь запросом [GET campaigns](../../reference/campaigns/getCampaigns.md).  ℹ️ [Что такое кабинет и магазин на Маркете](https://yandex.ru/support/marketplace/account/introduction.html) 
#' var_update_stocks_request <- UpdateStocksRequest$new(c(UpdateStockDTO$new("sku_example", c(UpdateStockItemDTO$new(123, "updatedAt_example"))))) # UpdateStocksRequest | 
#'
#' #Передача информации об остатках
#' api_instance <- StocksApi$new()
#'
#' # Configure OAuth2 access token for authorization: OAuth
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$UpdateStocks(var_campaign_id, var_update_stocks_requestdata_file = "result.txt")
#' result <- api_instance$UpdateStocks(var_campaign_id, var_update_stocks_request)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
StocksApi <- R6::R6Class(
  "StocksApi",
  public = list(
    api_client = NULL,
    #' Initialize a new StocksApi.
    #'
    #' @description
    #' Initialize a new StocksApi.
    #'
    #' @param api_client An instance of API client.
    #' @export
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },
    #' Информация об остатках и оборачиваемости
    #'
    #' @description
    #' Информация об остатках и оборачиваемости
    #'
    #' @param campaign_id Идентификатор кампании в API и магазина в кабинете. Каждая кампания в API соответствует магазину в кабинете.  Чтобы узнать идентификаторы своих магазинов, воспользуйтесь запросом [GET campaigns](../../reference/campaigns/getCampaigns.md).  ℹ️ [Что такое кабинет и магазин на Маркете](https://yandex.ru/support/marketplace/account/introduction.html) 
    #' @param page_token (optional) Идентификатор страницы c результатами.  Если параметр не указан, возвращается первая страница.  Рекомендуется передавать значение выходного параметра `nextPageToken`, полученное при последнем запросе.  Если задан `page_token` и в запросе есть параметры `offset`, `page_number` и `page_size`, они игнорируются. 
    #' @param limit (optional) Количество значений на одной странице. 
    #' @param get_warehouse_stocks_request (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return GetWarehouseStocksResponse
    #' @export
    GetStocks = function(campaign_id, page_token = NULL, limit = NULL, get_warehouse_stocks_request = NULL, data_file = NULL, ...) {
      local_var_response <- self$GetStocksWithHttpInfo(campaign_id, page_token, limit, get_warehouse_stocks_request, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Информация об остатках и оборачиваемости
    #'
    #' @description
    #' Информация об остатках и оборачиваемости
    #'
    #' @param campaign_id Идентификатор кампании в API и магазина в кабинете. Каждая кампания в API соответствует магазину в кабинете.  Чтобы узнать идентификаторы своих магазинов, воспользуйтесь запросом [GET campaigns](../../reference/campaigns/getCampaigns.md).  ℹ️ [Что такое кабинет и магазин на Маркете](https://yandex.ru/support/marketplace/account/introduction.html) 
    #' @param page_token (optional) Идентификатор страницы c результатами.  Если параметр не указан, возвращается первая страница.  Рекомендуется передавать значение выходного параметра `nextPageToken`, полученное при последнем запросе.  Если задан `page_token` и в запросе есть параметры `offset`, `page_number` и `page_size`, они игнорируются. 
    #' @param limit (optional) Количество значений на одной странице. 
    #' @param get_warehouse_stocks_request (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (GetWarehouseStocksResponse) with additional information such as HTTP status code, headers
    #' @export
    GetStocksWithHttpInfo = function(campaign_id, page_token = NULL, limit = NULL, get_warehouse_stocks_request = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`campaign_id`)) {
        stop("Missing required parameter `campaign_id`.")
      }





      query_params[["page_token"]] <- `page_token`

      query_params[["limit"]] <- `limit`

      if (!is.null(`get_warehouse_stocks_request`)) {
        local_var_body <- `get_warehouse_stocks_request`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/campaigns/{campaignId}/offers/stocks"
      if (!missing(`campaign_id`)) {
        local_var_url_path <- gsub("\\{campaignId\\}", URLencode(as.character(`campaign_id`), reserved = TRUE), local_var_url_path)
      }

      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "market:partner-api"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "GetWarehouseStocksResponse", loadNamespace("ympa_r_client")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Передача информации об остатках
    #'
    #' @description
    #' Передача информации об остатках
    #'
    #' @param campaign_id Идентификатор кампании в API и магазина в кабинете. Каждая кампания в API соответствует магазину в кабинете.  Чтобы узнать идентификаторы своих магазинов, воспользуйтесь запросом [GET campaigns](../../reference/campaigns/getCampaigns.md).  ℹ️ [Что такое кабинет и магазин на Маркете](https://yandex.ru/support/marketplace/account/introduction.html) 
    #' @param update_stocks_request 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return EmptyApiResponse
    #' @export
    UpdateStocks = function(campaign_id, update_stocks_request, data_file = NULL, ...) {
      local_var_response <- self$UpdateStocksWithHttpInfo(campaign_id, update_stocks_request, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Передача информации об остатках
    #'
    #' @description
    #' Передача информации об остатках
    #'
    #' @param campaign_id Идентификатор кампании в API и магазина в кабинете. Каждая кампания в API соответствует магазину в кабинете.  Чтобы узнать идентификаторы своих магазинов, воспользуйтесь запросом [GET campaigns](../../reference/campaigns/getCampaigns.md).  ℹ️ [Что такое кабинет и магазин на Маркете](https://yandex.ru/support/marketplace/account/introduction.html) 
    #' @param update_stocks_request 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (EmptyApiResponse) with additional information such as HTTP status code, headers
    #' @export
    UpdateStocksWithHttpInfo = function(campaign_id, update_stocks_request, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`campaign_id`)) {
        stop("Missing required parameter `campaign_id`.")
      }

      if (missing(`update_stocks_request`)) {
        stop("Missing required parameter `update_stocks_request`.")
      }



      if (!is.null(`update_stocks_request`)) {
        local_var_body <- `update_stocks_request`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/campaigns/{campaignId}/offers/stocks"
      if (!missing(`campaign_id`)) {
        local_var_url_path <- gsub("\\{campaignId\\}", URLencode(as.character(`campaign_id`), reserved = TRUE), local_var_url_path)
      }

      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "market:partner-api"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "PUT",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "EmptyApiResponse", loadNamespace("ympa_r_client")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    }
  )
)
