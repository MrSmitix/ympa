#' Партнерский API Маркета
#'
#' API Яндекс Маркета помогает продавцам автоматизировать и упростить работу с маркетплейсом.  В числе возможностей интеграции:  * управление каталогом товаров и витриной,  * обработка заказов,  * изменение настроек магазина,  * получение отчетов. 
#'
#' The version of the OpenAPI document: LATEST
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title OutletLicenses operations
#' @description OutletLicensesApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @section Methods:
#' \describe{
#' \strong{ DeleteOutletLicenses } \emph{ Удаление лицензий для точек продаж }
#' Удаляет информацию о лицензиях для точек продаж.  В течение суток этим и другими запросами о точках продаж, кроме запроса [GET delivery/services](../../reference/orders/getDeliveryServices.md), можно получить и изменить информацию об определенном суммарном количестве точек продаж. Оно зависит от количества точек продаж магазина.  |**⚙️ Лимит:** 100 000 запросов в час| |-|
#'
#' \itemize{
#' \item \emph{ @param } campaign_id integer
#' \item \emph{ @param } ids list( integer )
#' \item \emph{ @returnType } \link{EmptyApiResponse} \cr
#'
#'
#' \item status code : 200 | Пустой ответ.
#'
#' \item return type : EmptyApiResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 400 | Запрос содержит неправильные данные.
#'
#' \item return type : ApiClientDataErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 401 | В запросе не указаны данные для авторизации.
#'
#' \item return type : ApiUnauthorizedErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 403 | Данные для авторизации неверны или доступ к ресурсу запрещен.
#'
#' \item return type : ApiForbiddenErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 404 | Запрашиваемый ресурс не найден.
#'
#' \item return type : ApiNotFoundErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 420 | Превышено ограничение на доступ к ресурсу.
#'
#' \item return type : ApiLimitErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 500 | Внутренняя ошибка сервера.
#'
#' \item return type : ApiServerErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ GetOutletLicenses } \emph{ Информация о лицензиях для точек продаж }
#' Возвращает информацию о лицензиях для точек продаж.  В течение суток этим и другими запросами о точках продаж, кроме запроса [GET delivery/services](../../reference/orders/getDeliveryServices.md), можно получить и изменить информацию об определенном суммарном количестве точек продаж. Оно зависит от количества точек продаж магазина.  |**⚙️ Лимит:** 100 000 запросов в час| |-|
#'
#' \itemize{
#' \item \emph{ @param } campaign_id integer
#' \item \emph{ @param } outlet_ids list( integer )
#' \item \emph{ @param } ids list( integer )
#' \item \emph{ @returnType } \link{GetOutletLicensesResponse} \cr
#'
#'
#' \item status code : 200 | Найденные лицензии собственных точек продаж.
#'
#' \item return type : GetOutletLicensesResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 400 | Запрос содержит неправильные данные.
#'
#' \item return type : ApiClientDataErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 401 | В запросе не указаны данные для авторизации.
#'
#' \item return type : ApiUnauthorizedErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 403 | Данные для авторизации неверны или доступ к ресурсу запрещен.
#'
#' \item return type : ApiForbiddenErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 404 | Запрашиваемый ресурс не найден.
#'
#' \item return type : ApiNotFoundErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 420 | Превышено ограничение на доступ к ресурсу.
#'
#' \item return type : ApiLimitErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 500 | Внутренняя ошибка сервера.
#'
#' \item return type : ApiServerErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ UpdateOutletLicenses } \emph{ Создание и изменение лицензий для точек продаж }
#' Передает информацию о новых и существующих лицензиях для точек продаж. Поддерживаются только лицензии на розничную продажу алкоголя.  Чтобы размещать алкогольную продукцию на Маркете, надо также прислать гарантийное письмо (если вы еще не делали этого раньше) и правильно оформить предложения в прайс-листе. Далее информация о лицензиях проходит проверку.  В течение суток этим и другими запросами о точках продаж, кроме запроса [GET delivery/services](../../reference/orders/getDeliveryServices.md), можно получить и изменить информацию об определенном суммарном количестве точек продаж. Оно зависит от количества точек продаж магазина.  |**⚙️ Лимит:** 100 000 запросов в час| |-|
#'
#' \itemize{
#' \item \emph{ @param } campaign_id integer
#' \item \emph{ @param } update_outlet_license_request \link{UpdateOutletLicenseRequest}
#' \item \emph{ @returnType } \link{EmptyApiResponse} \cr
#'
#'
#' \item status code : 200 | Пустой ответ.
#'
#' \item return type : EmptyApiResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 400 | Запрос содержит неправильные данные.
#'
#' \item return type : ApiClientDataErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 401 | В запросе не указаны данные для авторизации.
#'
#' \item return type : ApiUnauthorizedErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 403 | Данные для авторизации неверны или доступ к ресурсу запрещен.
#'
#' \item return type : ApiForbiddenErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 404 | Запрашиваемый ресурс не найден.
#'
#' \item return type : ApiNotFoundErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 420 | Превышено ограничение на доступ к ресурсу.
#'
#' \item return type : ApiLimitErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 500 | Внутренняя ошибка сервера.
#'
#' \item return type : ApiServerErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' }
#'
#'
#' @examples
#' \dontrun{
#' ####################  DeleteOutletLicenses  ####################
#'
#' library(ympa_r_client)
#' var_campaign_id <- 56 # integer | Идентификатор кампании в API и магазина в кабинете. Каждая кампания в API соответствует магазину в кабинете.  Чтобы узнать идентификаторы своих магазинов, воспользуйтесь запросом [GET campaigns](../../reference/campaigns/getCampaigns.md).  ℹ️ [Что такое кабинет и магазин на Маркете](https://yandex.ru/support/marketplace/account/introduction.html) 
#' var_ids <- c(123) # set[integer] | Список идентификаторов лицензий. (Optional)
#'
#' #Удаление лицензий для точек продаж
#' api_instance <- OutletLicensesApi$new()
#'
#' # Configure OAuth2 access token for authorization: OAuth
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$DeleteOutletLicenses(var_campaign_id, ids = var_idsdata_file = "result.txt")
#' result <- api_instance$DeleteOutletLicenses(var_campaign_id, ids = var_ids)
#' dput(result)
#'
#'
#' ####################  GetOutletLicenses  ####################
#'
#' library(ympa_r_client)
#' var_campaign_id <- 56 # integer | Идентификатор кампании в API и магазина в кабинете. Каждая кампания в API соответствует магазину в кабинете.  Чтобы узнать идентификаторы своих магазинов, воспользуйтесь запросом [GET campaigns](../../reference/campaigns/getCampaigns.md).  ℹ️ [Что такое кабинет и магазин на Маркете](https://yandex.ru/support/marketplace/account/introduction.html) 
#' var_outlet_ids <- c(123) # set[integer] | Список идентификаторов точек продаж, для которых нужно получить информацию о лицензиях. Идентификаторы указываются через запятую.  В запросе должен быть либо параметр `outletIds`, либо параметр `ids`. Запрос с обоими параметрами или без них приведет к ошибке.  (Optional)
#' var_ids <- c(123) # set[integer] | Список идентификаторов лицензий. (Optional)
#'
#' #Информация о лицензиях для точек продаж
#' api_instance <- OutletLicensesApi$new()
#'
#' # Configure OAuth2 access token for authorization: OAuth
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetOutletLicenses(var_campaign_id, outlet_ids = var_outlet_ids, ids = var_idsdata_file = "result.txt")
#' result <- api_instance$GetOutletLicenses(var_campaign_id, outlet_ids = var_outlet_ids, ids = var_ids)
#' dput(result)
#'
#'
#' ####################  UpdateOutletLicenses  ####################
#'
#' library(ympa_r_client)
#' var_campaign_id <- 56 # integer | Идентификатор кампании в API и магазина в кабинете. Каждая кампания в API соответствует магазину в кабинете.  Чтобы узнать идентификаторы своих магазинов, воспользуйтесь запросом [GET campaigns](../../reference/campaigns/getCampaigns.md).  ℹ️ [Что такое кабинет и магазин на Маркете](https://yandex.ru/support/marketplace/account/introduction.html) 
#' var_update_outlet_license_request <- UpdateOutletLicenseRequest$new(c(OutletLicenseDTO$new(123, 123, LicenseType$new(), "number_example", "dateOfIssue_example", "dateOfExpiry_example"))) # UpdateOutletLicenseRequest | 
#'
#' #Создание и изменение лицензий для точек продаж
#' api_instance <- OutletLicensesApi$new()
#'
#' # Configure OAuth2 access token for authorization: OAuth
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$UpdateOutletLicenses(var_campaign_id, var_update_outlet_license_requestdata_file = "result.txt")
#' result <- api_instance$UpdateOutletLicenses(var_campaign_id, var_update_outlet_license_request)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
OutletLicensesApi <- R6::R6Class(
  "OutletLicensesApi",
  public = list(
    api_client = NULL,
    #' Initialize a new OutletLicensesApi.
    #'
    #' @description
    #' Initialize a new OutletLicensesApi.
    #'
    #' @param api_client An instance of API client.
    #' @export
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },
    #' Удаление лицензий для точек продаж
    #'
    #' @description
    #' Удаление лицензий для точек продаж
    #'
    #' @param campaign_id Идентификатор кампании в API и магазина в кабинете. Каждая кампания в API соответствует магазину в кабинете.  Чтобы узнать идентификаторы своих магазинов, воспользуйтесь запросом [GET campaigns](../../reference/campaigns/getCampaigns.md).  ℹ️ [Что такое кабинет и магазин на Маркете](https://yandex.ru/support/marketplace/account/introduction.html) 
    #' @param ids (optional) Список идентификаторов лицензий.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return EmptyApiResponse
    #' @export
    DeleteOutletLicenses = function(campaign_id, ids = NULL, data_file = NULL, ...) {
      local_var_response <- self$DeleteOutletLicensesWithHttpInfo(campaign_id, ids, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Удаление лицензий для точек продаж
    #'
    #' @description
    #' Удаление лицензий для точек продаж
    #'
    #' @param campaign_id Идентификатор кампании в API и магазина в кабинете. Каждая кампания в API соответствует магазину в кабинете.  Чтобы узнать идентификаторы своих магазинов, воспользуйтесь запросом [GET campaigns](../../reference/campaigns/getCampaigns.md).  ℹ️ [Что такое кабинет и магазин на Маркете](https://yandex.ru/support/marketplace/account/introduction.html) 
    #' @param ids (optional) Список идентификаторов лицензий.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (EmptyApiResponse) with additional information such as HTTP status code, headers
    #' @export
    DeleteOutletLicensesWithHttpInfo = function(campaign_id, ids = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`campaign_id`)) {
        stop("Missing required parameter `campaign_id`.")
      }



      # check if items are unique
      if (!identical(`ids`, unique(`ids`))) {
        stop("Invalid value for ids when calling OutletLicensesApi$DeleteOutletLicenses. Items must be unique.")
      }
      # no explore
      query_params[["ids"]] <- I(paste(lapply(`ids`, URLencode, reserved = TRUE), collapse = ","))

      local_var_url_path <- "/campaigns/{campaignId}/outlets/licenses"
      if (!missing(`campaign_id`)) {
        local_var_url_path <- gsub("\\{campaignId\\}", URLencode(as.character(`campaign_id`), reserved = TRUE), local_var_url_path)
      }

      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "market:partner-api"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "DELETE",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "EmptyApiResponse", loadNamespace("ympa_r_client")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Информация о лицензиях для точек продаж
    #'
    #' @description
    #' Информация о лицензиях для точек продаж
    #'
    #' @param campaign_id Идентификатор кампании в API и магазина в кабинете. Каждая кампания в API соответствует магазину в кабинете.  Чтобы узнать идентификаторы своих магазинов, воспользуйтесь запросом [GET campaigns](../../reference/campaigns/getCampaigns.md).  ℹ️ [Что такое кабинет и магазин на Маркете](https://yandex.ru/support/marketplace/account/introduction.html) 
    #' @param outlet_ids (optional) Список идентификаторов точек продаж, для которых нужно получить информацию о лицензиях. Идентификаторы указываются через запятую.  В запросе должен быть либо параметр `outletIds`, либо параметр `ids`. Запрос с обоими параметрами или без них приведет к ошибке. 
    #' @param ids (optional) Список идентификаторов лицензий.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return GetOutletLicensesResponse
    #' @export
    GetOutletLicenses = function(campaign_id, outlet_ids = NULL, ids = NULL, data_file = NULL, ...) {
      local_var_response <- self$GetOutletLicensesWithHttpInfo(campaign_id, outlet_ids, ids, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Информация о лицензиях для точек продаж
    #'
    #' @description
    #' Информация о лицензиях для точек продаж
    #'
    #' @param campaign_id Идентификатор кампании в API и магазина в кабинете. Каждая кампания в API соответствует магазину в кабинете.  Чтобы узнать идентификаторы своих магазинов, воспользуйтесь запросом [GET campaigns](../../reference/campaigns/getCampaigns.md).  ℹ️ [Что такое кабинет и магазин на Маркете](https://yandex.ru/support/marketplace/account/introduction.html) 
    #' @param outlet_ids (optional) Список идентификаторов точек продаж, для которых нужно получить информацию о лицензиях. Идентификаторы указываются через запятую.  В запросе должен быть либо параметр `outletIds`, либо параметр `ids`. Запрос с обоими параметрами или без них приведет к ошибке. 
    #' @param ids (optional) Список идентификаторов лицензий.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (GetOutletLicensesResponse) with additional information such as HTTP status code, headers
    #' @export
    GetOutletLicensesWithHttpInfo = function(campaign_id, outlet_ids = NULL, ids = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`campaign_id`)) {
        stop("Missing required parameter `campaign_id`.")
      }




      # check if items are unique
      if (!identical(`outlet_ids`, unique(`outlet_ids`))) {
        stop("Invalid value for outlet_ids when calling OutletLicensesApi$GetOutletLicenses. Items must be unique.")
      }
      # no explore
      query_params[["outletIds"]] <- I(paste(lapply(`outlet_ids`, URLencode, reserved = TRUE), collapse = ","))

      # check if items are unique
      if (!identical(`ids`, unique(`ids`))) {
        stop("Invalid value for ids when calling OutletLicensesApi$GetOutletLicenses. Items must be unique.")
      }
      # no explore
      query_params[["ids"]] <- I(paste(lapply(`ids`, URLencode, reserved = TRUE), collapse = ","))

      local_var_url_path <- "/campaigns/{campaignId}/outlets/licenses"
      if (!missing(`campaign_id`)) {
        local_var_url_path <- gsub("\\{campaignId\\}", URLencode(as.character(`campaign_id`), reserved = TRUE), local_var_url_path)
      }

      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "market:partner-api"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "GetOutletLicensesResponse", loadNamespace("ympa_r_client")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Создание и изменение лицензий для точек продаж
    #'
    #' @description
    #' Создание и изменение лицензий для точек продаж
    #'
    #' @param campaign_id Идентификатор кампании в API и магазина в кабинете. Каждая кампания в API соответствует магазину в кабинете.  Чтобы узнать идентификаторы своих магазинов, воспользуйтесь запросом [GET campaigns](../../reference/campaigns/getCampaigns.md).  ℹ️ [Что такое кабинет и магазин на Маркете](https://yandex.ru/support/marketplace/account/introduction.html) 
    #' @param update_outlet_license_request 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return EmptyApiResponse
    #' @export
    UpdateOutletLicenses = function(campaign_id, update_outlet_license_request, data_file = NULL, ...) {
      local_var_response <- self$UpdateOutletLicensesWithHttpInfo(campaign_id, update_outlet_license_request, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Создание и изменение лицензий для точек продаж
    #'
    #' @description
    #' Создание и изменение лицензий для точек продаж
    #'
    #' @param campaign_id Идентификатор кампании в API и магазина в кабинете. Каждая кампания в API соответствует магазину в кабинете.  Чтобы узнать идентификаторы своих магазинов, воспользуйтесь запросом [GET campaigns](../../reference/campaigns/getCampaigns.md).  ℹ️ [Что такое кабинет и магазин на Маркете](https://yandex.ru/support/marketplace/account/introduction.html) 
    #' @param update_outlet_license_request 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (EmptyApiResponse) with additional information such as HTTP status code, headers
    #' @export
    UpdateOutletLicensesWithHttpInfo = function(campaign_id, update_outlet_license_request, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`campaign_id`)) {
        stop("Missing required parameter `campaign_id`.")
      }

      if (missing(`update_outlet_license_request`)) {
        stop("Missing required parameter `update_outlet_license_request`.")
      }



      if (!is.null(`update_outlet_license_request`)) {
        local_var_body <- `update_outlet_license_request`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/campaigns/{campaignId}/outlets/licenses"
      if (!missing(`campaign_id`)) {
        local_var_url_path <- gsub("\\{campaignId\\}", URLencode(as.character(`campaign_id`), reserved = TRUE), local_var_url_path)
      }

      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "market:partner-api"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "EmptyApiResponse", loadNamespace("ympa_r_client")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    }
  )
)
