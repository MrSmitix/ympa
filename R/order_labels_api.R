#' Партнерский API Маркета
#'
#' API Яндекс Маркета помогает продавцам автоматизировать и упростить работу с маркетплейсом.  В числе возможностей интеграции:  * управление каталогом товаров и витриной,  * обработка заказов,  * изменение настроек магазина,  * получение отчетов. 
#'
#' The version of the OpenAPI document: LATEST
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title OrderLabels operations
#' @description OrderLabelsApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @section Methods:
#' \describe{
#' \strong{ GenerateOrderLabel } \emph{ Готовый ярлык‑наклейка для коробки в заказе }
#' Формирует ярлык‑наклейку для коробки в заказе и возвращает ярлык в PDF‑файле.  |**⚙️ Лимит:** 1 000 000 запросов в час| |-|
#'
#' \itemize{
#' \item \emph{ @param } campaign_id integer
#' \item \emph{ @param } order_id integer
#' \item \emph{ @param } shipment_id integer
#' \item \emph{ @param } box_id integer
#' \item \emph{ @param } format \link{PageFormatType}
#'
#'
#' \item status code : 200 | PDF‑файл с ярлыками для коробки. Файл содержит одну страницу с ярлыком.
#'
#' \item return type : data.frame
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 400 | Запрос содержит неправильные данные.
#'
#' \item return type : ApiClientDataErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 401 | В запросе не указаны данные для авторизации.
#'
#' \item return type : ApiUnauthorizedErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 403 | Данные для авторизации неверны или доступ к ресурсу запрещен.
#'
#' \item return type : ApiForbiddenErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 404 | Запрашиваемый ресурс не найден.
#'
#' \item return type : ApiNotFoundErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 420 | Превышено ограничение на доступ к ресурсу.
#'
#' \item return type : ApiLimitErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 500 | Внутренняя ошибка сервера.
#'
#' \item return type : ApiServerErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ GenerateOrderLabels } \emph{ Готовые ярлыки‑наклейки на все коробки в одном заказе }
#' Возвращает PDF-файл с ярлыками, которые нужно наклеить на коробки перед отгрузкой. Подробно о том, зачем они нужны и как выглядят, рассказано [в Справке Маркета для продавцов](https://yandex.ru/support/marketplace/orders/fbs/packaging/marking.html).  На вход нужно передать идентификатор заказа и один необязательный параметр, который управляет версткой PDF-файла.  |**⚙️ Лимит:** 1 000 000 запросов в час| |-|
#'
#' \itemize{
#' \item \emph{ @param } campaign_id integer
#' \item \emph{ @param } order_id integer
#' \item \emph{ @param } format \link{PageFormatType}
#'
#'
#' \item status code : 200 | PDF‑файл с ярлыками на все коробки.
#'
#' \item return type : data.frame
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 400 | Запрос содержит неправильные данные.
#'
#' \item return type : ApiClientDataErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 401 | В запросе не указаны данные для авторизации.
#'
#' \item return type : ApiUnauthorizedErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 403 | Данные для авторизации неверны или доступ к ресурсу запрещен.
#'
#' \item return type : ApiForbiddenErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 404 | Запрашиваемый ресурс не найден.
#'
#' \item return type : ApiNotFoundErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 420 | Превышено ограничение на доступ к ресурсу.
#'
#' \item return type : ApiLimitErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 500 | Внутренняя ошибка сервера.
#'
#' \item return type : ApiServerErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ GetOrderLabelsData } \emph{ Данные для самостоятельного изготовления ярлыков }
#' Возвращает информацию на ярлыках, которые клеятся на коробки в заказе.  |**⚙️ Лимит:** 1 000 000 запросов в час| |-|
#'
#' \itemize{
#' \item \emph{ @param } campaign_id integer
#' \item \emph{ @param } order_id integer
#' \item \emph{ @returnType } \link{GetOrderLabelsDataResponse} \cr
#'
#'
#' \item status code : 200 | Информация для печати ярлыков.
#'
#' \item return type : GetOrderLabelsDataResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 400 | Запрос содержит неправильные данные.
#'
#' \item return type : ApiClientDataErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 401 | В запросе не указаны данные для авторизации.
#'
#' \item return type : ApiUnauthorizedErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 403 | Данные для авторизации неверны или доступ к ресурсу запрещен.
#'
#' \item return type : ApiForbiddenErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 404 | Запрашиваемый ресурс не найден.
#'
#' \item return type : ApiNotFoundErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 420 | Превышено ограничение на доступ к ресурсу.
#'
#' \item return type : ApiLimitErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 500 | Внутренняя ошибка сервера.
#'
#' \item return type : ApiServerErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' }
#'
#'
#' @examples
#' \dontrun{
#' ####################  GenerateOrderLabel  ####################
#'
#' library(ympa_r_client)
#' var_campaign_id <- 56 # integer | Идентификатор кампании в API и магазина в кабинете. Каждая кампания в API соответствует магазину в кабинете.  Чтобы узнать идентификаторы своих магазинов, воспользуйтесь запросом [GET campaigns](../../reference/campaigns/getCampaigns.md).  ℹ️ [Что такое кабинет и магазин на Маркете](https://yandex.ru/support/marketplace/account/introduction.html) 
#' var_order_id <- 56 # integer | Идентификатор заказа.
#' var_shipment_id <- 56 # integer | Идентификатор грузоместа.
#' var_box_id <- 56 # integer | Идентификатор коробки.
#' var_format <- PageFormatType$new() # PageFormatType | Настройка размещения ярлыков на странице. Если параметра нет, возвращается PDF с ярлыками формата A6. (Optional)
#'
#' #Готовый ярлык‑наклейка для коробки в заказе
#' api_instance <- OrderLabelsApi$new()
#'
#' # Configure OAuth2 access token for authorization: OAuth
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GenerateOrderLabel(var_campaign_id, var_order_id, var_shipment_id, var_box_id, format = var_formatdata_file = "result.txt")
#' result <- api_instance$GenerateOrderLabel(var_campaign_id, var_order_id, var_shipment_id, var_box_id, format = var_format)
#' dput(result)
#'
#'
#' ####################  GenerateOrderLabels  ####################
#'
#' library(ympa_r_client)
#' var_campaign_id <- 56 # integer | Идентификатор кампании в API и магазина в кабинете. Каждая кампания в API соответствует магазину в кабинете.  Чтобы узнать идентификаторы своих магазинов, воспользуйтесь запросом [GET campaigns](../../reference/campaigns/getCampaigns.md).  ℹ️ [Что такое кабинет и магазин на Маркете](https://yandex.ru/support/marketplace/account/introduction.html) 
#' var_order_id <- 56 # integer | Идентификатор заказа.
#' var_format <- PageFormatType$new() # PageFormatType | Настройка размещения ярлыков на странице. Если параметра нет, возвращается PDF с ярлыками формата A6. (Optional)
#'
#' #Готовые ярлыки‑наклейки на все коробки в одном заказе
#' api_instance <- OrderLabelsApi$new()
#'
#' # Configure OAuth2 access token for authorization: OAuth
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GenerateOrderLabels(var_campaign_id, var_order_id, format = var_formatdata_file = "result.txt")
#' result <- api_instance$GenerateOrderLabels(var_campaign_id, var_order_id, format = var_format)
#' dput(result)
#'
#'
#' ####################  GetOrderLabelsData  ####################
#'
#' library(ympa_r_client)
#' var_campaign_id <- 56 # integer | Идентификатор кампании в API и магазина в кабинете. Каждая кампания в API соответствует магазину в кабинете.  Чтобы узнать идентификаторы своих магазинов, воспользуйтесь запросом [GET campaigns](../../reference/campaigns/getCampaigns.md).  ℹ️ [Что такое кабинет и магазин на Маркете](https://yandex.ru/support/marketplace/account/introduction.html) 
#' var_order_id <- 56 # integer | Идентификатор заказа.
#'
#' #Данные для самостоятельного изготовления ярлыков
#' api_instance <- OrderLabelsApi$new()
#'
#' # Configure OAuth2 access token for authorization: OAuth
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetOrderLabelsData(var_campaign_id, var_order_iddata_file = "result.txt")
#' result <- api_instance$GetOrderLabelsData(var_campaign_id, var_order_id)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
OrderLabelsApi <- R6::R6Class(
  "OrderLabelsApi",
  public = list(
    api_client = NULL,
    #' Initialize a new OrderLabelsApi.
    #'
    #' @description
    #' Initialize a new OrderLabelsApi.
    #'
    #' @param api_client An instance of API client.
    #' @export
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },
    #' Готовый ярлык‑наклейка для коробки в заказе
    #'
    #' @description
    #' Готовый ярлык‑наклейка для коробки в заказе
    #'
    #' @param campaign_id Идентификатор кампании в API и магазина в кабинете. Каждая кампания в API соответствует магазину в кабинете.  Чтобы узнать идентификаторы своих магазинов, воспользуйтесь запросом [GET campaigns](../../reference/campaigns/getCampaigns.md).  ℹ️ [Что такое кабинет и магазин на Маркете](https://yandex.ru/support/marketplace/account/introduction.html) 
    #' @param order_id Идентификатор заказа.
    #' @param shipment_id Идентификатор грузоместа.
    #' @param box_id Идентификатор коробки.
    #' @param format (optional) Настройка размещения ярлыков на странице. Если параметра нет, возвращается PDF с ярлыками формата A6.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return data.frame
    #' @export
    GenerateOrderLabel = function(campaign_id, order_id, shipment_id, box_id, format = NULL, data_file = NULL, ...) {
      local_var_response <- self$GenerateOrderLabelWithHttpInfo(campaign_id, order_id, shipment_id, box_id, format, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Готовый ярлык‑наклейка для коробки в заказе
    #'
    #' @description
    #' Готовый ярлык‑наклейка для коробки в заказе
    #'
    #' @param campaign_id Идентификатор кампании в API и магазина в кабинете. Каждая кампания в API соответствует магазину в кабинете.  Чтобы узнать идентификаторы своих магазинов, воспользуйтесь запросом [GET campaigns](../../reference/campaigns/getCampaigns.md).  ℹ️ [Что такое кабинет и магазин на Маркете](https://yandex.ru/support/marketplace/account/introduction.html) 
    #' @param order_id Идентификатор заказа.
    #' @param shipment_id Идентификатор грузоместа.
    #' @param box_id Идентификатор коробки.
    #' @param format (optional) Настройка размещения ярлыков на странице. Если параметра нет, возвращается PDF с ярлыками формата A6.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (data.frame) with additional information such as HTTP status code, headers
    #' @export
    GenerateOrderLabelWithHttpInfo = function(campaign_id, order_id, shipment_id, box_id, format = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`campaign_id`)) {
        stop("Missing required parameter `campaign_id`.")
      }

      if (missing(`order_id`)) {
        stop("Missing required parameter `order_id`.")
      }

      if (missing(`shipment_id`)) {
        stop("Missing required parameter `shipment_id`.")
      }

      if (missing(`box_id`)) {
        stop("Missing required parameter `box_id`.")
      }






      query_params[["format"]] <- `format`

      local_var_url_path <- "/campaigns/{campaignId}/orders/{orderId}/delivery/shipments/{shipmentId}/boxes/{boxId}/label"
      if (!missing(`campaign_id`)) {
        local_var_url_path <- gsub("\\{campaignId\\}", URLencode(as.character(`campaign_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`order_id`)) {
        local_var_url_path <- gsub("\\{orderId\\}", URLencode(as.character(`order_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`shipment_id`)) {
        local_var_url_path <- gsub("\\{shipmentId\\}", URLencode(as.character(`shipment_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`box_id`)) {
        local_var_url_path <- gsub("\\{boxId\\}", URLencode(as.character(`box_id`), reserved = TRUE), local_var_url_path)
      }

      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "market:partner-api"

      # The Accept request HTTP header
      local_var_accepts <- list("application/pdf", "application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "data.frame", loadNamespace("ympa_r_client")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Готовые ярлыки‑наклейки на все коробки в одном заказе
    #'
    #' @description
    #' Готовые ярлыки‑наклейки на все коробки в одном заказе
    #'
    #' @param campaign_id Идентификатор кампании в API и магазина в кабинете. Каждая кампания в API соответствует магазину в кабинете.  Чтобы узнать идентификаторы своих магазинов, воспользуйтесь запросом [GET campaigns](../../reference/campaigns/getCampaigns.md).  ℹ️ [Что такое кабинет и магазин на Маркете](https://yandex.ru/support/marketplace/account/introduction.html) 
    #' @param order_id Идентификатор заказа.
    #' @param format (optional) Настройка размещения ярлыков на странице. Если параметра нет, возвращается PDF с ярлыками формата A6.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return data.frame
    #' @export
    GenerateOrderLabels = function(campaign_id, order_id, format = NULL, data_file = NULL, ...) {
      local_var_response <- self$GenerateOrderLabelsWithHttpInfo(campaign_id, order_id, format, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Готовые ярлыки‑наклейки на все коробки в одном заказе
    #'
    #' @description
    #' Готовые ярлыки‑наклейки на все коробки в одном заказе
    #'
    #' @param campaign_id Идентификатор кампании в API и магазина в кабинете. Каждая кампания в API соответствует магазину в кабинете.  Чтобы узнать идентификаторы своих магазинов, воспользуйтесь запросом [GET campaigns](../../reference/campaigns/getCampaigns.md).  ℹ️ [Что такое кабинет и магазин на Маркете](https://yandex.ru/support/marketplace/account/introduction.html) 
    #' @param order_id Идентификатор заказа.
    #' @param format (optional) Настройка размещения ярлыков на странице. Если параметра нет, возвращается PDF с ярлыками формата A6.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (data.frame) with additional information such as HTTP status code, headers
    #' @export
    GenerateOrderLabelsWithHttpInfo = function(campaign_id, order_id, format = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`campaign_id`)) {
        stop("Missing required parameter `campaign_id`.")
      }

      if (missing(`order_id`)) {
        stop("Missing required parameter `order_id`.")
      }




      query_params[["format"]] <- `format`

      local_var_url_path <- "/campaigns/{campaignId}/orders/{orderId}/delivery/labels"
      if (!missing(`campaign_id`)) {
        local_var_url_path <- gsub("\\{campaignId\\}", URLencode(as.character(`campaign_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`order_id`)) {
        local_var_url_path <- gsub("\\{orderId\\}", URLencode(as.character(`order_id`), reserved = TRUE), local_var_url_path)
      }

      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "market:partner-api"

      # The Accept request HTTP header
      local_var_accepts <- list("application/pdf", "application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "data.frame", loadNamespace("ympa_r_client")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Данные для самостоятельного изготовления ярлыков
    #'
    #' @description
    #' Данные для самостоятельного изготовления ярлыков
    #'
    #' @param campaign_id Идентификатор кампании в API и магазина в кабинете. Каждая кампания в API соответствует магазину в кабинете.  Чтобы узнать идентификаторы своих магазинов, воспользуйтесь запросом [GET campaigns](../../reference/campaigns/getCampaigns.md).  ℹ️ [Что такое кабинет и магазин на Маркете](https://yandex.ru/support/marketplace/account/introduction.html) 
    #' @param order_id Идентификатор заказа.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return GetOrderLabelsDataResponse
    #' @export
    GetOrderLabelsData = function(campaign_id, order_id, data_file = NULL, ...) {
      local_var_response <- self$GetOrderLabelsDataWithHttpInfo(campaign_id, order_id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Данные для самостоятельного изготовления ярлыков
    #'
    #' @description
    #' Данные для самостоятельного изготовления ярлыков
    #'
    #' @param campaign_id Идентификатор кампании в API и магазина в кабинете. Каждая кампания в API соответствует магазину в кабинете.  Чтобы узнать идентификаторы своих магазинов, воспользуйтесь запросом [GET campaigns](../../reference/campaigns/getCampaigns.md).  ℹ️ [Что такое кабинет и магазин на Маркете](https://yandex.ru/support/marketplace/account/introduction.html) 
    #' @param order_id Идентификатор заказа.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (GetOrderLabelsDataResponse) with additional information such as HTTP status code, headers
    #' @export
    GetOrderLabelsDataWithHttpInfo = function(campaign_id, order_id, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`campaign_id`)) {
        stop("Missing required parameter `campaign_id`.")
      }

      if (missing(`order_id`)) {
        stop("Missing required parameter `order_id`.")
      }



      local_var_url_path <- "/campaigns/{campaignId}/orders/{orderId}/delivery/labels/data"
      if (!missing(`campaign_id`)) {
        local_var_url_path <- gsub("\\{campaignId\\}", URLencode(as.character(`campaign_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`order_id`)) {
        local_var_url_path <- gsub("\\{orderId\\}", URLencode(as.character(`order_id`), reserved = TRUE), local_var_url_path)
      }

      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "market:partner-api"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "GetOrderLabelsDataResponse", loadNamespace("ympa_r_client")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    }
  )
)
