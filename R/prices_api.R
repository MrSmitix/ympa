#' Партнерский API Маркета
#'
#' API Яндекс Маркета помогает продавцам автоматизировать и упростить работу с маркетплейсом.  В числе возможностей интеграции:  * управление каталогом товаров и витриной,  * обработка заказов,  * изменение настроек магазина,  * получение отчетов. 
#'
#' The version of the OpenAPI document: LATEST
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title Prices operations
#' @description PricesApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @section Methods:
#' \describe{
#' \strong{ GetPrices } \emph{ Список цен }
#' {\% note warning \"\" \%}  Этот метод устарел. Вместо него используйте [POST campaigns/{campaignId}/offer-prices](../../reference/assortment/getPricesByOfferIds.md).  {\% endnote \%}  Возвращает список цен, установленных вами на товары любым способом: например, через партнерский API или в файле с каталогом.  {\% note info \%}  Общее количество товаров считается по данным за последние семь дней (не включая сегодня) и не может быть выше 2 миллионов.  {\% endnote \%}  Способы установки цен описаны [в Справке Маркета для продавцов](https://yandex.ru/support/marketplace/assortment/operations/prices.html).  |**⚙️ Лимит:** ```(количество товаров партнера на витрине) * 25``` товаров в сутки| |-|
#'
#' \itemize{
#' \item \emph{ @param } campaign_id integer
#' \item \emph{ @param } page_token character
#' \item \emph{ @param } limit integer
#' \item \emph{ @param } archived character
#' \item \emph{ @returnType } \link{GetPricesResponse} \cr
#'
#'
#' \item status code : 200 | Список всех товаров с установленными ценами.
#'
#' \item return type : GetPricesResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 400 | Запрос содержит неправильные данные.
#'
#' \item return type : ApiClientDataErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 401 | В запросе не указаны данные для авторизации.
#'
#' \item return type : ApiUnauthorizedErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 403 | Данные для авторизации неверны или доступ к ресурсу запрещен.
#'
#' \item return type : ApiForbiddenErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 404 | Запрашиваемый ресурс не найден.
#'
#' \item return type : ApiNotFoundErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 420 | Превышено ограничение на доступ к ресурсу.
#'
#' \item return type : ApiLimitErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 500 | Внутренняя ошибка сервера.
#'
#' \item return type : ApiServerErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ GetPricesByOfferIds } \emph{ Просмотр цен на указанные товары в магазине }
#' Возвращает список цен на указанные товары в магазине.  {\% note warning \"Метод только для отдельных магазинов\" \%}  Используйте этот метод, только если в кабинете установлены уникальные цены в отдельных магазинах.  Для просмотра базовых цен используйте [POST businesses/{businessId}/offer-mappings](../../reference/business-assortment/getOfferMappings.md).  {\% endnote \%}  |**⚙️ Лимит:** рассчитывается по [формуле](*rule)| |-|  [//]: <> (rule: суточный лимит товаров — количество товаров на витрине * 25)
#'
#' \itemize{
#' \item \emph{ @param } campaign_id integer
#' \item \emph{ @param } page_token character
#' \item \emph{ @param } limit integer
#' \item \emph{ @param } get_prices_by_offer_ids_request \link{GetPricesByOfferIdsRequest}
#' \item \emph{ @returnType } \link{GetPricesByOfferIdsResponse} \cr
#'
#'
#' \item status code : 200 | Список товаров с установленными для заданного магазина ценами.
#'
#' \item return type : GetPricesByOfferIdsResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 400 | Запрос содержит неправильные данные.
#'
#' \item return type : ApiClientDataErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 401 | В запросе не указаны данные для авторизации.
#'
#' \item return type : ApiUnauthorizedErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 403 | Данные для авторизации неверны или доступ к ресурсу запрещен.
#'
#' \item return type : ApiForbiddenErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 404 | Запрашиваемый ресурс не найден.
#'
#' \item return type : ApiNotFoundErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 420 | Превышено ограничение на доступ к ресурсу.
#'
#' \item return type : ApiLimitErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 500 | Внутренняя ошибка сервера.
#'
#' \item return type : ApiServerErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ GetSuggestedPrices } \emph{ Цены для продвижения товаров }
#' {\% note warning \"\" \%}  Этот метод устарел. Вместо него используйте [отчет «Цены на рынке»](../../reference/reports/generatePricesReport.md).  {\% endnote \%}  {\% note warning \"\" \%}  Этот метод подходит только тем магазинам, которые устанавливают цены на товары в рублях.  {\% endnote \%}  Возвращает цены для продвижения товаров, которые вы размещаете на Маркете.  Товары, для которых нужно получить цены, передаются в теле POST-запроса.  Цены для продвижения зависят от цен, установленных на товары другими партнерами. Если один товар поставляют несколько партнеров, на Маркете сначала продается товар с более низкой ценой. Когда закончится товар по низкой цене, начнет продаваться товар по более высокой цене.  Выходные данные содержат для каждого товара несколько цен, соответствующих разным типам продвижения.  Установить цены на товары можно с помощью запроса [POST campaigns/{campaignId}/offer-prices/updates](../../reference/assortment/updatePrices.md) или другими способами: например, указать их в файле с каталогом. Также вы можете использовать стратегии для автоматической установки рекомендованных цен или минимальных цен на Маркете.  Подробно об автоматическом управлении ценами рассказано [в Справке Маркета для продавцов](https://yandex.ru/support/marketplace/marketing/prices.html).  |**⚙️ Лимит:** 100 000 товаров в час| |-|
#'
#' \itemize{
#' \item \emph{ @param } campaign_id integer
#' \item \emph{ @param } suggest_prices_request \link{SuggestPricesRequest}
#' \item \emph{ @returnType } \link{SuggestPricesResponse} \cr
#'
#'
#' \item status code : 200 | Список цен для продвижения на Маркете.
#'
#' \item return type : SuggestPricesResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 400 | Запрос содержит неправильные данные.
#'
#' \item return type : ApiClientDataErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 401 | В запросе не указаны данные для авторизации.
#'
#' \item return type : ApiUnauthorizedErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 403 | Данные для авторизации неверны или доступ к ресурсу запрещен.
#'
#' \item return type : ApiForbiddenErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 404 | Запрашиваемый ресурс не найден.
#'
#' \item return type : ApiNotFoundErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 420 | Превышено ограничение на доступ к ресурсу.
#'
#' \item return type : ApiLimitErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 500 | Внутренняя ошибка сервера.
#'
#' \item return type : ApiServerErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ UpdateBusinessPrices } \emph{ Установка цен на товары во всех магазинах }
#' Устанавливает [базовые цены](*default-price). Чтобы получить рекомендации Маркета, касающиеся цен, выполните запрос [POST businesses/{businessId}/offers/recommendations](../../reference/business-assortment/getOfferRecommendations.md).  {\% note info \"Данные в каталоге обновляются не мгновенно\" \%}  Это занимает до нескольких минут.  {\% endnote \%}  |**⚙️ Лимит:** 5000 товаров в минуту, не более 500 товаров в одном запросе| |-|
#'
#' \itemize{
#' \item \emph{ @param } business_id integer
#' \item \emph{ @param } update_business_prices_request \link{UpdateBusinessPricesRequest}
#' \item \emph{ @returnType } \link{EmptyApiResponse} \cr
#'
#'
#' \item status code : 200 | Маркет принял информацию о новых ценах.
#'
#' \item return type : EmptyApiResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 400 | Запрос содержит неправильные данные.
#'
#' \item return type : ApiClientDataErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 401 | В запросе не указаны данные для авторизации.
#'
#' \item return type : ApiUnauthorizedErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 403 | Данные для авторизации неверны или доступ к ресурсу запрещен.
#'
#' \item return type : ApiForbiddenErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 404 | Запрашиваемый ресурс не найден.
#'
#' \item return type : ApiNotFoundErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 420 | Превышено ограничение на доступ к ресурсу.
#'
#' \item return type : ApiLimitErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 423 | К ресурсу нельзя применить указанный метод.
#'
#' \item return type : ApiLockedErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 500 | Внутренняя ошибка сервера.
#'
#' \item return type : ApiServerErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ UpdatePrices } \emph{ Установка цен на товары в конкретном магазине }
#' Устанавливает цены на товары в магазине. Чтобы получить рекомендации Маркета, касающиеся цен, выполните запрос [POST businesses/{businessId}/offers/recommendations](../../reference/business-assortment/getOfferRecommendations.md).  {\% note warning \"Метод только для отдельных магазинов\" \%}  Используйте этот метод, только если в кабинете установлены уникальные цены в отдельных магазинах.  Для управления базовыми ценами используйте [POST businesses/{businessId}/offer-prices/updates](../../reference/business-assortment/updateBusinessPrices.md).  {\% endnote \%}  {\% note info \"Данные в каталоге обновляются не мгновенно\" \%}  Это занимает до нескольких минут.  {\% endnote \%}  |**⚙️ Лимит:** 5000 товаров в минуту| |-|
#'
#' \itemize{
#' \item \emph{ @param } campaign_id integer
#' \item \emph{ @param } update_prices_request \link{UpdatePricesRequest}
#' \item \emph{ @returnType } \link{EmptyApiResponse} \cr
#'
#'
#' \item status code : 200 | Маркет принял информацию о новых ценах.
#'
#' \item return type : EmptyApiResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 400 | Запрос содержит неправильные данные.
#'
#' \item return type : ApiClientDataErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 401 | В запросе не указаны данные для авторизации.
#'
#' \item return type : ApiUnauthorizedErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 403 | Данные для авторизации неверны или доступ к ресурсу запрещен.
#'
#' \item return type : ApiForbiddenErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 404 | Запрашиваемый ресурс не найден.
#'
#' \item return type : ApiNotFoundErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 420 | Превышено ограничение на доступ к ресурсу.
#'
#' \item return type : ApiLimitErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 423 | К ресурсу нельзя применить указанный метод.
#'
#' \item return type : ApiLockedErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 500 | Внутренняя ошибка сервера.
#'
#' \item return type : ApiServerErrorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' }
#'
#'
#' @examples
#' \dontrun{
#' ####################  GetPrices  ####################
#'
#' library(ympa_r_client)
#' var_campaign_id <- 56 # integer | Идентификатор кампании в API и магазина в кабинете. Каждая кампания в API соответствует магазину в кабинете.  Чтобы узнать идентификаторы своих магазинов, воспользуйтесь запросом [GET campaigns](../../reference/campaigns/getCampaigns.md).  ℹ️ [Что такое кабинет и магазин на Маркете](https://yandex.ru/support/marketplace/account/introduction.html) 
#' var_page_token <- "eyBuZXh0SWQ6IDIzNDIgfQ==" # character | Идентификатор страницы c результатами.  Если параметр не указан, возвращается первая страница.  Рекомендуется передавать значение выходного параметра `nextPageToken`, полученное при последнем запросе.  Если задан `page_token` и в запросе есть параметры `offset`, `page_number` и `page_size`, они игнорируются.  (Optional)
#' var_limit <- 20 # integer | Количество значений на одной странице.  (Optional)
#' var_archived <- FALSE # character | Фильтр по нахождению в архиве. (Optional)
#'
#' #Список цен
#' api_instance <- PricesApi$new()
#'
#' # Configure OAuth2 access token for authorization: OAuth
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetPrices(var_campaign_id, page_token = var_page_token, limit = var_limit, archived = var_archiveddata_file = "result.txt")
#' result <- api_instance$GetPrices(var_campaign_id, page_token = var_page_token, limit = var_limit, archived = var_archived)
#' dput(result)
#'
#'
#' ####################  GetPricesByOfferIds  ####################
#'
#' library(ympa_r_client)
#' var_campaign_id <- 56 # integer | Идентификатор кампании в API и магазина в кабинете. Каждая кампания в API соответствует магазину в кабинете.  Чтобы узнать идентификаторы своих магазинов, воспользуйтесь запросом [GET campaigns](../../reference/campaigns/getCampaigns.md).  ℹ️ [Что такое кабинет и магазин на Маркете](https://yandex.ru/support/marketplace/account/introduction.html) 
#' var_page_token <- "eyBuZXh0SWQ6IDIzNDIgfQ==" # character | Идентификатор страницы c результатами.  Если параметр не указан, возвращается первая страница.  Рекомендуется передавать значение выходного параметра `nextPageToken`, полученное при последнем запросе.  Если задан `page_token` и в запросе есть параметры `offset`, `page_number` и `page_size`, они игнорируются.  (Optional)
#' var_limit <- 20 # integer | Количество значений на одной странице.  (Optional)
#' var_get_prices_by_offer_ids_request <- GetPricesByOfferIdsRequest$new(c("offerIds_example")) # GetPricesByOfferIdsRequest |  (Optional)
#'
#' #Просмотр цен на указанные товары в магазине
#' api_instance <- PricesApi$new()
#'
#' # Configure OAuth2 access token for authorization: OAuth
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetPricesByOfferIds(var_campaign_id, page_token = var_page_token, limit = var_limit, get_prices_by_offer_ids_request = var_get_prices_by_offer_ids_requestdata_file = "result.txt")
#' result <- api_instance$GetPricesByOfferIds(var_campaign_id, page_token = var_page_token, limit = var_limit, get_prices_by_offer_ids_request = var_get_prices_by_offer_ids_request)
#' dput(result)
#'
#'
#' ####################  GetSuggestedPrices  ####################
#'
#' library(ympa_r_client)
#' var_campaign_id <- 56 # integer | Идентификатор кампании в API и магазина в кабинете. Каждая кампания в API соответствует магазину в кабинете.  Чтобы узнать идентификаторы своих магазинов, воспользуйтесь запросом [GET campaigns](../../reference/campaigns/getCampaigns.md).  ℹ️ [Что такое кабинет и магазин на Маркете](https://yandex.ru/support/marketplace/account/introduction.html) 
#' var_suggest_prices_request <- SuggestPricesRequest$new(c(SuggestOfferPriceDTO$new("offerId_example", 123))) # SuggestPricesRequest | 
#'
#' #Цены для продвижения товаров
#' api_instance <- PricesApi$new()
#'
#' # Configure OAuth2 access token for authorization: OAuth
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetSuggestedPrices(var_campaign_id, var_suggest_prices_requestdata_file = "result.txt")
#' result <- api_instance$GetSuggestedPrices(var_campaign_id, var_suggest_prices_request)
#' dput(result)
#'
#'
#' ####################  UpdateBusinessPrices  ####################
#'
#' library(ympa_r_client)
#' var_business_id <- 56 # integer | Идентификатор кабинета. Чтобы узнать идентификатор, воспользуйтесь запросом [GET campaigns](../../reference/campaigns/getCampaigns.md#businessdto).  ℹ️ [Что такое кабинет и магазин на Маркете](https://yandex.ru/support/marketplace/account/introduction.html) 
#' var_update_business_prices_request <- UpdateBusinessPricesRequest$new(c(UpdateBusinessOfferPriceDTO$new("offerId_example", UpdatePriceWithDiscountDTO$new(123, CurrencyType$new(), 123)))) # UpdateBusinessPricesRequest | 
#'
#' #Установка цен на товары во всех магазинах
#' api_instance <- PricesApi$new()
#'
#' # Configure OAuth2 access token for authorization: OAuth
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$UpdateBusinessPrices(var_business_id, var_update_business_prices_requestdata_file = "result.txt")
#' result <- api_instance$UpdateBusinessPrices(var_business_id, var_update_business_prices_request)
#' dput(result)
#'
#'
#' ####################  UpdatePrices  ####################
#'
#' library(ympa_r_client)
#' var_campaign_id <- 56 # integer | Идентификатор кампании в API и магазина в кабинете. Каждая кампания в API соответствует магазину в кабинете.  Чтобы узнать идентификаторы своих магазинов, воспользуйтесь запросом [GET campaigns](../../reference/campaigns/getCampaigns.md).  ℹ️ [Что такое кабинет и магазин на Маркете](https://yandex.ru/support/marketplace/account/introduction.html) 
#' var_update_prices_request <- UpdatePricesRequest$new(c(OfferPriceDTO$new("offerId_example", PriceDTO$new(123, 123, CurrencyType$new(), 123)))) # UpdatePricesRequest | 
#'
#' #Установка цен на товары в конкретном магазине
#' api_instance <- PricesApi$new()
#'
#' # Configure OAuth2 access token for authorization: OAuth
#' api_instance$api_client$access_token <- Sys.getenv("ACCESS_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$UpdatePrices(var_campaign_id, var_update_prices_requestdata_file = "result.txt")
#' result <- api_instance$UpdatePrices(var_campaign_id, var_update_prices_request)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
PricesApi <- R6::R6Class(
  "PricesApi",
  public = list(
    api_client = NULL,
    #' Initialize a new PricesApi.
    #'
    #' @description
    #' Initialize a new PricesApi.
    #'
    #' @param api_client An instance of API client.
    #' @export
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },
    #' Список цен
    #'
    #' @description
    #' Список цен
    #'
    #' @param campaign_id Идентификатор кампании в API и магазина в кабинете. Каждая кампания в API соответствует магазину в кабинете.  Чтобы узнать идентификаторы своих магазинов, воспользуйтесь запросом [GET campaigns](../../reference/campaigns/getCampaigns.md).  ℹ️ [Что такое кабинет и магазин на Маркете](https://yandex.ru/support/marketplace/account/introduction.html) 
    #' @param page_token (optional) Идентификатор страницы c результатами.  Если параметр не указан, возвращается первая страница.  Рекомендуется передавать значение выходного параметра `nextPageToken`, полученное при последнем запросе.  Если задан `page_token` и в запросе есть параметры `offset`, `page_number` и `page_size`, они игнорируются. 
    #' @param limit (optional) Количество значений на одной странице. 
    #' @param archived (optional) Фильтр по нахождению в архиве. (default value: FALSE)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return GetPricesResponse
    #' @export
    GetPrices = function(campaign_id, page_token = NULL, limit = NULL, archived = FALSE, data_file = NULL, ...) {
      local_var_response <- self$GetPricesWithHttpInfo(campaign_id, page_token, limit, archived, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Список цен
    #'
    #' @description
    #' Список цен
    #'
    #' @param campaign_id Идентификатор кампании в API и магазина в кабинете. Каждая кампания в API соответствует магазину в кабинете.  Чтобы узнать идентификаторы своих магазинов, воспользуйтесь запросом [GET campaigns](../../reference/campaigns/getCampaigns.md).  ℹ️ [Что такое кабинет и магазин на Маркете](https://yandex.ru/support/marketplace/account/introduction.html) 
    #' @param page_token (optional) Идентификатор страницы c результатами.  Если параметр не указан, возвращается первая страница.  Рекомендуется передавать значение выходного параметра `nextPageToken`, полученное при последнем запросе.  Если задан `page_token` и в запросе есть параметры `offset`, `page_number` и `page_size`, они игнорируются. 
    #' @param limit (optional) Количество значений на одной странице. 
    #' @param archived (optional) Фильтр по нахождению в архиве. (default value: FALSE)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (GetPricesResponse) with additional information such as HTTP status code, headers
    #' @export
    GetPricesWithHttpInfo = function(campaign_id, page_token = NULL, limit = NULL, archived = FALSE, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`campaign_id`)) {
        stop("Missing required parameter `campaign_id`.")
      }





      query_params[["page_token"]] <- `page_token`

      query_params[["limit"]] <- `limit`

      query_params[["archived"]] <- `archived`

      local_var_url_path <- "/campaigns/{campaignId}/offer-prices"
      if (!missing(`campaign_id`)) {
        local_var_url_path <- gsub("\\{campaignId\\}", URLencode(as.character(`campaign_id`), reserved = TRUE), local_var_url_path)
      }

      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "market:partner-api"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "GetPricesResponse", loadNamespace("ympa_r_client")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Просмотр цен на указанные товары в магазине
    #'
    #' @description
    #' Просмотр цен на указанные товары в магазине
    #'
    #' @param campaign_id Идентификатор кампании в API и магазина в кабинете. Каждая кампания в API соответствует магазину в кабинете.  Чтобы узнать идентификаторы своих магазинов, воспользуйтесь запросом [GET campaigns](../../reference/campaigns/getCampaigns.md).  ℹ️ [Что такое кабинет и магазин на Маркете](https://yandex.ru/support/marketplace/account/introduction.html) 
    #' @param page_token (optional) Идентификатор страницы c результатами.  Если параметр не указан, возвращается первая страница.  Рекомендуется передавать значение выходного параметра `nextPageToken`, полученное при последнем запросе.  Если задан `page_token` и в запросе есть параметры `offset`, `page_number` и `page_size`, они игнорируются. 
    #' @param limit (optional) Количество значений на одной странице. 
    #' @param get_prices_by_offer_ids_request (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return GetPricesByOfferIdsResponse
    #' @export
    GetPricesByOfferIds = function(campaign_id, page_token = NULL, limit = NULL, get_prices_by_offer_ids_request = NULL, data_file = NULL, ...) {
      local_var_response <- self$GetPricesByOfferIdsWithHttpInfo(campaign_id, page_token, limit, get_prices_by_offer_ids_request, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Просмотр цен на указанные товары в магазине
    #'
    #' @description
    #' Просмотр цен на указанные товары в магазине
    #'
    #' @param campaign_id Идентификатор кампании в API и магазина в кабинете. Каждая кампания в API соответствует магазину в кабинете.  Чтобы узнать идентификаторы своих магазинов, воспользуйтесь запросом [GET campaigns](../../reference/campaigns/getCampaigns.md).  ℹ️ [Что такое кабинет и магазин на Маркете](https://yandex.ru/support/marketplace/account/introduction.html) 
    #' @param page_token (optional) Идентификатор страницы c результатами.  Если параметр не указан, возвращается первая страница.  Рекомендуется передавать значение выходного параметра `nextPageToken`, полученное при последнем запросе.  Если задан `page_token` и в запросе есть параметры `offset`, `page_number` и `page_size`, они игнорируются. 
    #' @param limit (optional) Количество значений на одной странице. 
    #' @param get_prices_by_offer_ids_request (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (GetPricesByOfferIdsResponse) with additional information such as HTTP status code, headers
    #' @export
    GetPricesByOfferIdsWithHttpInfo = function(campaign_id, page_token = NULL, limit = NULL, get_prices_by_offer_ids_request = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`campaign_id`)) {
        stop("Missing required parameter `campaign_id`.")
      }





      query_params[["page_token"]] <- `page_token`

      query_params[["limit"]] <- `limit`

      if (!is.null(`get_prices_by_offer_ids_request`)) {
        local_var_body <- `get_prices_by_offer_ids_request`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/campaigns/{campaignId}/offer-prices"
      if (!missing(`campaign_id`)) {
        local_var_url_path <- gsub("\\{campaignId\\}", URLencode(as.character(`campaign_id`), reserved = TRUE), local_var_url_path)
      }

      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "market:partner-api"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "GetPricesByOfferIdsResponse", loadNamespace("ympa_r_client")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Цены для продвижения товаров
    #'
    #' @description
    #' Цены для продвижения товаров
    #'
    #' @param campaign_id Идентификатор кампании в API и магазина в кабинете. Каждая кампания в API соответствует магазину в кабинете.  Чтобы узнать идентификаторы своих магазинов, воспользуйтесь запросом [GET campaigns](../../reference/campaigns/getCampaigns.md).  ℹ️ [Что такое кабинет и магазин на Маркете](https://yandex.ru/support/marketplace/account/introduction.html) 
    #' @param suggest_prices_request 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return SuggestPricesResponse
    #' @export
    GetSuggestedPrices = function(campaign_id, suggest_prices_request, data_file = NULL, ...) {
      local_var_response <- self$GetSuggestedPricesWithHttpInfo(campaign_id, suggest_prices_request, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Цены для продвижения товаров
    #'
    #' @description
    #' Цены для продвижения товаров
    #'
    #' @param campaign_id Идентификатор кампании в API и магазина в кабинете. Каждая кампания в API соответствует магазину в кабинете.  Чтобы узнать идентификаторы своих магазинов, воспользуйтесь запросом [GET campaigns](../../reference/campaigns/getCampaigns.md).  ℹ️ [Что такое кабинет и магазин на Маркете](https://yandex.ru/support/marketplace/account/introduction.html) 
    #' @param suggest_prices_request 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (SuggestPricesResponse) with additional information such as HTTP status code, headers
    #' @export
    GetSuggestedPricesWithHttpInfo = function(campaign_id, suggest_prices_request, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`campaign_id`)) {
        stop("Missing required parameter `campaign_id`.")
      }

      if (missing(`suggest_prices_request`)) {
        stop("Missing required parameter `suggest_prices_request`.")
      }



      if (!is.null(`suggest_prices_request`)) {
        local_var_body <- `suggest_prices_request`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/campaigns/{campaignId}/offer-prices/suggestions"
      if (!missing(`campaign_id`)) {
        local_var_url_path <- gsub("\\{campaignId\\}", URLencode(as.character(`campaign_id`), reserved = TRUE), local_var_url_path)
      }

      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "market:partner-api"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "SuggestPricesResponse", loadNamespace("ympa_r_client")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Установка цен на товары во всех магазинах
    #'
    #' @description
    #' Установка цен на товары во всех магазинах
    #'
    #' @param business_id Идентификатор кабинета. Чтобы узнать идентификатор, воспользуйтесь запросом [GET campaigns](../../reference/campaigns/getCampaigns.md#businessdto).  ℹ️ [Что такое кабинет и магазин на Маркете](https://yandex.ru/support/marketplace/account/introduction.html) 
    #' @param update_business_prices_request 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return EmptyApiResponse
    #' @export
    UpdateBusinessPrices = function(business_id, update_business_prices_request, data_file = NULL, ...) {
      local_var_response <- self$UpdateBusinessPricesWithHttpInfo(business_id, update_business_prices_request, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Установка цен на товары во всех магазинах
    #'
    #' @description
    #' Установка цен на товары во всех магазинах
    #'
    #' @param business_id Идентификатор кабинета. Чтобы узнать идентификатор, воспользуйтесь запросом [GET campaigns](../../reference/campaigns/getCampaigns.md#businessdto).  ℹ️ [Что такое кабинет и магазин на Маркете](https://yandex.ru/support/marketplace/account/introduction.html) 
    #' @param update_business_prices_request 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (EmptyApiResponse) with additional information such as HTTP status code, headers
    #' @export
    UpdateBusinessPricesWithHttpInfo = function(business_id, update_business_prices_request, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`business_id`)) {
        stop("Missing required parameter `business_id`.")
      }

      if (missing(`update_business_prices_request`)) {
        stop("Missing required parameter `update_business_prices_request`.")
      }



      if (!is.null(`update_business_prices_request`)) {
        local_var_body <- `update_business_prices_request`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/businesses/{businessId}/offer-prices/updates"
      if (!missing(`business_id`)) {
        local_var_url_path <- gsub("\\{businessId\\}", URLencode(as.character(`business_id`), reserved = TRUE), local_var_url_path)
      }

      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "market:partner-api"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "EmptyApiResponse", loadNamespace("ympa_r_client")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Установка цен на товары в конкретном магазине
    #'
    #' @description
    #' Установка цен на товары в конкретном магазине
    #'
    #' @param campaign_id Идентификатор кампании в API и магазина в кабинете. Каждая кампания в API соответствует магазину в кабинете.  Чтобы узнать идентификаторы своих магазинов, воспользуйтесь запросом [GET campaigns](../../reference/campaigns/getCampaigns.md).  ℹ️ [Что такое кабинет и магазин на Маркете](https://yandex.ru/support/marketplace/account/introduction.html) 
    #' @param update_prices_request 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return EmptyApiResponse
    #' @export
    UpdatePrices = function(campaign_id, update_prices_request, data_file = NULL, ...) {
      local_var_response <- self$UpdatePricesWithHttpInfo(campaign_id, update_prices_request, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Установка цен на товары в конкретном магазине
    #'
    #' @description
    #' Установка цен на товары в конкретном магазине
    #'
    #' @param campaign_id Идентификатор кампании в API и магазина в кабинете. Каждая кампания в API соответствует магазину в кабинете.  Чтобы узнать идентификаторы своих магазинов, воспользуйтесь запросом [GET campaigns](../../reference/campaigns/getCampaigns.md).  ℹ️ [Что такое кабинет и магазин на Маркете](https://yandex.ru/support/marketplace/account/introduction.html) 
    #' @param update_prices_request 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (EmptyApiResponse) with additional information such as HTTP status code, headers
    #' @export
    UpdatePricesWithHttpInfo = function(campaign_id, update_prices_request, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`campaign_id`)) {
        stop("Missing required parameter `campaign_id`.")
      }

      if (missing(`update_prices_request`)) {
        stop("Missing required parameter `update_prices_request`.")
      }



      if (!is.null(`update_prices_request`)) {
        local_var_body <- `update_prices_request`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/campaigns/{campaignId}/offer-prices/updates"
      if (!missing(`campaign_id`)) {
        local_var_url_path <- gsub("\\{campaignId\\}", URLencode(as.character(`campaign_id`), reserved = TRUE), local_var_url_path)
      }

      # OAuth-related settings
      is_oauth <- TRUE
      oauth_scopes <- "market:partner-api"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "EmptyApiResponse", loadNamespace("ympa_r_client")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    }
  )
)
