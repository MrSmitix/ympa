--
-- Партнерский API Маркета.
-- Prepared SQL queries for 'EnrichedModelsDTO' definition.
--


--
-- SELECT template for table `EnrichedModelsDTO`
--
SELECT `models` FROM `EnrichedModelsDTO` WHERE 1;

--
-- INSERT template for table `EnrichedModelsDTO`
--
INSERT INTO `EnrichedModelsDTO`(`models`) VALUES (?);

--
-- UPDATE template for table `EnrichedModelsDTO`
--
UPDATE `EnrichedModelsDTO` SET `models` = ? WHERE 1;

--
-- DELETE template for table `EnrichedModelsDTO`
--
DELETE FROM `EnrichedModelsDTO` WHERE 0;

