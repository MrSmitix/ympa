/**
* Партнерский API Маркета
* API Яндекс Маркета помогает продавцам автоматизировать и упростить работу с маркетплейсом.  В числе возможностей интеграции:  * управление каталогом товаров и витриной,  * обработка заказов,  * изменение настроек магазина,  * получение отчетов. 
*
* The version of the OpenAPI document: LATEST
* 
*
* NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
* https://openapi-generator.tech
* Do not edit the class manually.
*/

#include "OrderDeliveryApi.h"
#include "Helpers.h"

namespace org::openapitools::server::api
{

using namespace org::openapitools::server::helpers;
using namespace org::openapitools::server::model;

const std::string OrderDeliveryApi::base = "";

OrderDeliveryApi::OrderDeliveryApi(const std::shared_ptr<Pistache::Rest::Router>& rtr)
    : ApiBase(rtr)
{
}

void OrderDeliveryApi::init() {
    setupRoutes();
}

void OrderDeliveryApi::setupRoutes() {
    using namespace Pistache::Rest;

    Routes::Get(*router, base + "/campaigns/:campaignId/orders/:orderId/buyer", Routes::bind(&OrderDeliveryApi::get_order_buyer_info_handler, this));
    Routes::Put(*router, base + "/campaigns/:campaignId/orders/:orderId/delivery/date", Routes::bind(&OrderDeliveryApi::set_order_delivery_date_handler, this));
    Routes::Post(*router, base + "/campaigns/:campaignId/orders/:orderId/delivery/track", Routes::bind(&OrderDeliveryApi::set_order_delivery_track_code_handler, this));
    Routes::Put(*router, base + "/campaigns/:campaignId/orders/:orderId/delivery/storage-limit", Routes::bind(&OrderDeliveryApi::update_order_storage_limit_handler, this));
    Routes::Put(*router, base + "/campaigns/:campaignId/orders/:orderId/verifyEac", Routes::bind(&OrderDeliveryApi::verify_order_eac_handler, this));

    // Default handler, called when a route is not found
    router->addCustomHandler(Routes::bind(&OrderDeliveryApi::order_delivery_api_default_handler, this));
}

void OrderDeliveryApi::handleParsingException(const std::exception& ex, Pistache::Http::ResponseWriter &response) const noexcept
{
    std::pair<Pistache::Http::Code, std::string> codeAndError = handleParsingException(ex);
    response.send(codeAndError.first, codeAndError.second);
}

std::pair<Pistache::Http::Code, std::string> OrderDeliveryApi::handleParsingException(const std::exception& ex) const noexcept
{
    try {
        throw;
    } catch (nlohmann::detail::exception &e) {
        return std::make_pair(Pistache::Http::Code::Bad_Request, e.what());
    } catch (org::openapitools::server::helpers::ValidationException &e) {
        return std::make_pair(Pistache::Http::Code::Bad_Request, e.what());
    } catch (std::exception &e) {
        return std::make_pair(Pistache::Http::Code::Internal_Server_Error, e.what());
    }
}

void OrderDeliveryApi::handleOperationException(const std::exception& ex, Pistache::Http::ResponseWriter &response) const noexcept
{
    std::pair<Pistache::Http::Code, std::string> codeAndError = handleOperationException(ex);
    response.send(codeAndError.first, codeAndError.second);
}

std::pair<Pistache::Http::Code, std::string> OrderDeliveryApi::handleOperationException(const std::exception& ex) const noexcept
{
    return std::make_pair(Pistache::Http::Code::Internal_Server_Error, ex.what());
}

void OrderDeliveryApi::get_order_buyer_info_handler(const Pistache::Rest::Request &request, Pistache::Http::ResponseWriter response) {
    try {

    // Getting the path params
    auto campaignId = request.param(":campaignId").as<int64_t>();
    auto orderId = request.param(":orderId").as<int64_t>();
    
    try {
        this->get_order_buyer_info(campaignId, orderId, response);
    } catch (Pistache::Http::HttpError &e) {
        response.send(static_cast<Pistache::Http::Code>(e.code()), e.what());
        return;
    } catch (std::exception &e) {
        this->handleOperationException(e, response);
        return;
    }

    } catch (std::exception &e) {
        response.send(Pistache::Http::Code::Internal_Server_Error, e.what());
    }

}
void OrderDeliveryApi::set_order_delivery_date_handler(const Pistache::Rest::Request &request, Pistache::Http::ResponseWriter response) {
    try {

    // Getting the path params
    auto campaignId = request.param(":campaignId").as<int64_t>();
    auto orderId = request.param(":orderId").as<int64_t>();
    
    // Getting the body param
    
    SetOrderDeliveryDateRequest setOrderDeliveryDateRequest;
    
    try {
        nlohmann::json::parse(request.body()).get_to(setOrderDeliveryDateRequest);
        setOrderDeliveryDateRequest.validate();
    } catch (std::exception &e) {
        this->handleParsingException(e, response);
        return;
    }

    try {
        this->set_order_delivery_date(campaignId, orderId, setOrderDeliveryDateRequest, response);
    } catch (Pistache::Http::HttpError &e) {
        response.send(static_cast<Pistache::Http::Code>(e.code()), e.what());
        return;
    } catch (std::exception &e) {
        this->handleOperationException(e, response);
        return;
    }

    } catch (std::exception &e) {
        response.send(Pistache::Http::Code::Internal_Server_Error, e.what());
    }

}
void OrderDeliveryApi::set_order_delivery_track_code_handler(const Pistache::Rest::Request &request, Pistache::Http::ResponseWriter response) {
    try {

    // Getting the path params
    auto campaignId = request.param(":campaignId").as<int64_t>();
    auto orderId = request.param(":orderId").as<int64_t>();
    
    // Getting the body param
    
    SetOrderDeliveryTrackCodeRequest setOrderDeliveryTrackCodeRequest;
    
    try {
        nlohmann::json::parse(request.body()).get_to(setOrderDeliveryTrackCodeRequest);
        setOrderDeliveryTrackCodeRequest.validate();
    } catch (std::exception &e) {
        this->handleParsingException(e, response);
        return;
    }

    try {
        this->set_order_delivery_track_code(campaignId, orderId, setOrderDeliveryTrackCodeRequest, response);
    } catch (Pistache::Http::HttpError &e) {
        response.send(static_cast<Pistache::Http::Code>(e.code()), e.what());
        return;
    } catch (std::exception &e) {
        this->handleOperationException(e, response);
        return;
    }

    } catch (std::exception &e) {
        response.send(Pistache::Http::Code::Internal_Server_Error, e.what());
    }

}
void OrderDeliveryApi::update_order_storage_limit_handler(const Pistache::Rest::Request &request, Pistache::Http::ResponseWriter response) {
    try {

    // Getting the path params
    auto campaignId = request.param(":campaignId").as<int64_t>();
    auto orderId = request.param(":orderId").as<int64_t>();
    
    // Getting the body param
    
    UpdateOrderStorageLimitRequest updateOrderStorageLimitRequest;
    
    try {
        nlohmann::json::parse(request.body()).get_to(updateOrderStorageLimitRequest);
        updateOrderStorageLimitRequest.validate();
    } catch (std::exception &e) {
        this->handleParsingException(e, response);
        return;
    }

    try {
        this->update_order_storage_limit(campaignId, orderId, updateOrderStorageLimitRequest, response);
    } catch (Pistache::Http::HttpError &e) {
        response.send(static_cast<Pistache::Http::Code>(e.code()), e.what());
        return;
    } catch (std::exception &e) {
        this->handleOperationException(e, response);
        return;
    }

    } catch (std::exception &e) {
        response.send(Pistache::Http::Code::Internal_Server_Error, e.what());
    }

}
void OrderDeliveryApi::verify_order_eac_handler(const Pistache::Rest::Request &request, Pistache::Http::ResponseWriter response) {
    try {

    // Getting the path params
    auto campaignId = request.param(":campaignId").as<int64_t>();
    auto orderId = request.param(":orderId").as<int64_t>();
    
    // Getting the body param
    
    VerifyOrderEacRequest verifyOrderEacRequest;
    
    try {
        nlohmann::json::parse(request.body()).get_to(verifyOrderEacRequest);
        verifyOrderEacRequest.validate();
    } catch (std::exception &e) {
        this->handleParsingException(e, response);
        return;
    }

    try {
        this->verify_order_eac(campaignId, orderId, verifyOrderEacRequest, response);
    } catch (Pistache::Http::HttpError &e) {
        response.send(static_cast<Pistache::Http::Code>(e.code()), e.what());
        return;
    } catch (std::exception &e) {
        this->handleOperationException(e, response);
        return;
    }

    } catch (std::exception &e) {
        response.send(Pistache::Http::Code::Internal_Server_Error, e.what());
    }

}

void OrderDeliveryApi::order_delivery_api_default_handler(const Pistache::Rest::Request &, Pistache::Http::ResponseWriter response) {
    response.send(Pistache::Http::Code::Not_Found, "The requested method does not exist");
}

} // namespace org::openapitools::server::api

