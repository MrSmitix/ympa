/**
 * Партнерский API Маркета
 * API Яндекс Маркета помогает продавцам автоматизировать и упростить работу с маркетплейсом.  В числе возможностей интеграции:  * управление каталогом товаров и витриной,  * обработка заказов,  * изменение настроек магазина,  * получение отчетов. 
 *
 * The version of the OpenAPI document: LATEST
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import HiddenOfferDTO from './HiddenOfferDTO';
import ScrollingPagerDTO from './ScrollingPagerDTO';

/**
 * The GetHiddenOffersResultDTO model module.
 * @module model/GetHiddenOffersResultDTO
 * @version LATEST
 */
class GetHiddenOffersResultDTO {
    /**
     * Constructs a new <code>GetHiddenOffersResultDTO</code>.
     * Список скрытых вами товаров. 
     * @alias module:model/GetHiddenOffersResultDTO
     * @param hiddenOffers {Array.<module:model/HiddenOfferDTO>} Список скрытых товаров.
     */
    constructor(hiddenOffers) { 
        
        GetHiddenOffersResultDTO.initialize(this, hiddenOffers);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, hiddenOffers) { 
        obj['hiddenOffers'] = hiddenOffers;
    }

    /**
     * Constructs a <code>GetHiddenOffersResultDTO</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/GetHiddenOffersResultDTO} obj Optional instance to populate.
     * @return {module:model/GetHiddenOffersResultDTO} The populated <code>GetHiddenOffersResultDTO</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new GetHiddenOffersResultDTO();

            if (data.hasOwnProperty('paging')) {
                obj['paging'] = ScrollingPagerDTO.constructFromObject(data['paging']);
            }
            if (data.hasOwnProperty('hiddenOffers')) {
                obj['hiddenOffers'] = ApiClient.convertToType(data['hiddenOffers'], [HiddenOfferDTO]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>GetHiddenOffersResultDTO</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>GetHiddenOffersResultDTO</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of GetHiddenOffersResultDTO.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // validate the optional field `paging`
        if (data['paging']) { // data not null
          ScrollingPagerDTO.validateJSON(data['paging']);
        }
        if (data['hiddenOffers']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['hiddenOffers'])) {
                throw new Error("Expected the field `hiddenOffers` to be an array in the JSON data but got " + data['hiddenOffers']);
            }
            // validate the optional field `hiddenOffers` (array)
            for (const item of data['hiddenOffers']) {
                HiddenOfferDTO.validateJSON(item);
            };
        }

        return true;
    }


}

GetHiddenOffersResultDTO.RequiredProperties = ["hiddenOffers"];

/**
 * @member {module:model/ScrollingPagerDTO} paging
 */
GetHiddenOffersResultDTO.prototype['paging'] = undefined;

/**
 * Список скрытых товаров.
 * @member {Array.<module:model/HiddenOfferDTO>} hiddenOffers
 */
GetHiddenOffersResultDTO.prototype['hiddenOffers'] = undefined;






export default GetHiddenOffersResultDTO;

