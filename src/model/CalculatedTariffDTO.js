/**
 * Партнерский API Маркета
 * API Яндекс Маркета помогает продавцам автоматизировать и упростить работу с маркетплейсом.  В числе возможностей интеграции:  * управление каталогом товаров и витриной,  * обработка заказов,  * изменение настроек магазина,  * получение отчетов. 
 *
 * The version of the OpenAPI document: LATEST
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import CalculatedTariffType from './CalculatedTariffType';
import TariffParameterDTO from './TariffParameterDTO';

/**
 * The CalculatedTariffDTO model module.
 * @module model/CalculatedTariffDTO
 * @version LATEST
 */
class CalculatedTariffDTO {
    /**
     * Constructs a new <code>CalculatedTariffDTO</code>.
     * Информация об услугах Маркета.
     * @alias module:model/CalculatedTariffDTO
     * @param type {module:model/CalculatedTariffType} 
     * @param parameters {Array.<module:model/TariffParameterDTO>} Параметры расчета тарифа.
     */
    constructor(type, parameters) { 
        
        CalculatedTariffDTO.initialize(this, type, parameters);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, type, parameters) { 
        obj['type'] = type;
        obj['parameters'] = parameters;
    }

    /**
     * Constructs a <code>CalculatedTariffDTO</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/CalculatedTariffDTO} obj Optional instance to populate.
     * @return {module:model/CalculatedTariffDTO} The populated <code>CalculatedTariffDTO</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new CalculatedTariffDTO();

            if (data.hasOwnProperty('type')) {
                obj['type'] = CalculatedTariffType.constructFromObject(data['type']);
            }
            if (data.hasOwnProperty('amount')) {
                obj['amount'] = ApiClient.convertToType(data['amount'], 'Number');
            }
            if (data.hasOwnProperty('parameters')) {
                obj['parameters'] = ApiClient.convertToType(data['parameters'], [TariffParameterDTO]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>CalculatedTariffDTO</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>CalculatedTariffDTO</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of CalculatedTariffDTO.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['parameters']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['parameters'])) {
                throw new Error("Expected the field `parameters` to be an array in the JSON data but got " + data['parameters']);
            }
            // validate the optional field `parameters` (array)
            for (const item of data['parameters']) {
                TariffParameterDTO.validateJSON(item);
            };
        }

        return true;
    }


}

CalculatedTariffDTO.RequiredProperties = ["type", "parameters"];

/**
 * @member {module:model/CalculatedTariffType} type
 */
CalculatedTariffDTO.prototype['type'] = undefined;

/**
 * Стоимость услуги в рублях.
 * @member {Number} amount
 */
CalculatedTariffDTO.prototype['amount'] = undefined;

/**
 * Параметры расчета тарифа.
 * @member {Array.<module:model/TariffParameterDTO>} parameters
 */
CalculatedTariffDTO.prototype['parameters'] = undefined;






export default CalculatedTariffDTO;

