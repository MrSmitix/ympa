/**
 * Партнерский API Маркета
 * API Яндекс Маркета помогает продавцам автоматизировать и упростить работу с маркетплейсом.  В числе возможностей интеграции:  * управление каталогом товаров и витриной,  * обработка заказов,  * изменение настроек магазина,  * получение отчетов. 
 *
 * The version of the OpenAPI document: LATEST
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import GetPromoDTO from './GetPromoDTO';

/**
 * The GetPromosResultDTO model module.
 * @module model/GetPromosResultDTO
 * @version LATEST
 */
class GetPromosResultDTO {
    /**
     * Constructs a new <code>GetPromosResultDTO</code>.
     * Информация об акциях Маркета.
     * @alias module:model/GetPromosResultDTO
     * @param promos {Array.<module:model/GetPromoDTO>} Акции Маркета.
     */
    constructor(promos) { 
        
        GetPromosResultDTO.initialize(this, promos);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, promos) { 
        obj['promos'] = promos;
    }

    /**
     * Constructs a <code>GetPromosResultDTO</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/GetPromosResultDTO} obj Optional instance to populate.
     * @return {module:model/GetPromosResultDTO} The populated <code>GetPromosResultDTO</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new GetPromosResultDTO();

            if (data.hasOwnProperty('promos')) {
                obj['promos'] = ApiClient.convertToType(data['promos'], [GetPromoDTO]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>GetPromosResultDTO</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>GetPromosResultDTO</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of GetPromosResultDTO.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['promos']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['promos'])) {
                throw new Error("Expected the field `promos` to be an array in the JSON data but got " + data['promos']);
            }
            // validate the optional field `promos` (array)
            for (const item of data['promos']) {
                GetPromoDTO.validateJSON(item);
            };
        }

        return true;
    }


}

GetPromosResultDTO.RequiredProperties = ["promos"];

/**
 * Акции Маркета.
 * @member {Array.<module:model/GetPromoDTO>} promos
 */
GetPromosResultDTO.prototype['promos'] = undefined;






export default GetPromosResultDTO;

