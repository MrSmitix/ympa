/**
 * Партнерский API Маркета
 * API Яндекс Маркета помогает продавцам автоматизировать и упростить работу с маркетплейсом.  В числе возможностей интеграции:  * управление каталогом товаров и витриной,  * обработка заказов,  * изменение настроек магазина,  * получение отчетов. 
 *
 * The version of the OpenAPI document: LATEST
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import UpdatePromoOfferDTO from './UpdatePromoOfferDTO';

/**
 * The UpdatePromoOffersRequest model module.
 * @module model/UpdatePromoOffersRequest
 * @version LATEST
 */
class UpdatePromoOffersRequest {
    /**
     * Constructs a new <code>UpdatePromoOffersRequest</code>.
     * Добавление товаров в акцию или обновление их параметров.  Чтобы добавить товары в акцию или обновить параметры каких-то товаров, передайте их в параметре &#x60;offers&#x60;. 
     * @alias module:model/UpdatePromoOffersRequest
     * @param promoId {String} Идентификатор акции.
     * @param offers {Array.<module:model/UpdatePromoOfferDTO>} Товары, которые необходимо добавить в акцию или цены которых нужно изменить.
     */
    constructor(promoId, offers) { 
        
        UpdatePromoOffersRequest.initialize(this, promoId, offers);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, promoId, offers) { 
        obj['promoId'] = promoId;
        obj['offers'] = offers;
    }

    /**
     * Constructs a <code>UpdatePromoOffersRequest</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/UpdatePromoOffersRequest} obj Optional instance to populate.
     * @return {module:model/UpdatePromoOffersRequest} The populated <code>UpdatePromoOffersRequest</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new UpdatePromoOffersRequest();

            if (data.hasOwnProperty('promoId')) {
                obj['promoId'] = ApiClient.convertToType(data['promoId'], 'String');
            }
            if (data.hasOwnProperty('offers')) {
                obj['offers'] = ApiClient.convertToType(data['offers'], [UpdatePromoOfferDTO]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>UpdatePromoOffersRequest</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>UpdatePromoOffersRequest</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of UpdatePromoOffersRequest.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['promoId'] && !(typeof data['promoId'] === 'string' || data['promoId'] instanceof String)) {
            throw new Error("Expected the field `promoId` to be a primitive type in the JSON string but got " + data['promoId']);
        }
        if (data['offers']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['offers'])) {
                throw new Error("Expected the field `offers` to be an array in the JSON data but got " + data['offers']);
            }
            // validate the optional field `offers` (array)
            for (const item of data['offers']) {
                UpdatePromoOfferDTO.validateJSON(item);
            };
        }

        return true;
    }


}

UpdatePromoOffersRequest.RequiredProperties = ["promoId", "offers"];

/**
 * Идентификатор акции.
 * @member {String} promoId
 */
UpdatePromoOffersRequest.prototype['promoId'] = undefined;

/**
 * Товары, которые необходимо добавить в акцию или цены которых нужно изменить.
 * @member {Array.<module:model/UpdatePromoOfferDTO>} offers
 */
UpdatePromoOffersRequest.prototype['offers'] = undefined;






export default UpdatePromoOffersRequest;

