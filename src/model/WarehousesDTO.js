/**
 * Партнерский API Маркета
 * API Яндекс Маркета помогает продавцам автоматизировать и упростить работу с маркетплейсом.  В числе возможностей интеграции:  * управление каталогом товаров и витриной,  * обработка заказов,  * изменение настроек магазина,  * получение отчетов. 
 *
 * The version of the OpenAPI document: LATEST
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import WarehouseDTO from './WarehouseDTO';
import WarehouseGroupDTO from './WarehouseGroupDTO';

/**
 * The WarehousesDTO model module.
 * @module model/WarehousesDTO
 * @version LATEST
 */
class WarehousesDTO {
    /**
     * Constructs a new <code>WarehousesDTO</code>.
     * Информация о складах и группах складов.
     * @alias module:model/WarehousesDTO
     * @param warehouses {Array.<module:model/WarehouseDTO>} Список складов, не входящих в группы.
     * @param warehouseGroups {Array.<module:model/WarehouseGroupDTO>} Список групп складов.
     */
    constructor(warehouses, warehouseGroups) { 
        
        WarehousesDTO.initialize(this, warehouses, warehouseGroups);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, warehouses, warehouseGroups) { 
        obj['warehouses'] = warehouses;
        obj['warehouseGroups'] = warehouseGroups;
    }

    /**
     * Constructs a <code>WarehousesDTO</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/WarehousesDTO} obj Optional instance to populate.
     * @return {module:model/WarehousesDTO} The populated <code>WarehousesDTO</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new WarehousesDTO();

            if (data.hasOwnProperty('warehouses')) {
                obj['warehouses'] = ApiClient.convertToType(data['warehouses'], [WarehouseDTO]);
            }
            if (data.hasOwnProperty('warehouseGroups')) {
                obj['warehouseGroups'] = ApiClient.convertToType(data['warehouseGroups'], [WarehouseGroupDTO]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>WarehousesDTO</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>WarehousesDTO</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of WarehousesDTO.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['warehouses']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['warehouses'])) {
                throw new Error("Expected the field `warehouses` to be an array in the JSON data but got " + data['warehouses']);
            }
            // validate the optional field `warehouses` (array)
            for (const item of data['warehouses']) {
                WarehouseDTO.validateJSON(item);
            };
        }
        if (data['warehouseGroups']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['warehouseGroups'])) {
                throw new Error("Expected the field `warehouseGroups` to be an array in the JSON data but got " + data['warehouseGroups']);
            }
            // validate the optional field `warehouseGroups` (array)
            for (const item of data['warehouseGroups']) {
                WarehouseGroupDTO.validateJSON(item);
            };
        }

        return true;
    }


}

WarehousesDTO.RequiredProperties = ["warehouses", "warehouseGroups"];

/**
 * Список складов, не входящих в группы.
 * @member {Array.<module:model/WarehouseDTO>} warehouses
 */
WarehousesDTO.prototype['warehouses'] = undefined;

/**
 * Список групп складов.
 * @member {Array.<module:model/WarehouseGroupDTO>} warehouseGroups
 */
WarehousesDTO.prototype['warehouseGroups'] = undefined;






export default WarehousesDTO;

