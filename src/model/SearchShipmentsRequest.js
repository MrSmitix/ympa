/**
 * Партнерский API Маркета
 * API Яндекс Маркета помогает продавцам автоматизировать и упростить работу с маркетплейсом.  В числе возможностей интеграции:  * управление каталогом товаров и витриной,  * обработка заказов,  * изменение настроек магазина,  * получение отчетов. 
 *
 * The version of the OpenAPI document: LATEST
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import ShipmentStatusType from './ShipmentStatusType';

/**
 * The SearchShipmentsRequest model module.
 * @module model/SearchShipmentsRequest
 * @version LATEST
 */
class SearchShipmentsRequest {
    /**
     * Constructs a new <code>SearchShipmentsRequest</code>.
     * Запрос информации об отгрузках.
     * @alias module:model/SearchShipmentsRequest
     * @param dateFrom {Date} Начальная дата для фильтрации по дате отгрузки (включительно).  Формат даты: `ДД-ММ-ГГГГ`. 
     * @param dateTo {Date} Конечная дата для фильтрации по дате отгрузки (включительно).  Формат даты: `ДД-ММ-ГГГГ`. 
     */
    constructor(dateFrom, dateTo) { 
        
        SearchShipmentsRequest.initialize(this, dateFrom, dateTo);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, dateFrom, dateTo) { 
        obj['dateFrom'] = dateFrom;
        obj['dateTo'] = dateTo;
    }

    /**
     * Constructs a <code>SearchShipmentsRequest</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/SearchShipmentsRequest} obj Optional instance to populate.
     * @return {module:model/SearchShipmentsRequest} The populated <code>SearchShipmentsRequest</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new SearchShipmentsRequest();

            if (data.hasOwnProperty('dateFrom')) {
                obj['dateFrom'] = ApiClient.convertToType(data['dateFrom'], 'Date');
            }
            if (data.hasOwnProperty('dateTo')) {
                obj['dateTo'] = ApiClient.convertToType(data['dateTo'], 'Date');
            }
            if (data.hasOwnProperty('statuses')) {
                obj['statuses'] = ApiClient.convertToType(data['statuses'], [ShipmentStatusType]);
            }
            if (data.hasOwnProperty('orderIds')) {
                obj['orderIds'] = ApiClient.convertToType(data['orderIds'], ['Number']);
            }
            if (data.hasOwnProperty('cancelledOrders')) {
                obj['cancelledOrders'] = ApiClient.convertToType(data['cancelledOrders'], 'Boolean');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>SearchShipmentsRequest</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>SearchShipmentsRequest</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of SearchShipmentsRequest.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is an array
        if (!Array.isArray(data['statuses'])) {
            throw new Error("Expected the field `statuses` to be an array in the JSON data but got " + data['statuses']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['orderIds'])) {
            throw new Error("Expected the field `orderIds` to be an array in the JSON data but got " + data['orderIds']);
        }

        return true;
    }


}

SearchShipmentsRequest.RequiredProperties = ["dateFrom", "dateTo"];

/**
 * Начальная дата для фильтрации по дате отгрузки (включительно).  Формат даты: `ДД-ММ-ГГГГ`. 
 * @member {Date} dateFrom
 */
SearchShipmentsRequest.prototype['dateFrom'] = undefined;

/**
 * Конечная дата для фильтрации по дате отгрузки (включительно).  Формат даты: `ДД-ММ-ГГГГ`. 
 * @member {Date} dateTo
 */
SearchShipmentsRequest.prototype['dateTo'] = undefined;

/**
 * Список статусов отгрузок.
 * @member {Array.<module:model/ShipmentStatusType>} statuses
 */
SearchShipmentsRequest.prototype['statuses'] = undefined;

/**
 * Список идентификаторов заказов из отгрузок.
 * @member {Array.<Number>} orderIds
 */
SearchShipmentsRequest.prototype['orderIds'] = undefined;

/**
 * Возвращать ли отмененные заказы.  Значение по умолчанию — `true`. Если возвращать отмененные заказы не нужно, передайте значение `false`. 
 * @member {Boolean} cancelledOrders
 * @default true
 */
SearchShipmentsRequest.prototype['cancelledOrders'] = true;






export default SearchShipmentsRequest;

