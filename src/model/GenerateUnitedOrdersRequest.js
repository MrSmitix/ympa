/**
 * Партнерский API Маркета
 * API Яндекс Маркета помогает продавцам автоматизировать и упростить работу с маркетплейсом.  В числе возможностей интеграции:  * управление каталогом товаров и витриной,  * обработка заказов,  * изменение настроек магазина,  * получение отчетов. 
 *
 * The version of the OpenAPI document: LATEST
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The GenerateUnitedOrdersRequest model module.
 * @module model/GenerateUnitedOrdersRequest
 * @version LATEST
 */
class GenerateUnitedOrdersRequest {
    /**
     * Constructs a new <code>GenerateUnitedOrdersRequest</code>.
     * Данные, необходимые для генерации отчета. 
     * @alias module:model/GenerateUnitedOrdersRequest
     * @param businessId {Number} Идентификатор бизнеса.
     * @param dateFrom {Date} Начало периода, включительно.
     * @param dateTo {Date} Конец периода, включительно. Максимальный период — 1 год.
     */
    constructor(businessId, dateFrom, dateTo) { 
        
        GenerateUnitedOrdersRequest.initialize(this, businessId, dateFrom, dateTo);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, businessId, dateFrom, dateTo) { 
        obj['businessId'] = businessId;
        obj['dateFrom'] = dateFrom;
        obj['dateTo'] = dateTo;
    }

    /**
     * Constructs a <code>GenerateUnitedOrdersRequest</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/GenerateUnitedOrdersRequest} obj Optional instance to populate.
     * @return {module:model/GenerateUnitedOrdersRequest} The populated <code>GenerateUnitedOrdersRequest</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new GenerateUnitedOrdersRequest();

            if (data.hasOwnProperty('businessId')) {
                obj['businessId'] = ApiClient.convertToType(data['businessId'], 'Number');
            }
            if (data.hasOwnProperty('dateFrom')) {
                obj['dateFrom'] = ApiClient.convertToType(data['dateFrom'], 'Date');
            }
            if (data.hasOwnProperty('dateTo')) {
                obj['dateTo'] = ApiClient.convertToType(data['dateTo'], 'Date');
            }
            if (data.hasOwnProperty('campaignIds')) {
                obj['campaignIds'] = ApiClient.convertToType(data['campaignIds'], ['Number']);
            }
            if (data.hasOwnProperty('promoId')) {
                obj['promoId'] = ApiClient.convertToType(data['promoId'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>GenerateUnitedOrdersRequest</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>GenerateUnitedOrdersRequest</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of GenerateUnitedOrdersRequest.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is an array
        if (!Array.isArray(data['campaignIds'])) {
            throw new Error("Expected the field `campaignIds` to be an array in the JSON data but got " + data['campaignIds']);
        }
        // ensure the json data is a string
        if (data['promoId'] && !(typeof data['promoId'] === 'string' || data['promoId'] instanceof String)) {
            throw new Error("Expected the field `promoId` to be a primitive type in the JSON string but got " + data['promoId']);
        }

        return true;
    }


}

GenerateUnitedOrdersRequest.RequiredProperties = ["businessId", "dateFrom", "dateTo"];

/**
 * Идентификатор бизнеса.
 * @member {Number} businessId
 */
GenerateUnitedOrdersRequest.prototype['businessId'] = undefined;

/**
 * Начало периода, включительно.
 * @member {Date} dateFrom
 */
GenerateUnitedOrdersRequest.prototype['dateFrom'] = undefined;

/**
 * Конец периода, включительно. Максимальный период — 1 год.
 * @member {Date} dateTo
 */
GenerateUnitedOrdersRequest.prototype['dateTo'] = undefined;

/**
 * Список магазинов, которые нужны в отчете.
 * @member {Array.<Number>} campaignIds
 */
GenerateUnitedOrdersRequest.prototype['campaignIds'] = undefined;

/**
 * Идентификатор акции, товары из которой нужны в отчете.
 * @member {String} promoId
 */
GenerateUnitedOrdersRequest.prototype['promoId'] = undefined;






export default GenerateUnitedOrdersRequest;

