/**
 * Партнерский API Маркета
 * API Яндекс Маркета помогает продавцам автоматизировать и упростить работу с маркетплейсом.  В числе возможностей интеграции:  * управление каталогом товаров и витриной,  * обработка заказов,  * изменение настроек магазина,  * получение отчетов. 
 *
 * The version of the OpenAPI document: LATEST
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import CampaignQualityRatingDTO from './CampaignQualityRatingDTO';

/**
 * The CampaignsQualityRatingDTO model module.
 * @module model/CampaignsQualityRatingDTO
 * @version LATEST
 */
class CampaignsQualityRatingDTO {
    /**
     * Constructs a new <code>CampaignsQualityRatingDTO</code>.
     * Информация об индексе качества магазинов.
     * @alias module:model/CampaignsQualityRatingDTO
     * @param campaignRatings {Array.<module:model/CampaignQualityRatingDTO>} Список магазинов c информацией об их индексе качества.
     */
    constructor(campaignRatings) { 
        
        CampaignsQualityRatingDTO.initialize(this, campaignRatings);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, campaignRatings) { 
        obj['campaignRatings'] = campaignRatings;
    }

    /**
     * Constructs a <code>CampaignsQualityRatingDTO</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/CampaignsQualityRatingDTO} obj Optional instance to populate.
     * @return {module:model/CampaignsQualityRatingDTO} The populated <code>CampaignsQualityRatingDTO</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new CampaignsQualityRatingDTO();

            if (data.hasOwnProperty('campaignRatings')) {
                obj['campaignRatings'] = ApiClient.convertToType(data['campaignRatings'], [CampaignQualityRatingDTO]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>CampaignsQualityRatingDTO</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>CampaignsQualityRatingDTO</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of CampaignsQualityRatingDTO.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['campaignRatings']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['campaignRatings'])) {
                throw new Error("Expected the field `campaignRatings` to be an array in the JSON data but got " + data['campaignRatings']);
            }
            // validate the optional field `campaignRatings` (array)
            for (const item of data['campaignRatings']) {
                CampaignQualityRatingDTO.validateJSON(item);
            };
        }

        return true;
    }


}

CampaignsQualityRatingDTO.RequiredProperties = ["campaignRatings"];

/**
 * Список магазинов c информацией об их индексе качества.
 * @member {Array.<module:model/CampaignQualityRatingDTO>} campaignRatings
 */
CampaignsQualityRatingDTO.prototype['campaignRatings'] = undefined;






export default CampaignsQualityRatingDTO;

