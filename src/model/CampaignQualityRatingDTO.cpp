/**
 * Партнерский API Маркета
 * API Яндекс Маркета помогает продавцам автоматизировать и упростить работу с маркетплейсом.  В числе возможностей интеграции:  * управление каталогом товаров и витриной,  * обработка заказов,  * изменение настроек магазина,  * получение отчетов. 
 *
 * The version of the OpenAPI document: LATEST
 *
 * NOTE: This class is auto generated by OpenAPI-Generator 7.8.0.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */



#include "ympa_cpp_restsdk_client/model/CampaignQualityRatingDTO.h"

namespace org {
namespace openapitools {
namespace client {
namespace model {



CampaignQualityRatingDTO::CampaignQualityRatingDTO()
{
    m_CampaignId = 0L;
    m_CampaignIdIsSet = false;
    m_RatingsIsSet = false;
}

CampaignQualityRatingDTO::~CampaignQualityRatingDTO()
{
}

void CampaignQualityRatingDTO::validate()
{
    // TODO: implement validation
}

web::json::value CampaignQualityRatingDTO::toJson() const
{

    web::json::value val = web::json::value::object();
    
    if(m_CampaignIdIsSet)
    {
        val[utility::conversions::to_string_t(U("campaignId"))] = ModelBase::toJson(m_CampaignId);
    }
    if(m_RatingsIsSet)
    {
        val[utility::conversions::to_string_t(U("ratings"))] = ModelBase::toJson(m_Ratings);
    }

    return val;
}

bool CampaignQualityRatingDTO::fromJson(const web::json::value& val)
{
    bool ok = true;
    
    if(val.has_field(utility::conversions::to_string_t(U("campaignId"))))
    {
        const web::json::value& fieldValue = val.at(utility::conversions::to_string_t(U("campaignId")));
        if(!fieldValue.is_null())
        {
            int64_t refVal_setCampaignId;
            ok &= ModelBase::fromJson(fieldValue, refVal_setCampaignId);
            setCampaignId(refVal_setCampaignId);
        }
    }
    if(val.has_field(utility::conversions::to_string_t(U("ratings"))))
    {
        const web::json::value& fieldValue = val.at(utility::conversions::to_string_t(U("ratings")));
        if(!fieldValue.is_null())
        {
            std::vector<std::shared_ptr<QualityRatingDTO>> refVal_setRatings;
            ok &= ModelBase::fromJson(fieldValue, refVal_setRatings);
            setRatings(refVal_setRatings);
        }
    }
    return ok;
}

void CampaignQualityRatingDTO::toMultipart(std::shared_ptr<MultipartFormData> multipart, const utility::string_t& prefix) const
{
    utility::string_t namePrefix = prefix;
    if(namePrefix.size() > 0 && namePrefix.substr(namePrefix.size() - 1) != utility::conversions::to_string_t(U(".")))
    {
        namePrefix += utility::conversions::to_string_t(U("."));
    }
    if(m_CampaignIdIsSet)
    {
        multipart->add(ModelBase::toHttpContent(namePrefix + utility::conversions::to_string_t(U("campaignId")), m_CampaignId));
    }
    if(m_RatingsIsSet)
    {
        multipart->add(ModelBase::toHttpContent(namePrefix + utility::conversions::to_string_t(U("ratings")), m_Ratings));
    }
}

bool CampaignQualityRatingDTO::fromMultiPart(std::shared_ptr<MultipartFormData> multipart, const utility::string_t& prefix)
{
    bool ok = true;
    utility::string_t namePrefix = prefix;
    if(namePrefix.size() > 0 && namePrefix.substr(namePrefix.size() - 1) != utility::conversions::to_string_t(U(".")))
    {
        namePrefix += utility::conversions::to_string_t(U("."));
    }

    if(multipart->hasContent(utility::conversions::to_string_t(U("campaignId"))))
    {
        int64_t refVal_setCampaignId;
        ok &= ModelBase::fromHttpContent(multipart->getContent(utility::conversions::to_string_t(U("campaignId"))), refVal_setCampaignId );
        setCampaignId(refVal_setCampaignId);
    }
    if(multipart->hasContent(utility::conversions::to_string_t(U("ratings"))))
    {
        std::vector<std::shared_ptr<QualityRatingDTO>> refVal_setRatings;
        ok &= ModelBase::fromHttpContent(multipart->getContent(utility::conversions::to_string_t(U("ratings"))), refVal_setRatings );
        setRatings(refVal_setRatings);
    }
    return ok;
}

int64_t CampaignQualityRatingDTO::getCampaignId() const
{
    return m_CampaignId;
}

void CampaignQualityRatingDTO::setCampaignId(int64_t value)
{
    m_CampaignId = value;
    m_CampaignIdIsSet = true;
}

bool CampaignQualityRatingDTO::campaignIdIsSet() const
{
    return m_CampaignIdIsSet;
}

void CampaignQualityRatingDTO::unsetCampaignId()
{
    m_CampaignIdIsSet = false;
}
std::vector<std::shared_ptr<QualityRatingDTO>>& CampaignQualityRatingDTO::getRatings()
{
    return m_Ratings;
}

void CampaignQualityRatingDTO::setRatings(const std::vector<std::shared_ptr<QualityRatingDTO>>& value)
{
    m_Ratings = value;
    m_RatingsIsSet = true;
}

bool CampaignQualityRatingDTO::ratingsIsSet() const
{
    return m_RatingsIsSet;
}

void CampaignQualityRatingDTO::unsetRatings()
{
    m_RatingsIsSet = false;
}
}
}
}
}


