/**
 * Партнерский API Маркета
 * API Яндекс Маркета помогает продавцам автоматизировать и упростить работу с маркетплейсом.  В числе возможностей интеграции:  * управление каталогом товаров и витриной,  * обработка заказов,  * изменение настроек магазина,  * получение отчетов. 
 *
 * The version of the OpenAPI document: LATEST
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The DeletePromoOffersRequest model module.
 * @module model/DeletePromoOffersRequest
 * @version LATEST
 */
class DeletePromoOffersRequest {
    /**
     * Constructs a new <code>DeletePromoOffersRequest</code>.
     * Удаление товаров из акции.  Чтобы убрать:  * все товары из акции и больше не участвовать в ней, передайте значение &#x60;true&#x60; в параметре &#x60;deleteAllOffers&#x60;;  * часть товаров, передайте их идентификаторы в параметре &#x60;offersIds&#x60;. 
     * @alias module:model/DeletePromoOffersRequest
     * @param promoId {String} Идентификатор акции.
     */
    constructor(promoId) { 
        
        DeletePromoOffersRequest.initialize(this, promoId);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, promoId) { 
        obj['promoId'] = promoId;
    }

    /**
     * Constructs a <code>DeletePromoOffersRequest</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/DeletePromoOffersRequest} obj Optional instance to populate.
     * @return {module:model/DeletePromoOffersRequest} The populated <code>DeletePromoOffersRequest</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new DeletePromoOffersRequest();

            if (data.hasOwnProperty('promoId')) {
                obj['promoId'] = ApiClient.convertToType(data['promoId'], 'String');
            }
            if (data.hasOwnProperty('deleteAllOffers')) {
                obj['deleteAllOffers'] = ApiClient.convertToType(data['deleteAllOffers'], 'Boolean');
            }
            if (data.hasOwnProperty('offerIds')) {
                obj['offerIds'] = ApiClient.convertToType(data['offerIds'], ['String']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>DeletePromoOffersRequest</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>DeletePromoOffersRequest</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of DeletePromoOffersRequest.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['promoId'] && !(typeof data['promoId'] === 'string' || data['promoId'] instanceof String)) {
            throw new Error("Expected the field `promoId` to be a primitive type in the JSON string but got " + data['promoId']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['offerIds'])) {
            throw new Error("Expected the field `offerIds` to be an array in the JSON data but got " + data['offerIds']);
        }

        return true;
    }


}

DeletePromoOffersRequest.RequiredProperties = ["promoId"];

/**
 * Идентификатор акции.
 * @member {String} promoId
 */
DeletePromoOffersRequest.prototype['promoId'] = undefined;

/**
 * Чтобы убрать все товары из акции и больше не участвовать в ней, передайте значение `true` и не передавайте параметр `offerIds`.
 * @member {Boolean} deleteAllOffers
 */
DeletePromoOffersRequest.prototype['deleteAllOffers'] = undefined;

/**
 * Товары, которые нужно убрать из акции.
 * @member {Array.<String>} offerIds
 */
DeletePromoOffersRequest.prototype['offerIds'] = undefined;






export default DeletePromoOffersRequest;

