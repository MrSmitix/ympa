/**
 * Партнерский API Маркета
 * API Яндекс Маркета помогает продавцам автоматизировать и упростить работу с маркетплейсом.  В числе возможностей интеграции:  * управление каталогом товаров и витриной,  * обработка заказов,  * изменение настроек магазина,  * получение отчетов. 
 *
 * The version of the OpenAPI document: LATEST
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import PriceSuggestDTO from './PriceSuggestDTO';

/**
 * The PriceSuggestOfferDTO model module.
 * @module model/PriceSuggestOfferDTO
 * @version LATEST
 */
class PriceSuggestOfferDTO {
    /**
     * Constructs a new <code>PriceSuggestOfferDTO</code>.
     * Товар с ценами для продвижения.
     * @alias module:model/PriceSuggestOfferDTO
     */
    constructor() { 
        
        PriceSuggestOfferDTO.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>PriceSuggestOfferDTO</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/PriceSuggestOfferDTO} obj Optional instance to populate.
     * @return {module:model/PriceSuggestOfferDTO} The populated <code>PriceSuggestOfferDTO</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new PriceSuggestOfferDTO();

            if (data.hasOwnProperty('marketSku')) {
                obj['marketSku'] = ApiClient.convertToType(data['marketSku'], 'Number');
            }
            if (data.hasOwnProperty('offerId')) {
                obj['offerId'] = ApiClient.convertToType(data['offerId'], 'String');
            }
            if (data.hasOwnProperty('priceSuggestion')) {
                obj['priceSuggestion'] = ApiClient.convertToType(data['priceSuggestion'], [PriceSuggestDTO]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>PriceSuggestOfferDTO</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>PriceSuggestOfferDTO</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['offerId'] && !(typeof data['offerId'] === 'string' || data['offerId'] instanceof String)) {
            throw new Error("Expected the field `offerId` to be a primitive type in the JSON string but got " + data['offerId']);
        }
        if (data['priceSuggestion']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['priceSuggestion'])) {
                throw new Error("Expected the field `priceSuggestion` to be an array in the JSON data but got " + data['priceSuggestion']);
            }
            // validate the optional field `priceSuggestion` (array)
            for (const item of data['priceSuggestion']) {
                PriceSuggestDTO.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * SKU на Маркете.
 * @member {Number} marketSku
 */
PriceSuggestOfferDTO.prototype['marketSku'] = undefined;

/**
 * Ваш SKU — идентификатор товара в вашей системе.  Разрешена любая последовательность длиной до 255 знаков.  Правила использования SKU:  * У каждого товара SKU должен быть свой.  * SKU товара нельзя менять — можно только удалить товар и добавить заново с новым SKU.  * Уже заданный SKU нельзя освободить и использовать заново для другого товара. Каждый товар должен получать новый идентификатор, до того никогда не использовавшийся в вашем каталоге.  [Что такое SKU и как его назначать](https://yandex.ru/support/marketplace/assortment/add/index.html#fields) 
 * @member {String} offerId
 */
PriceSuggestOfferDTO.prototype['offerId'] = undefined;

/**
 * Цены для продвижения. 
 * @member {Array.<module:model/PriceSuggestDTO>} priceSuggestion
 */
PriceSuggestOfferDTO.prototype['priceSuggestion'] = undefined;






export default PriceSuggestOfferDTO;

