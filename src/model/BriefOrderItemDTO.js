/**
 * Партнерский API Маркета
 * API Яндекс Маркета помогает продавцам автоматизировать и упростить работу с маркетплейсом.  В числе возможностей интеграции:  * управление каталогом товаров и витриной,  * обработка заказов,  * изменение настроек магазина,  * получение отчетов. 
 *
 * The version of the OpenAPI document: LATEST
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import OrderItemInstanceDTO from './OrderItemInstanceDTO';
import OrderVatType from './OrderVatType';

/**
 * The BriefOrderItemDTO model module.
 * @module model/BriefOrderItemDTO
 * @version LATEST
 */
class BriefOrderItemDTO {
    /**
     * Constructs a new <code>BriefOrderItemDTO</code>.
     * Информация о маркированном товаре.
     * @alias module:model/BriefOrderItemDTO
     */
    constructor() { 
        
        BriefOrderItemDTO.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>BriefOrderItemDTO</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/BriefOrderItemDTO} obj Optional instance to populate.
     * @return {module:model/BriefOrderItemDTO} The populated <code>BriefOrderItemDTO</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new BriefOrderItemDTO();

            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'Number');
            }
            if (data.hasOwnProperty('vat')) {
                obj['vat'] = OrderVatType.constructFromObject(data['vat']);
            }
            if (data.hasOwnProperty('count')) {
                obj['count'] = ApiClient.convertToType(data['count'], 'Number');
            }
            if (data.hasOwnProperty('price')) {
                obj['price'] = ApiClient.convertToType(data['price'], 'Number');
            }
            if (data.hasOwnProperty('offerName')) {
                obj['offerName'] = ApiClient.convertToType(data['offerName'], 'String');
            }
            if (data.hasOwnProperty('offerId')) {
                obj['offerId'] = ApiClient.convertToType(data['offerId'], 'String');
            }
            if (data.hasOwnProperty('instances')) {
                obj['instances'] = ApiClient.convertToType(data['instances'], [OrderItemInstanceDTO]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>BriefOrderItemDTO</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>BriefOrderItemDTO</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['offerName'] && !(typeof data['offerName'] === 'string' || data['offerName'] instanceof String)) {
            throw new Error("Expected the field `offerName` to be a primitive type in the JSON string but got " + data['offerName']);
        }
        // ensure the json data is a string
        if (data['offerId'] && !(typeof data['offerId'] === 'string' || data['offerId'] instanceof String)) {
            throw new Error("Expected the field `offerId` to be a primitive type in the JSON string but got " + data['offerId']);
        }
        if (data['instances']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['instances'])) {
                throw new Error("Expected the field `instances` to be an array in the JSON data but got " + data['instances']);
            }
            // validate the optional field `instances` (array)
            for (const item of data['instances']) {
                OrderItemInstanceDTO.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * Идентификатор товара в заказе.  Позволяет идентифицировать товар в рамках данного заказа. 
 * @member {Number} id
 */
BriefOrderItemDTO.prototype['id'] = undefined;

/**
 * @member {module:model/OrderVatType} vat
 */
BriefOrderItemDTO.prototype['vat'] = undefined;

/**
 * Количество единиц товара.
 * @member {Number} count
 */
BriefOrderItemDTO.prototype['count'] = undefined;

/**
 * Цена на товар. Указана в той валюте, которая была задана в каталоге. Разделитель целой и дробной части — точка. 
 * @member {Number} price
 */
BriefOrderItemDTO.prototype['price'] = undefined;

/**
 * Название товара.
 * @member {String} offerName
 */
BriefOrderItemDTO.prototype['offerName'] = undefined;

/**
 * Ваш SKU — идентификатор товара в вашей системе.  Разрешена любая последовательность длиной до 255 знаков.  Правила использования SKU:  * У каждого товара SKU должен быть свой.  * SKU товара нельзя менять — можно только удалить товар и добавить заново с новым SKU.  * Уже заданный SKU нельзя освободить и использовать заново для другого товара. Каждый товар должен получать новый идентификатор, до того никогда не использовавшийся в вашем каталоге.  [Что такое SKU и как его назначать](https://yandex.ru/support/marketplace/assortment/add/index.html#fields) 
 * @member {String} offerId
 */
BriefOrderItemDTO.prototype['offerId'] = undefined;

/**
 * Переданные вами коды маркировки.
 * @member {Array.<module:model/OrderItemInstanceDTO>} instances
 */
BriefOrderItemDTO.prototype['instances'] = undefined;






export default BriefOrderItemDTO;

