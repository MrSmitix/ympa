/**
 * Партнерский API Маркета
 * API Яндекс Маркета помогает продавцам автоматизировать и упростить работу с маркетплейсом.  В числе возможностей интеграции:  * управление каталогом товаров и витриной,  * обработка заказов,  * изменение настроек магазина,  * получение отчетов. 
 *
 * The version of the OpenAPI document: LATEST
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import BidRecommendationItemDTO from './BidRecommendationItemDTO';
import PriceRecommendationItemDTO from './PriceRecommendationItemDTO';

/**
 * The SkuBidRecommendationItemDTO model module.
 * @module model/SkuBidRecommendationItemDTO
 * @version LATEST
 */
class SkuBidRecommendationItemDTO {
    /**
     * Constructs a new <code>SkuBidRecommendationItemDTO</code>.
     * Список товаров с рекомендованными ставками.
     * @alias module:model/SkuBidRecommendationItemDTO
     * @param sku {String} Ваш SKU — идентификатор товара в вашей системе.  Разрешена любая последовательность длиной до 255 знаков.  Правила использования SKU:  * У каждого товара SKU должен быть свой.  * SKU товара нельзя менять — можно только удалить товар и добавить заново с новым SKU.  * Уже заданный SKU нельзя освободить и использовать заново для другого товара. Каждый товар должен получать новый идентификатор, до того никогда не использовавшийся в вашем каталоге.  [Что такое SKU и как его назначать](https://yandex.ru/support/marketplace/assortment/add/index.html#fields) 
     * @param bid {Number} Значение ставки.
     */
    constructor(sku, bid) { 
        
        SkuBidRecommendationItemDTO.initialize(this, sku, bid);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, sku, bid) { 
        obj['sku'] = sku;
        obj['bid'] = bid;
    }

    /**
     * Constructs a <code>SkuBidRecommendationItemDTO</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/SkuBidRecommendationItemDTO} obj Optional instance to populate.
     * @return {module:model/SkuBidRecommendationItemDTO} The populated <code>SkuBidRecommendationItemDTO</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new SkuBidRecommendationItemDTO();

            if (data.hasOwnProperty('sku')) {
                obj['sku'] = ApiClient.convertToType(data['sku'], 'String');
            }
            if (data.hasOwnProperty('bid')) {
                obj['bid'] = ApiClient.convertToType(data['bid'], 'Number');
            }
            if (data.hasOwnProperty('bidRecommendations')) {
                obj['bidRecommendations'] = ApiClient.convertToType(data['bidRecommendations'], [BidRecommendationItemDTO]);
            }
            if (data.hasOwnProperty('priceRecommendations')) {
                obj['priceRecommendations'] = ApiClient.convertToType(data['priceRecommendations'], [PriceRecommendationItemDTO]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>SkuBidRecommendationItemDTO</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>SkuBidRecommendationItemDTO</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of SkuBidRecommendationItemDTO.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['sku'] && !(typeof data['sku'] === 'string' || data['sku'] instanceof String)) {
            throw new Error("Expected the field `sku` to be a primitive type in the JSON string but got " + data['sku']);
        }
        if (data['bidRecommendations']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['bidRecommendations'])) {
                throw new Error("Expected the field `bidRecommendations` to be an array in the JSON data but got " + data['bidRecommendations']);
            }
            // validate the optional field `bidRecommendations` (array)
            for (const item of data['bidRecommendations']) {
                BidRecommendationItemDTO.validateJSON(item);
            };
        }
        if (data['priceRecommendations']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['priceRecommendations'])) {
                throw new Error("Expected the field `priceRecommendations` to be an array in the JSON data but got " + data['priceRecommendations']);
            }
            // validate the optional field `priceRecommendations` (array)
            for (const item of data['priceRecommendations']) {
                PriceRecommendationItemDTO.validateJSON(item);
            };
        }

        return true;
    }


}

SkuBidRecommendationItemDTO.RequiredProperties = ["sku", "bid"];

/**
 * Ваш SKU — идентификатор товара в вашей системе.  Разрешена любая последовательность длиной до 255 знаков.  Правила использования SKU:  * У каждого товара SKU должен быть свой.  * SKU товара нельзя менять — можно только удалить товар и добавить заново с новым SKU.  * Уже заданный SKU нельзя освободить и использовать заново для другого товара. Каждый товар должен получать новый идентификатор, до того никогда не использовавшийся в вашем каталоге.  [Что такое SKU и как его назначать](https://yandex.ru/support/marketplace/assortment/add/index.html#fields) 
 * @member {String} sku
 */
SkuBidRecommendationItemDTO.prototype['sku'] = undefined;

/**
 * Значение ставки.
 * @member {Number} bid
 */
SkuBidRecommendationItemDTO.prototype['bid'] = undefined;

/**
 * Список рекомендованных ставок с соответствующими долями показов. Чем больше ставка, тем большую долю показов она помогает получить. 
 * @member {Array.<module:model/BidRecommendationItemDTO>} bidRecommendations
 */
SkuBidRecommendationItemDTO.prototype['bidRecommendations'] = undefined;

/**
 * Рекомендованные цены.
 * @member {Array.<module:model/PriceRecommendationItemDTO>} priceRecommendations
 */
SkuBidRecommendationItemDTO.prototype['priceRecommendations'] = undefined;






export default SkuBidRecommendationItemDTO;

