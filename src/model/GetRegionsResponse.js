/**
 * Партнерский API Маркета
 * API Яндекс Маркета помогает продавцам автоматизировать и упростить работу с маркетплейсом.  В числе возможностей интеграции:  * управление каталогом товаров и витриной,  * обработка заказов,  * изменение настроек магазина,  * получение отчетов. 
 *
 * The version of the OpenAPI document: LATEST
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import ForwardScrollingPagerDTO from './ForwardScrollingPagerDTO';
import RegionDTO from './RegionDTO';

/**
 * The GetRegionsResponse model module.
 * @module model/GetRegionsResponse
 * @version LATEST
 */
class GetRegionsResponse {
    /**
     * Constructs a new <code>GetRegionsResponse</code>.
     * @alias module:model/GetRegionsResponse
     * @param regions {Array.<module:model/RegionDTO>} Регион доставки.
     */
    constructor(regions) { 
        
        GetRegionsResponse.initialize(this, regions);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, regions) { 
        obj['regions'] = regions;
    }

    /**
     * Constructs a <code>GetRegionsResponse</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/GetRegionsResponse} obj Optional instance to populate.
     * @return {module:model/GetRegionsResponse} The populated <code>GetRegionsResponse</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new GetRegionsResponse();

            if (data.hasOwnProperty('regions')) {
                obj['regions'] = ApiClient.convertToType(data['regions'], [RegionDTO]);
            }
            if (data.hasOwnProperty('paging')) {
                obj['paging'] = ForwardScrollingPagerDTO.constructFromObject(data['paging']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>GetRegionsResponse</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>GetRegionsResponse</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of GetRegionsResponse.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['regions']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['regions'])) {
                throw new Error("Expected the field `regions` to be an array in the JSON data but got " + data['regions']);
            }
            // validate the optional field `regions` (array)
            for (const item of data['regions']) {
                RegionDTO.validateJSON(item);
            };
        }
        // validate the optional field `paging`
        if (data['paging']) { // data not null
          ForwardScrollingPagerDTO.validateJSON(data['paging']);
        }

        return true;
    }


}

GetRegionsResponse.RequiredProperties = ["regions"];

/**
 * Регион доставки.
 * @member {Array.<module:model/RegionDTO>} regions
 */
GetRegionsResponse.prototype['regions'] = undefined;

/**
 * @member {module:model/ForwardScrollingPagerDTO} paging
 */
GetRegionsResponse.prototype['paging'] = undefined;






export default GetRegionsResponse;

