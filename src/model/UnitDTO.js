/**
 * Партнерский API Маркета
 * API Яндекс Маркета помогает продавцам автоматизировать и упростить работу с маркетплейсом.  В числе возможностей интеграции:  * управление каталогом товаров и витриной,  * обработка заказов,  * изменение настроек магазина,  * получение отчетов. 
 *
 * The version of the OpenAPI document: LATEST
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The UnitDTO model module.
 * @module model/UnitDTO
 * @version LATEST
 */
class UnitDTO {
    /**
     * Constructs a new <code>UnitDTO</code>.
     * Единица измерения.
     * @alias module:model/UnitDTO
     * @param id {Number} Идентификатор единицы измерения.
     * @param name {String} Сокращенное название единицы измерения.
     * @param fullName {String} Полное название единицы измерения.
     */
    constructor(id, name, fullName) { 
        
        UnitDTO.initialize(this, id, name, fullName);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, id, name, fullName) { 
        obj['id'] = id;
        obj['name'] = name;
        obj['fullName'] = fullName;
    }

    /**
     * Constructs a <code>UnitDTO</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/UnitDTO} obj Optional instance to populate.
     * @return {module:model/UnitDTO} The populated <code>UnitDTO</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new UnitDTO();

            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'Number');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('fullName')) {
                obj['fullName'] = ApiClient.convertToType(data['fullName'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>UnitDTO</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>UnitDTO</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of UnitDTO.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['fullName'] && !(typeof data['fullName'] === 'string' || data['fullName'] instanceof String)) {
            throw new Error("Expected the field `fullName` to be a primitive type in the JSON string but got " + data['fullName']);
        }

        return true;
    }


}

UnitDTO.RequiredProperties = ["id", "name", "fullName"];

/**
 * Идентификатор единицы измерения.
 * @member {Number} id
 */
UnitDTO.prototype['id'] = undefined;

/**
 * Сокращенное название единицы измерения.
 * @member {String} name
 */
UnitDTO.prototype['name'] = undefined;

/**
 * Полное название единицы измерения.
 * @member {String} fullName
 */
UnitDTO.prototype['fullName'] = undefined;






export default UnitDTO;

