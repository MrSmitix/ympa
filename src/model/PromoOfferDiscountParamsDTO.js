/**
 * Партнерский API Маркета
 * API Яндекс Маркета помогает продавцам автоматизировать и упростить работу с маркетплейсом.  В числе возможностей интеграции:  * управление каталогом товаров и витриной,  * обработка заказов,  * изменение настроек магазина,  * получение отчетов. 
 *
 * The version of the OpenAPI document: LATEST
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The PromoOfferDiscountParamsDTO model module.
 * @module model/PromoOfferDiscountParamsDTO
 * @version LATEST
 */
class PromoOfferDiscountParamsDTO {
    /**
     * Constructs a new <code>PromoOfferDiscountParamsDTO</code>.
     * Параметры товара в акции с типом &#x60;DIRECT_DISCOUNT&#x60; или &#x60;BLUE_FLASH&#x60;.
     * @alias module:model/PromoOfferDiscountParamsDTO
     * @param maxPromoPrice {Number} Максимально возможная цена для участия в акции.  Указывается в рублях.  Возвращается для всех товаров. 
     */
    constructor(maxPromoPrice) { 
        
        PromoOfferDiscountParamsDTO.initialize(this, maxPromoPrice);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, maxPromoPrice) { 
        obj['maxPromoPrice'] = maxPromoPrice;
    }

    /**
     * Constructs a <code>PromoOfferDiscountParamsDTO</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/PromoOfferDiscountParamsDTO} obj Optional instance to populate.
     * @return {module:model/PromoOfferDiscountParamsDTO} The populated <code>PromoOfferDiscountParamsDTO</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new PromoOfferDiscountParamsDTO();

            if (data.hasOwnProperty('price')) {
                obj['price'] = ApiClient.convertToType(data['price'], 'Number');
            }
            if (data.hasOwnProperty('promoPrice')) {
                obj['promoPrice'] = ApiClient.convertToType(data['promoPrice'], 'Number');
            }
            if (data.hasOwnProperty('maxPromoPrice')) {
                obj['maxPromoPrice'] = ApiClient.convertToType(data['maxPromoPrice'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>PromoOfferDiscountParamsDTO</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>PromoOfferDiscountParamsDTO</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of PromoOfferDiscountParamsDTO.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }

        return true;
    }


}

PromoOfferDiscountParamsDTO.RequiredProperties = ["maxPromoPrice"];

/**
 * Зачеркнутая цена — та, по которой товар продавался до акции.  Указывается в рублях.  Возвращается, только если товар участвует в акции. 
 * @member {Number} price
 */
PromoOfferDiscountParamsDTO.prototype['price'] = undefined;

/**
 * Цена по акции — та, по которой вы хотите продавать товар.  Указывается в рублях.  Возвращается, только если товар участвует в акции. 
 * @member {Number} promoPrice
 */
PromoOfferDiscountParamsDTO.prototype['promoPrice'] = undefined;

/**
 * Максимально возможная цена для участия в акции.  Указывается в рублях.  Возвращается для всех товаров. 
 * @member {Number} maxPromoPrice
 */
PromoOfferDiscountParamsDTO.prototype['maxPromoPrice'] = undefined;






export default PromoOfferDiscountParamsDTO;

