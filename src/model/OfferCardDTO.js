/**
 * Партнерский API Маркета
 * API Яндекс Маркета помогает продавцам автоматизировать и упростить работу с маркетплейсом.  В числе возможностей интеграции:  * управление каталогом товаров и витриной,  * обработка заказов,  * изменение настроек магазина,  * получение отчетов. 
 *
 * The version of the OpenAPI document: LATEST
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import GetMappingDTO from './GetMappingDTO';
import OfferCardRecommendationDTO from './OfferCardRecommendationDTO';
import OfferCardStatusType from './OfferCardStatusType';
import OfferErrorDTO from './OfferErrorDTO';
import ParameterValueDTO from './ParameterValueDTO';

/**
 * The OfferCardDTO model module.
 * @module model/OfferCardDTO
 * @version LATEST
 */
class OfferCardDTO {
    /**
     * Constructs a new <code>OfferCardDTO</code>.
     * Информация о состоянии карточки товара.  Если поле &#x60;mapping&#x60; отсутствует в ответе, Маркет еще не успел обработать информацию о товаре. Чтобы определить категорию такого товара, повторите запрос через несколько минут. 
     * @alias module:model/OfferCardDTO
     * @param offerId {String} Ваш SKU — идентификатор товара в вашей системе.  Разрешена любая последовательность длиной до 255 знаков.  Правила использования SKU:  * У каждого товара SKU должен быть свой.  * SKU товара нельзя менять — можно только удалить товар и добавить заново с новым SKU.  * Уже заданный SKU нельзя освободить и использовать заново для другого товара. Каждый товар должен получать новый идентификатор, до того никогда не использовавшийся в вашем каталоге.  [Что такое SKU и как его назначать](https://yandex.ru/support/marketplace/assortment/add/index.html#fields) 
     */
    constructor(offerId) { 
        
        OfferCardDTO.initialize(this, offerId);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, offerId) { 
        obj['offerId'] = offerId;
    }

    /**
     * Constructs a <code>OfferCardDTO</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/OfferCardDTO} obj Optional instance to populate.
     * @return {module:model/OfferCardDTO} The populated <code>OfferCardDTO</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new OfferCardDTO();

            if (data.hasOwnProperty('offerId')) {
                obj['offerId'] = ApiClient.convertToType(data['offerId'], 'String');
            }
            if (data.hasOwnProperty('mapping')) {
                obj['mapping'] = GetMappingDTO.constructFromObject(data['mapping']);
            }
            if (data.hasOwnProperty('parameterValues')) {
                obj['parameterValues'] = ApiClient.convertToType(data['parameterValues'], [ParameterValueDTO]);
            }
            if (data.hasOwnProperty('cardStatus')) {
                obj['cardStatus'] = OfferCardStatusType.constructFromObject(data['cardStatus']);
            }
            if (data.hasOwnProperty('contentRating')) {
                obj['contentRating'] = ApiClient.convertToType(data['contentRating'], 'Number');
            }
            if (data.hasOwnProperty('recommendations')) {
                obj['recommendations'] = ApiClient.convertToType(data['recommendations'], [OfferCardRecommendationDTO]);
            }
            if (data.hasOwnProperty('errors')) {
                obj['errors'] = ApiClient.convertToType(data['errors'], [OfferErrorDTO]);
            }
            if (data.hasOwnProperty('warnings')) {
                obj['warnings'] = ApiClient.convertToType(data['warnings'], [OfferErrorDTO]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>OfferCardDTO</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>OfferCardDTO</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of OfferCardDTO.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['offerId'] && !(typeof data['offerId'] === 'string' || data['offerId'] instanceof String)) {
            throw new Error("Expected the field `offerId` to be a primitive type in the JSON string but got " + data['offerId']);
        }
        // validate the optional field `mapping`
        if (data['mapping']) { // data not null
          GetMappingDTO.validateJSON(data['mapping']);
        }
        if (data['parameterValues']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['parameterValues'])) {
                throw new Error("Expected the field `parameterValues` to be an array in the JSON data but got " + data['parameterValues']);
            }
            // validate the optional field `parameterValues` (array)
            for (const item of data['parameterValues']) {
                ParameterValueDTO.validateJSON(item);
            };
        }
        if (data['recommendations']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['recommendations'])) {
                throw new Error("Expected the field `recommendations` to be an array in the JSON data but got " + data['recommendations']);
            }
            // validate the optional field `recommendations` (array)
            for (const item of data['recommendations']) {
                OfferCardRecommendationDTO.validateJSON(item);
            };
        }
        if (data['errors']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['errors'])) {
                throw new Error("Expected the field `errors` to be an array in the JSON data but got " + data['errors']);
            }
            // validate the optional field `errors` (array)
            for (const item of data['errors']) {
                OfferErrorDTO.validateJSON(item);
            };
        }
        if (data['warnings']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['warnings'])) {
                throw new Error("Expected the field `warnings` to be an array in the JSON data but got " + data['warnings']);
            }
            // validate the optional field `warnings` (array)
            for (const item of data['warnings']) {
                OfferErrorDTO.validateJSON(item);
            };
        }

        return true;
    }


}

OfferCardDTO.RequiredProperties = ["offerId"];

/**
 * Ваш SKU — идентификатор товара в вашей системе.  Разрешена любая последовательность длиной до 255 знаков.  Правила использования SKU:  * У каждого товара SKU должен быть свой.  * SKU товара нельзя менять — можно только удалить товар и добавить заново с новым SKU.  * Уже заданный SKU нельзя освободить и использовать заново для другого товара. Каждый товар должен получать новый идентификатор, до того никогда не использовавшийся в вашем каталоге.  [Что такое SKU и как его назначать](https://yandex.ru/support/marketplace/assortment/add/index.html#fields) 
 * @member {String} offerId
 */
OfferCardDTO.prototype['offerId'] = undefined;

/**
 * @member {module:model/GetMappingDTO} mapping
 */
OfferCardDTO.prototype['mapping'] = undefined;

/**
 * Список характеристик с их значениями. 
 * @member {Array.<module:model/ParameterValueDTO>} parameterValues
 */
OfferCardDTO.prototype['parameterValues'] = undefined;

/**
 * @member {module:model/OfferCardStatusType} cardStatus
 */
OfferCardDTO.prototype['cardStatus'] = undefined;

/**
 * Процент заполненности карточки.
 * @member {Number} contentRating
 */
OfferCardDTO.prototype['contentRating'] = undefined;

/**
 * Список рекомендаций к заполнению карточки.  Рекомендации Маркета помогают заполнять карточку так, чтобы покупателям было проще найти ваш товар и решиться на покупку. 
 * @member {Array.<module:model/OfferCardRecommendationDTO>} recommendations
 */
OfferCardDTO.prototype['recommendations'] = undefined;

/**
 * Ошибки в контенте, препятствующие размещению товара на витрине.
 * @member {Array.<module:model/OfferErrorDTO>} errors
 */
OfferCardDTO.prototype['errors'] = undefined;

/**
 * Связанные с контентом предупреждения, не препятствующие размещению товара на витрине.
 * @member {Array.<module:model/OfferErrorDTO>} warnings
 */
OfferCardDTO.prototype['warnings'] = undefined;






export default OfferCardDTO;

