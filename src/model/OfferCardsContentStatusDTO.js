/**
 * Партнерский API Маркета
 * API Яндекс Маркета помогает продавцам автоматизировать и упростить работу с маркетплейсом.  В числе возможностей интеграции:  * управление каталогом товаров и витриной,  * обработка заказов,  * изменение настроек магазина,  * получение отчетов. 
 *
 * The version of the OpenAPI document: LATEST
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import ForwardScrollingPagerDTO from './ForwardScrollingPagerDTO';
import OfferCardDTO from './OfferCardDTO';

/**
 * The OfferCardsContentStatusDTO model module.
 * @module model/OfferCardsContentStatusDTO
 * @version LATEST
 */
class OfferCardsContentStatusDTO {
    /**
     * Constructs a new <code>OfferCardsContentStatusDTO</code>.
     * Список товаров с информацией о состоянии карточек.
     * @alias module:model/OfferCardsContentStatusDTO
     * @param offerCards {Array.<module:model/OfferCardDTO>} Страница списка товаров с информацией о состоянии карточек.
     */
    constructor(offerCards) { 
        
        OfferCardsContentStatusDTO.initialize(this, offerCards);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, offerCards) { 
        obj['offerCards'] = offerCards;
    }

    /**
     * Constructs a <code>OfferCardsContentStatusDTO</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/OfferCardsContentStatusDTO} obj Optional instance to populate.
     * @return {module:model/OfferCardsContentStatusDTO} The populated <code>OfferCardsContentStatusDTO</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new OfferCardsContentStatusDTO();

            if (data.hasOwnProperty('offerCards')) {
                obj['offerCards'] = ApiClient.convertToType(data['offerCards'], [OfferCardDTO]);
            }
            if (data.hasOwnProperty('paging')) {
                obj['paging'] = ForwardScrollingPagerDTO.constructFromObject(data['paging']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>OfferCardsContentStatusDTO</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>OfferCardsContentStatusDTO</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of OfferCardsContentStatusDTO.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['offerCards']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['offerCards'])) {
                throw new Error("Expected the field `offerCards` to be an array in the JSON data but got " + data['offerCards']);
            }
            // validate the optional field `offerCards` (array)
            for (const item of data['offerCards']) {
                OfferCardDTO.validateJSON(item);
            };
        }
        // validate the optional field `paging`
        if (data['paging']) { // data not null
          ForwardScrollingPagerDTO.validateJSON(data['paging']);
        }

        return true;
    }


}

OfferCardsContentStatusDTO.RequiredProperties = ["offerCards"];

/**
 * Страница списка товаров с информацией о состоянии карточек.
 * @member {Array.<module:model/OfferCardDTO>} offerCards
 */
OfferCardsContentStatusDTO.prototype['offerCards'] = undefined;

/**
 * @member {module:model/ForwardScrollingPagerDTO} paging
 */
OfferCardsContentStatusDTO.prototype['paging'] = undefined;






export default OfferCardsContentStatusDTO;

