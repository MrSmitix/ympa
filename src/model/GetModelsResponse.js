/**
 * Партнерский API Маркета
 * API Яндекс Маркета помогает продавцам автоматизировать и упростить работу с маркетплейсом.  В числе возможностей интеграции:  * управление каталогом товаров и витриной,  * обработка заказов,  * изменение настроек магазина,  * получение отчетов. 
 *
 * The version of the OpenAPI document: LATEST
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import CurrencyType from './CurrencyType';
import ModelDTO from './ModelDTO';
import ModelsDTO from './ModelsDTO';
import RegionalModelInfoDTO from './RegionalModelInfoDTO';

/**
 * The GetModelsResponse model module.
 * @module model/GetModelsResponse
 * @version LATEST
 */
class GetModelsResponse {
    /**
     * Constructs a new <code>GetModelsResponse</code>.
     * Ответ на запрос информации о моделях.
     * @alias module:model/GetModelsResponse
     * @implements module:model/ModelsDTO
     * @implements module:model/RegionalModelInfoDTO
     * @param models {Array.<module:model/ModelDTO>} Список моделей товаров.
     */
    constructor(models) { 
        ModelsDTO.initialize(this, models);RegionalModelInfoDTO.initialize(this);
        GetModelsResponse.initialize(this, models);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, models) { 
        obj['models'] = models;
    }

    /**
     * Constructs a <code>GetModelsResponse</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/GetModelsResponse} obj Optional instance to populate.
     * @return {module:model/GetModelsResponse} The populated <code>GetModelsResponse</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new GetModelsResponse();
            ModelsDTO.constructFromObject(data, obj);
            RegionalModelInfoDTO.constructFromObject(data, obj);

            if (data.hasOwnProperty('models')) {
                obj['models'] = ApiClient.convertToType(data['models'], [ModelDTO]);
            }
            if (data.hasOwnProperty('currency')) {
                obj['currency'] = CurrencyType.constructFromObject(data['currency']);
            }
            if (data.hasOwnProperty('regionId')) {
                obj['regionId'] = ApiClient.convertToType(data['regionId'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>GetModelsResponse</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>GetModelsResponse</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of GetModelsResponse.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['models']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['models'])) {
                throw new Error("Expected the field `models` to be an array in the JSON data but got " + data['models']);
            }
            // validate the optional field `models` (array)
            for (const item of data['models']) {
                ModelDTO.validateJSON(item);
            };
        }

        return true;
    }


}

GetModelsResponse.RequiredProperties = ["models"];

/**
 * Список моделей товаров.
 * @member {Array.<module:model/ModelDTO>} models
 */
GetModelsResponse.prototype['models'] = undefined;

/**
 * @member {module:model/CurrencyType} currency
 */
GetModelsResponse.prototype['currency'] = undefined;

/**
 * Идентификатор региона, для которого выводится информация о предложениях модели (доставляемых в этот регион).  Информацию о регионе по идентификатору можно получить с помощью запроса [GET regions/{regionId}](../../reference/regions/searchRegionsById.md). 
 * @member {Number} regionId
 */
GetModelsResponse.prototype['regionId'] = undefined;


// Implement ModelsDTO interface:
/**
 * Список моделей товаров.
 * @member {Array.<module:model/ModelDTO>} models
 */
ModelsDTO.prototype['models'] = undefined;
// Implement RegionalModelInfoDTO interface:
/**
 * @member {module:model/CurrencyType} currency
 */
RegionalModelInfoDTO.prototype['currency'] = undefined;
/**
 * Идентификатор региона, для которого выводится информация о предложениях модели (доставляемых в этот регион).  Информацию о регионе по идентификатору можно получить с помощью запроса [GET regions/{regionId}](../../reference/regions/searchRegionsById.md). 
 * @member {Number} regionId
 */
RegionalModelInfoDTO.prototype['regionId'] = undefined;




export default GetModelsResponse;

