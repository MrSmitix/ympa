/**
 * Партнерский API Маркета
 * API Яндекс Маркета помогает продавцам автоматизировать и упростить работу с маркетплейсом.  В числе возможностей интеграции:  * управление каталогом товаров и витриной,  * обработка заказов,  * изменение настроек магазина,  * получение отчетов. 
 *
 * The version of the OpenAPI document: LATEST
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import BasePriceDTO from './BasePriceDTO';
import GetPriceDTO from './GetPriceDTO';
import PriceCompetitivenessType from './PriceCompetitivenessType';

/**
 * The OfferForRecommendationDTO model module.
 * @module model/OfferForRecommendationDTO
 * @version LATEST
 */
class OfferForRecommendationDTO {
    /**
     * Constructs a new <code>OfferForRecommendationDTO</code>.
     * Информация о состоянии цены на товар.
     * @alias module:model/OfferForRecommendationDTO
     */
    constructor() { 
        
        OfferForRecommendationDTO.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>OfferForRecommendationDTO</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/OfferForRecommendationDTO} obj Optional instance to populate.
     * @return {module:model/OfferForRecommendationDTO} The populated <code>OfferForRecommendationDTO</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new OfferForRecommendationDTO();

            if (data.hasOwnProperty('offerId')) {
                obj['offerId'] = ApiClient.convertToType(data['offerId'], 'String');
            }
            if (data.hasOwnProperty('price')) {
                obj['price'] = BasePriceDTO.constructFromObject(data['price']);
            }
            if (data.hasOwnProperty('cofinancePrice')) {
                obj['cofinancePrice'] = GetPriceDTO.constructFromObject(data['cofinancePrice']);
            }
            if (data.hasOwnProperty('competitiveness')) {
                obj['competitiveness'] = PriceCompetitivenessType.constructFromObject(data['competitiveness']);
            }
            if (data.hasOwnProperty('shows')) {
                obj['shows'] = ApiClient.convertToType(data['shows'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>OfferForRecommendationDTO</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>OfferForRecommendationDTO</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['offerId'] && !(typeof data['offerId'] === 'string' || data['offerId'] instanceof String)) {
            throw new Error("Expected the field `offerId` to be a primitive type in the JSON string but got " + data['offerId']);
        }
        // validate the optional field `price`
        if (data['price']) { // data not null
          BasePriceDTO.validateJSON(data['price']);
        }
        // validate the optional field `cofinancePrice`
        if (data['cofinancePrice']) { // data not null
          GetPriceDTO.validateJSON(data['cofinancePrice']);
        }

        return true;
    }


}



/**
 * Ваш SKU — идентификатор товара в вашей системе.  Разрешена любая последовательность длиной до 255 знаков.  Правила использования SKU:  * У каждого товара SKU должен быть свой.  * SKU товара нельзя менять — можно только удалить товар и добавить заново с новым SKU.  * Уже заданный SKU нельзя освободить и использовать заново для другого товара. Каждый товар должен получать новый идентификатор, до того никогда не использовавшийся в вашем каталоге.  [Что такое SKU и как его назначать](https://yandex.ru/support/marketplace/assortment/add/index.html#fields) 
 * @member {String} offerId
 */
OfferForRecommendationDTO.prototype['offerId'] = undefined;

/**
 * @member {module:model/BasePriceDTO} price
 */
OfferForRecommendationDTO.prototype['price'] = undefined;

/**
 * @member {module:model/GetPriceDTO} cofinancePrice
 */
OfferForRecommendationDTO.prototype['cofinancePrice'] = undefined;

/**
 * @member {module:model/PriceCompetitivenessType} competitiveness
 */
OfferForRecommendationDTO.prototype['competitiveness'] = undefined;

/**
 * Количество показов карточки товара за последние 7 дней.
 * @member {Number} shows
 */
OfferForRecommendationDTO.prototype['shows'] = undefined;






export default OfferForRecommendationDTO;

