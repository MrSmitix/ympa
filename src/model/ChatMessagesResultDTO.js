/**
 * Партнерский API Маркета
 * API Яндекс Маркета помогает продавцам автоматизировать и упростить работу с маркетплейсом.  В числе возможностей интеграции:  * управление каталогом товаров и витриной,  * обработка заказов,  * изменение настроек магазина,  * получение отчетов. 
 *
 * The version of the OpenAPI document: LATEST
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import ChatMessageDTO from './ChatMessageDTO';
import ForwardScrollingPagerDTO from './ForwardScrollingPagerDTO';

/**
 * The ChatMessagesResultDTO model module.
 * @module model/ChatMessagesResultDTO
 * @version LATEST
 */
class ChatMessagesResultDTO {
    /**
     * Constructs a new <code>ChatMessagesResultDTO</code>.
     * Информация о сообщениях.
     * @alias module:model/ChatMessagesResultDTO
     * @param orderId {Number} Идентификатор заказа.
     * @param messages {Array.<module:model/ChatMessageDTO>} Информация о сообщениях.
     */
    constructor(orderId, messages) { 
        
        ChatMessagesResultDTO.initialize(this, orderId, messages);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, orderId, messages) { 
        obj['orderId'] = orderId;
        obj['messages'] = messages;
    }

    /**
     * Constructs a <code>ChatMessagesResultDTO</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ChatMessagesResultDTO} obj Optional instance to populate.
     * @return {module:model/ChatMessagesResultDTO} The populated <code>ChatMessagesResultDTO</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ChatMessagesResultDTO();

            if (data.hasOwnProperty('orderId')) {
                obj['orderId'] = ApiClient.convertToType(data['orderId'], 'Number');
            }
            if (data.hasOwnProperty('messages')) {
                obj['messages'] = ApiClient.convertToType(data['messages'], [ChatMessageDTO]);
            }
            if (data.hasOwnProperty('paging')) {
                obj['paging'] = ForwardScrollingPagerDTO.constructFromObject(data['paging']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ChatMessagesResultDTO</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ChatMessagesResultDTO</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of ChatMessagesResultDTO.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['messages']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['messages'])) {
                throw new Error("Expected the field `messages` to be an array in the JSON data but got " + data['messages']);
            }
            // validate the optional field `messages` (array)
            for (const item of data['messages']) {
                ChatMessageDTO.validateJSON(item);
            };
        }
        // validate the optional field `paging`
        if (data['paging']) { // data not null
          ForwardScrollingPagerDTO.validateJSON(data['paging']);
        }

        return true;
    }


}

ChatMessagesResultDTO.RequiredProperties = ["orderId", "messages"];

/**
 * Идентификатор заказа.
 * @member {Number} orderId
 */
ChatMessagesResultDTO.prototype['orderId'] = undefined;

/**
 * Информация о сообщениях.
 * @member {Array.<module:model/ChatMessageDTO>} messages
 */
ChatMessagesResultDTO.prototype['messages'] = undefined;

/**
 * @member {module:model/ForwardScrollingPagerDTO} paging
 */
ChatMessagesResultDTO.prototype['paging'] = undefined;






export default ChatMessagesResultDTO;

