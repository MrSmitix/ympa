/**
* Партнерский API Маркета
* API Яндекс Маркета помогает продавцам автоматизировать и упростить работу с маркетплейсом.  В числе возможностей интеграции:  * управление каталогом товаров и витриной,  * обработка заказов,  * изменение настроек магазина,  * получение отчетов. 
*
* The version of the OpenAPI document: LATEST
* 
*
* NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
* https://openapi-generator.tech
* Do not edit the class manually.
*/
package ympa_ktorm_schema.models

import org.ktorm.dsl.*
import org.ktorm.schema.*
import org.ktorm.database.Database
import .*


/**
 * 
 * @param parameters 
 * @param offers Товары, для которых нужно рассчитать стоимость услуг.
 */
object CalculateTariffsRequests : BaseTable<CalculateTariffsRequest>("CalculateTariffsRequest") {
    val parameters = long("parameters")

    /**
     * Create an entity of type CalculateTariffsRequest from the model
     */
    override fun doCreateEntity(row: QueryRowSet, withReferences: Boolean) = CalculateTariffsRequest(
        parameters = CalculateTariffsParametersDTOs.createEntity(row, withReferences) /* CalculateTariffsParametersDTO */,
        offers = emptyList() /* kotlin.Array<CalculateTariffsOfferDTO> */ /* Товары, для которых нужно рассчитать стоимость услуг. */
    )

    /**
    * Assign all the columns from the entity of type CalculateTariffsRequest to the DML expression.
    *
    * Usage:
    *
    * ```kotlin
    * let entity = CalculateTariffsRequest()
    * database.update(CalculateTariffsRequests, {
    *     assignFrom(entity)
    * })
    * ```
    * @return the builder with the columns for the update or insert.
    */
    fun AssignmentsBuilder.assignFrom(entity: CalculateTariffsRequest) {
        this.apply {
            set(CalculateTariffsRequests.parameters, entity.parameters)
        }
    }

}


object CalculateTariffsRequestCalculateTariffsOfferDTO : BaseTable<Pair<kotlin.Long, kotlin.Long>>("CalculateTariffsRequestCalculateTariffsOfferDTO") {
    val calculateTariffsRequest = long("calculateTariffsRequest")
    val calculateTariffsOfferDTO = long("calculateTariffsOfferDTO")

    override fun doCreateEntity(row: QueryRowSet, withReferences: Boolean): Pair<kotlin.Long, kotlin.Long> =
        Pair(row[calculateTariffsRequest] ?: 0, row[calculateTariffsOfferDTO] ?: 0)

    fun AssignmentsBuilder.assignFrom(entity: Pair<kotlin.Long, kotlin.Long>) {
        this.apply {
            set(CalculateTariffsRequestCalculateTariffsOfferDTO.calculateTariffsRequest, entity.first)
            set(CalculateTariffsRequestCalculateTariffsOfferDTO.calculateTariffsOfferDTO, entity.second)
        }
    }

}

