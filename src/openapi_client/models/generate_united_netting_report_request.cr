# #Партнерский API Маркета
#
##API Яндекс Маркета помогает продавцам автоматизировать и упростить работу с маркетплейсом.  В числе возможностей интеграции:  * управление каталогом товаров и витриной,  * обработка заказов,  * изменение настроек магазина,  * получение отчетов. 
#
#The version of the OpenAPI document: LATEST
#
#Generated by: https://openapi-generator.tech
#Generator version: 7.8.0
#

require "big"
require "json"
require "time"

module OpenAPIClient
  # Данные, необходимые для генерации отчета: идентификатор магазина, период, за который нужен отчет, а также фильтры. 
  class GenerateUnitedNettingReportRequest
    include JSON::Serializable

    # Required properties
    # Идентификатор бизнеса.
    @[JSON::Field(key: "businessId", type: Int64, nillable: false, emit_null: false)]
    property business_id : Int64

    # Optional properties
    # {% note warning \"\" %}  Этот параметр устарел. Не используйте его.  {% endnote %}  Начало периода, включительно. 
    @[JSON::Field(key: "dateTimeFrom", type: Time?, nillable: true, emit_null: false)]
    property date_time_from : Time?

    # {% note warning \"\" %}  Этот параметр устарел. Не используйте его.  {% endnote %}  Конец периода, включительно. Максимальный период — 1 год. 
    @[JSON::Field(key: "dateTimeTo", type: Time?, nillable: true, emit_null: false)]
    property date_time_to : Time?

    # Начало периода, включительно.
    @[JSON::Field(key: "dateFrom", type: Time?, nillable: true, emit_null: false)]
    property date_from : Time?

    # Конец периода, включительно. Максимальный период — 1 год.
    @[JSON::Field(key: "dateTo", type: Time?, nillable: true, emit_null: false)]
    property date_to : Time?

    # Номер платежного поручения.
    @[JSON::Field(key: "bankOrderId", type: Int64?, nillable: true, emit_null: false)]
    property bank_order_id : Int64?

    # Дата платежного поручения.
    @[JSON::Field(key: "bankOrderDateTime", type: Time?, nillable: true, emit_null: false)]
    property bank_order_date_time : Time?

    # Список моделей, которые нужны в отчете. 
    @[JSON::Field(key: "placementPrograms", type: Array(PlacementType)?, nillable: true, emit_null: false)]
    property placement_programs : Array(PlacementType)?

    # Список ИНН, которые нужны в отчете.
    @[JSON::Field(key: "inns", type: Array(String)?, nillable: true, emit_null: false)]
    property inns : Array(String)?

    # Список магазинов, которые нужны в отчете.
    @[JSON::Field(key: "campaignIds", type: Array(Int64)?, nillable: true, emit_null: false)]
    property campaign_ids : Array(Int64)?

    # Initializes the object
    # @param [Hash] attributes Model attributes in the form of hash
    def initialize(@business_id : Int64, @date_time_from : Time?, @date_time_to : Time?, @date_from : Time?, @date_to : Time?, @bank_order_id : Int64?, @bank_order_date_time : Time?, @placement_programs : Array(PlacementType)?, @inns : Array(String)?, @campaign_ids : Array(Int64)?)
    end

    # Show invalid properties with the reasons. Usually used together with valid?
    # @return Array for valid properties with the reasons
    def list_invalid_properties
      invalid_properties = Array(String).new
      invalid_properties
    end

    # Check to see if the all the properties in the model are valid
    # @return true if the model is valid
    def valid?
      true
    end

    # Checks equality by comparing each attribute.
    # @param [Object] Object to be compared
    def ==(o)
      return true if self.same?(o)
      self.class == o.class &&
          business_id == o.business_id &&
          date_time_from == o.date_time_from &&
          date_time_to == o.date_time_to &&
          date_from == o.date_from &&
          date_to == o.date_to &&
          bank_order_id == o.bank_order_id &&
          bank_order_date_time == o.bank_order_date_time &&
          placement_programs == o.placement_programs &&
          inns == o.inns &&
          campaign_ids == o.campaign_ids
    end

    # @see the `==` method
    # @param [Object] Object to be compared
    def eql?(o)
      self == o
    end

    # Calculates hash code according to all attributes.
    # @return [Integer] Hash code
    def hash
      [business_id, date_time_from, date_time_to, date_from, date_to, bank_order_id, bank_order_date_time, placement_programs, inns, campaign_ids].hash
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def self.build_from_hash(attributes)
      new.build_from_hash(attributes)
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def build_from_hash(attributes)
      return nil unless attributes.is_a?(Hash)
      self.class.openapi_types.each_pair do |key, type|
        if !attributes[self.class.attribute_map[key]]? && self.class.openapi_nullable.includes?(key)
          self.send("#{key}=", nil)
        elsif type =~ /\AArray<(.*)>/i
          # check to ensure the input is an array given that the attribute
          # is documented as an array but the input is not
          if attributes[self.class.attribute_map[key]].is_a?(Array)
            self.send("#{key}=", attributes[self.class.attribute_map[key]].map { |v| _deserialize($1, v) })
          end
        elsif !attributes[self.class.attribute_map[key]].nil?
          self.send("#{key}=", _deserialize(type, attributes[self.class.attribute_map[key]]))
        end
      end

      self
    end

    # Deserializes the data based on type
    # @param string type Data type
    # @param string value Value to be deserialized
    # @return [Object] Deserialized data
    def _deserialize(type, value)
      case type.to_sym
      when :Time
        Time.parse(value)
      when :Date
        Date.parse(value)
      when :String
        value.to_s
      when :Integer
        value.to_i
      when :Float
        value.to_f
      when :Boolean
        if value.to_s =~ /\A(true|t|yes|y|1)\z/i
          true
        else
          false
        end
      when :Object
        # generic object (usually a Hash), return directly
        value
      when /\AArray<(?<inner_type>.+)>\z/
        inner_type = Regexp.last_match[:inner_type]
        value.map { |v| _deserialize(inner_type, v) }
      when /\AHash<(?<k_type>.+?), (?<v_type>.+)>\z/
        k_type = Regexp.last_match[:k_type]
        v_type = Regexp.last_match[:v_type]
        ({} of Symbol => String).tap do |hash|
          value.each do |k, v|
            hash[_deserialize(k_type, k)] = _deserialize(v_type, v)
          end
        end
      else # model
        # models (e.g. Pet) or oneOf
        klass = OpenAPIClient.const_get(type)
        klass.respond_to?(:openapi_one_of) ? klass.build(value) : klass.build_from_hash(value)
      end
    end

    # Returns the string representation of the object
    # @return [String] String presentation of the object
    def to_s
      to_hash.to_s
    end

    # to_body is an alias to to_hash (backward compatibility)
    # @return [Hash] Returns the object in the form of hash
    def to_body
      to_hash
    end

    # Returns the object in the form of hash
    # @return [Hash] Returns the object in the form of hash
    def to_hash
      hash = {} of Symbol => String
      self.class.attribute_map.each_pair do |attr, param|
        value = self.send(attr)
        if value.nil?
          is_nullable = self.class.openapi_nullable.includes?(attr)
          next if !is_nullable || (is_nullable && !instance_variable_defined?(:"@#{attr}"))
        end

        hash[param] = _to_hash(value)
      end
      hash
    end

    # Outputs non-array value in the form of hash
    # For object, use to_hash. Otherwise, just return the value
    # @param [Object] value Any valid value
    # @return [Hash] Returns the value in the form of hash
    def _to_hash(value)
      if value.is_a?(Array)
        value.compact.map { |v| _to_hash(v) }
      elsif value.is_a?(Hash)
        ({} of Symbol => String).tap do |hash|
          value.each { |k, v| hash[k] = _to_hash(v) }
        end
      elsif value.respond_to? :to_hash
        value.to_hash
      else
        value
      end
    end

  end

end
