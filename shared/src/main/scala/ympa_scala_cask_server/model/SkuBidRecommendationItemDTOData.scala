/**
 * Партнерский API Маркета
 * API Яндекс Маркета помогает продавцам автоматизировать и упростить работу с маркетплейсом.  В числе возможностей интеграции:  * управление каталогом товаров и витриной,  * обработка заказов,  * изменение настроек магазина,  * получение отчетов. 
 *
 * OpenAPI spec version: LATEST
 *
 * Contact: team@openapitools.org
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 *
 * https://openapi-generator.tech
 */

// this model was generated using modelData.mustache
package ympa_scala_cask_server.model
import ympa_scala_cask_server.model.BidRecommendationItemDTO
import ympa_scala_cask_server.model.PriceRecommendationItemDTO
import scala.util.control.NonFatal
import scala.util.*

// see https://com-lihaoyi.github.io/upickle/
import upickle.default.{ReadWriter => RW, macroRW}
import upickle.default.*

/** SkuBidRecommendationItemDTOData a data transfer object, primarily for simple json serialisation.
  * It has no validation - there may be nulls, values out of range, etc
  */
case class SkuBidRecommendationItemDTOData(
/* Ваш SKU — идентификатор товара в вашей системе.  Разрешена любая последовательность длиной до 255 знаков.  Правила использования SKU:  * У каждого товара SKU должен быть свой.  * SKU товара нельзя менять — можно только удалить товар и добавить заново с новым SKU.  * Уже заданный SKU нельзя освободить и использовать заново для другого товара. Каждый товар должен получать новый идентификатор, до того никогда не использовавшийся в вашем каталоге.  [Что такое SKU и как его назначать](https://yandex.ru/support/marketplace/assortment/add/index.html#fields)  */
  sku: String,

  /* Значение ставки. */
  bid: Int,

  /* Список рекомендованных ставок с соответствующими долями показов. Чем больше ставка, тем большую долю показов она помогает получить.  */
  bidRecommendations: Seq[BidRecommendationItemDTOData] = Nil ,

  /* Рекомендованные цены. */
  priceRecommendations: Seq[PriceRecommendationItemDTOData] = Nil 

  ) {

  def asJson: String = write(this)

  def validationErrors(path : Seq[Field], failFast : Boolean) : Seq[ValidationError] = {
    val errors = scala.collection.mutable.ListBuffer[ValidationError]()
        // ==================
        // sku
        // validate against pattern '^[^\\x00-\\x08\\x0A-\\x1f\\x7f]{1,255}$'
        if (errors.isEmpty || !failFast) {
           val regex = """^[^\\x00-\\x08\\x0A-\\x1f\\x7f]{1,255}$"""
           if sku == null || !regex.r.matches(sku) then
              errors += ValidationError(path :+ SkuBidRecommendationItemDTO.Fields.sku, s"value '$sku' doesn't match pattern $regex")
        }



        // validate min length 1
        if (errors.isEmpty || !failFast)  {
          val len = if sku == null then 0 else sku.length
            if (len < 1) {
               errors += ValidationError(path :+ SkuBidRecommendationItemDTO.Fields.sku, s"length $len is shorter than the min length 1")
            }
        }

        // validate max length 255
        if (errors.isEmpty || !failFast)  {
          val len = if sku == null then 0 else sku.length
            if (len < 255) {
               errors += ValidationError(path :+ SkuBidRecommendationItemDTO.Fields.sku, s"length $len is longer than the max length 255")
            }
        }


        







        

        // ==================
        // bid

        // validate against minimum 0
        if (errors.isEmpty || !failFast) {
            if !(bid >= 0) then
            errors += ValidationError(path :+ SkuBidRecommendationItemDTO.Fields.bid, s"value '$bid' is not greater than the minimum value 0")
        }

        // validate against maximum 9999
        if (errors.isEmpty || !failFast) {
            if !(bid <= 9999) then
            errors += ValidationError(path :+ SkuBidRecommendationItemDTO.Fields.bid, s"value '$bid' is not greater than the maximum value 9999")
        }




        







        

        // ==================
        // bidRecommendations






        







        
        if (errors.isEmpty || !failFast) {
            if (bidRecommendations != null) {
                bidRecommendations.zipWithIndex.foreach {
                    case (value, i) if errors.isEmpty || !failFast =>
                      errors ++= value.validationErrors(
                        path :+ SkuBidRecommendationItemDTO.Fields.bidRecommendations :+ Field(i.toString),
                        failFast)
                    case (value, i) =>
                }
            }
        }
        

        // ==================
        // priceRecommendations






        







        
        if (errors.isEmpty || !failFast) {
            if (priceRecommendations != null) {
                priceRecommendations.zipWithIndex.foreach {
                    case (value, i) if errors.isEmpty || !failFast =>
                      errors ++= value.validationErrors(
                        path :+ SkuBidRecommendationItemDTO.Fields.priceRecommendations :+ Field(i.toString),
                        failFast)
                    case (value, i) =>
                }
            }
        }
        

    errors.toSeq
  }

  def validated(failFast : Boolean = false) : scala.util.Try[SkuBidRecommendationItemDTO] = {
    validationErrors(Vector(), failFast) match {
      case Seq() => Success(asModel)
      case first +: theRest => Failure(ValidationErrors(first, theRest))
    }
  }

  /** use 'validated' to check validation */
  def asModel : SkuBidRecommendationItemDTO = {
    SkuBidRecommendationItemDTO(
        sku = 
        sku
        
        ,
        bid = 
        bid
        
        ,
        bidRecommendations = 
        bidRecommendations
        
        .map(_.asModel),
        priceRecommendations = 
        priceRecommendations
        
        .map(_.asModel)
    )
  }
}

object SkuBidRecommendationItemDTOData {

  given readWriter : RW[SkuBidRecommendationItemDTOData] = macroRW

  def fromJsonString(jason : String) : SkuBidRecommendationItemDTOData = try {
        read[SkuBidRecommendationItemDTOData](jason)
     } catch {
          case NonFatal(e) => sys.error(s"Error parsing json '$jason': $e")
     }

  def manyFromJsonString(jason : String) : Seq[SkuBidRecommendationItemDTOData] = try {
        read[List[SkuBidRecommendationItemDTOData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as list: $e")
    }

  def manyFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Seq[SkuBidRecommendationItemDTO]] = {
      Try(manyFromJsonString(jason)).flatMap { list =>
        list.zipWithIndex.foldLeft(Try(Vector[SkuBidRecommendationItemDTO]())) {
          case (Success(list), (next, i)) => 
            next.validated(failFast) match {
              case Success(ok) => Success(list :+ ok)
              case Failure(err) => Failure(new Exception(s"Validation error on element $i: ${err.getMessage}", err))
            }
          case (fail, _)  => fail
        }
      }
    }

  def mapFromJsonString(jason : String) : Map[String, SkuBidRecommendationItemDTOData] = try {
        read[Map[String, SkuBidRecommendationItemDTOData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as map: $e")
    }


  def mapFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Map[String, SkuBidRecommendationItemDTO]] = {
     Try(mapFromJsonString(jason)).flatMap { map =>
       map.foldLeft(Try(Map[String, SkuBidRecommendationItemDTO]())) {
         case (Success(map), (key, next)) =>
           next.validated(failFast) match {
             case Success(ok) => Success(map.updated(key, ok))
             case Failure(err) => Failure(new Exception(s"Validation error on element $key: ${err.getMessage}", err))
           }
         case (fail, _) => fail
       }
     }
  }
}

