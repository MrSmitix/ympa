/**
 * Партнерский API Маркета
 * API Яндекс Маркета помогает продавцам автоматизировать и упростить работу с маркетплейсом.  В числе возможностей интеграции:  * управление каталогом товаров и витриной,  * обработка заказов,  * изменение настроек магазина,  * получение отчетов. 
 *
 * OpenAPI spec version: LATEST
 *
 * Contact: team@openapitools.org
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 *
 * https://openapi-generator.tech
 */

// this model was generated using modelData.mustache
package ympa_scala_cask_server.model
import ympa_scala_cask_server.model.MappingsOfferDTO
import ympa_scala_cask_server.model.OfferMappingDTO
import scala.util.control.NonFatal
import scala.util.*

// see https://com-lihaoyi.github.io/upickle/
import upickle.default.{ReadWriter => RW, macroRW}
import upickle.default.*

/** OfferMappingEntryDTOData a data transfer object, primarily for simple json serialisation.
  * It has no validation - there may be nulls, values out of range, etc
  */
case class OfferMappingEntryDTOData(
  mapping: OfferMappingDTOData = null ,

    awaitingModerationMapping: OfferMappingDTOData = null ,

    rejectedMapping: OfferMappingDTOData = null ,

    offer: MappingsOfferDTOData = null 

  ) {

  def asJson: String = write(this)

  def validationErrors(path : Seq[Field], failFast : Boolean) : Seq[ValidationError] = {
    val errors = scala.collection.mutable.ListBuffer[ValidationError]()
        // ==================
        // mapping






        







        
        // validating mapping
        if (errors.isEmpty || !failFast) {
            if mapping != null then errors ++= mapping.validationErrors(path :+ OfferMappingEntryDTO.Fields.mapping, failFast)
        }

        // ==================
        // awaitingModerationMapping






        







        
        // validating awaitingModerationMapping
        if (errors.isEmpty || !failFast) {
            if awaitingModerationMapping != null then errors ++= awaitingModerationMapping.validationErrors(path :+ OfferMappingEntryDTO.Fields.awaitingModerationMapping, failFast)
        }

        // ==================
        // rejectedMapping






        







        
        // validating rejectedMapping
        if (errors.isEmpty || !failFast) {
            if rejectedMapping != null then errors ++= rejectedMapping.validationErrors(path :+ OfferMappingEntryDTO.Fields.rejectedMapping, failFast)
        }

        // ==================
        // offer






        







        
        // validating offer
        if (errors.isEmpty || !failFast) {
            if offer != null then errors ++= offer.validationErrors(path :+ OfferMappingEntryDTO.Fields.offer, failFast)
        }

    errors.toSeq
  }

  def validated(failFast : Boolean = false) : scala.util.Try[OfferMappingEntryDTO] = {
    validationErrors(Vector(), failFast) match {
      case Seq() => Success(asModel)
      case first +: theRest => Failure(ValidationErrors(first, theRest))
    }
  }

  /** use 'validated' to check validation */
  def asModel : OfferMappingEntryDTO = {
    OfferMappingEntryDTO(
        mapping = Option(
        mapping
        )
        .map(_.asModel),
        awaitingModerationMapping = Option(
        awaitingModerationMapping
        )
        .map(_.asModel),
        rejectedMapping = Option(
        rejectedMapping
        )
        .map(_.asModel),
        offer = Option(
        offer
        )
        .map(_.asModel)
    )
  }
}

object OfferMappingEntryDTOData {

  given readWriter : RW[OfferMappingEntryDTOData] = macroRW

  def fromJsonString(jason : String) : OfferMappingEntryDTOData = try {
        read[OfferMappingEntryDTOData](jason)
     } catch {
          case NonFatal(e) => sys.error(s"Error parsing json '$jason': $e")
     }

  def manyFromJsonString(jason : String) : Seq[OfferMappingEntryDTOData] = try {
        read[List[OfferMappingEntryDTOData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as list: $e")
    }

  def manyFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Seq[OfferMappingEntryDTO]] = {
      Try(manyFromJsonString(jason)).flatMap { list =>
        list.zipWithIndex.foldLeft(Try(Vector[OfferMappingEntryDTO]())) {
          case (Success(list), (next, i)) => 
            next.validated(failFast) match {
              case Success(ok) => Success(list :+ ok)
              case Failure(err) => Failure(new Exception(s"Validation error on element $i: ${err.getMessage}", err))
            }
          case (fail, _)  => fail
        }
      }
    }

  def mapFromJsonString(jason : String) : Map[String, OfferMappingEntryDTOData] = try {
        read[Map[String, OfferMappingEntryDTOData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as map: $e")
    }


  def mapFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Map[String, OfferMappingEntryDTO]] = {
     Try(mapFromJsonString(jason)).flatMap { map =>
       map.foldLeft(Try(Map[String, OfferMappingEntryDTO]())) {
         case (Success(map), (key, next)) =>
           next.validated(failFast) match {
             case Success(ok) => Success(map.updated(key, ok))
             case Failure(err) => Failure(new Exception(s"Validation error on element $key: ${err.getMessage}", err))
           }
         case (fail, _) => fail
       }
     }
  }
}

