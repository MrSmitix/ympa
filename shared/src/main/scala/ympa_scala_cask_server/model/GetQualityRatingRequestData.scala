/**
 * Партнерский API Маркета
 * API Яндекс Маркета помогает продавцам автоматизировать и упростить работу с маркетплейсом.  В числе возможностей интеграции:  * управление каталогом товаров и витриной,  * обработка заказов,  * изменение настроек магазина,  * получение отчетов. 
 *
 * OpenAPI spec version: LATEST
 *
 * Contact: team@openapitools.org
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 *
 * https://openapi-generator.tech
 */

// this model was generated using modelData.mustache
package ympa_scala_cask_server.model
import java.time.LocalDate
import scala.util.control.NonFatal
import scala.util.*

// see https://com-lihaoyi.github.io/upickle/
import upickle.default.{ReadWriter => RW, macroRW}
import upickle.default.*

/** GetQualityRatingRequestData a data transfer object, primarily for simple json serialisation.
  * It has no validation - there may be nulls, values out of range, etc
  */
case class GetQualityRatingRequestData(
/* Начало периода.  Формат даты: `ГГГГ‑ММ‑ДД`.  Не может быть раньше 30 дней от текущей даты.  */
  dateFrom: LocalDate = null ,

  /* Конец периода.  Формат даты: `ГГГГ‑ММ‑ДД`.  Не может быть позже текущей даты.  */
  dateTo: LocalDate = null ,

  /* Список идентификаторов магазинов. */
  campaignIds: Set[Long]

  ) {

  def asJson: String = write(this)

  def validationErrors(path : Seq[Field], failFast : Boolean) : Seq[ValidationError] = {
    val errors = scala.collection.mutable.ListBuffer[ValidationError]()
        // ==================
        // dateFrom






        







        

        // ==================
        // dateTo






        







        

        // ==================
        // campaignIds






        

          // validate campaignIds has unique items
          if (errors.isEmpty || !failFast) {
              if (campaignIds != null) {
                campaignIds.foldLeft(Set[Long]()) {
                  case (set, next) if set.contains(next) =>
                    errors += ValidationError(
                      path :+ GetQualityRatingRequest.Fields.campaignIds,
                      s"duplicate value: $next"
                    )
                    set + next
                  case (set, next) => set + next
                }
              }
          }


        // validate min items 1
        if (errors.isEmpty || !failFast) {
          val len = if campaignIds == null then 0 else campaignIds.size
            if (len < 1) {
               errors += ValidationError(path :+ GetQualityRatingRequest.Fields.campaignIds, s"campaignIds has $len, which is less than the min items 1")
            }
        }

        // validate min items 50
        if (errors.isEmpty || !failFast) {
          val len = if campaignIds == null then 0 else campaignIds.size
            if (len > 50) {
               errors += ValidationError(path :+ GetQualityRatingRequest.Fields.campaignIds, s"campaignIds has $len, which is greater than the max items 50")
            }
        }



        

    errors.toSeq
  }

  def validated(failFast : Boolean = false) : scala.util.Try[GetQualityRatingRequest] = {
    validationErrors(Vector(), failFast) match {
      case Seq() => Success(asModel)
      case first +: theRest => Failure(ValidationErrors(first, theRest))
    }
  }

  /** use 'validated' to check validation */
  def asModel : GetQualityRatingRequest = {
    GetQualityRatingRequest(
        dateFrom = Option(
        dateFrom
        )
        ,
        dateTo = Option(
        dateTo
        )
        ,
        campaignIds = 
        campaignIds
        
        
    )
  }
}

object GetQualityRatingRequestData {

  given readWriter : RW[GetQualityRatingRequestData] = macroRW

  def fromJsonString(jason : String) : GetQualityRatingRequestData = try {
        read[GetQualityRatingRequestData](jason)
     } catch {
          case NonFatal(e) => sys.error(s"Error parsing json '$jason': $e")
     }

  def manyFromJsonString(jason : String) : Seq[GetQualityRatingRequestData] = try {
        read[List[GetQualityRatingRequestData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as list: $e")
    }

  def manyFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Seq[GetQualityRatingRequest]] = {
      Try(manyFromJsonString(jason)).flatMap { list =>
        list.zipWithIndex.foldLeft(Try(Vector[GetQualityRatingRequest]())) {
          case (Success(list), (next, i)) => 
            next.validated(failFast) match {
              case Success(ok) => Success(list :+ ok)
              case Failure(err) => Failure(new Exception(s"Validation error on element $i: ${err.getMessage}", err))
            }
          case (fail, _)  => fail
        }
      }
    }

  def mapFromJsonString(jason : String) : Map[String, GetQualityRatingRequestData] = try {
        read[Map[String, GetQualityRatingRequestData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as map: $e")
    }


  def mapFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Map[String, GetQualityRatingRequest]] = {
     Try(mapFromJsonString(jason)).flatMap { map =>
       map.foldLeft(Try(Map[String, GetQualityRatingRequest]())) {
         case (Success(map), (key, next)) =>
           next.validated(failFast) match {
             case Success(ok) => Success(map.updated(key, ok))
             case Failure(err) => Failure(new Exception(s"Validation error on element $key: ${err.getMessage}", err))
           }
         case (fail, _) => fail
       }
     }
  }
}

