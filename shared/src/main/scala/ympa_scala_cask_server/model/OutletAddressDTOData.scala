/**
 * Партнерский API Маркета
 * API Яндекс Маркета помогает продавцам автоматизировать и упростить работу с маркетплейсом.  В числе возможностей интеграции:  * управление каталогом товаров и витриной,  * обработка заказов,  * изменение настроек магазина,  * получение отчетов. 
 *
 * OpenAPI spec version: LATEST
 *
 * Contact: team@openapitools.org
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 *
 * https://openapi-generator.tech
 */

// this model was generated using modelData.mustache
package ympa_scala_cask_server.model
import scala.util.control.NonFatal
import scala.util.*

// see https://com-lihaoyi.github.io/upickle/
import upickle.default.{ReadWriter => RW, macroRW}
import upickle.default.*

/** OutletAddressDTOData a data transfer object, primarily for simple json serialisation.
  * It has no validation - there may be nulls, values out of range, etc
  */
case class OutletAddressDTOData(
/* Идентификатор региона.  Идентификатор можно получить c помощью запроса [GET regions](../../reference/regions/searchRegionsByName.md).  {% note alert %}  При создании и редактировании точек продаж можно указывать только регионы типов `TOWN` (город), `CITY` (крупный город) и `REPUBLIC_AREA` (район субъекта федерации). Тип региона указан в выходных параметрах type запросов [GET regions](../../reference/regions/searchRegionsByName.md) и [GET regions/{regionId}](../../reference/regions/searchRegionsById.md).  {% endnote %}  */
  regionId: Long,

  /* Улица. */
  street: String = "" ,

  /* Номер дома. */
  number: String = "" ,

  /* Номер строения. */
  building: String = "" ,

  /* Номер владения. */
  estate: String = "" ,

  /* Номер корпуса. */
  block: String = "" ,

  /* Дополнительная информация. */
  additional: String = "" ,

  /* Порядковый номер километра дороги, на котором располагается точка продаж, если отсутствует улица. */
  km: Int = 0 ,

  /* {% note warning \"\" %}  Этот параметр устарел. Не используйте его. Город или населенный пункт возвращается в параметре `regionId`.  {% endnote %}  */
  city: String = "" 

  ) {

  def asJson: String = write(this)

  def validationErrors(path : Seq[Field], failFast : Boolean) : Seq[ValidationError] = {
    val errors = scala.collection.mutable.ListBuffer[ValidationError]()
        // ==================
        // regionId






        







        

        // ==================
        // street




        // validate max length 512
        if (errors.isEmpty || !failFast)  {
          val len = if street == null then 0 else street.length
            if (len < 512) {
               errors += ValidationError(path :+ OutletAddressDTO.Fields.street, s"length $len is longer than the max length 512")
            }
        }


        







        

        // ==================
        // number




        // validate max length 256
        if (errors.isEmpty || !failFast)  {
          val len = if number == null then 0 else number.length
            if (len < 256) {
               errors += ValidationError(path :+ OutletAddressDTO.Fields.number, s"length $len is longer than the max length 256")
            }
        }


        







        

        // ==================
        // building




        // validate max length 16
        if (errors.isEmpty || !failFast)  {
          val len = if building == null then 0 else building.length
            if (len < 16) {
               errors += ValidationError(path :+ OutletAddressDTO.Fields.building, s"length $len is longer than the max length 16")
            }
        }


        







        

        // ==================
        // estate




        // validate max length 16
        if (errors.isEmpty || !failFast)  {
          val len = if estate == null then 0 else estate.length
            if (len < 16) {
               errors += ValidationError(path :+ OutletAddressDTO.Fields.estate, s"length $len is longer than the max length 16")
            }
        }


        







        

        // ==================
        // block




        // validate max length 16
        if (errors.isEmpty || !failFast)  {
          val len = if block == null then 0 else block.length
            if (len < 16) {
               errors += ValidationError(path :+ OutletAddressDTO.Fields.block, s"length $len is longer than the max length 16")
            }
        }


        







        

        // ==================
        // additional






        







        

        // ==================
        // km






        







        

        // ==================
        // city




        // validate max length 200
        if (errors.isEmpty || !failFast)  {
          val len = if city == null then 0 else city.length
            if (len < 200) {
               errors += ValidationError(path :+ OutletAddressDTO.Fields.city, s"length $len is longer than the max length 200")
            }
        }


        







        

    errors.toSeq
  }

  def validated(failFast : Boolean = false) : scala.util.Try[OutletAddressDTO] = {
    validationErrors(Vector(), failFast) match {
      case Seq() => Success(asModel)
      case first +: theRest => Failure(ValidationErrors(first, theRest))
    }
  }

  /** use 'validated' to check validation */
  def asModel : OutletAddressDTO = {
    OutletAddressDTO(
        regionId = 
        regionId
        
        ,
        street = Option(
        street
        )
        ,
        number = Option(
        number
        )
        ,
        building = Option(
        building
        )
        ,
        estate = Option(
        estate
        )
        ,
        block = Option(
        block
        )
        ,
        additional = Option(
        additional
        )
        ,
        km = Option(
        km
        )
        ,
        city = Option(
        city
        )
        
    )
  }
}

object OutletAddressDTOData {

  given readWriter : RW[OutletAddressDTOData] = macroRW

  def fromJsonString(jason : String) : OutletAddressDTOData = try {
        read[OutletAddressDTOData](jason)
     } catch {
          case NonFatal(e) => sys.error(s"Error parsing json '$jason': $e")
     }

  def manyFromJsonString(jason : String) : Seq[OutletAddressDTOData] = try {
        read[List[OutletAddressDTOData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as list: $e")
    }

  def manyFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Seq[OutletAddressDTO]] = {
      Try(manyFromJsonString(jason)).flatMap { list =>
        list.zipWithIndex.foldLeft(Try(Vector[OutletAddressDTO]())) {
          case (Success(list), (next, i)) => 
            next.validated(failFast) match {
              case Success(ok) => Success(list :+ ok)
              case Failure(err) => Failure(new Exception(s"Validation error on element $i: ${err.getMessage}", err))
            }
          case (fail, _)  => fail
        }
      }
    }

  def mapFromJsonString(jason : String) : Map[String, OutletAddressDTOData] = try {
        read[Map[String, OutletAddressDTOData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as map: $e")
    }


  def mapFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Map[String, OutletAddressDTO]] = {
     Try(mapFromJsonString(jason)).flatMap { map =>
       map.foldLeft(Try(Map[String, OutletAddressDTO]())) {
         case (Success(map), (key, next)) =>
           next.validated(failFast) match {
             case Success(ok) => Success(map.updated(key, ok))
             case Failure(err) => Failure(new Exception(s"Validation error on element $key: ${err.getMessage}", err))
           }
         case (fail, _) => fail
       }
     }
  }
}

