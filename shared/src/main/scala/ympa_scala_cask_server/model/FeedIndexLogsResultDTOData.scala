/**
 * Партнерский API Маркета
 * API Яндекс Маркета помогает продавцам автоматизировать и упростить работу с маркетплейсом.  В числе возможностей интеграции:  * управление каталогом товаров и витриной,  * обработка заказов,  * изменение настроек магазина,  * получение отчетов. 
 *
 * OpenAPI spec version: LATEST
 *
 * Contact: team@openapitools.org
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 *
 * https://openapi-generator.tech
 */

// this model was generated using modelData.mustache
package ympa_scala_cask_server.model
import ympa_scala_cask_server.model.FeedIndexLogsFeedDTO
import ympa_scala_cask_server.model.FeedIndexLogsRecordDTO
import scala.util.control.NonFatal
import scala.util.*

// see https://com-lihaoyi.github.io/upickle/
import upickle.default.{ReadWriter => RW, macroRW}
import upickle.default.*

/** FeedIndexLogsResultDTOData a data transfer object, primarily for simple json serialisation.
  * It has no validation - there may be nulls, values out of range, etc
  */
case class FeedIndexLogsResultDTOData(
  feed: FeedIndexLogsFeedDTOData = null ,

  /* Список отчетов по индексации прайс-листа. */
  indexLogRecords: Seq[FeedIndexLogsRecordDTOData],

  /* Количество отчетов на всех страницах выходных данных. */
  total: Long = 0 

  ) {

  def asJson: String = write(this)

  def validationErrors(path : Seq[Field], failFast : Boolean) : Seq[ValidationError] = {
    val errors = scala.collection.mutable.ListBuffer[ValidationError]()
        // ==================
        // feed






        







        
        // validating feed
        if (errors.isEmpty || !failFast) {
            if feed != null then errors ++= feed.validationErrors(path :+ FeedIndexLogsResultDTO.Fields.feed, failFast)
        }

        // ==================
        // indexLogRecords






        
        if (errors.isEmpty || !failFast) {
            if (indexLogRecords == null) {
            errors += ValidationError(path :+ FeedIndexLogsResultDTO.Fields.indexLogRecords, "indexLogRecords is a required field and cannot be null")
            }
        }
        







        
        if (errors.isEmpty || !failFast) {
            if (indexLogRecords != null) {
                indexLogRecords.zipWithIndex.foreach {
                    case (value, i) if errors.isEmpty || !failFast =>
                      errors ++= value.validationErrors(
                        path :+ FeedIndexLogsResultDTO.Fields.indexLogRecords :+ Field(i.toString),
                        failFast)
                    case (value, i) =>
                }
            }
        }
        

        // ==================
        // total






        







        

    errors.toSeq
  }

  def validated(failFast : Boolean = false) : scala.util.Try[FeedIndexLogsResultDTO] = {
    validationErrors(Vector(), failFast) match {
      case Seq() => Success(asModel)
      case first +: theRest => Failure(ValidationErrors(first, theRest))
    }
  }

  /** use 'validated' to check validation */
  def asModel : FeedIndexLogsResultDTO = {
    FeedIndexLogsResultDTO(
        feed = Option(
        feed
        )
        .map(_.asModel),
        indexLogRecords = 
        indexLogRecords
        
        .map(_.asModel),
        total = Option(
        total
        )
        
    )
  }
}

object FeedIndexLogsResultDTOData {

  given readWriter : RW[FeedIndexLogsResultDTOData] = macroRW

  def fromJsonString(jason : String) : FeedIndexLogsResultDTOData = try {
        read[FeedIndexLogsResultDTOData](jason)
     } catch {
          case NonFatal(e) => sys.error(s"Error parsing json '$jason': $e")
     }

  def manyFromJsonString(jason : String) : Seq[FeedIndexLogsResultDTOData] = try {
        read[List[FeedIndexLogsResultDTOData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as list: $e")
    }

  def manyFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Seq[FeedIndexLogsResultDTO]] = {
      Try(manyFromJsonString(jason)).flatMap { list =>
        list.zipWithIndex.foldLeft(Try(Vector[FeedIndexLogsResultDTO]())) {
          case (Success(list), (next, i)) => 
            next.validated(failFast) match {
              case Success(ok) => Success(list :+ ok)
              case Failure(err) => Failure(new Exception(s"Validation error on element $i: ${err.getMessage}", err))
            }
          case (fail, _)  => fail
        }
      }
    }

  def mapFromJsonString(jason : String) : Map[String, FeedIndexLogsResultDTOData] = try {
        read[Map[String, FeedIndexLogsResultDTOData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as map: $e")
    }


  def mapFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Map[String, FeedIndexLogsResultDTO]] = {
     Try(mapFromJsonString(jason)).flatMap { map =>
       map.foldLeft(Try(Map[String, FeedIndexLogsResultDTO]())) {
         case (Success(map), (key, next)) =>
           next.validated(failFast) match {
             case Success(ok) => Success(map.updated(key, ok))
             case Failure(err) => Failure(new Exception(s"Validation error on element $key: ${err.getMessage}", err))
           }
         case (fail, _) => fail
       }
     }
  }
}

