/**
 * Партнерский API Маркета
 * API Яндекс Маркета помогает продавцам автоматизировать и упростить работу с маркетплейсом.  В числе возможностей интеграции:  * управление каталогом товаров и витриной,  * обработка заказов,  * изменение настроек магазина,  * получение отчетов. 
 *
 * OpenAPI spec version: LATEST
 *
 * Contact: team@openapitools.org
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 *
 * https://openapi-generator.tech
 */

// this model was generated using modelData.mustache
package ympa_scala_cask_server.model
import ympa_scala_cask_server.model.FlippingPagerDTO
import ympa_scala_cask_server.model.RegionDTO
import scala.util.control.NonFatal
import scala.util.*

// see https://com-lihaoyi.github.io/upickle/
import upickle.default.{ReadWriter => RW, macroRW}
import upickle.default.*

/** GetRegionWithChildrenResponseData a data transfer object, primarily for simple json serialisation.
  * It has no validation - there may be nulls, values out of range, etc
  */
case class GetRegionWithChildrenResponseData(
  pager: FlippingPagerDTOData = null ,

    regions: RegionDTOData = null 

  ) {

  def asJson: String = write(this)

  def validationErrors(path : Seq[Field], failFast : Boolean) : Seq[ValidationError] = {
    val errors = scala.collection.mutable.ListBuffer[ValidationError]()
        // ==================
        // pager






        







        
        // validating pager
        if (errors.isEmpty || !failFast) {
            if pager != null then errors ++= pager.validationErrors(path :+ GetRegionWithChildrenResponse.Fields.pager, failFast)
        }

        // ==================
        // regions






        







        
        // validating regions
        if (errors.isEmpty || !failFast) {
            if regions != null then errors ++= regions.validationErrors(path :+ GetRegionWithChildrenResponse.Fields.regions, failFast)
        }

    errors.toSeq
  }

  def validated(failFast : Boolean = false) : scala.util.Try[GetRegionWithChildrenResponse] = {
    validationErrors(Vector(), failFast) match {
      case Seq() => Success(asModel)
      case first +: theRest => Failure(ValidationErrors(first, theRest))
    }
  }

  /** use 'validated' to check validation */
  def asModel : GetRegionWithChildrenResponse = {
    GetRegionWithChildrenResponse(
        pager = Option(
        pager
        )
        .map(_.asModel),
        regions = Option(
        regions
        )
        .map(_.asModel)
    )
  }
}

object GetRegionWithChildrenResponseData {

  given readWriter : RW[GetRegionWithChildrenResponseData] = macroRW

  def fromJsonString(jason : String) : GetRegionWithChildrenResponseData = try {
        read[GetRegionWithChildrenResponseData](jason)
     } catch {
          case NonFatal(e) => sys.error(s"Error parsing json '$jason': $e")
     }

  def manyFromJsonString(jason : String) : Seq[GetRegionWithChildrenResponseData] = try {
        read[List[GetRegionWithChildrenResponseData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as list: $e")
    }

  def manyFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Seq[GetRegionWithChildrenResponse]] = {
      Try(manyFromJsonString(jason)).flatMap { list =>
        list.zipWithIndex.foldLeft(Try(Vector[GetRegionWithChildrenResponse]())) {
          case (Success(list), (next, i)) => 
            next.validated(failFast) match {
              case Success(ok) => Success(list :+ ok)
              case Failure(err) => Failure(new Exception(s"Validation error on element $i: ${err.getMessage}", err))
            }
          case (fail, _)  => fail
        }
      }
    }

  def mapFromJsonString(jason : String) : Map[String, GetRegionWithChildrenResponseData] = try {
        read[Map[String, GetRegionWithChildrenResponseData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as map: $e")
    }


  def mapFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Map[String, GetRegionWithChildrenResponse]] = {
     Try(mapFromJsonString(jason)).flatMap { map =>
       map.foldLeft(Try(Map[String, GetRegionWithChildrenResponse]())) {
         case (Success(map), (key, next)) =>
           next.validated(failFast) match {
             case Success(ok) => Success(map.updated(key, ok))
             case Failure(err) => Failure(new Exception(s"Validation error on element $key: ${err.getMessage}", err))
           }
         case (fail, _) => fail
       }
     }
  }
}

