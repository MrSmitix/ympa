/**
 * Партнерский API Маркета
 * API Яндекс Маркета помогает продавцам автоматизировать и упростить работу с маркетплейсом.  В числе возможностей интеграции:  * управление каталогом товаров и витриной,  * обработка заказов,  * изменение настроек магазина,  * получение отчетов. 
 *
 * OpenAPI spec version: LATEST
 *
 * Contact: team@openapitools.org
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 *
 * https://openapi-generator.tech
 */

// this model was generated using modelData.mustache
package ympa_scala_cask_server.model
import ympa_scala_cask_server.model.LogisticPointType
import ympa_scala_cask_server.model.PickupAddressDTO
import scala.util.control.NonFatal
import scala.util.*

// see https://com-lihaoyi.github.io/upickle/
import upickle.default.{ReadWriter => RW, macroRW}
import upickle.default.*

/** LogisticPickupPointDTOData a data transfer object, primarily for simple json serialisation.
  * It has no validation - there may be nulls, values out of range, etc
  */
case class LogisticPickupPointDTOData(
/* Идентификатор пункта вывоза. */
  id: Long = 0 ,

  /* Название пункта вывоза. */
  name: String = "" ,

    address: PickupAddressDTOData = null ,

  /* Дополнительные инструкции к вывозу. */
  instruction: String = "" ,

    `type`: LogisticPointType = null ,

  /* Идентификатор логистического партнера, к которому относится логистическая точка. */
  logisticPartnerId: Long = 0 

  ) {

  def asJson: String = write(this)

  def validationErrors(path : Seq[Field], failFast : Boolean) : Seq[ValidationError] = {
    val errors = scala.collection.mutable.ListBuffer[ValidationError]()
        // ==================
        // id






        







        

        // ==================
        // name






        







        

        // ==================
        // address






        







        
        // validating address
        if (errors.isEmpty || !failFast) {
            if address != null then errors ++= address.validationErrors(path :+ LogisticPickupPointDTO.Fields.address, failFast)
        }

        // ==================
        // instruction






        







        

        // ==================
        // `type`






        







        

        // ==================
        // logisticPartnerId






        







        

    errors.toSeq
  }

  def validated(failFast : Boolean = false) : scala.util.Try[LogisticPickupPointDTO] = {
    validationErrors(Vector(), failFast) match {
      case Seq() => Success(asModel)
      case first +: theRest => Failure(ValidationErrors(first, theRest))
    }
  }

  /** use 'validated' to check validation */
  def asModel : LogisticPickupPointDTO = {
    LogisticPickupPointDTO(
        id = Option(
        id
        )
        ,
        name = Option(
        name
        )
        ,
        address = Option(
        address
        )
        .map(_.asModel),
        instruction = Option(
        instruction
        )
        ,
        `type` = Option(
        `type`
        )
        ,
        logisticPartnerId = Option(
        logisticPartnerId
        )
        
    )
  }
}

object LogisticPickupPointDTOData {

  given readWriter : RW[LogisticPickupPointDTOData] = macroRW

  def fromJsonString(jason : String) : LogisticPickupPointDTOData = try {
        read[LogisticPickupPointDTOData](jason)
     } catch {
          case NonFatal(e) => sys.error(s"Error parsing json '$jason': $e")
     }

  def manyFromJsonString(jason : String) : Seq[LogisticPickupPointDTOData] = try {
        read[List[LogisticPickupPointDTOData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as list: $e")
    }

  def manyFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Seq[LogisticPickupPointDTO]] = {
      Try(manyFromJsonString(jason)).flatMap { list =>
        list.zipWithIndex.foldLeft(Try(Vector[LogisticPickupPointDTO]())) {
          case (Success(list), (next, i)) => 
            next.validated(failFast) match {
              case Success(ok) => Success(list :+ ok)
              case Failure(err) => Failure(new Exception(s"Validation error on element $i: ${err.getMessage}", err))
            }
          case (fail, _)  => fail
        }
      }
    }

  def mapFromJsonString(jason : String) : Map[String, LogisticPickupPointDTOData] = try {
        read[Map[String, LogisticPickupPointDTOData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as map: $e")
    }


  def mapFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Map[String, LogisticPickupPointDTO]] = {
     Try(mapFromJsonString(jason)).flatMap { map =>
       map.foldLeft(Try(Map[String, LogisticPickupPointDTO]())) {
         case (Success(map), (key, next)) =>
           next.validated(failFast) match {
             case Success(ok) => Success(map.updated(key, ok))
             case Failure(err) => Failure(new Exception(s"Validation error on element $key: ${err.getMessage}", err))
           }
         case (fail, _) => fail
       }
     }
  }
}

