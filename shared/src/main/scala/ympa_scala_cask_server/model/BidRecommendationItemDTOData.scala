/**
 * Партнерский API Маркета
 * API Яндекс Маркета помогает продавцам автоматизировать и упростить работу с маркетплейсом.  В числе возможностей интеграции:  * управление каталогом товаров и витриной,  * обработка заказов,  * изменение настроек магазина,  * получение отчетов. 
 *
 * OpenAPI spec version: LATEST
 *
 * Contact: team@openapitools.org
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 *
 * https://openapi-generator.tech
 */

// this model was generated using modelData.mustache
package ympa_scala_cask_server.model
import scala.util.control.NonFatal
import scala.util.*

// see https://com-lihaoyi.github.io/upickle/
import upickle.default.{ReadWriter => RW, macroRW}
import upickle.default.*

/** BidRecommendationItemDTOData a data transfer object, primarily for simple json serialisation.
  * It has no validation - there may be nulls, values out of range, etc
  */
case class BidRecommendationItemDTOData(
/* Значение ставки. */
  bid: Int,

  /* Доля показов.  */
  showPercent: Long

  ) {

  def asJson: String = write(this)

  def validationErrors(path : Seq[Field], failFast : Boolean) : Seq[ValidationError] = {
    val errors = scala.collection.mutable.ListBuffer[ValidationError]()
        // ==================
        // bid

        // validate against minimum 0
        if (errors.isEmpty || !failFast) {
            if !(bid >= 0) then
            errors += ValidationError(path :+ BidRecommendationItemDTO.Fields.bid, s"value '$bid' is not greater than the minimum value 0")
        }

        // validate against maximum 9999
        if (errors.isEmpty || !failFast) {
            if !(bid <= 9999) then
            errors += ValidationError(path :+ BidRecommendationItemDTO.Fields.bid, s"value '$bid' is not greater than the maximum value 9999")
        }




        







        

        // ==================
        // showPercent

        // validate against minimum 0
        if (errors.isEmpty || !failFast) {
            if !(showPercent >= 0) then
            errors += ValidationError(path :+ BidRecommendationItemDTO.Fields.showPercent, s"value '$showPercent' is not greater than the minimum value 0")
        }

        // validate against maximum 100
        if (errors.isEmpty || !failFast) {
            if !(showPercent <= 100) then
            errors += ValidationError(path :+ BidRecommendationItemDTO.Fields.showPercent, s"value '$showPercent' is not greater than the maximum value 100")
        }




        







        

    errors.toSeq
  }

  def validated(failFast : Boolean = false) : scala.util.Try[BidRecommendationItemDTO] = {
    validationErrors(Vector(), failFast) match {
      case Seq() => Success(asModel)
      case first +: theRest => Failure(ValidationErrors(first, theRest))
    }
  }

  /** use 'validated' to check validation */
  def asModel : BidRecommendationItemDTO = {
    BidRecommendationItemDTO(
        bid = 
        bid
        
        ,
        showPercent = 
        showPercent
        
        
    )
  }
}

object BidRecommendationItemDTOData {

  given readWriter : RW[BidRecommendationItemDTOData] = macroRW

  def fromJsonString(jason : String) : BidRecommendationItemDTOData = try {
        read[BidRecommendationItemDTOData](jason)
     } catch {
          case NonFatal(e) => sys.error(s"Error parsing json '$jason': $e")
     }

  def manyFromJsonString(jason : String) : Seq[BidRecommendationItemDTOData] = try {
        read[List[BidRecommendationItemDTOData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as list: $e")
    }

  def manyFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Seq[BidRecommendationItemDTO]] = {
      Try(manyFromJsonString(jason)).flatMap { list =>
        list.zipWithIndex.foldLeft(Try(Vector[BidRecommendationItemDTO]())) {
          case (Success(list), (next, i)) => 
            next.validated(failFast) match {
              case Success(ok) => Success(list :+ ok)
              case Failure(err) => Failure(new Exception(s"Validation error on element $i: ${err.getMessage}", err))
            }
          case (fail, _)  => fail
        }
      }
    }

  def mapFromJsonString(jason : String) : Map[String, BidRecommendationItemDTOData] = try {
        read[Map[String, BidRecommendationItemDTOData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as map: $e")
    }


  def mapFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Map[String, BidRecommendationItemDTO]] = {
     Try(mapFromJsonString(jason)).flatMap { map =>
       map.foldLeft(Try(Map[String, BidRecommendationItemDTO]())) {
         case (Success(map), (key, next)) =>
           next.validated(failFast) match {
             case Success(ok) => Success(map.updated(key, ok))
             case Failure(err) => Failure(new Exception(s"Validation error on element $key: ${err.getMessage}", err))
           }
         case (fail, _) => fail
       }
     }
  }
}

