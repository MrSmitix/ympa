/**
 * Партнерский API Маркета
 * API Яндекс Маркета помогает продавцам автоматизировать и упростить работу с маркетплейсом.  В числе возможностей интеграции:  * управление каталогом товаров и витриной,  * обработка заказов,  * изменение настроек магазина,  * получение отчетов. 
 *
 * OpenAPI spec version: LATEST
 *
 * Contact: team@openapitools.org
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 *
 * https://openapi-generator.tech
 */

// this model was generated using modelData.mustache
package ympa_scala_cask_server.model
import java.time.OffsetDateTime
import ympa_scala_cask_server.model.ChatMessagePayloadDTO
import ympa_scala_cask_server.model.ChatMessageSenderType
import scala.util.control.NonFatal
import scala.util.*

// see https://com-lihaoyi.github.io/upickle/
import upickle.default.{ReadWriter => RW, macroRW}
import upickle.default.*

/** ChatMessageDTOData a data transfer object, primarily for simple json serialisation.
  * It has no validation - there may be nulls, values out of range, etc
  */
case class ChatMessageDTOData(
/* Идентификатор сообщения. */
  messageId: Long,

  /* Дата и время создания сообщения.  Формат даты: ISO 8601 со смещением относительно UTC. Например, `2017-11-21T00:00:00+03:00`.  */
  createdAt: OffsetDateTime,

    sender: ChatMessageSenderType,

  /* Текст сообщения.  Необязательный параметр, если возвращается параметр `payload`.  */
  message: String = "" ,

  /* Информация о приложенных к сообщению файлах.  Необязательный параметр, если возвращается параметр `message`.  */
  payload: Seq[ChatMessagePayloadDTOData] = Nil 

  ) {

  def asJson: String = write(this)

  def validationErrors(path : Seq[Field], failFast : Boolean) : Seq[ValidationError] = {
    val errors = scala.collection.mutable.ListBuffer[ValidationError]()
        // ==================
        // messageId






        







        

        // ==================
        // createdAt






        
        if (errors.isEmpty || !failFast) {
            if (createdAt == null) {
            errors += ValidationError(path :+ ChatMessageDTO.Fields.createdAt, "createdAt is a required field and cannot be null")
            }
        }
        







        

        // ==================
        // sender






        
        if (errors.isEmpty || !failFast) {
            if (sender == null) {
            errors += ValidationError(path :+ ChatMessageDTO.Fields.sender, "sender is a required field and cannot be null")
            }
        }
        







        

        // ==================
        // message






        







        

        // ==================
        // payload






        







        
        if (errors.isEmpty || !failFast) {
            if (payload != null) {
                payload.zipWithIndex.foreach {
                    case (value, i) if errors.isEmpty || !failFast =>
                      errors ++= value.validationErrors(
                        path :+ ChatMessageDTO.Fields.payload :+ Field(i.toString),
                        failFast)
                    case (value, i) =>
                }
            }
        }
        

    errors.toSeq
  }

  def validated(failFast : Boolean = false) : scala.util.Try[ChatMessageDTO] = {
    validationErrors(Vector(), failFast) match {
      case Seq() => Success(asModel)
      case first +: theRest => Failure(ValidationErrors(first, theRest))
    }
  }

  /** use 'validated' to check validation */
  def asModel : ChatMessageDTO = {
    ChatMessageDTO(
        messageId = 
        messageId
        
        ,
        createdAt = 
        createdAt
        
        ,
        sender = 
        sender
        
        ,
        message = Option(
        message
        )
        ,
        payload = 
        payload
        
        .map(_.asModel)
    )
  }
}

object ChatMessageDTOData {

  given readWriter : RW[ChatMessageDTOData] = macroRW

  def fromJsonString(jason : String) : ChatMessageDTOData = try {
        read[ChatMessageDTOData](jason)
     } catch {
          case NonFatal(e) => sys.error(s"Error parsing json '$jason': $e")
     }

  def manyFromJsonString(jason : String) : Seq[ChatMessageDTOData] = try {
        read[List[ChatMessageDTOData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as list: $e")
    }

  def manyFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Seq[ChatMessageDTO]] = {
      Try(manyFromJsonString(jason)).flatMap { list =>
        list.zipWithIndex.foldLeft(Try(Vector[ChatMessageDTO]())) {
          case (Success(list), (next, i)) => 
            next.validated(failFast) match {
              case Success(ok) => Success(list :+ ok)
              case Failure(err) => Failure(new Exception(s"Validation error on element $i: ${err.getMessage}", err))
            }
          case (fail, _)  => fail
        }
      }
    }

  def mapFromJsonString(jason : String) : Map[String, ChatMessageDTOData] = try {
        read[Map[String, ChatMessageDTOData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as map: $e")
    }


  def mapFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Map[String, ChatMessageDTO]] = {
     Try(mapFromJsonString(jason)).flatMap { map =>
       map.foldLeft(Try(Map[String, ChatMessageDTO]())) {
         case (Success(map), (key, next)) =>
           next.validated(failFast) match {
             case Success(ok) => Success(map.updated(key, ok))
             case Failure(err) => Failure(new Exception(s"Validation error on element $key: ${err.getMessage}", err))
           }
         case (fail, _) => fail
       }
     }
  }
}

