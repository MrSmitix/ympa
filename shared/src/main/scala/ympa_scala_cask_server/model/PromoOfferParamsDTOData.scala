/**
 * Партнерский API Маркета
 * API Яндекс Маркета помогает продавцам автоматизировать и упростить работу с маркетплейсом.  В числе возможностей интеграции:  * управление каталогом товаров и витриной,  * обработка заказов,  * изменение настроек магазина,  * получение отчетов. 
 *
 * OpenAPI spec version: LATEST
 *
 * Contact: team@openapitools.org
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 *
 * https://openapi-generator.tech
 */

// this model was generated using modelData.mustache
package ympa_scala_cask_server.model
import ympa_scala_cask_server.model.PromoOfferDiscountParamsDTO
import ympa_scala_cask_server.model.PromoOfferPromocodeParamsDTO
import scala.util.control.NonFatal
import scala.util.*

// see https://com-lihaoyi.github.io/upickle/
import upickle.default.{ReadWriter => RW, macroRW}
import upickle.default.*

/** PromoOfferParamsDTOData a data transfer object, primarily for simple json serialisation.
  * It has no validation - there may be nulls, values out of range, etc
  */
case class PromoOfferParamsDTOData(
  discountParams: PromoOfferDiscountParamsDTOData = null ,

    promocodeParams: PromoOfferPromocodeParamsDTOData = null 

  ) {

  def asJson: String = write(this)

  def validationErrors(path : Seq[Field], failFast : Boolean) : Seq[ValidationError] = {
    val errors = scala.collection.mutable.ListBuffer[ValidationError]()
        // ==================
        // discountParams






        







        
        // validating discountParams
        if (errors.isEmpty || !failFast) {
            if discountParams != null then errors ++= discountParams.validationErrors(path :+ PromoOfferParamsDTO.Fields.discountParams, failFast)
        }

        // ==================
        // promocodeParams






        







        
        // validating promocodeParams
        if (errors.isEmpty || !failFast) {
            if promocodeParams != null then errors ++= promocodeParams.validationErrors(path :+ PromoOfferParamsDTO.Fields.promocodeParams, failFast)
        }

    errors.toSeq
  }

  def validated(failFast : Boolean = false) : scala.util.Try[PromoOfferParamsDTO] = {
    validationErrors(Vector(), failFast) match {
      case Seq() => Success(asModel)
      case first +: theRest => Failure(ValidationErrors(first, theRest))
    }
  }

  /** use 'validated' to check validation */
  def asModel : PromoOfferParamsDTO = {
    PromoOfferParamsDTO(
        discountParams = Option(
        discountParams
        )
        .map(_.asModel),
        promocodeParams = Option(
        promocodeParams
        )
        .map(_.asModel)
    )
  }
}

object PromoOfferParamsDTOData {

  given readWriter : RW[PromoOfferParamsDTOData] = macroRW

  def fromJsonString(jason : String) : PromoOfferParamsDTOData = try {
        read[PromoOfferParamsDTOData](jason)
     } catch {
          case NonFatal(e) => sys.error(s"Error parsing json '$jason': $e")
     }

  def manyFromJsonString(jason : String) : Seq[PromoOfferParamsDTOData] = try {
        read[List[PromoOfferParamsDTOData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as list: $e")
    }

  def manyFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Seq[PromoOfferParamsDTO]] = {
      Try(manyFromJsonString(jason)).flatMap { list =>
        list.zipWithIndex.foldLeft(Try(Vector[PromoOfferParamsDTO]())) {
          case (Success(list), (next, i)) => 
            next.validated(failFast) match {
              case Success(ok) => Success(list :+ ok)
              case Failure(err) => Failure(new Exception(s"Validation error on element $i: ${err.getMessage}", err))
            }
          case (fail, _)  => fail
        }
      }
    }

  def mapFromJsonString(jason : String) : Map[String, PromoOfferParamsDTOData] = try {
        read[Map[String, PromoOfferParamsDTOData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as map: $e")
    }


  def mapFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Map[String, PromoOfferParamsDTO]] = {
     Try(mapFromJsonString(jason)).flatMap { map =>
       map.foldLeft(Try(Map[String, PromoOfferParamsDTO]())) {
         case (Success(map), (key, next)) =>
           next.validated(failFast) match {
             case Success(ok) => Success(map.updated(key, ok))
             case Failure(err) => Failure(new Exception(s"Validation error on element $key: ${err.getMessage}", err))
           }
         case (fail, _) => fail
       }
     }
  }
}

