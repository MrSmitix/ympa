/**
 * Партнерский API Маркета
 * API Яндекс Маркета помогает продавцам автоматизировать и упростить работу с маркетплейсом.  В числе возможностей интеграции:  * управление каталогом товаров и витриной,  * обработка заказов,  * изменение настроек магазина,  * получение отчетов. 
 *
 * OpenAPI spec version: LATEST
 *
 * Contact: team@openapitools.org
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 *
 * https://openapi-generator.tech
 */

// this model was generated using modelData.mustache
package ympa_scala_cask_server.model
import scala.math.BigDecimal
import ympa_scala_cask_server.model.TariffParameterDTO
import ympa_scala_cask_server.model.TariffType
import scala.util.control.NonFatal
import scala.util.*

// see https://com-lihaoyi.github.io/upickle/
import upickle.default.{ReadWriter => RW, macroRW}
import upickle.default.*

/** TariffDTOData a data transfer object, primarily for simple json serialisation.
  * It has no validation - there may be nulls, values out of range, etc
  */
case class TariffDTOData(
  `type`: TariffType,

  /* {% note warning \"\" %}  Этот параметр устарел. Не используйте его.  {% endnote %}  Значение тарифа в процентах.  */
  percent: BigDecimal = 0 ,

  /* Значение тарифа в рублях. */
  amount: BigDecimal,

  /* Параметры расчета тарифа. */
  parameters: Seq[TariffParameterDTOData]

  ) {

  def asJson: String = write(this)

  def validationErrors(path : Seq[Field], failFast : Boolean) : Seq[ValidationError] = {
    val errors = scala.collection.mutable.ListBuffer[ValidationError]()
        // ==================
        // `type`






        
        if (errors.isEmpty || !failFast) {
            if (`type` == null) {
            errors += ValidationError(path :+ TariffDTO.Fields.`type`, "`type` is a required field and cannot be null")
            }
        }
        







        

        // ==================
        // percent






        







        

        // ==================
        // amount






        
        if (errors.isEmpty || !failFast) {
            if (amount == null) {
            errors += ValidationError(path :+ TariffDTO.Fields.amount, "amount is a required field and cannot be null")
            }
        }
        







        

        // ==================
        // parameters






        
        if (errors.isEmpty || !failFast) {
            if (parameters == null) {
            errors += ValidationError(path :+ TariffDTO.Fields.parameters, "parameters is a required field and cannot be null")
            }
        }
        







        
        if (errors.isEmpty || !failFast) {
            if (parameters != null) {
                parameters.zipWithIndex.foreach {
                    case (value, i) if errors.isEmpty || !failFast =>
                      errors ++= value.validationErrors(
                        path :+ TariffDTO.Fields.parameters :+ Field(i.toString),
                        failFast)
                    case (value, i) =>
                }
            }
        }
        

    errors.toSeq
  }

  def validated(failFast : Boolean = false) : scala.util.Try[TariffDTO] = {
    validationErrors(Vector(), failFast) match {
      case Seq() => Success(asModel)
      case first +: theRest => Failure(ValidationErrors(first, theRest))
    }
  }

  /** use 'validated' to check validation */
  def asModel : TariffDTO = {
    TariffDTO(
        `type` = 
        `type`
        
        ,
        percent = Option(
        percent
        )
        ,
        amount = 
        amount
        
        ,
        parameters = 
        parameters
        
        .map(_.asModel)
    )
  }
}

object TariffDTOData {

  given readWriter : RW[TariffDTOData] = macroRW

  def fromJsonString(jason : String) : TariffDTOData = try {
        read[TariffDTOData](jason)
     } catch {
          case NonFatal(e) => sys.error(s"Error parsing json '$jason': $e")
     }

  def manyFromJsonString(jason : String) : Seq[TariffDTOData] = try {
        read[List[TariffDTOData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as list: $e")
    }

  def manyFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Seq[TariffDTO]] = {
      Try(manyFromJsonString(jason)).flatMap { list =>
        list.zipWithIndex.foldLeft(Try(Vector[TariffDTO]())) {
          case (Success(list), (next, i)) => 
            next.validated(failFast) match {
              case Success(ok) => Success(list :+ ok)
              case Failure(err) => Failure(new Exception(s"Validation error on element $i: ${err.getMessage}", err))
            }
          case (fail, _)  => fail
        }
      }
    }

  def mapFromJsonString(jason : String) : Map[String, TariffDTOData] = try {
        read[Map[String, TariffDTOData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as map: $e")
    }


  def mapFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Map[String, TariffDTO]] = {
     Try(mapFromJsonString(jason)).flatMap { map =>
       map.foldLeft(Try(Map[String, TariffDTO]())) {
         case (Success(map), (key, next)) =>
           next.validated(failFast) match {
             case Success(ok) => Success(map.updated(key, ok))
             case Failure(err) => Failure(new Exception(s"Validation error on element $key: ${err.getMessage}", err))
           }
         case (fail, _) => fail
       }
     }
  }
}

