/**
 * Партнерский API Маркета
 * API Яндекс Маркета помогает продавцам автоматизировать и упростить работу с маркетплейсом.  В числе возможностей интеграции:  * управление каталогом товаров и витриной,  * обработка заказов,  * изменение настроек магазина,  * получение отчетов. 
 *
 * OpenAPI spec version: LATEST
 *
 * Contact: team@openapitools.org
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 *
 * https://openapi-generator.tech
 */

// this model was generated using modelData.mustache
package ympa_scala_cask_server.model
import ympa_scala_cask_server.model.UpdateStockDTO
import scala.util.control.NonFatal
import scala.util.*

// see https://com-lihaoyi.github.io/upickle/
import upickle.default.{ReadWriter => RW, macroRW}
import upickle.default.*

/** UpdateStocksRequestData a data transfer object, primarily for simple json serialisation.
  * It has no validation - there may be nulls, values out of range, etc
  */
case class UpdateStocksRequestData(
/* Данные об остатках товаров.  */
  skus: Set[UpdateStockDTOData]

  ) {

  def asJson: String = write(this)

  def validationErrors(path : Seq[Field], failFast : Boolean) : Seq[ValidationError] = {
    val errors = scala.collection.mutable.ListBuffer[ValidationError]()
        // ==================
        // skus






        
        if (errors.isEmpty || !failFast) {
            if (skus == null) {
            errors += ValidationError(path :+ UpdateStocksRequest.Fields.skus, "skus is a required field and cannot be null")
            }
        }
        

          // validate skus has unique items
          if (errors.isEmpty || !failFast) {
              if (skus != null) {
                skus.foldLeft(Set[UpdateStockDTOData]()) {
                  case (set, next) if set.contains(next) =>
                    errors += ValidationError(
                      path :+ UpdateStocksRequest.Fields.skus,
                      s"duplicate value: $next"
                    )
                    set + next
                  case (set, next) => set + next
                }
              }
          }


        // validate min items 1
        if (errors.isEmpty || !failFast) {
          val len = if skus == null then 0 else skus.size
            if (len < 1) {
               errors += ValidationError(path :+ UpdateStocksRequest.Fields.skus, s"skus has $len, which is less than the min items 1")
            }
        }

        // validate min items 2000
        if (errors.isEmpty || !failFast) {
          val len = if skus == null then 0 else skus.size
            if (len > 2000) {
               errors += ValidationError(path :+ UpdateStocksRequest.Fields.skus, s"skus has $len, which is greater than the max items 2000")
            }
        }



        
        if (errors.isEmpty || !failFast) {
            if (skus != null) {
                skus.zipWithIndex.foreach {
                    case (value, i) if errors.isEmpty || !failFast =>
                      errors ++= value.validationErrors(
                        path :+ UpdateStocksRequest.Fields.skus :+ Field(i.toString),
                        failFast)
                    case (value, i) =>
                }
            }
        }
        

    errors.toSeq
  }

  def validated(failFast : Boolean = false) : scala.util.Try[UpdateStocksRequest] = {
    validationErrors(Vector(), failFast) match {
      case Seq() => Success(asModel)
      case first +: theRest => Failure(ValidationErrors(first, theRest))
    }
  }

  /** use 'validated' to check validation */
  def asModel : UpdateStocksRequest = {
    UpdateStocksRequest(
        skus = 
        skus
        
        .map(_.asModel)
    )
  }
}

object UpdateStocksRequestData {

  given readWriter : RW[UpdateStocksRequestData] = macroRW

  def fromJsonString(jason : String) : UpdateStocksRequestData = try {
        read[UpdateStocksRequestData](jason)
     } catch {
          case NonFatal(e) => sys.error(s"Error parsing json '$jason': $e")
     }

  def manyFromJsonString(jason : String) : Seq[UpdateStocksRequestData] = try {
        read[List[UpdateStocksRequestData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as list: $e")
    }

  def manyFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Seq[UpdateStocksRequest]] = {
      Try(manyFromJsonString(jason)).flatMap { list =>
        list.zipWithIndex.foldLeft(Try(Vector[UpdateStocksRequest]())) {
          case (Success(list), (next, i)) => 
            next.validated(failFast) match {
              case Success(ok) => Success(list :+ ok)
              case Failure(err) => Failure(new Exception(s"Validation error on element $i: ${err.getMessage}", err))
            }
          case (fail, _)  => fail
        }
      }
    }

  def mapFromJsonString(jason : String) : Map[String, UpdateStocksRequestData] = try {
        read[Map[String, UpdateStocksRequestData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as map: $e")
    }


  def mapFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Map[String, UpdateStocksRequest]] = {
     Try(mapFromJsonString(jason)).flatMap { map =>
       map.foldLeft(Try(Map[String, UpdateStocksRequest]())) {
         case (Success(map), (key, next)) =>
           next.validated(failFast) match {
             case Success(ok) => Success(map.updated(key, ok))
             case Failure(err) => Failure(new Exception(s"Validation error on element $key: ${err.getMessage}", err))
           }
         case (fail, _) => fail
       }
     }
  }
}

