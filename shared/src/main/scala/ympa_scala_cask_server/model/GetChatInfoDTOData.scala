/**
 * Партнерский API Маркета
 * API Яндекс Маркета помогает продавцам автоматизировать и упростить работу с маркетплейсом.  В числе возможностей интеграции:  * управление каталогом товаров и витриной,  * обработка заказов,  * изменение настроек магазина,  * получение отчетов. 
 *
 * OpenAPI spec version: LATEST
 *
 * Contact: team@openapitools.org
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 *
 * https://openapi-generator.tech
 */

// this model was generated using modelData.mustache
package ympa_scala_cask_server.model
import java.time.OffsetDateTime
import ympa_scala_cask_server.model.ChatStatusType
import ympa_scala_cask_server.model.ChatType
import scala.util.control.NonFatal
import scala.util.*

// see https://com-lihaoyi.github.io/upickle/
import upickle.default.{ReadWriter => RW, macroRW}
import upickle.default.*

/** GetChatInfoDTOData a data transfer object, primarily for simple json serialisation.
  * It has no validation - there may be nulls, values out of range, etc
  */
case class GetChatInfoDTOData(
/* Идентификатор чата. */
  chatId: Long,

  /* Идентификатор заказа. */
  orderId: Long,

    `type`: ChatType,

    status: ChatStatusType,

  /* Дата и время создания чата.  Формат даты: ISO 8601 со смещением относительно UTC. Например, `2017-11-21T00:00:00+03:00`.  */
  createdAt: OffsetDateTime,

  /* Дата и время последнего сообщения в чате. */
  updatedAt: OffsetDateTime

  ) {

  def asJson: String = write(this)

  def validationErrors(path : Seq[Field], failFast : Boolean) : Seq[ValidationError] = {
    val errors = scala.collection.mutable.ListBuffer[ValidationError]()
        // ==================
        // chatId






        







        

        // ==================
        // orderId






        







        

        // ==================
        // `type`






        
        if (errors.isEmpty || !failFast) {
            if (`type` == null) {
            errors += ValidationError(path :+ GetChatInfoDTO.Fields.`type`, "`type` is a required field and cannot be null")
            }
        }
        







        

        // ==================
        // status






        
        if (errors.isEmpty || !failFast) {
            if (status == null) {
            errors += ValidationError(path :+ GetChatInfoDTO.Fields.status, "status is a required field and cannot be null")
            }
        }
        







        

        // ==================
        // createdAt






        
        if (errors.isEmpty || !failFast) {
            if (createdAt == null) {
            errors += ValidationError(path :+ GetChatInfoDTO.Fields.createdAt, "createdAt is a required field and cannot be null")
            }
        }
        







        

        // ==================
        // updatedAt






        
        if (errors.isEmpty || !failFast) {
            if (updatedAt == null) {
            errors += ValidationError(path :+ GetChatInfoDTO.Fields.updatedAt, "updatedAt is a required field and cannot be null")
            }
        }
        







        

    errors.toSeq
  }

  def validated(failFast : Boolean = false) : scala.util.Try[GetChatInfoDTO] = {
    validationErrors(Vector(), failFast) match {
      case Seq() => Success(asModel)
      case first +: theRest => Failure(ValidationErrors(first, theRest))
    }
  }

  /** use 'validated' to check validation */
  def asModel : GetChatInfoDTO = {
    GetChatInfoDTO(
        chatId = 
        chatId
        
        ,
        orderId = 
        orderId
        
        ,
        `type` = 
        `type`
        
        ,
        status = 
        status
        
        ,
        createdAt = 
        createdAt
        
        ,
        updatedAt = 
        updatedAt
        
        
    )
  }
}

object GetChatInfoDTOData {

  given readWriter : RW[GetChatInfoDTOData] = macroRW

  def fromJsonString(jason : String) : GetChatInfoDTOData = try {
        read[GetChatInfoDTOData](jason)
     } catch {
          case NonFatal(e) => sys.error(s"Error parsing json '$jason': $e")
     }

  def manyFromJsonString(jason : String) : Seq[GetChatInfoDTOData] = try {
        read[List[GetChatInfoDTOData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as list: $e")
    }

  def manyFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Seq[GetChatInfoDTO]] = {
      Try(manyFromJsonString(jason)).flatMap { list =>
        list.zipWithIndex.foldLeft(Try(Vector[GetChatInfoDTO]())) {
          case (Success(list), (next, i)) => 
            next.validated(failFast) match {
              case Success(ok) => Success(list :+ ok)
              case Failure(err) => Failure(new Exception(s"Validation error on element $i: ${err.getMessage}", err))
            }
          case (fail, _)  => fail
        }
      }
    }

  def mapFromJsonString(jason : String) : Map[String, GetChatInfoDTOData] = try {
        read[Map[String, GetChatInfoDTOData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as map: $e")
    }


  def mapFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Map[String, GetChatInfoDTO]] = {
     Try(mapFromJsonString(jason)).flatMap { map =>
       map.foldLeft(Try(Map[String, GetChatInfoDTO]())) {
         case (Success(map), (key, next)) =>
           next.validated(failFast) match {
             case Success(ok) => Success(map.updated(key, ok))
             case Failure(err) => Failure(new Exception(s"Validation error on element $key: ${err.getMessage}", err))
           }
         case (fail, _) => fail
       }
     }
  }
}

