/**
 * Партнерский API Маркета
 * API Яндекс Маркета помогает продавцам автоматизировать и упростить работу с маркетплейсом.  В числе возможностей интеграции:  * управление каталогом товаров и витриной,  * обработка заказов,  * изменение настроек магазина,  * получение отчетов. 
 *
 * OpenAPI spec version: LATEST
 *
 * Contact: team@openapitools.org
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 *
 * https://openapi-generator.tech
 */

// this model was generated using modelData.mustache
package ympa_scala_cask_server.model
import ympa_scala_cask_server.model.ModelOfferDTO
import ympa_scala_cask_server.model.ModelPriceDTO
import scala.util.control.NonFatal
import scala.util.*

// see https://com-lihaoyi.github.io/upickle/
import upickle.default.{ReadWriter => RW, macroRW}
import upickle.default.*

/** EnrichedModelDTOData a data transfer object, primarily for simple json serialisation.
  * It has no validation - there may be nulls, values out of range, etc
  */
case class EnrichedModelDTOData(
/* Идентификатор модели товара. */
  id: Long = 0 ,

  /* Название модели товара. */
  name: String = "" ,

    prices: ModelPriceDTOData = null ,

  /* Список первых десяти предложений, расположенных на карточке модели.  В ответе на запрос возвращаются предложения различных магазинов. Если есть несколько предложений от одного магазина, в ответе отображается только одно, наиболее релевантное из них.  */
  offers: Seq[ModelOfferDTOData] = Nil ,

  /* Суммарное количество предложений в розничных магазинах в регионе. Учитываются все предложения от каждого магазина. */
  offlineOffers: Int = 0 ,

  /* Суммарное количество предложений в интернет-магазинах в регионе. Учитываются все предложения от каждого магазина. */
  onlineOffers: Int = 0 

  ) {

  def asJson: String = write(this)

  def validationErrors(path : Seq[Field], failFast : Boolean) : Seq[ValidationError] = {
    val errors = scala.collection.mutable.ListBuffer[ValidationError]()
        // ==================
        // id






        







        

        // ==================
        // name






        







        

        // ==================
        // prices






        







        
        // validating prices
        if (errors.isEmpty || !failFast) {
            if prices != null then errors ++= prices.validationErrors(path :+ EnrichedModelDTO.Fields.prices, failFast)
        }

        // ==================
        // offers






        







        
        if (errors.isEmpty || !failFast) {
            if (offers != null) {
                offers.zipWithIndex.foreach {
                    case (value, i) if errors.isEmpty || !failFast =>
                      errors ++= value.validationErrors(
                        path :+ EnrichedModelDTO.Fields.offers :+ Field(i.toString),
                        failFast)
                    case (value, i) =>
                }
            }
        }
        

        // ==================
        // offlineOffers






        







        

        // ==================
        // onlineOffers






        







        

    errors.toSeq
  }

  def validated(failFast : Boolean = false) : scala.util.Try[EnrichedModelDTO] = {
    validationErrors(Vector(), failFast) match {
      case Seq() => Success(asModel)
      case first +: theRest => Failure(ValidationErrors(first, theRest))
    }
  }

  /** use 'validated' to check validation */
  def asModel : EnrichedModelDTO = {
    EnrichedModelDTO(
        id = Option(
        id
        )
        ,
        name = Option(
        name
        )
        ,
        prices = Option(
        prices
        )
        .map(_.asModel),
        offers = 
        offers
        
        .map(_.asModel),
        offlineOffers = Option(
        offlineOffers
        )
        ,
        onlineOffers = Option(
        onlineOffers
        )
        
    )
  }
}

object EnrichedModelDTOData {

  given readWriter : RW[EnrichedModelDTOData] = macroRW

  def fromJsonString(jason : String) : EnrichedModelDTOData = try {
        read[EnrichedModelDTOData](jason)
     } catch {
          case NonFatal(e) => sys.error(s"Error parsing json '$jason': $e")
     }

  def manyFromJsonString(jason : String) : Seq[EnrichedModelDTOData] = try {
        read[List[EnrichedModelDTOData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as list: $e")
    }

  def manyFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Seq[EnrichedModelDTO]] = {
      Try(manyFromJsonString(jason)).flatMap { list =>
        list.zipWithIndex.foldLeft(Try(Vector[EnrichedModelDTO]())) {
          case (Success(list), (next, i)) => 
            next.validated(failFast) match {
              case Success(ok) => Success(list :+ ok)
              case Failure(err) => Failure(new Exception(s"Validation error on element $i: ${err.getMessage}", err))
            }
          case (fail, _)  => fail
        }
      }
    }

  def mapFromJsonString(jason : String) : Map[String, EnrichedModelDTOData] = try {
        read[Map[String, EnrichedModelDTOData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as map: $e")
    }


  def mapFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Map[String, EnrichedModelDTO]] = {
     Try(mapFromJsonString(jason)).flatMap { map =>
       map.foldLeft(Try(Map[String, EnrichedModelDTO]())) {
         case (Success(map), (key, next)) =>
           next.validated(failFast) match {
             case Success(ok) => Success(map.updated(key, ok))
             case Failure(err) => Failure(new Exception(s"Validation error on element $key: ${err.getMessage}", err))
           }
         case (fail, _) => fail
       }
     }
  }
}

