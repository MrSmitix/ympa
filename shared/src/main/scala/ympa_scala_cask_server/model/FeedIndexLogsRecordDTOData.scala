/**
 * Партнерский API Маркета
 * API Яндекс Маркета помогает продавцам автоматизировать и упростить работу с маркетплейсом.  В числе возможностей интеграции:  * управление каталогом товаров и витриной,  * обработка заказов,  * изменение настроек магазина,  * получение отчетов. 
 *
 * OpenAPI spec version: LATEST
 *
 * Contact: team@openapitools.org
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 *
 * https://openapi-generator.tech
 */

// this model was generated using modelData.mustache
package ympa_scala_cask_server.model
import java.time.OffsetDateTime
import ympa_scala_cask_server.model.FeedIndexLogsErrorDTO
import ympa_scala_cask_server.model.FeedIndexLogsIndexType
import ympa_scala_cask_server.model.FeedIndexLogsOffersDTO
import ympa_scala_cask_server.model.FeedIndexLogsStatusType
import scala.util.control.NonFatal
import scala.util.*

// see https://com-lihaoyi.github.io/upickle/
import upickle.default.{ReadWriter => RW, macroRW}
import upickle.default.*

/** FeedIndexLogsRecordDTOData a data transfer object, primarily for simple json serialisation.
  * It has no validation - there may be nulls, values out of range, etc
  */
case class FeedIndexLogsRecordDTOData(
/* Дата и время загрузки прайс-листа.  Формат даты: ISO 8601 со смещением относительно UTC. Например, `2017-11-21T00:42:42+03:00`.  */
  downloadTime: OffsetDateTime = null ,

  /* Дата и время, которые магазин указал в прайс-листе.  Формат даты: ISO 8601 со смещением относительно UTC. Например, `2017-11-21T00:42:42+03:00`.  */
  fileTime: OffsetDateTime = null ,

  /* Идентификатор индексации. */
  generationId: Long = 0 ,

    indexType: FeedIndexLogsIndexType = null ,

  /* Дата и время публикации предложений из прайс-листа на Яндекс Маркете.  Формат даты: ISO 8601 со смещением относительно UTC. Например, `2017-11-21T00:42:42+03:00`.  */
  publishedTime: OffsetDateTime = null ,

    status: FeedIndexLogsStatusType = null ,

    error: FeedIndexLogsErrorDTOData = null ,

    offers: FeedIndexLogsOffersDTOData = null 

  ) {

  def asJson: String = write(this)

  def validationErrors(path : Seq[Field], failFast : Boolean) : Seq[ValidationError] = {
    val errors = scala.collection.mutable.ListBuffer[ValidationError]()
        // ==================
        // downloadTime






        







        

        // ==================
        // fileTime






        







        

        // ==================
        // generationId






        







        

        // ==================
        // indexType






        







        

        // ==================
        // publishedTime






        







        

        // ==================
        // status






        







        

        // ==================
        // error






        







        
        // validating error
        if (errors.isEmpty || !failFast) {
            if error != null then errors ++= error.validationErrors(path :+ FeedIndexLogsRecordDTO.Fields.error, failFast)
        }

        // ==================
        // offers






        







        
        // validating offers
        if (errors.isEmpty || !failFast) {
            if offers != null then errors ++= offers.validationErrors(path :+ FeedIndexLogsRecordDTO.Fields.offers, failFast)
        }

    errors.toSeq
  }

  def validated(failFast : Boolean = false) : scala.util.Try[FeedIndexLogsRecordDTO] = {
    validationErrors(Vector(), failFast) match {
      case Seq() => Success(asModel)
      case first +: theRest => Failure(ValidationErrors(first, theRest))
    }
  }

  /** use 'validated' to check validation */
  def asModel : FeedIndexLogsRecordDTO = {
    FeedIndexLogsRecordDTO(
        downloadTime = Option(
        downloadTime
        )
        ,
        fileTime = Option(
        fileTime
        )
        ,
        generationId = Option(
        generationId
        )
        ,
        indexType = Option(
        indexType
        )
        ,
        publishedTime = Option(
        publishedTime
        )
        ,
        status = Option(
        status
        )
        ,
        error = Option(
        error
        )
        .map(_.asModel),
        offers = Option(
        offers
        )
        .map(_.asModel)
    )
  }
}

object FeedIndexLogsRecordDTOData {

  given readWriter : RW[FeedIndexLogsRecordDTOData] = macroRW

  def fromJsonString(jason : String) : FeedIndexLogsRecordDTOData = try {
        read[FeedIndexLogsRecordDTOData](jason)
     } catch {
          case NonFatal(e) => sys.error(s"Error parsing json '$jason': $e")
     }

  def manyFromJsonString(jason : String) : Seq[FeedIndexLogsRecordDTOData] = try {
        read[List[FeedIndexLogsRecordDTOData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as list: $e")
    }

  def manyFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Seq[FeedIndexLogsRecordDTO]] = {
      Try(manyFromJsonString(jason)).flatMap { list =>
        list.zipWithIndex.foldLeft(Try(Vector[FeedIndexLogsRecordDTO]())) {
          case (Success(list), (next, i)) => 
            next.validated(failFast) match {
              case Success(ok) => Success(list :+ ok)
              case Failure(err) => Failure(new Exception(s"Validation error on element $i: ${err.getMessage}", err))
            }
          case (fail, _)  => fail
        }
      }
    }

  def mapFromJsonString(jason : String) : Map[String, FeedIndexLogsRecordDTOData] = try {
        read[Map[String, FeedIndexLogsRecordDTOData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as map: $e")
    }


  def mapFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Map[String, FeedIndexLogsRecordDTO]] = {
     Try(mapFromJsonString(jason)).flatMap { map =>
       map.foldLeft(Try(Map[String, FeedIndexLogsRecordDTO]())) {
         case (Success(map), (key, next)) =>
           next.validated(failFast) match {
             case Success(ok) => Success(map.updated(key, ok))
             case Failure(err) => Failure(new Exception(s"Validation error on element $key: ${err.getMessage}", err))
           }
         case (fail, _) => fail
       }
     }
  }
}

