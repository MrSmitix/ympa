/**
 * Партнерский API Маркета
 * API Яндекс Маркета помогает продавцам автоматизировать и упростить работу с маркетплейсом.  В числе возможностей интеграции:  * управление каталогом товаров и витриной,  * обработка заказов,  * изменение настроек магазина,  * получение отчетов. 
 *
 * OpenAPI spec version: LATEST
 *
 * Contact: team@openapitools.org
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 *
 * https://openapi-generator.tech
 */

// this model was generated using modelData.mustache
package ympa_scala_cask_server.model
import ympa_scala_cask_server.model.ShipmentActionType
import ympa_scala_cask_server.model.ShipmentStatusChangeDTO
import scala.util.control.NonFatal
import scala.util.*

// see https://com-lihaoyi.github.io/upickle/
import upickle.default.{ReadWriter => RW, macroRW}
import upickle.default.*

/** ExtensionShipmentDTOData a data transfer object, primarily for simple json serialisation.
  * It has no validation - there may be nulls, values out of range, etc
  */
case class ExtensionShipmentDTOData(
  currentStatus: ShipmentStatusChangeDTOData = null ,

  /* Доступные действия над отгрузкой. */
  availableActions: Set[ShipmentActionType]

  ) {

  def asJson: String = write(this)

  def validationErrors(path : Seq[Field], failFast : Boolean) : Seq[ValidationError] = {
    val errors = scala.collection.mutable.ListBuffer[ValidationError]()
        // ==================
        // currentStatus






        







        
        // validating currentStatus
        if (errors.isEmpty || !failFast) {
            if currentStatus != null then errors ++= currentStatus.validationErrors(path :+ ExtensionShipmentDTO.Fields.currentStatus, failFast)
        }

        // ==================
        // availableActions






        
        if (errors.isEmpty || !failFast) {
            if (availableActions == null) {
            errors += ValidationError(path :+ ExtensionShipmentDTO.Fields.availableActions, "availableActions is a required field and cannot be null")
            }
        }
        

          // validate availableActions has unique items
          if (errors.isEmpty || !failFast) {
              if (availableActions != null) {
                availableActions.foldLeft(Set[ShipmentActionType]()) {
                  case (set, next) if set.contains(next) =>
                    errors += ValidationError(
                      path :+ ExtensionShipmentDTO.Fields.availableActions,
                      s"duplicate value: $next"
                    )
                    set + next
                  case (set, next) => set + next
                }
              }
          }






        

    errors.toSeq
  }

  def validated(failFast : Boolean = false) : scala.util.Try[ExtensionShipmentDTO] = {
    validationErrors(Vector(), failFast) match {
      case Seq() => Success(asModel)
      case first +: theRest => Failure(ValidationErrors(first, theRest))
    }
  }

  /** use 'validated' to check validation */
  def asModel : ExtensionShipmentDTO = {
    ExtensionShipmentDTO(
        currentStatus = Option(
        currentStatus
        )
        .map(_.asModel),
        availableActions = 
        availableActions
        
        
    )
  }
}

object ExtensionShipmentDTOData {

  given readWriter : RW[ExtensionShipmentDTOData] = macroRW

  def fromJsonString(jason : String) : ExtensionShipmentDTOData = try {
        read[ExtensionShipmentDTOData](jason)
     } catch {
          case NonFatal(e) => sys.error(s"Error parsing json '$jason': $e")
     }

  def manyFromJsonString(jason : String) : Seq[ExtensionShipmentDTOData] = try {
        read[List[ExtensionShipmentDTOData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as list: $e")
    }

  def manyFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Seq[ExtensionShipmentDTO]] = {
      Try(manyFromJsonString(jason)).flatMap { list =>
        list.zipWithIndex.foldLeft(Try(Vector[ExtensionShipmentDTO]())) {
          case (Success(list), (next, i)) => 
            next.validated(failFast) match {
              case Success(ok) => Success(list :+ ok)
              case Failure(err) => Failure(new Exception(s"Validation error on element $i: ${err.getMessage}", err))
            }
          case (fail, _)  => fail
        }
      }
    }

  def mapFromJsonString(jason : String) : Map[String, ExtensionShipmentDTOData] = try {
        read[Map[String, ExtensionShipmentDTOData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as map: $e")
    }


  def mapFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Map[String, ExtensionShipmentDTO]] = {
     Try(mapFromJsonString(jason)).flatMap { map =>
       map.foldLeft(Try(Map[String, ExtensionShipmentDTO]())) {
         case (Success(map), (key, next)) =>
           next.validated(failFast) match {
             case Success(ok) => Success(map.updated(key, ok))
             case Failure(err) => Failure(new Exception(s"Validation error on element $key: ${err.getMessage}", err))
           }
         case (fail, _) => fail
       }
     }
  }
}

