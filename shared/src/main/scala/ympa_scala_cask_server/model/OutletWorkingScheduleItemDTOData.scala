/**
 * Партнерский API Маркета
 * API Яндекс Маркета помогает продавцам автоматизировать и упростить работу с маркетплейсом.  В числе возможностей интеграции:  * управление каталогом товаров и витриной,  * обработка заказов,  * изменение настроек магазина,  * получение отчетов. 
 *
 * OpenAPI spec version: LATEST
 *
 * Contact: team@openapitools.org
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 *
 * https://openapi-generator.tech
 */

// this model was generated using modelData.mustache
package ympa_scala_cask_server.model
import ympa_scala_cask_server.model.DayOfWeekType
import scala.util.control.NonFatal
import scala.util.*

// see https://com-lihaoyi.github.io/upickle/
import upickle.default.{ReadWriter => RW, macroRW}
import upickle.default.*

/** OutletWorkingScheduleItemDTOData a data transfer object, primarily for simple json serialisation.
  * It has no validation - there may be nulls, values out of range, etc
  */
case class OutletWorkingScheduleItemDTOData(
  startDay: DayOfWeekType,

    endDay: DayOfWeekType,

  /* Точка продаж работает c указанного часа.  Формат: `ЧЧ:ММ`.  */
  startTime: String,

  /* Точка продаж работает до указанного часа.  Формат: `ЧЧ:ММ`.  */
  endTime: String

  ) {

  def asJson: String = write(this)

  def validationErrors(path : Seq[Field], failFast : Boolean) : Seq[ValidationError] = {
    val errors = scala.collection.mutable.ListBuffer[ValidationError]()
        // ==================
        // startDay






        
        if (errors.isEmpty || !failFast) {
            if (startDay == null) {
            errors += ValidationError(path :+ OutletWorkingScheduleItemDTO.Fields.startDay, "startDay is a required field and cannot be null")
            }
        }
        







        

        // ==================
        // endDay






        
        if (errors.isEmpty || !failFast) {
            if (endDay == null) {
            errors += ValidationError(path :+ OutletWorkingScheduleItemDTO.Fields.endDay, "endDay is a required field and cannot be null")
            }
        }
        







        

        // ==================
        // startTime



        // validate min length 1
        if (errors.isEmpty || !failFast)  {
          val len = if startTime == null then 0 else startTime.length
            if (len < 1) {
               errors += ValidationError(path :+ OutletWorkingScheduleItemDTO.Fields.startTime, s"length $len is shorter than the min length 1")
            }
        }



        







        

        // ==================
        // endTime



        // validate min length 1
        if (errors.isEmpty || !failFast)  {
          val len = if endTime == null then 0 else endTime.length
            if (len < 1) {
               errors += ValidationError(path :+ OutletWorkingScheduleItemDTO.Fields.endTime, s"length $len is shorter than the min length 1")
            }
        }



        







        

    errors.toSeq
  }

  def validated(failFast : Boolean = false) : scala.util.Try[OutletWorkingScheduleItemDTO] = {
    validationErrors(Vector(), failFast) match {
      case Seq() => Success(asModel)
      case first +: theRest => Failure(ValidationErrors(first, theRest))
    }
  }

  /** use 'validated' to check validation */
  def asModel : OutletWorkingScheduleItemDTO = {
    OutletWorkingScheduleItemDTO(
        startDay = 
        startDay
        
        ,
        endDay = 
        endDay
        
        ,
        startTime = 
        startTime
        
        ,
        endTime = 
        endTime
        
        
    )
  }
}

object OutletWorkingScheduleItemDTOData {

  given readWriter : RW[OutletWorkingScheduleItemDTOData] = macroRW

  def fromJsonString(jason : String) : OutletWorkingScheduleItemDTOData = try {
        read[OutletWorkingScheduleItemDTOData](jason)
     } catch {
          case NonFatal(e) => sys.error(s"Error parsing json '$jason': $e")
     }

  def manyFromJsonString(jason : String) : Seq[OutletWorkingScheduleItemDTOData] = try {
        read[List[OutletWorkingScheduleItemDTOData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as list: $e")
    }

  def manyFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Seq[OutletWorkingScheduleItemDTO]] = {
      Try(manyFromJsonString(jason)).flatMap { list =>
        list.zipWithIndex.foldLeft(Try(Vector[OutletWorkingScheduleItemDTO]())) {
          case (Success(list), (next, i)) => 
            next.validated(failFast) match {
              case Success(ok) => Success(list :+ ok)
              case Failure(err) => Failure(new Exception(s"Validation error on element $i: ${err.getMessage}", err))
            }
          case (fail, _)  => fail
        }
      }
    }

  def mapFromJsonString(jason : String) : Map[String, OutletWorkingScheduleItemDTOData] = try {
        read[Map[String, OutletWorkingScheduleItemDTOData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as map: $e")
    }


  def mapFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Map[String, OutletWorkingScheduleItemDTO]] = {
     Try(mapFromJsonString(jason)).flatMap { map =>
       map.foldLeft(Try(Map[String, OutletWorkingScheduleItemDTO]())) {
         case (Success(map), (key, next)) =>
           next.validated(failFast) match {
             case Success(ok) => Success(map.updated(key, ok))
             case Failure(err) => Failure(new Exception(s"Validation error on element $key: ${err.getMessage}", err))
           }
         case (fail, _) => fail
       }
     }
  }
}

