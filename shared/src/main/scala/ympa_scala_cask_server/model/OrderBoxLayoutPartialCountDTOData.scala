/**
 * Партнерский API Маркета
 * API Яндекс Маркета помогает продавцам автоматизировать и упростить работу с маркетплейсом.  В числе возможностей интеграции:  * управление каталогом товаров и витриной,  * обработка заказов,  * изменение настроек магазина,  * получение отчетов. 
 *
 * OpenAPI spec version: LATEST
 *
 * Contact: team@openapitools.org
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 *
 * https://openapi-generator.tech
 */

// this model was generated using modelData.mustache
package ympa_scala_cask_server.model
import scala.util.control.NonFatal
import scala.util.*

// see https://com-lihaoyi.github.io/upickle/
import upickle.default.{ReadWriter => RW, macroRW}
import upickle.default.*

/** OrderBoxLayoutPartialCountDTOData a data transfer object, primarily for simple json serialisation.
  * It has no validation - there may be nulls, values out of range, etc
  */
case class OrderBoxLayoutPartialCountDTOData(
/* Номер части, начиная с 1. */
  current: Int,

  /* На сколько всего частей разделен товар. */
  total: Int

  ) {

  def asJson: String = write(this)

  def validationErrors(path : Seq[Field], failFast : Boolean) : Seq[ValidationError] = {
    val errors = scala.collection.mutable.ListBuffer[ValidationError]()
        // ==================
        // current

        // validate against minimum 1
        if (errors.isEmpty || !failFast) {
            if !(current >= 1) then
            errors += ValidationError(path :+ OrderBoxLayoutPartialCountDTO.Fields.current, s"value '$current' is not greater than the minimum value 1")
        }





        







        

        // ==================
        // total

        // validate against minimum 2
        if (errors.isEmpty || !failFast) {
            if !(total >= 2) then
            errors += ValidationError(path :+ OrderBoxLayoutPartialCountDTO.Fields.total, s"value '$total' is not greater than the minimum value 2")
        }





        







        

    errors.toSeq
  }

  def validated(failFast : Boolean = false) : scala.util.Try[OrderBoxLayoutPartialCountDTO] = {
    validationErrors(Vector(), failFast) match {
      case Seq() => Success(asModel)
      case first +: theRest => Failure(ValidationErrors(first, theRest))
    }
  }

  /** use 'validated' to check validation */
  def asModel : OrderBoxLayoutPartialCountDTO = {
    OrderBoxLayoutPartialCountDTO(
        current = 
        current
        
        ,
        total = 
        total
        
        
    )
  }
}

object OrderBoxLayoutPartialCountDTOData {

  given readWriter : RW[OrderBoxLayoutPartialCountDTOData] = macroRW

  def fromJsonString(jason : String) : OrderBoxLayoutPartialCountDTOData = try {
        read[OrderBoxLayoutPartialCountDTOData](jason)
     } catch {
          case NonFatal(e) => sys.error(s"Error parsing json '$jason': $e")
     }

  def manyFromJsonString(jason : String) : Seq[OrderBoxLayoutPartialCountDTOData] = try {
        read[List[OrderBoxLayoutPartialCountDTOData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as list: $e")
    }

  def manyFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Seq[OrderBoxLayoutPartialCountDTO]] = {
      Try(manyFromJsonString(jason)).flatMap { list =>
        list.zipWithIndex.foldLeft(Try(Vector[OrderBoxLayoutPartialCountDTO]())) {
          case (Success(list), (next, i)) => 
            next.validated(failFast) match {
              case Success(ok) => Success(list :+ ok)
              case Failure(err) => Failure(new Exception(s"Validation error on element $i: ${err.getMessage}", err))
            }
          case (fail, _)  => fail
        }
      }
    }

  def mapFromJsonString(jason : String) : Map[String, OrderBoxLayoutPartialCountDTOData] = try {
        read[Map[String, OrderBoxLayoutPartialCountDTOData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as map: $e")
    }


  def mapFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Map[String, OrderBoxLayoutPartialCountDTO]] = {
     Try(mapFromJsonString(jason)).flatMap { map =>
       map.foldLeft(Try(Map[String, OrderBoxLayoutPartialCountDTO]())) {
         case (Success(map), (key, next)) =>
           next.validated(failFast) match {
             case Success(ok) => Success(map.updated(key, ok))
             case Failure(err) => Failure(new Exception(s"Validation error on element $key: ${err.getMessage}", err))
           }
         case (fail, _) => fail
       }
     }
  }
}

