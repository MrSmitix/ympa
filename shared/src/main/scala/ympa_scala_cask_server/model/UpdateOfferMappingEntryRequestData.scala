/**
 * Партнерский API Маркета
 * API Яндекс Маркета помогает продавцам автоматизировать и упростить работу с маркетплейсом.  В числе возможностей интеграции:  * управление каталогом товаров и витриной,  * обработка заказов,  * изменение настроек магазина,  * получение отчетов. 
 *
 * OpenAPI spec version: LATEST
 *
 * Contact: team@openapitools.org
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 *
 * https://openapi-generator.tech
 */

// this model was generated using modelData.mustache
package ympa_scala_cask_server.model
import ympa_scala_cask_server.model.UpdateOfferMappingEntryDTO
import scala.util.control.NonFatal
import scala.util.*

// see https://com-lihaoyi.github.io/upickle/
import upickle.default.{ReadWriter => RW, macroRW}
import upickle.default.*

/** UpdateOfferMappingEntryRequestData a data transfer object, primarily for simple json serialisation.
  * It has no validation - there may be nulls, values out of range, etc
  */
case class UpdateOfferMappingEntryRequestData(
/* Информация о товарах в каталоге. */
  offerMappingEntries: Seq[UpdateOfferMappingEntryDTOData]

  ) {

  def asJson: String = write(this)

  def validationErrors(path : Seq[Field], failFast : Boolean) : Seq[ValidationError] = {
    val errors = scala.collection.mutable.ListBuffer[ValidationError]()
        // ==================
        // offerMappingEntries






        
        if (errors.isEmpty || !failFast) {
            if (offerMappingEntries == null) {
            errors += ValidationError(path :+ UpdateOfferMappingEntryRequest.Fields.offerMappingEntries, "offerMappingEntries is a required field and cannot be null")
            }
        }
        



        // validate min items 1
        if (errors.isEmpty || !failFast) {
          val len = if offerMappingEntries == null then 0 else offerMappingEntries.size
            if (len < 1) {
               errors += ValidationError(path :+ UpdateOfferMappingEntryRequest.Fields.offerMappingEntries, s"offerMappingEntries has $len, which is less than the min items 1")
            }
        }

        // validate min items 500
        if (errors.isEmpty || !failFast) {
          val len = if offerMappingEntries == null then 0 else offerMappingEntries.size
            if (len > 500) {
               errors += ValidationError(path :+ UpdateOfferMappingEntryRequest.Fields.offerMappingEntries, s"offerMappingEntries has $len, which is greater than the max items 500")
            }
        }



        
        if (errors.isEmpty || !failFast) {
            if (offerMappingEntries != null) {
                offerMappingEntries.zipWithIndex.foreach {
                    case (value, i) if errors.isEmpty || !failFast =>
                      errors ++= value.validationErrors(
                        path :+ UpdateOfferMappingEntryRequest.Fields.offerMappingEntries :+ Field(i.toString),
                        failFast)
                    case (value, i) =>
                }
            }
        }
        

    errors.toSeq
  }

  def validated(failFast : Boolean = false) : scala.util.Try[UpdateOfferMappingEntryRequest] = {
    validationErrors(Vector(), failFast) match {
      case Seq() => Success(asModel)
      case first +: theRest => Failure(ValidationErrors(first, theRest))
    }
  }

  /** use 'validated' to check validation */
  def asModel : UpdateOfferMappingEntryRequest = {
    UpdateOfferMappingEntryRequest(
        offerMappingEntries = 
        offerMappingEntries
        
        .map(_.asModel)
    )
  }
}

object UpdateOfferMappingEntryRequestData {

  given readWriter : RW[UpdateOfferMappingEntryRequestData] = macroRW

  def fromJsonString(jason : String) : UpdateOfferMappingEntryRequestData = try {
        read[UpdateOfferMappingEntryRequestData](jason)
     } catch {
          case NonFatal(e) => sys.error(s"Error parsing json '$jason': $e")
     }

  def manyFromJsonString(jason : String) : Seq[UpdateOfferMappingEntryRequestData] = try {
        read[List[UpdateOfferMappingEntryRequestData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as list: $e")
    }

  def manyFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Seq[UpdateOfferMappingEntryRequest]] = {
      Try(manyFromJsonString(jason)).flatMap { list =>
        list.zipWithIndex.foldLeft(Try(Vector[UpdateOfferMappingEntryRequest]())) {
          case (Success(list), (next, i)) => 
            next.validated(failFast) match {
              case Success(ok) => Success(list :+ ok)
              case Failure(err) => Failure(new Exception(s"Validation error on element $i: ${err.getMessage}", err))
            }
          case (fail, _)  => fail
        }
      }
    }

  def mapFromJsonString(jason : String) : Map[String, UpdateOfferMappingEntryRequestData] = try {
        read[Map[String, UpdateOfferMappingEntryRequestData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as map: $e")
    }


  def mapFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Map[String, UpdateOfferMappingEntryRequest]] = {
     Try(mapFromJsonString(jason)).flatMap { map =>
       map.foldLeft(Try(Map[String, UpdateOfferMappingEntryRequest]())) {
         case (Success(map), (key, next)) =>
           next.validated(failFast) match {
             case Success(ok) => Success(map.updated(key, ok))
             case Failure(err) => Failure(new Exception(s"Validation error on element $key: ${err.getMessage}", err))
           }
         case (fail, _) => fail
       }
     }
  }
}

