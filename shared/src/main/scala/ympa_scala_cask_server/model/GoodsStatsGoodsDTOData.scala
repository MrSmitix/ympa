/**
 * Партнерский API Маркета
 * API Яндекс Маркета помогает продавцам автоматизировать и упростить работу с маркетплейсом.  В числе возможностей интеграции:  * управление каталогом товаров и витриной,  * обработка заказов,  * изменение настроек магазина,  * получение отчетов. 
 *
 * OpenAPI spec version: LATEST
 *
 * Contact: team@openapitools.org
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 *
 * https://openapi-generator.tech
 */

// this model was generated using modelData.mustache
package ympa_scala_cask_server.model
import scala.math.BigDecimal
import ympa_scala_cask_server.model.GoodsStatsWarehouseDTO
import ympa_scala_cask_server.model.GoodsStatsWeightDimensionsDTO
import ympa_scala_cask_server.model.TariffDTO
import scala.util.control.NonFatal
import scala.util.*

// see https://com-lihaoyi.github.io/upickle/
import upickle.default.{ReadWriter => RW, macroRW}
import upickle.default.*

/** GoodsStatsGoodsDTOData a data transfer object, primarily for simple json serialisation.
  * It has no validation - there may be nulls, values out of range, etc
  */
case class GoodsStatsGoodsDTOData(
/* Ваш SKU — идентификатор товара в вашей системе.  Разрешена любая последовательность длиной до 255 знаков.  Правила использования SKU:  * У каждого товара SKU должен быть свой.  * SKU товара нельзя менять — можно только удалить товар и добавить заново с новым SKU.  * Уже заданный SKU нельзя освободить и использовать заново для другого товара. Каждый товар должен получать новый идентификатор, до того никогда не использовавшийся в вашем каталоге.  [Что такое SKU и как его назначать](https://yandex.ru/support/marketplace/assortment/add/index.html#fields)  */
  shopSku: String = "" ,

  /* SKU на Маркете. */
  marketSku: Long = 0 ,

  /* Название товара. */
  name: String = "" ,

  /* Цена на товар в валюте, которая установлена [в кабинете продавца на Маркете](https://partner.market.yandex.ru/). */
  price: BigDecimal = 0 ,

  /* Идентификатор категории товара на Маркете. */
  categoryId: Long = 0 ,

  /* Название категории товара на Маркете. */
  categoryName: String = "" ,

    weightDimensions: GoodsStatsWeightDimensionsDTOData = null ,

  /* Информация о складах, на которых хранится товар.  Параметр не приходит, если товара нет ни на одном складе.  */
  warehouses: Seq[GoodsStatsWarehouseDTOData] = Nil ,

  /* Информация о тарифах, по которым нужно заплатить за услуги Маркета.  По некоторым услугам могут возвращаться несколько разных стоимостей. Например, в модели FBS стоимость услуги `SORTING` (обработка заказа) зависит от способа отгрузки и количества заказов в отгрузке. Подробнее о тарифах на услуги читайте [в Справке Маркета для продавцов](https://yandex.ru/support2/marketplace/ru/introduction/rates/models/).  */
  tariffs: Seq[TariffDTOData] = Nil ,

  /* Ссылки (URL) изображений товара в хорошем качестве. */
  pictures: Seq[String] = Nil 

  ) {

  def asJson: String = write(this)

  def validationErrors(path : Seq[Field], failFast : Boolean) : Seq[ValidationError] = {
    val errors = scala.collection.mutable.ListBuffer[ValidationError]()
        // ==================
        // shopSku
        // validate against pattern '^[^\\x00-\\x08\\x0A-\\x1f\\x7f]{1,255}$'
        if (errors.isEmpty || !failFast) {
           val regex = """^[^\\x00-\\x08\\x0A-\\x1f\\x7f]{1,255}$"""
           if shopSku == null || !regex.r.matches(shopSku) then
              errors += ValidationError(path :+ GoodsStatsGoodsDTO.Fields.shopSku, s"value '$shopSku' doesn't match pattern $regex")
        }



        // validate min length 1
        if (errors.isEmpty || !failFast)  {
          val len = if shopSku == null then 0 else shopSku.length
            if (len < 1) {
               errors += ValidationError(path :+ GoodsStatsGoodsDTO.Fields.shopSku, s"length $len is shorter than the min length 1")
            }
        }

        // validate max length 255
        if (errors.isEmpty || !failFast)  {
          val len = if shopSku == null then 0 else shopSku.length
            if (len < 255) {
               errors += ValidationError(path :+ GoodsStatsGoodsDTO.Fields.shopSku, s"length $len is longer than the max length 255")
            }
        }


        







        

        // ==================
        // marketSku






        







        

        // ==================
        // name






        







        

        // ==================
        // price






        







        

        // ==================
        // categoryId






        







        

        // ==================
        // categoryName






        







        

        // ==================
        // weightDimensions






        







        
        // validating weightDimensions
        if (errors.isEmpty || !failFast) {
            if weightDimensions != null then errors ++= weightDimensions.validationErrors(path :+ GoodsStatsGoodsDTO.Fields.weightDimensions, failFast)
        }

        // ==================
        // warehouses






        







        
        if (errors.isEmpty || !failFast) {
            if (warehouses != null) {
                warehouses.zipWithIndex.foreach {
                    case (value, i) if errors.isEmpty || !failFast =>
                      errors ++= value.validationErrors(
                        path :+ GoodsStatsGoodsDTO.Fields.warehouses :+ Field(i.toString),
                        failFast)
                    case (value, i) =>
                }
            }
        }
        

        // ==================
        // tariffs






        







        
        if (errors.isEmpty || !failFast) {
            if (tariffs != null) {
                tariffs.zipWithIndex.foreach {
                    case (value, i) if errors.isEmpty || !failFast =>
                      errors ++= value.validationErrors(
                        path :+ GoodsStatsGoodsDTO.Fields.tariffs :+ Field(i.toString),
                        failFast)
                    case (value, i) =>
                }
            }
        }
        

        // ==================
        // pictures






        







        

    errors.toSeq
  }

  def validated(failFast : Boolean = false) : scala.util.Try[GoodsStatsGoodsDTO] = {
    validationErrors(Vector(), failFast) match {
      case Seq() => Success(asModel)
      case first +: theRest => Failure(ValidationErrors(first, theRest))
    }
  }

  /** use 'validated' to check validation */
  def asModel : GoodsStatsGoodsDTO = {
    GoodsStatsGoodsDTO(
        shopSku = Option(
        shopSku
        )
        ,
        marketSku = Option(
        marketSku
        )
        ,
        name = Option(
        name
        )
        ,
        price = Option(
        price
        )
        ,
        categoryId = Option(
        categoryId
        )
        ,
        categoryName = Option(
        categoryName
        )
        ,
        weightDimensions = Option(
        weightDimensions
        )
        .map(_.asModel),
        warehouses = 
        warehouses
        
        .map(_.asModel),
        tariffs = 
        tariffs
        
        .map(_.asModel),
        pictures = 
        pictures
        
        
    )
  }
}

object GoodsStatsGoodsDTOData {

  given readWriter : RW[GoodsStatsGoodsDTOData] = macroRW

  def fromJsonString(jason : String) : GoodsStatsGoodsDTOData = try {
        read[GoodsStatsGoodsDTOData](jason)
     } catch {
          case NonFatal(e) => sys.error(s"Error parsing json '$jason': $e")
     }

  def manyFromJsonString(jason : String) : Seq[GoodsStatsGoodsDTOData] = try {
        read[List[GoodsStatsGoodsDTOData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as list: $e")
    }

  def manyFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Seq[GoodsStatsGoodsDTO]] = {
      Try(manyFromJsonString(jason)).flatMap { list =>
        list.zipWithIndex.foldLeft(Try(Vector[GoodsStatsGoodsDTO]())) {
          case (Success(list), (next, i)) => 
            next.validated(failFast) match {
              case Success(ok) => Success(list :+ ok)
              case Failure(err) => Failure(new Exception(s"Validation error on element $i: ${err.getMessage}", err))
            }
          case (fail, _)  => fail
        }
      }
    }

  def mapFromJsonString(jason : String) : Map[String, GoodsStatsGoodsDTOData] = try {
        read[Map[String, GoodsStatsGoodsDTOData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as map: $e")
    }


  def mapFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Map[String, GoodsStatsGoodsDTO]] = {
     Try(mapFromJsonString(jason)).flatMap { map =>
       map.foldLeft(Try(Map[String, GoodsStatsGoodsDTO]())) {
         case (Success(map), (key, next)) =>
           next.validated(failFast) match {
             case Success(ok) => Success(map.updated(key, ok))
             case Failure(err) => Failure(new Exception(s"Validation error on element $key: ${err.getMessage}", err))
           }
         case (fail, _) => fail
       }
     }
  }
}

