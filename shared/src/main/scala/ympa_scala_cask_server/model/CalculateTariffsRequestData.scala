/**
 * Партнерский API Маркета
 * API Яндекс Маркета помогает продавцам автоматизировать и упростить работу с маркетплейсом.  В числе возможностей интеграции:  * управление каталогом товаров и витриной,  * обработка заказов,  * изменение настроек магазина,  * получение отчетов. 
 *
 * OpenAPI spec version: LATEST
 *
 * Contact: team@openapitools.org
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 *
 * https://openapi-generator.tech
 */

// this model was generated using modelData.mustache
package ympa_scala_cask_server.model
import ympa_scala_cask_server.model.CalculateTariffsOfferDTO
import ympa_scala_cask_server.model.CalculateTariffsParametersDTO
import scala.util.control.NonFatal
import scala.util.*

// see https://com-lihaoyi.github.io/upickle/
import upickle.default.{ReadWriter => RW, macroRW}
import upickle.default.*

/** CalculateTariffsRequestData a data transfer object, primarily for simple json serialisation.
  * It has no validation - there may be nulls, values out of range, etc
  */
case class CalculateTariffsRequestData(
  parameters: CalculateTariffsParametersDTOData,

  /* Товары, для которых нужно рассчитать стоимость услуг. */
  offers: Seq[CalculateTariffsOfferDTOData]

  ) {

  def asJson: String = write(this)

  def validationErrors(path : Seq[Field], failFast : Boolean) : Seq[ValidationError] = {
    val errors = scala.collection.mutable.ListBuffer[ValidationError]()
        // ==================
        // parameters






        
        if (errors.isEmpty || !failFast) {
            if (parameters == null) {
            errors += ValidationError(path :+ CalculateTariffsRequest.Fields.parameters, "parameters is a required field and cannot be null")
            }
        }
        







        
        // validating parameters
        if (errors.isEmpty || !failFast) {
            if parameters != null then errors ++= parameters.validationErrors(path :+ CalculateTariffsRequest.Fields.parameters, failFast)
        }

        // ==================
        // offers






        
        if (errors.isEmpty || !failFast) {
            if (offers == null) {
            errors += ValidationError(path :+ CalculateTariffsRequest.Fields.offers, "offers is a required field and cannot be null")
            }
        }
        



        // validate min items 1
        if (errors.isEmpty || !failFast) {
          val len = if offers == null then 0 else offers.size
            if (len < 1) {
               errors += ValidationError(path :+ CalculateTariffsRequest.Fields.offers, s"offers has $len, which is less than the min items 1")
            }
        }

        // validate min items 200
        if (errors.isEmpty || !failFast) {
          val len = if offers == null then 0 else offers.size
            if (len > 200) {
               errors += ValidationError(path :+ CalculateTariffsRequest.Fields.offers, s"offers has $len, which is greater than the max items 200")
            }
        }



        
        if (errors.isEmpty || !failFast) {
            if (offers != null) {
                offers.zipWithIndex.foreach {
                    case (value, i) if errors.isEmpty || !failFast =>
                      errors ++= value.validationErrors(
                        path :+ CalculateTariffsRequest.Fields.offers :+ Field(i.toString),
                        failFast)
                    case (value, i) =>
                }
            }
        }
        

    errors.toSeq
  }

  def validated(failFast : Boolean = false) : scala.util.Try[CalculateTariffsRequest] = {
    validationErrors(Vector(), failFast) match {
      case Seq() => Success(asModel)
      case first +: theRest => Failure(ValidationErrors(first, theRest))
    }
  }

  /** use 'validated' to check validation */
  def asModel : CalculateTariffsRequest = {
    CalculateTariffsRequest(
        parameters = 
        parameters
        
        ,
        offers = 
        offers
        
        .map(_.asModel)
    )
  }
}

object CalculateTariffsRequestData {

  given readWriter : RW[CalculateTariffsRequestData] = macroRW

  def fromJsonString(jason : String) : CalculateTariffsRequestData = try {
        read[CalculateTariffsRequestData](jason)
     } catch {
          case NonFatal(e) => sys.error(s"Error parsing json '$jason': $e")
     }

  def manyFromJsonString(jason : String) : Seq[CalculateTariffsRequestData] = try {
        read[List[CalculateTariffsRequestData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as list: $e")
    }

  def manyFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Seq[CalculateTariffsRequest]] = {
      Try(manyFromJsonString(jason)).flatMap { list =>
        list.zipWithIndex.foldLeft(Try(Vector[CalculateTariffsRequest]())) {
          case (Success(list), (next, i)) => 
            next.validated(failFast) match {
              case Success(ok) => Success(list :+ ok)
              case Failure(err) => Failure(new Exception(s"Validation error on element $i: ${err.getMessage}", err))
            }
          case (fail, _)  => fail
        }
      }
    }

  def mapFromJsonString(jason : String) : Map[String, CalculateTariffsRequestData] = try {
        read[Map[String, CalculateTariffsRequestData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as map: $e")
    }


  def mapFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Map[String, CalculateTariffsRequest]] = {
     Try(mapFromJsonString(jason)).flatMap { map =>
       map.foldLeft(Try(Map[String, CalculateTariffsRequest]())) {
         case (Success(map), (key, next)) =>
           next.validated(failFast) match {
             case Success(ok) => Success(map.updated(key, ok))
             case Failure(err) => Failure(new Exception(s"Validation error on element $key: ${err.getMessage}", err))
           }
         case (fail, _) => fail
       }
     }
  }
}

