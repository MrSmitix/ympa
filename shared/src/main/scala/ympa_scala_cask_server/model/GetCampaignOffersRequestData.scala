/**
 * Партнерский API Маркета
 * API Яндекс Маркета помогает продавцам автоматизировать и упростить работу с маркетплейсом.  В числе возможностей интеграции:  * управление каталогом товаров и витриной,  * обработка заказов,  * изменение настроек магазина,  * получение отчетов. 
 *
 * OpenAPI spec version: LATEST
 *
 * Contact: team@openapitools.org
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 *
 * https://openapi-generator.tech
 */

// this model was generated using modelData.mustache
package ympa_scala_cask_server.model
import ympa_scala_cask_server.model.OfferCampaignStatusType
import scala.util.control.NonFatal
import scala.util.*

// see https://com-lihaoyi.github.io/upickle/
import upickle.default.{ReadWriter => RW, macroRW}
import upickle.default.*

/** GetCampaignOffersRequestData a data transfer object, primarily for simple json serialisation.
  * It has no validation - there may be nulls, values out of range, etc
  */
case class GetCampaignOffersRequestData(
/* Идентификаторы товаров, информация о которых нужна.  {% note warning \"Такой список возвращается только целиком\" %}  Не используйте это поле одновременно с фильтрами по статусам карточек, категориям, брендам или тегам. Если вы хотите воспользоваться фильтрами, оставьте поле пустым.  Если вы запрашиваете информацию по конкретным SKU, не заполняйте:  * `page_token` * `limit`  {% endnote %}     */
  offerIds: Seq[String] = Nil ,

  /* Фильтр по статусам товаров.  */
  statuses: Seq[OfferCampaignStatusType] = Nil ,

  /* Фильтр по категориям на Маркете. */
  categoryIds: Seq[Int] = Nil ,

  /* Фильтр по брендам. */
  vendorNames: Seq[String] = Nil ,

  /* Фильтр по тегам. */
  tags: Seq[String] = Nil 

  ) {

  def asJson: String = write(this)

  def validationErrors(path : Seq[Field], failFast : Boolean) : Seq[ValidationError] = {
    val errors = scala.collection.mutable.ListBuffer[ValidationError]()
        // ==================
        // offerIds






        




        // validate min items 200
        if (errors.isEmpty || !failFast) {
          val len = if offerIds == null then 0 else offerIds.size
            if (len > 200) {
               errors += ValidationError(path :+ GetCampaignOffersRequest.Fields.offerIds, s"offerIds has $len, which is greater than the max items 200")
            }
        }



        

        // ==================
        // statuses






        







        

        // ==================
        // categoryIds






        







        

        // ==================
        // vendorNames






        







        

        // ==================
        // tags






        







        

    errors.toSeq
  }

  def validated(failFast : Boolean = false) : scala.util.Try[GetCampaignOffersRequest] = {
    validationErrors(Vector(), failFast) match {
      case Seq() => Success(asModel)
      case first +: theRest => Failure(ValidationErrors(first, theRest))
    }
  }

  /** use 'validated' to check validation */
  def asModel : GetCampaignOffersRequest = {
    GetCampaignOffersRequest(
        offerIds = 
        offerIds
        
        ,
        statuses = 
        statuses
        
        ,
        categoryIds = 
        categoryIds
        
        ,
        vendorNames = 
        vendorNames
        
        ,
        tags = 
        tags
        
        
    )
  }
}

object GetCampaignOffersRequestData {

  given readWriter : RW[GetCampaignOffersRequestData] = macroRW

  def fromJsonString(jason : String) : GetCampaignOffersRequestData = try {
        read[GetCampaignOffersRequestData](jason)
     } catch {
          case NonFatal(e) => sys.error(s"Error parsing json '$jason': $e")
     }

  def manyFromJsonString(jason : String) : Seq[GetCampaignOffersRequestData] = try {
        read[List[GetCampaignOffersRequestData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as list: $e")
    }

  def manyFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Seq[GetCampaignOffersRequest]] = {
      Try(manyFromJsonString(jason)).flatMap { list =>
        list.zipWithIndex.foldLeft(Try(Vector[GetCampaignOffersRequest]())) {
          case (Success(list), (next, i)) => 
            next.validated(failFast) match {
              case Success(ok) => Success(list :+ ok)
              case Failure(err) => Failure(new Exception(s"Validation error on element $i: ${err.getMessage}", err))
            }
          case (fail, _)  => fail
        }
      }
    }

  def mapFromJsonString(jason : String) : Map[String, GetCampaignOffersRequestData] = try {
        read[Map[String, GetCampaignOffersRequestData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as map: $e")
    }


  def mapFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Map[String, GetCampaignOffersRequest]] = {
     Try(mapFromJsonString(jason)).flatMap { map =>
       map.foldLeft(Try(Map[String, GetCampaignOffersRequest]())) {
         case (Success(map), (key, next)) =>
           next.validated(failFast) match {
             case Success(ok) => Success(map.updated(key, ok))
             case Failure(err) => Failure(new Exception(s"Validation error on element $key: ${err.getMessage}", err))
           }
         case (fail, _) => fail
       }
     }
  }
}

