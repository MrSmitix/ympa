/**
 * Партнерский API Маркета
 * API Яндекс Маркета помогает продавцам автоматизировать и упростить работу с маркетплейсом.  В числе возможностей интеграции:  * управление каталогом товаров и витриной,  * обработка заказов,  * изменение настроек магазина,  * получение отчетов. 
 *
 * OpenAPI spec version: LATEST
 *
 * Contact: team@openapitools.org
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 *
 * https://openapi-generator.tech
 */

// this model was generated using modelData.mustache
package ympa_scala_cask_server.model
import ympa_scala_cask_server.model.DocumentDTO
import scala.util.control.NonFatal
import scala.util.*

// see https://com-lihaoyi.github.io/upickle/
import upickle.default.{ReadWriter => RW, macroRW}
import upickle.default.*

/** OrderBusinessDocumentsDTOData a data transfer object, primarily for simple json serialisation.
  * It has no validation - there may be nulls, values out of range, etc
  */
case class OrderBusinessDocumentsDTOData(
  upd: DocumentDTOData = null ,

    ukd: DocumentDTOData = null ,

    torgTwelve: DocumentDTOData = null ,

    sf: DocumentDTOData = null ,

    ksf: DocumentDTOData = null 

  ) {

  def asJson: String = write(this)

  def validationErrors(path : Seq[Field], failFast : Boolean) : Seq[ValidationError] = {
    val errors = scala.collection.mutable.ListBuffer[ValidationError]()
        // ==================
        // upd






        







        
        // validating upd
        if (errors.isEmpty || !failFast) {
            if upd != null then errors ++= upd.validationErrors(path :+ OrderBusinessDocumentsDTO.Fields.upd, failFast)
        }

        // ==================
        // ukd






        







        
        // validating ukd
        if (errors.isEmpty || !failFast) {
            if ukd != null then errors ++= ukd.validationErrors(path :+ OrderBusinessDocumentsDTO.Fields.ukd, failFast)
        }

        // ==================
        // torgTwelve






        







        
        // validating torgTwelve
        if (errors.isEmpty || !failFast) {
            if torgTwelve != null then errors ++= torgTwelve.validationErrors(path :+ OrderBusinessDocumentsDTO.Fields.torgTwelve, failFast)
        }

        // ==================
        // sf






        







        
        // validating sf
        if (errors.isEmpty || !failFast) {
            if sf != null then errors ++= sf.validationErrors(path :+ OrderBusinessDocumentsDTO.Fields.sf, failFast)
        }

        // ==================
        // ksf






        







        
        // validating ksf
        if (errors.isEmpty || !failFast) {
            if ksf != null then errors ++= ksf.validationErrors(path :+ OrderBusinessDocumentsDTO.Fields.ksf, failFast)
        }

    errors.toSeq
  }

  def validated(failFast : Boolean = false) : scala.util.Try[OrderBusinessDocumentsDTO] = {
    validationErrors(Vector(), failFast) match {
      case Seq() => Success(asModel)
      case first +: theRest => Failure(ValidationErrors(first, theRest))
    }
  }

  /** use 'validated' to check validation */
  def asModel : OrderBusinessDocumentsDTO = {
    OrderBusinessDocumentsDTO(
        upd = Option(
        upd
        )
        .map(_.asModel),
        ukd = Option(
        ukd
        )
        .map(_.asModel),
        torgTwelve = Option(
        torgTwelve
        )
        .map(_.asModel),
        sf = Option(
        sf
        )
        .map(_.asModel),
        ksf = Option(
        ksf
        )
        .map(_.asModel)
    )
  }
}

object OrderBusinessDocumentsDTOData {

  given readWriter : RW[OrderBusinessDocumentsDTOData] = macroRW

  def fromJsonString(jason : String) : OrderBusinessDocumentsDTOData = try {
        read[OrderBusinessDocumentsDTOData](jason)
     } catch {
          case NonFatal(e) => sys.error(s"Error parsing json '$jason': $e")
     }

  def manyFromJsonString(jason : String) : Seq[OrderBusinessDocumentsDTOData] = try {
        read[List[OrderBusinessDocumentsDTOData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as list: $e")
    }

  def manyFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Seq[OrderBusinessDocumentsDTO]] = {
      Try(manyFromJsonString(jason)).flatMap { list =>
        list.zipWithIndex.foldLeft(Try(Vector[OrderBusinessDocumentsDTO]())) {
          case (Success(list), (next, i)) => 
            next.validated(failFast) match {
              case Success(ok) => Success(list :+ ok)
              case Failure(err) => Failure(new Exception(s"Validation error on element $i: ${err.getMessage}", err))
            }
          case (fail, _)  => fail
        }
      }
    }

  def mapFromJsonString(jason : String) : Map[String, OrderBusinessDocumentsDTOData] = try {
        read[Map[String, OrderBusinessDocumentsDTOData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as map: $e")
    }


  def mapFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Map[String, OrderBusinessDocumentsDTO]] = {
     Try(mapFromJsonString(jason)).flatMap { map =>
       map.foldLeft(Try(Map[String, OrderBusinessDocumentsDTO]())) {
         case (Success(map), (key, next)) =>
           next.validated(failFast) match {
             case Success(ok) => Success(map.updated(key, ok))
             case Failure(err) => Failure(new Exception(s"Validation error on element $key: ${err.getMessage}", err))
           }
         case (fail, _) => fail
       }
     }
  }
}

