/**
 * Партнерский API Маркета
 * API Яндекс Маркета помогает продавцам автоматизировать и упростить работу с маркетплейсом.  В числе возможностей интеграции:  * управление каталогом товаров и витриной,  * обработка заказов,  * изменение настроек магазина,  * получение отчетов. 
 *
 * OpenAPI spec version: LATEST
 *
 * Contact: team@openapitools.org
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 *
 * https://openapi-generator.tech
 */

// this model was generated using model.mustache
package ympa_scala_cask_server.model
import ympa_scala_cask_server.model.RegionType
import scala.util.control.NonFatal

// see https://com-lihaoyi.github.io/upickle/
import upickle.default.{ReadWriter => RW, macroRW}
import upickle.default.*

case class RegionDTO(
/* Идентификатор региона. */
  id: Option[Long] = None ,

  /* Название региона. */
  name: String,

    `type`: RegionType,

    parent: Option[RegionDTO] = None ,

  /* Дочерние регионы. */
  children: Seq[RegionDTO] = Nil 

  ) {

  def asJson: String = asData.asJson

  def asData : RegionDTOData = {
    RegionDTOData(
            id = id.getOrElse(0),
            name = name,
            `type` = `type`,
            parent = parent.map(_.asData).getOrElse(null),
            children = children.map(_.asData)
    )
  }

}

object RegionDTO{

    given RW[RegionDTO] = RegionDTOData.readWriter.bimap[RegionDTO](_.asData, _.asModel)

    enum Fields(fieldName : String) extends Field(fieldName) {
            case id extends Fields("id")
            case name extends Fields("name")
            case `type` extends Fields("`type`")
            case parent extends Fields("parent")
            case children extends Fields("children")
    }


}

