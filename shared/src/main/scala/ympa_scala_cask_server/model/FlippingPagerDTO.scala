/**
 * Партнерский API Маркета
 * API Яндекс Маркета помогает продавцам автоматизировать и упростить работу с маркетплейсом.  В числе возможностей интеграции:  * управление каталогом товаров и витриной,  * обработка заказов,  * изменение настроек магазина,  * получение отчетов. 
 *
 * OpenAPI spec version: LATEST
 *
 * Contact: team@openapitools.org
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 *
 * https://openapi-generator.tech
 */

// this model was generated using model.mustache
package ympa_scala_cask_server.model
import scala.util.control.NonFatal

// see https://com-lihaoyi.github.io/upickle/
import upickle.default.{ReadWriter => RW, macroRW}
import upickle.default.*

case class FlippingPagerDTO(
/* Сколько всего найдено элементов. */
  total: Option[Int] = None ,

  /* Начальный номер найденного элемента на странице. */
  from: Option[Int] = None ,

  /* Конечный номер найденного элемента на странице. */
  to: Option[Int] = None ,

  /* Текущая страница. */
  currentPage: Option[Int] = None ,

  /* Общее количество страниц. */
  pagesCount: Option[Int] = None ,

  /* Размер страницы. */
  pageSize: Option[Int] = None 

  ) {

  def asJson: String = asData.asJson

  def asData : FlippingPagerDTOData = {
    FlippingPagerDTOData(
            total = total.getOrElse(0),
            from = from.getOrElse(0),
            to = to.getOrElse(0),
            currentPage = currentPage.getOrElse(0),
            pagesCount = pagesCount.getOrElse(0),
            pageSize = pageSize.getOrElse(0)
    )
  }

}

object FlippingPagerDTO{

    given RW[FlippingPagerDTO] = FlippingPagerDTOData.readWriter.bimap[FlippingPagerDTO](_.asData, _.asModel)

    enum Fields(fieldName : String) extends Field(fieldName) {
            case total extends Fields("total")
            case from extends Fields("from")
            case to extends Fields("to")
            case currentPage extends Fields("currentPage")
            case pagesCount extends Fields("pagesCount")
            case pageSize extends Fields("pageSize")
    }


}

