/**
 * Партнерский API Маркета
 * API Яндекс Маркета помогает продавцам автоматизировать и упростить работу с маркетплейсом.  В числе возможностей интеграции:  * управление каталогом товаров и витриной,  * обработка заказов,  * изменение настроек магазина,  * получение отчетов. 
 *
 * OpenAPI spec version: LATEST
 *
 * Contact: team@openapitools.org
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 *
 * https://openapi-generator.tech
 */

// this model was generated using modelData.mustache
package ympa_scala_cask_server.model
import ympa_scala_cask_server.model.WarehouseDTO
import scala.util.control.NonFatal
import scala.util.*

// see https://com-lihaoyi.github.io/upickle/
import upickle.default.{ReadWriter => RW, macroRW}
import upickle.default.*

/** WarehouseGroupDTOData a data transfer object, primarily for simple json serialisation.
  * It has no validation - there may be nulls, values out of range, etc
  */
case class WarehouseGroupDTOData(
/* Название группы складов. */
  name: String,

    mainWarehouse: WarehouseDTOData,

  /* Список складов, входящих в группу. */
  warehouses: Seq[WarehouseDTOData]

  ) {

  def asJson: String = write(this)

  def validationErrors(path : Seq[Field], failFast : Boolean) : Seq[ValidationError] = {
    val errors = scala.collection.mutable.ListBuffer[ValidationError]()
        // ==================
        // name






        







        

        // ==================
        // mainWarehouse






        
        if (errors.isEmpty || !failFast) {
            if (mainWarehouse == null) {
            errors += ValidationError(path :+ WarehouseGroupDTO.Fields.mainWarehouse, "mainWarehouse is a required field and cannot be null")
            }
        }
        







        
        // validating mainWarehouse
        if (errors.isEmpty || !failFast) {
            if mainWarehouse != null then errors ++= mainWarehouse.validationErrors(path :+ WarehouseGroupDTO.Fields.mainWarehouse, failFast)
        }

        // ==================
        // warehouses






        
        if (errors.isEmpty || !failFast) {
            if (warehouses == null) {
            errors += ValidationError(path :+ WarehouseGroupDTO.Fields.warehouses, "warehouses is a required field and cannot be null")
            }
        }
        







        
        if (errors.isEmpty || !failFast) {
            if (warehouses != null) {
                warehouses.zipWithIndex.foreach {
                    case (value, i) if errors.isEmpty || !failFast =>
                      errors ++= value.validationErrors(
                        path :+ WarehouseGroupDTO.Fields.warehouses :+ Field(i.toString),
                        failFast)
                    case (value, i) =>
                }
            }
        }
        

    errors.toSeq
  }

  def validated(failFast : Boolean = false) : scala.util.Try[WarehouseGroupDTO] = {
    validationErrors(Vector(), failFast) match {
      case Seq() => Success(asModel)
      case first +: theRest => Failure(ValidationErrors(first, theRest))
    }
  }

  /** use 'validated' to check validation */
  def asModel : WarehouseGroupDTO = {
    WarehouseGroupDTO(
        name = 
        name
        
        ,
        mainWarehouse = 
        mainWarehouse
        
        ,
        warehouses = 
        warehouses
        
        .map(_.asModel)
    )
  }
}

object WarehouseGroupDTOData {

  given readWriter : RW[WarehouseGroupDTOData] = macroRW

  def fromJsonString(jason : String) : WarehouseGroupDTOData = try {
        read[WarehouseGroupDTOData](jason)
     } catch {
          case NonFatal(e) => sys.error(s"Error parsing json '$jason': $e")
     }

  def manyFromJsonString(jason : String) : Seq[WarehouseGroupDTOData] = try {
        read[List[WarehouseGroupDTOData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as list: $e")
    }

  def manyFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Seq[WarehouseGroupDTO]] = {
      Try(manyFromJsonString(jason)).flatMap { list =>
        list.zipWithIndex.foldLeft(Try(Vector[WarehouseGroupDTO]())) {
          case (Success(list), (next, i)) => 
            next.validated(failFast) match {
              case Success(ok) => Success(list :+ ok)
              case Failure(err) => Failure(new Exception(s"Validation error on element $i: ${err.getMessage}", err))
            }
          case (fail, _)  => fail
        }
      }
    }

  def mapFromJsonString(jason : String) : Map[String, WarehouseGroupDTOData] = try {
        read[Map[String, WarehouseGroupDTOData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as map: $e")
    }


  def mapFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Map[String, WarehouseGroupDTO]] = {
     Try(mapFromJsonString(jason)).flatMap { map =>
       map.foldLeft(Try(Map[String, WarehouseGroupDTO]())) {
         case (Success(map), (key, next)) =>
           next.validated(failFast) match {
             case Success(ok) => Success(map.updated(key, ok))
             case Failure(err) => Failure(new Exception(s"Validation error on element $key: ${err.getMessage}", err))
           }
         case (fail, _) => fail
       }
     }
  }
}

