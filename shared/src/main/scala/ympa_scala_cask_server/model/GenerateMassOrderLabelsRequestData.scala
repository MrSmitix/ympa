/**
 * Партнерский API Маркета
 * API Яндекс Маркета помогает продавцам автоматизировать и упростить работу с маркетплейсом.  В числе возможностей интеграции:  * управление каталогом товаров и витриной,  * обработка заказов,  * изменение настроек магазина,  * получение отчетов. 
 *
 * OpenAPI spec version: LATEST
 *
 * Contact: team@openapitools.org
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 *
 * https://openapi-generator.tech
 */

// this model was generated using modelData.mustache
package ympa_scala_cask_server.model
import scala.util.control.NonFatal
import scala.util.*

// see https://com-lihaoyi.github.io/upickle/
import upickle.default.{ReadWriter => RW, macroRW}
import upickle.default.*

/** GenerateMassOrderLabelsRequestData a data transfer object, primarily for simple json serialisation.
  * It has no validation - there may be nulls, values out of range, etc
  */
case class GenerateMassOrderLabelsRequestData(
/* Идентификатор кабинета. */
  businessId: Long,

  /* Список идентификаторов заказов. */
  orderIds: Set[Long]

  ) {

  def asJson: String = write(this)

  def validationErrors(path : Seq[Field], failFast : Boolean) : Seq[ValidationError] = {
    val errors = scala.collection.mutable.ListBuffer[ValidationError]()
        // ==================
        // businessId






        







        

        // ==================
        // orderIds






        

          // validate orderIds has unique items
          if (errors.isEmpty || !failFast) {
              if (orderIds != null) {
                orderIds.foldLeft(Set[Long]()) {
                  case (set, next) if set.contains(next) =>
                    errors += ValidationError(
                      path :+ GenerateMassOrderLabelsRequest.Fields.orderIds,
                      s"duplicate value: $next"
                    )
                    set + next
                  case (set, next) => set + next
                }
              }
          }


        // validate min items 1
        if (errors.isEmpty || !failFast) {
          val len = if orderIds == null then 0 else orderIds.size
            if (len < 1) {
               errors += ValidationError(path :+ GenerateMassOrderLabelsRequest.Fields.orderIds, s"orderIds has $len, which is less than the min items 1")
            }
        }

        // validate min items 1000
        if (errors.isEmpty || !failFast) {
          val len = if orderIds == null then 0 else orderIds.size
            if (len > 1000) {
               errors += ValidationError(path :+ GenerateMassOrderLabelsRequest.Fields.orderIds, s"orderIds has $len, which is greater than the max items 1000")
            }
        }



        

    errors.toSeq
  }

  def validated(failFast : Boolean = false) : scala.util.Try[GenerateMassOrderLabelsRequest] = {
    validationErrors(Vector(), failFast) match {
      case Seq() => Success(asModel)
      case first +: theRest => Failure(ValidationErrors(first, theRest))
    }
  }

  /** use 'validated' to check validation */
  def asModel : GenerateMassOrderLabelsRequest = {
    GenerateMassOrderLabelsRequest(
        businessId = 
        businessId
        
        ,
        orderIds = 
        orderIds
        
        
    )
  }
}

object GenerateMassOrderLabelsRequestData {

  given readWriter : RW[GenerateMassOrderLabelsRequestData] = macroRW

  def fromJsonString(jason : String) : GenerateMassOrderLabelsRequestData = try {
        read[GenerateMassOrderLabelsRequestData](jason)
     } catch {
          case NonFatal(e) => sys.error(s"Error parsing json '$jason': $e")
     }

  def manyFromJsonString(jason : String) : Seq[GenerateMassOrderLabelsRequestData] = try {
        read[List[GenerateMassOrderLabelsRequestData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as list: $e")
    }

  def manyFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Seq[GenerateMassOrderLabelsRequest]] = {
      Try(manyFromJsonString(jason)).flatMap { list =>
        list.zipWithIndex.foldLeft(Try(Vector[GenerateMassOrderLabelsRequest]())) {
          case (Success(list), (next, i)) => 
            next.validated(failFast) match {
              case Success(ok) => Success(list :+ ok)
              case Failure(err) => Failure(new Exception(s"Validation error on element $i: ${err.getMessage}", err))
            }
          case (fail, _)  => fail
        }
      }
    }

  def mapFromJsonString(jason : String) : Map[String, GenerateMassOrderLabelsRequestData] = try {
        read[Map[String, GenerateMassOrderLabelsRequestData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as map: $e")
    }


  def mapFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Map[String, GenerateMassOrderLabelsRequest]] = {
     Try(mapFromJsonString(jason)).flatMap { map =>
       map.foldLeft(Try(Map[String, GenerateMassOrderLabelsRequest]())) {
         case (Success(map), (key, next)) =>
           next.validated(failFast) match {
             case Success(ok) => Success(map.updated(key, ok))
             case Failure(err) => Failure(new Exception(s"Validation error on element $key: ${err.getMessage}", err))
           }
         case (fail, _) => fail
       }
     }
  }
}

