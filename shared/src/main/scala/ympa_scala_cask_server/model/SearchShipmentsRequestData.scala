/**
 * Партнерский API Маркета
 * API Яндекс Маркета помогает продавцам автоматизировать и упростить работу с маркетплейсом.  В числе возможностей интеграции:  * управление каталогом товаров и витриной,  * обработка заказов,  * изменение настроек магазина,  * получение отчетов. 
 *
 * OpenAPI spec version: LATEST
 *
 * Contact: team@openapitools.org
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 *
 * https://openapi-generator.tech
 */

// this model was generated using modelData.mustache
package ympa_scala_cask_server.model
import java.time.LocalDate
import ympa_scala_cask_server.model.ShipmentStatusType
import scala.util.control.NonFatal
import scala.util.*

// see https://com-lihaoyi.github.io/upickle/
import upickle.default.{ReadWriter => RW, macroRW}
import upickle.default.*

/** SearchShipmentsRequestData a data transfer object, primarily for simple json serialisation.
  * It has no validation - there may be nulls, values out of range, etc
  */
case class SearchShipmentsRequestData(
/* Начальная дата для фильтрации по дате отгрузки (включительно).  Формат даты: `ДД-ММ-ГГГГ`.  */
  dateFrom: LocalDate,

  /* Конечная дата для фильтрации по дате отгрузки (включительно).  Формат даты: `ДД-ММ-ГГГГ`.  */
  dateTo: LocalDate,

  /* Список статусов отгрузок. */
  statuses: Set[ShipmentStatusType] = Set.empty ,

  /* Список идентификаторов заказов из отгрузок. */
  orderIds: Set[Long] = Set.empty ,

  /* Возвращать ли отмененные заказы.  Значение по умолчанию — `true`. Если возвращать отмененные заказы не нужно, передайте значение `false`.  */
  cancelledOrders: Boolean = false 

  ) {

  def asJson: String = write(this)

  def validationErrors(path : Seq[Field], failFast : Boolean) : Seq[ValidationError] = {
    val errors = scala.collection.mutable.ListBuffer[ValidationError]()
        // ==================
        // dateFrom






        
        if (errors.isEmpty || !failFast) {
            if (dateFrom == null) {
            errors += ValidationError(path :+ SearchShipmentsRequest.Fields.dateFrom, "dateFrom is a required field and cannot be null")
            }
        }
        







        

        // ==================
        // dateTo






        
        if (errors.isEmpty || !failFast) {
            if (dateTo == null) {
            errors += ValidationError(path :+ SearchShipmentsRequest.Fields.dateTo, "dateTo is a required field and cannot be null")
            }
        }
        







        

        // ==================
        // statuses






        

          // validate statuses has unique items
          if (errors.isEmpty || !failFast) {
              if (statuses != null) {
                statuses.foldLeft(Set[ShipmentStatusType]()) {
                  case (set, next) if set.contains(next) =>
                    errors += ValidationError(
                      path :+ SearchShipmentsRequest.Fields.statuses,
                      s"duplicate value: $next"
                    )
                    set + next
                  case (set, next) => set + next
                }
              }
          }


        // validate min items 1
        if (errors.isEmpty || !failFast) {
          val len = if statuses == null then 0 else statuses.size
            if (len < 1) {
               errors += ValidationError(path :+ SearchShipmentsRequest.Fields.statuses, s"statuses has $len, which is less than the min items 1")
            }
        }




        

        // ==================
        // orderIds






        

          // validate orderIds has unique items
          if (errors.isEmpty || !failFast) {
              if (orderIds != null) {
                orderIds.foldLeft(Set[Long]()) {
                  case (set, next) if set.contains(next) =>
                    errors += ValidationError(
                      path :+ SearchShipmentsRequest.Fields.orderIds,
                      s"duplicate value: $next"
                    )
                    set + next
                  case (set, next) => set + next
                }
              }
          }


        // validate min items 1
        if (errors.isEmpty || !failFast) {
          val len = if orderIds == null then 0 else orderIds.size
            if (len < 1) {
               errors += ValidationError(path :+ SearchShipmentsRequest.Fields.orderIds, s"orderIds has $len, which is less than the min items 1")
            }
        }




        

        // ==================
        // cancelledOrders






        







        

    errors.toSeq
  }

  def validated(failFast : Boolean = false) : scala.util.Try[SearchShipmentsRequest] = {
    validationErrors(Vector(), failFast) match {
      case Seq() => Success(asModel)
      case first +: theRest => Failure(ValidationErrors(first, theRest))
    }
  }

  /** use 'validated' to check validation */
  def asModel : SearchShipmentsRequest = {
    SearchShipmentsRequest(
        dateFrom = 
        dateFrom
        
        ,
        dateTo = 
        dateTo
        
        ,
        statuses = 
        statuses
        
        ,
        orderIds = 
        orderIds
        
        ,
        cancelledOrders = Option(
        cancelledOrders
        )
        
    )
  }
}

object SearchShipmentsRequestData {

  given readWriter : RW[SearchShipmentsRequestData] = macroRW

  def fromJsonString(jason : String) : SearchShipmentsRequestData = try {
        read[SearchShipmentsRequestData](jason)
     } catch {
          case NonFatal(e) => sys.error(s"Error parsing json '$jason': $e")
     }

  def manyFromJsonString(jason : String) : Seq[SearchShipmentsRequestData] = try {
        read[List[SearchShipmentsRequestData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as list: $e")
    }

  def manyFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Seq[SearchShipmentsRequest]] = {
      Try(manyFromJsonString(jason)).flatMap { list =>
        list.zipWithIndex.foldLeft(Try(Vector[SearchShipmentsRequest]())) {
          case (Success(list), (next, i)) => 
            next.validated(failFast) match {
              case Success(ok) => Success(list :+ ok)
              case Failure(err) => Failure(new Exception(s"Validation error on element $i: ${err.getMessage}", err))
            }
          case (fail, _)  => fail
        }
      }
    }

  def mapFromJsonString(jason : String) : Map[String, SearchShipmentsRequestData] = try {
        read[Map[String, SearchShipmentsRequestData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as map: $e")
    }


  def mapFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Map[String, SearchShipmentsRequest]] = {
     Try(mapFromJsonString(jason)).flatMap { map =>
       map.foldLeft(Try(Map[String, SearchShipmentsRequest]())) {
         case (Success(map), (key, next)) =>
           next.validated(failFast) match {
             case Success(ok) => Success(map.updated(key, ok))
             case Failure(err) => Failure(new Exception(s"Validation error on element $key: ${err.getMessage}", err))
           }
         case (fail, _) => fail
       }
     }
  }
}

