/**
 * Партнерский API Маркета
 * API Яндекс Маркета помогает продавцам автоматизировать и упростить работу с маркетплейсом.  В числе возможностей интеграции:  * управление каталогом товаров и витриной,  * обработка заказов,  * изменение настроек магазина,  * получение отчетов. 
 *
 * OpenAPI spec version: LATEST
 *
 * Contact: team@openapitools.org
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 *
 * https://openapi-generator.tech
 */

// this model was generated using modelData.mustache
package ympa_scala_cask_server.model
import scala.util.control.NonFatal
import scala.util.*

// see https://com-lihaoyi.github.io/upickle/
import upickle.default.{ReadWriter => RW, macroRW}
import upickle.default.*

/** UpdatePromoOfferDiscountParamsDTOData a data transfer object, primarily for simple json serialisation.
  * It has no validation - there may be nulls, values out of range, etc
  */
case class UpdatePromoOfferDiscountParamsDTOData(
/* Зачеркнутая цена — та, по которой товар продавался до акции.  Указывается в рублях.  Число должно быть целым.  */
  price: Long = 0 ,

  /* Цена по акции — та, по которой вы хотите продавать товар.  Указывается в рублях.  Число должно быть целым.  */
  promoPrice: Long = 0 

  ) {

  def asJson: String = write(this)

  def validationErrors(path : Seq[Field], failFast : Boolean) : Seq[ValidationError] = {
    val errors = scala.collection.mutable.ListBuffer[ValidationError]()
        // ==================
        // price

        // validate against minimum 1
        if (errors.isEmpty || !failFast) {
            if !(price >= 1) then
            errors += ValidationError(path :+ UpdatePromoOfferDiscountParamsDTO.Fields.price, s"value '$price' is not greater than the minimum value 1")
        }





        







        

        // ==================
        // promoPrice

        // validate against minimum 1
        if (errors.isEmpty || !failFast) {
            if !(promoPrice >= 1) then
            errors += ValidationError(path :+ UpdatePromoOfferDiscountParamsDTO.Fields.promoPrice, s"value '$promoPrice' is not greater than the minimum value 1")
        }





        







        

    errors.toSeq
  }

  def validated(failFast : Boolean = false) : scala.util.Try[UpdatePromoOfferDiscountParamsDTO] = {
    validationErrors(Vector(), failFast) match {
      case Seq() => Success(asModel)
      case first +: theRest => Failure(ValidationErrors(first, theRest))
    }
  }

  /** use 'validated' to check validation */
  def asModel : UpdatePromoOfferDiscountParamsDTO = {
    UpdatePromoOfferDiscountParamsDTO(
        price = Option(
        price
        )
        ,
        promoPrice = Option(
        promoPrice
        )
        
    )
  }
}

object UpdatePromoOfferDiscountParamsDTOData {

  given readWriter : RW[UpdatePromoOfferDiscountParamsDTOData] = macroRW

  def fromJsonString(jason : String) : UpdatePromoOfferDiscountParamsDTOData = try {
        read[UpdatePromoOfferDiscountParamsDTOData](jason)
     } catch {
          case NonFatal(e) => sys.error(s"Error parsing json '$jason': $e")
     }

  def manyFromJsonString(jason : String) : Seq[UpdatePromoOfferDiscountParamsDTOData] = try {
        read[List[UpdatePromoOfferDiscountParamsDTOData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as list: $e")
    }

  def manyFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Seq[UpdatePromoOfferDiscountParamsDTO]] = {
      Try(manyFromJsonString(jason)).flatMap { list =>
        list.zipWithIndex.foldLeft(Try(Vector[UpdatePromoOfferDiscountParamsDTO]())) {
          case (Success(list), (next, i)) => 
            next.validated(failFast) match {
              case Success(ok) => Success(list :+ ok)
              case Failure(err) => Failure(new Exception(s"Validation error on element $i: ${err.getMessage}", err))
            }
          case (fail, _)  => fail
        }
      }
    }

  def mapFromJsonString(jason : String) : Map[String, UpdatePromoOfferDiscountParamsDTOData] = try {
        read[Map[String, UpdatePromoOfferDiscountParamsDTOData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as map: $e")
    }


  def mapFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Map[String, UpdatePromoOfferDiscountParamsDTO]] = {
     Try(mapFromJsonString(jason)).flatMap { map =>
       map.foldLeft(Try(Map[String, UpdatePromoOfferDiscountParamsDTO]())) {
         case (Success(map), (key, next)) =>
           next.validated(failFast) match {
             case Success(ok) => Success(map.updated(key, ok))
             case Failure(err) => Failure(new Exception(s"Validation error on element $key: ${err.getMessage}", err))
           }
         case (fail, _) => fail
       }
     }
  }
}

