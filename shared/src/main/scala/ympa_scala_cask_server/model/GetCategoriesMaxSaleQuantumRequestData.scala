/**
 * Партнерский API Маркета
 * API Яндекс Маркета помогает продавцам автоматизировать и упростить работу с маркетплейсом.  В числе возможностей интеграции:  * управление каталогом товаров и витриной,  * обработка заказов,  * изменение настроек магазина,  * получение отчетов. 
 *
 * OpenAPI spec version: LATEST
 *
 * Contact: team@openapitools.org
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 *
 * https://openapi-generator.tech
 */

// this model was generated using modelData.mustache
package ympa_scala_cask_server.model
import scala.util.control.NonFatal
import scala.util.*

// see https://com-lihaoyi.github.io/upickle/
import upickle.default.{ReadWriter => RW, macroRW}
import upickle.default.*

/** GetCategoriesMaxSaleQuantumRequestData a data transfer object, primarily for simple json serialisation.
  * It has no validation - there may be nulls, values out of range, etc
  */
case class GetCategoriesMaxSaleQuantumRequestData(
/* Идентификаторы листовых категории на Маркете — тех, у которых нет дочерних категорий. */
  marketCategoryIds: Set[Long]

  ) {

  def asJson: String = write(this)

  def validationErrors(path : Seq[Field], failFast : Boolean) : Seq[ValidationError] = {
    val errors = scala.collection.mutable.ListBuffer[ValidationError]()
        // ==================
        // marketCategoryIds






        

          // validate marketCategoryIds has unique items
          if (errors.isEmpty || !failFast) {
              if (marketCategoryIds != null) {
                marketCategoryIds.foldLeft(Set[Long]()) {
                  case (set, next) if set.contains(next) =>
                    errors += ValidationError(
                      path :+ GetCategoriesMaxSaleQuantumRequest.Fields.marketCategoryIds,
                      s"duplicate value: $next"
                    )
                    set + next
                  case (set, next) => set + next
                }
              }
          }


        // validate min items 1
        if (errors.isEmpty || !failFast) {
          val len = if marketCategoryIds == null then 0 else marketCategoryIds.size
            if (len < 1) {
               errors += ValidationError(path :+ GetCategoriesMaxSaleQuantumRequest.Fields.marketCategoryIds, s"marketCategoryIds has $len, which is less than the min items 1")
            }
        }

        // validate min items 1500
        if (errors.isEmpty || !failFast) {
          val len = if marketCategoryIds == null then 0 else marketCategoryIds.size
            if (len > 1500) {
               errors += ValidationError(path :+ GetCategoriesMaxSaleQuantumRequest.Fields.marketCategoryIds, s"marketCategoryIds has $len, which is greater than the max items 1500")
            }
        }



        

    errors.toSeq
  }

  def validated(failFast : Boolean = false) : scala.util.Try[GetCategoriesMaxSaleQuantumRequest] = {
    validationErrors(Vector(), failFast) match {
      case Seq() => Success(asModel)
      case first +: theRest => Failure(ValidationErrors(first, theRest))
    }
  }

  /** use 'validated' to check validation */
  def asModel : GetCategoriesMaxSaleQuantumRequest = {
    GetCategoriesMaxSaleQuantumRequest(
        marketCategoryIds = 
        marketCategoryIds
        
        
    )
  }
}

object GetCategoriesMaxSaleQuantumRequestData {

  given readWriter : RW[GetCategoriesMaxSaleQuantumRequestData] = macroRW

  def fromJsonString(jason : String) : GetCategoriesMaxSaleQuantumRequestData = try {
        read[GetCategoriesMaxSaleQuantumRequestData](jason)
     } catch {
          case NonFatal(e) => sys.error(s"Error parsing json '$jason': $e")
     }

  def manyFromJsonString(jason : String) : Seq[GetCategoriesMaxSaleQuantumRequestData] = try {
        read[List[GetCategoriesMaxSaleQuantumRequestData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as list: $e")
    }

  def manyFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Seq[GetCategoriesMaxSaleQuantumRequest]] = {
      Try(manyFromJsonString(jason)).flatMap { list =>
        list.zipWithIndex.foldLeft(Try(Vector[GetCategoriesMaxSaleQuantumRequest]())) {
          case (Success(list), (next, i)) => 
            next.validated(failFast) match {
              case Success(ok) => Success(list :+ ok)
              case Failure(err) => Failure(new Exception(s"Validation error on element $i: ${err.getMessage}", err))
            }
          case (fail, _)  => fail
        }
      }
    }

  def mapFromJsonString(jason : String) : Map[String, GetCategoriesMaxSaleQuantumRequestData] = try {
        read[Map[String, GetCategoriesMaxSaleQuantumRequestData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as map: $e")
    }


  def mapFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Map[String, GetCategoriesMaxSaleQuantumRequest]] = {
     Try(mapFromJsonString(jason)).flatMap { map =>
       map.foldLeft(Try(Map[String, GetCategoriesMaxSaleQuantumRequest]())) {
         case (Success(map), (key, next)) =>
           next.validated(failFast) match {
             case Success(ok) => Success(map.updated(key, ok))
             case Failure(err) => Failure(new Exception(s"Validation error on element $key: ${err.getMessage}", err))
           }
         case (fail, _) => fail
       }
     }
  }
}

