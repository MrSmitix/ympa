/**
 * Партнерский API Маркета
 * API Яндекс Маркета помогает продавцам автоматизировать и упростить работу с маркетплейсом.  В числе возможностей интеграции:  * управление каталогом товаров и витриной,  * обработка заказов,  * изменение настроек магазина,  * получение отчетов. 
 *
 * OpenAPI spec version: LATEST
 *
 * Contact: team@openapitools.org
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 *
 * https://openapi-generator.tech
 */

// this model was generated using modelData.mustache
package ympa_scala_cask_server.model
import java.time.OffsetDateTime
import ympa_scala_cask_server.model.LogisticPickupPointDTO
import ympa_scala_cask_server.model.RecipientType
import ympa_scala_cask_server.model.RefundStatusType
import ympa_scala_cask_server.model.ReturnItemDTO
import ympa_scala_cask_server.model.ReturnShipmentStatusType
import ympa_scala_cask_server.model.ReturnType
import scala.util.control.NonFatal
import scala.util.*

// see https://com-lihaoyi.github.io/upickle/
import upickle.default.{ReadWriter => RW, macroRW}
import upickle.default.*

/** ReturnDTOData a data transfer object, primarily for simple json serialisation.
  * It has no validation - there may be nulls, values out of range, etc
  */
case class ReturnDTOData(
/* Идентификатор возврата. */
  id: Long = 0 ,

  /* Номер заказа. */
  orderId: Long = 0 ,

  /* Дата создания возврата клиентом. */
  creationDate: OffsetDateTime = null ,

  /* Дата обновления возврата. */
  updateDate: OffsetDateTime = null ,

    refundStatus: RefundStatusType = null ,

    logisticPickupPoint: LogisticPickupPointDTOData = null ,

    shipmentRecipientType: RecipientType = null ,

    shipmentStatus: ReturnShipmentStatusType = null ,

  /* Сумма возврата. */
  refundAmount: Long = 0 ,

  /* Список товаров в возврате. */
  items: Seq[ReturnItemDTOData],

    returnType: ReturnType = null ,

  /* Используется ли опция **Быстрый возврат денег за дешевый брак**.  */
  fastReturn: Boolean = false 

  ) {

  def asJson: String = write(this)

  def validationErrors(path : Seq[Field], failFast : Boolean) : Seq[ValidationError] = {
    val errors = scala.collection.mutable.ListBuffer[ValidationError]()
        // ==================
        // id






        







        

        // ==================
        // orderId






        







        

        // ==================
        // creationDate






        







        

        // ==================
        // updateDate






        







        

        // ==================
        // refundStatus






        







        

        // ==================
        // logisticPickupPoint






        







        
        // validating logisticPickupPoint
        if (errors.isEmpty || !failFast) {
            if logisticPickupPoint != null then errors ++= logisticPickupPoint.validationErrors(path :+ ReturnDTO.Fields.logisticPickupPoint, failFast)
        }

        // ==================
        // shipmentRecipientType






        







        

        // ==================
        // shipmentStatus






        







        

        // ==================
        // refundAmount






        







        

        // ==================
        // items






        
        if (errors.isEmpty || !failFast) {
            if (items == null) {
            errors += ValidationError(path :+ ReturnDTO.Fields.items, "items is a required field and cannot be null")
            }
        }
        







        
        if (errors.isEmpty || !failFast) {
            if (items != null) {
                items.zipWithIndex.foreach {
                    case (value, i) if errors.isEmpty || !failFast =>
                      errors ++= value.validationErrors(
                        path :+ ReturnDTO.Fields.items :+ Field(i.toString),
                        failFast)
                    case (value, i) =>
                }
            }
        }
        

        // ==================
        // returnType






        







        

        // ==================
        // fastReturn






        







        

    errors.toSeq
  }

  def validated(failFast : Boolean = false) : scala.util.Try[ReturnDTO] = {
    validationErrors(Vector(), failFast) match {
      case Seq() => Success(asModel)
      case first +: theRest => Failure(ValidationErrors(first, theRest))
    }
  }

  /** use 'validated' to check validation */
  def asModel : ReturnDTO = {
    ReturnDTO(
        id = Option(
        id
        )
        ,
        orderId = Option(
        orderId
        )
        ,
        creationDate = Option(
        creationDate
        )
        ,
        updateDate = Option(
        updateDate
        )
        ,
        refundStatus = Option(
        refundStatus
        )
        ,
        logisticPickupPoint = Option(
        logisticPickupPoint
        )
        .map(_.asModel),
        shipmentRecipientType = Option(
        shipmentRecipientType
        )
        ,
        shipmentStatus = Option(
        shipmentStatus
        )
        ,
        refundAmount = Option(
        refundAmount
        )
        ,
        items = 
        items
        
        .map(_.asModel),
        returnType = Option(
        returnType
        )
        ,
        fastReturn = Option(
        fastReturn
        )
        
    )
  }
}

object ReturnDTOData {

  given readWriter : RW[ReturnDTOData] = macroRW

  def fromJsonString(jason : String) : ReturnDTOData = try {
        read[ReturnDTOData](jason)
     } catch {
          case NonFatal(e) => sys.error(s"Error parsing json '$jason': $e")
     }

  def manyFromJsonString(jason : String) : Seq[ReturnDTOData] = try {
        read[List[ReturnDTOData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as list: $e")
    }

  def manyFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Seq[ReturnDTO]] = {
      Try(manyFromJsonString(jason)).flatMap { list =>
        list.zipWithIndex.foldLeft(Try(Vector[ReturnDTO]())) {
          case (Success(list), (next, i)) => 
            next.validated(failFast) match {
              case Success(ok) => Success(list :+ ok)
              case Failure(err) => Failure(new Exception(s"Validation error on element $i: ${err.getMessage}", err))
            }
          case (fail, _)  => fail
        }
      }
    }

  def mapFromJsonString(jason : String) : Map[String, ReturnDTOData] = try {
        read[Map[String, ReturnDTOData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as map: $e")
    }


  def mapFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Map[String, ReturnDTO]] = {
     Try(mapFromJsonString(jason)).flatMap { map =>
       map.foldLeft(Try(Map[String, ReturnDTO]())) {
         case (Success(map), (key, next)) =>
           next.validated(failFast) match {
             case Success(ok) => Success(map.updated(key, ok))
             case Failure(err) => Failure(new Exception(s"Validation error on element $key: ${err.getMessage}", err))
           }
         case (fail, _) => fail
       }
     }
  }
}

