/**
 * Партнерский API Маркета
 * API Яндекс Маркета помогает продавцам автоматизировать и упростить работу с маркетплейсом.  В числе возможностей интеграции:  * управление каталогом товаров и витриной,  * обработка заказов,  * изменение настроек магазина,  * получение отчетов. 
 *
 * OpenAPI spec version: LATEST
 *
 * Contact: team@openapitools.org
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 *
 * https://openapi-generator.tech
 */

// this model was generated using modelData.mustache
package ympa_scala_cask_server.model
import ympa_scala_cask_server.model.WarehouseDTO
import ympa_scala_cask_server.model.WarehouseGroupDTO
import scala.util.control.NonFatal
import scala.util.*

// see https://com-lihaoyi.github.io/upickle/
import upickle.default.{ReadWriter => RW, macroRW}
import upickle.default.*

/** WarehousesDTOData a data transfer object, primarily for simple json serialisation.
  * It has no validation - there may be nulls, values out of range, etc
  */
case class WarehousesDTOData(
/* Список складов, не входящих в группы. */
  warehouses: Seq[WarehouseDTOData],

  /* Список групп складов. */
  warehouseGroups: Seq[WarehouseGroupDTOData]

  ) {

  def asJson: String = write(this)

  def validationErrors(path : Seq[Field], failFast : Boolean) : Seq[ValidationError] = {
    val errors = scala.collection.mutable.ListBuffer[ValidationError]()
        // ==================
        // warehouses






        
        if (errors.isEmpty || !failFast) {
            if (warehouses == null) {
            errors += ValidationError(path :+ WarehousesDTO.Fields.warehouses, "warehouses is a required field and cannot be null")
            }
        }
        







        
        if (errors.isEmpty || !failFast) {
            if (warehouses != null) {
                warehouses.zipWithIndex.foreach {
                    case (value, i) if errors.isEmpty || !failFast =>
                      errors ++= value.validationErrors(
                        path :+ WarehousesDTO.Fields.warehouses :+ Field(i.toString),
                        failFast)
                    case (value, i) =>
                }
            }
        }
        

        // ==================
        // warehouseGroups






        
        if (errors.isEmpty || !failFast) {
            if (warehouseGroups == null) {
            errors += ValidationError(path :+ WarehousesDTO.Fields.warehouseGroups, "warehouseGroups is a required field and cannot be null")
            }
        }
        







        
        if (errors.isEmpty || !failFast) {
            if (warehouseGroups != null) {
                warehouseGroups.zipWithIndex.foreach {
                    case (value, i) if errors.isEmpty || !failFast =>
                      errors ++= value.validationErrors(
                        path :+ WarehousesDTO.Fields.warehouseGroups :+ Field(i.toString),
                        failFast)
                    case (value, i) =>
                }
            }
        }
        

    errors.toSeq
  }

  def validated(failFast : Boolean = false) : scala.util.Try[WarehousesDTO] = {
    validationErrors(Vector(), failFast) match {
      case Seq() => Success(asModel)
      case first +: theRest => Failure(ValidationErrors(first, theRest))
    }
  }

  /** use 'validated' to check validation */
  def asModel : WarehousesDTO = {
    WarehousesDTO(
        warehouses = 
        warehouses
        
        .map(_.asModel),
        warehouseGroups = 
        warehouseGroups
        
        .map(_.asModel)
    )
  }
}

object WarehousesDTOData {

  given readWriter : RW[WarehousesDTOData] = macroRW

  def fromJsonString(jason : String) : WarehousesDTOData = try {
        read[WarehousesDTOData](jason)
     } catch {
          case NonFatal(e) => sys.error(s"Error parsing json '$jason': $e")
     }

  def manyFromJsonString(jason : String) : Seq[WarehousesDTOData] = try {
        read[List[WarehousesDTOData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as list: $e")
    }

  def manyFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Seq[WarehousesDTO]] = {
      Try(manyFromJsonString(jason)).flatMap { list =>
        list.zipWithIndex.foldLeft(Try(Vector[WarehousesDTO]())) {
          case (Success(list), (next, i)) => 
            next.validated(failFast) match {
              case Success(ok) => Success(list :+ ok)
              case Failure(err) => Failure(new Exception(s"Validation error on element $i: ${err.getMessage}", err))
            }
          case (fail, _)  => fail
        }
      }
    }

  def mapFromJsonString(jason : String) : Map[String, WarehousesDTOData] = try {
        read[Map[String, WarehousesDTOData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as map: $e")
    }


  def mapFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Map[String, WarehousesDTO]] = {
     Try(mapFromJsonString(jason)).flatMap { map =>
       map.foldLeft(Try(Map[String, WarehousesDTO]())) {
         case (Success(map), (key, next)) =>
           next.validated(failFast) match {
             case Success(ok) => Success(map.updated(key, ok))
             case Failure(err) => Failure(new Exception(s"Validation error on element $key: ${err.getMessage}", err))
           }
         case (fail, _) => fail
       }
     }
  }
}

