/**
 * Партнерский API Маркета
 * API Яндекс Маркета помогает продавцам автоматизировать и упростить работу с маркетплейсом.  В числе возможностей интеграции:  * управление каталогом товаров и витриной,  * обработка заказов,  * изменение настроек магазина,  * получение отчетов. 
 *
 * OpenAPI spec version: LATEST
 *
 * Contact: team@openapitools.org
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 *
 * https://openapi-generator.tech
 */

// this model was generated using modelData.mustache
package ympa_scala_cask_server.model
import ympa_scala_cask_server.model.FeedPublicationFullDTO
import ympa_scala_cask_server.model.FeedPublicationPriceAndStockUpdateDTO
import ympa_scala_cask_server.model.FeedStatusType
import scala.util.control.NonFatal
import scala.util.*

// see https://com-lihaoyi.github.io/upickle/
import upickle.default.{ReadWriter => RW, macroRW}
import upickle.default.*

/** FeedPublicationDTOData a data transfer object, primarily for simple json serialisation.
  * It has no validation - there may be nulls, values out of range, etc
  */
case class FeedPublicationDTOData(
  status: FeedStatusType = null ,

    full: FeedPublicationFullDTOData = null ,

    priceAndStockUpdate: FeedPublicationPriceAndStockUpdateDTOData = null 

  ) {

  def asJson: String = write(this)

  def validationErrors(path : Seq[Field], failFast : Boolean) : Seq[ValidationError] = {
    val errors = scala.collection.mutable.ListBuffer[ValidationError]()
        // ==================
        // status






        







        

        // ==================
        // full






        







        
        // validating full
        if (errors.isEmpty || !failFast) {
            if full != null then errors ++= full.validationErrors(path :+ FeedPublicationDTO.Fields.full, failFast)
        }

        // ==================
        // priceAndStockUpdate






        







        
        // validating priceAndStockUpdate
        if (errors.isEmpty || !failFast) {
            if priceAndStockUpdate != null then errors ++= priceAndStockUpdate.validationErrors(path :+ FeedPublicationDTO.Fields.priceAndStockUpdate, failFast)
        }

    errors.toSeq
  }

  def validated(failFast : Boolean = false) : scala.util.Try[FeedPublicationDTO] = {
    validationErrors(Vector(), failFast) match {
      case Seq() => Success(asModel)
      case first +: theRest => Failure(ValidationErrors(first, theRest))
    }
  }

  /** use 'validated' to check validation */
  def asModel : FeedPublicationDTO = {
    FeedPublicationDTO(
        status = Option(
        status
        )
        ,
        full = Option(
        full
        )
        .map(_.asModel),
        priceAndStockUpdate = Option(
        priceAndStockUpdate
        )
        .map(_.asModel)
    )
  }
}

object FeedPublicationDTOData {

  given readWriter : RW[FeedPublicationDTOData] = macroRW

  def fromJsonString(jason : String) : FeedPublicationDTOData = try {
        read[FeedPublicationDTOData](jason)
     } catch {
          case NonFatal(e) => sys.error(s"Error parsing json '$jason': $e")
     }

  def manyFromJsonString(jason : String) : Seq[FeedPublicationDTOData] = try {
        read[List[FeedPublicationDTOData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as list: $e")
    }

  def manyFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Seq[FeedPublicationDTO]] = {
      Try(manyFromJsonString(jason)).flatMap { list =>
        list.zipWithIndex.foldLeft(Try(Vector[FeedPublicationDTO]())) {
          case (Success(list), (next, i)) => 
            next.validated(failFast) match {
              case Success(ok) => Success(list :+ ok)
              case Failure(err) => Failure(new Exception(s"Validation error on element $i: ${err.getMessage}", err))
            }
          case (fail, _)  => fail
        }
      }
    }

  def mapFromJsonString(jason : String) : Map[String, FeedPublicationDTOData] = try {
        read[Map[String, FeedPublicationDTOData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as map: $e")
    }


  def mapFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Map[String, FeedPublicationDTO]] = {
     Try(mapFromJsonString(jason)).flatMap { map =>
       map.foldLeft(Try(Map[String, FeedPublicationDTO]())) {
         case (Success(map), (key, next)) =>
           next.validated(failFast) match {
             case Success(ok) => Success(map.updated(key, ok))
             case Failure(err) => Failure(new Exception(s"Validation error on element $key: ${err.getMessage}", err))
           }
         case (fail, _) => fail
       }
     }
  }
}

