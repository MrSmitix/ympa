/**
 * Партнерский API Маркета
 * API Яндекс Маркета помогает продавцам автоматизировать и упростить работу с маркетплейсом.  В числе возможностей интеграции:  * управление каталогом товаров и витриной,  * обработка заказов,  * изменение настроек магазина,  * получение отчетов. 
 *
 * OpenAPI spec version: LATEST
 *
 * Contact: team@openapitools.org
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 *
 * https://openapi-generator.tech
 */

// this model was generated using modelData.mustache
package ympa_scala_cask_server.model
import ympa_scala_cask_server.model.ChatStatusType
import ympa_scala_cask_server.model.ChatType
import scala.util.control.NonFatal
import scala.util.*

// see https://com-lihaoyi.github.io/upickle/
import upickle.default.{ReadWriter => RW, macroRW}
import upickle.default.*

/** GetChatsRequestData a data transfer object, primarily for simple json serialisation.
  * It has no validation - there may be nulls, values out of range, etc
  */
case class GetChatsRequestData(
/* Фильтр по идентификаторам заказов на Маркете. */
  orderIds: Seq[Long] = Nil ,

  /* Фильтр по типам чатов. */
  types: Seq[ChatType] = Nil ,

  /* Фильтр по статусам чатов. */
  statuses: Seq[ChatStatusType] = Nil 

  ) {

  def asJson: String = write(this)

  def validationErrors(path : Seq[Field], failFast : Boolean) : Seq[ValidationError] = {
    val errors = scala.collection.mutable.ListBuffer[ValidationError]()
        // ==================
        // orderIds






        



        // validate min items 1
        if (errors.isEmpty || !failFast) {
          val len = if orderIds == null then 0 else orderIds.size
            if (len < 1) {
               errors += ValidationError(path :+ GetChatsRequest.Fields.orderIds, s"orderIds has $len, which is less than the min items 1")
            }
        }




        

        // ==================
        // types






        



        // validate min items 1
        if (errors.isEmpty || !failFast) {
          val len = if types == null then 0 else types.size
            if (len < 1) {
               errors += ValidationError(path :+ GetChatsRequest.Fields.types, s"types has $len, which is less than the min items 1")
            }
        }




        

        // ==================
        // statuses






        



        // validate min items 1
        if (errors.isEmpty || !failFast) {
          val len = if statuses == null then 0 else statuses.size
            if (len < 1) {
               errors += ValidationError(path :+ GetChatsRequest.Fields.statuses, s"statuses has $len, which is less than the min items 1")
            }
        }




        

    errors.toSeq
  }

  def validated(failFast : Boolean = false) : scala.util.Try[GetChatsRequest] = {
    validationErrors(Vector(), failFast) match {
      case Seq() => Success(asModel)
      case first +: theRest => Failure(ValidationErrors(first, theRest))
    }
  }

  /** use 'validated' to check validation */
  def asModel : GetChatsRequest = {
    GetChatsRequest(
        orderIds = 
        orderIds
        
        ,
        types = 
        types
        
        ,
        statuses = 
        statuses
        
        
    )
  }
}

object GetChatsRequestData {

  given readWriter : RW[GetChatsRequestData] = macroRW

  def fromJsonString(jason : String) : GetChatsRequestData = try {
        read[GetChatsRequestData](jason)
     } catch {
          case NonFatal(e) => sys.error(s"Error parsing json '$jason': $e")
     }

  def manyFromJsonString(jason : String) : Seq[GetChatsRequestData] = try {
        read[List[GetChatsRequestData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as list: $e")
    }

  def manyFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Seq[GetChatsRequest]] = {
      Try(manyFromJsonString(jason)).flatMap { list =>
        list.zipWithIndex.foldLeft(Try(Vector[GetChatsRequest]())) {
          case (Success(list), (next, i)) => 
            next.validated(failFast) match {
              case Success(ok) => Success(list :+ ok)
              case Failure(err) => Failure(new Exception(s"Validation error on element $i: ${err.getMessage}", err))
            }
          case (fail, _)  => fail
        }
      }
    }

  def mapFromJsonString(jason : String) : Map[String, GetChatsRequestData] = try {
        read[Map[String, GetChatsRequestData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as map: $e")
    }


  def mapFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Map[String, GetChatsRequest]] = {
     Try(mapFromJsonString(jason)).flatMap { map =>
       map.foldLeft(Try(Map[String, GetChatsRequest]())) {
         case (Success(map), (key, next)) =>
           next.validated(failFast) match {
             case Success(ok) => Success(map.updated(key, ok))
             case Failure(err) => Failure(new Exception(s"Validation error on element $key: ${err.getMessage}", err))
           }
         case (fail, _) => fail
       }
     }
  }
}

