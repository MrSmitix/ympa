/**
 * Партнерский API Маркета
 * API Яндекс Маркета помогает продавцам автоматизировать и упростить работу с маркетплейсом.  В числе возможностей интеграции:  * управление каталогом товаров и витриной,  * обработка заказов,  * изменение настроек магазина,  * получение отчетов. 
 *
 * OpenAPI spec version: LATEST
 *
 * Contact: team@openapitools.org
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 *
 * https://openapi-generator.tech
 */

// this model was generated using modelData.mustache
package ympa_scala_cask_server.model
import ympa_scala_cask_server.model.GpsDTO
import scala.util.control.NonFatal
import scala.util.*

// see https://com-lihaoyi.github.io/upickle/
import upickle.default.{ReadWriter => RW, macroRW}
import upickle.default.*

/** WarehouseAddressDTOData a data transfer object, primarily for simple json serialisation.
  * It has no validation - there may be nulls, values out of range, etc
  */
case class WarehouseAddressDTOData(
/* Город. */
  city: String,

  /* Улица. */
  street: String = "" ,

  /* Номер дома. */
  number: String = "" ,

  /* Номер строения. */
  building: String = "" ,

  /* Номер корпуса. */
  block: String = "" ,

    gps: GpsDTOData

  ) {

  def asJson: String = write(this)

  def validationErrors(path : Seq[Field], failFast : Boolean) : Seq[ValidationError] = {
    val errors = scala.collection.mutable.ListBuffer[ValidationError]()
        // ==================
        // city




        // validate max length 200
        if (errors.isEmpty || !failFast)  {
          val len = if city == null then 0 else city.length
            if (len < 200) {
               errors += ValidationError(path :+ WarehouseAddressDTO.Fields.city, s"length $len is longer than the max length 200")
            }
        }


        







        

        // ==================
        // street




        // validate max length 512
        if (errors.isEmpty || !failFast)  {
          val len = if street == null then 0 else street.length
            if (len < 512) {
               errors += ValidationError(path :+ WarehouseAddressDTO.Fields.street, s"length $len is longer than the max length 512")
            }
        }


        







        

        // ==================
        // number




        // validate max length 256
        if (errors.isEmpty || !failFast)  {
          val len = if number == null then 0 else number.length
            if (len < 256) {
               errors += ValidationError(path :+ WarehouseAddressDTO.Fields.number, s"length $len is longer than the max length 256")
            }
        }


        







        

        // ==================
        // building




        // validate max length 16
        if (errors.isEmpty || !failFast)  {
          val len = if building == null then 0 else building.length
            if (len < 16) {
               errors += ValidationError(path :+ WarehouseAddressDTO.Fields.building, s"length $len is longer than the max length 16")
            }
        }


        







        

        // ==================
        // block




        // validate max length 16
        if (errors.isEmpty || !failFast)  {
          val len = if block == null then 0 else block.length
            if (len < 16) {
               errors += ValidationError(path :+ WarehouseAddressDTO.Fields.block, s"length $len is longer than the max length 16")
            }
        }


        







        

        // ==================
        // gps






        
        if (errors.isEmpty || !failFast) {
            if (gps == null) {
            errors += ValidationError(path :+ WarehouseAddressDTO.Fields.gps, "gps is a required field and cannot be null")
            }
        }
        







        
        // validating gps
        if (errors.isEmpty || !failFast) {
            if gps != null then errors ++= gps.validationErrors(path :+ WarehouseAddressDTO.Fields.gps, failFast)
        }

    errors.toSeq
  }

  def validated(failFast : Boolean = false) : scala.util.Try[WarehouseAddressDTO] = {
    validationErrors(Vector(), failFast) match {
      case Seq() => Success(asModel)
      case first +: theRest => Failure(ValidationErrors(first, theRest))
    }
  }

  /** use 'validated' to check validation */
  def asModel : WarehouseAddressDTO = {
    WarehouseAddressDTO(
        city = 
        city
        
        ,
        street = Option(
        street
        )
        ,
        number = Option(
        number
        )
        ,
        building = Option(
        building
        )
        ,
        block = Option(
        block
        )
        ,
        gps = 
        gps
        
        
    )
  }
}

object WarehouseAddressDTOData {

  given readWriter : RW[WarehouseAddressDTOData] = macroRW

  def fromJsonString(jason : String) : WarehouseAddressDTOData = try {
        read[WarehouseAddressDTOData](jason)
     } catch {
          case NonFatal(e) => sys.error(s"Error parsing json '$jason': $e")
     }

  def manyFromJsonString(jason : String) : Seq[WarehouseAddressDTOData] = try {
        read[List[WarehouseAddressDTOData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as list: $e")
    }

  def manyFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Seq[WarehouseAddressDTO]] = {
      Try(manyFromJsonString(jason)).flatMap { list =>
        list.zipWithIndex.foldLeft(Try(Vector[WarehouseAddressDTO]())) {
          case (Success(list), (next, i)) => 
            next.validated(failFast) match {
              case Success(ok) => Success(list :+ ok)
              case Failure(err) => Failure(new Exception(s"Validation error on element $i: ${err.getMessage}", err))
            }
          case (fail, _)  => fail
        }
      }
    }

  def mapFromJsonString(jason : String) : Map[String, WarehouseAddressDTOData] = try {
        read[Map[String, WarehouseAddressDTOData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as map: $e")
    }


  def mapFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Map[String, WarehouseAddressDTO]] = {
     Try(mapFromJsonString(jason)).flatMap { map =>
       map.foldLeft(Try(Map[String, WarehouseAddressDTO]())) {
         case (Success(map), (key, next)) =>
           next.validated(failFast) match {
             case Success(ok) => Success(map.updated(key, ok))
             case Failure(err) => Failure(new Exception(s"Validation error on element $key: ${err.getMessage}", err))
           }
         case (fail, _) => fail
       }
     }
  }
}

