/**
 * Партнерский API Маркета
 * API Яндекс Маркета помогает продавцам автоматизировать и упростить работу с маркетплейсом.  В числе возможностей интеграции:  * управление каталогом товаров и витриной,  * обработка заказов,  * изменение настроек магазина,  * получение отчетов. 
 *
 * OpenAPI spec version: LATEST
 *
 * Contact: team@openapitools.org
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 *
 * https://openapi-generator.tech
 */

// this model was generated using modelData.mustache
package ympa_scala_cask_server.model
import ympa_scala_cask_server.model.GetPriceWithDiscountDTO
import ympa_scala_cask_server.model.GetPriceWithVatDTO
import ympa_scala_cask_server.model.OfferCampaignStatusType
import ympa_scala_cask_server.model.OfferErrorDTO
import ympa_scala_cask_server.model.QuantumDTO
import scala.util.control.NonFatal
import scala.util.*

// see https://com-lihaoyi.github.io/upickle/
import upickle.default.{ReadWriter => RW, macroRW}
import upickle.default.*

/** GetCampaignOfferDTOData a data transfer object, primarily for simple json serialisation.
  * It has no validation - there may be nulls, values out of range, etc
  */
case class GetCampaignOfferDTOData(
/* Ваш SKU — идентификатор товара в вашей системе.  Разрешена любая последовательность длиной до 255 знаков.  Правила использования SKU:  * У каждого товара SKU должен быть свой.  * SKU товара нельзя менять — можно только удалить товар и добавить заново с новым SKU.  * Уже заданный SKU нельзя освободить и использовать заново для другого товара. Каждый товар должен получать новый идентификатор, до того никогда не использовавшийся в вашем каталоге.  [Что такое SKU и как его назначать](https://yandex.ru/support/marketplace/assortment/add/index.html#fields)  */
  offerId: String,

    quantum: QuantumDTOData = null ,

  /* Есть ли товар в продаже.  */
  available: Boolean = false ,

    basicPrice: GetPriceWithDiscountDTOData = null ,

    campaignPrice: GetPriceWithVatDTOData = null ,

    status: OfferCampaignStatusType = null ,

  /* Ошибки, препятствующие размещению товара на витрине.  */
  errors: Seq[OfferErrorDTOData] = Nil ,

  /* Предупреждения, не препятствующие размещению товара на витрине.  */
  warnings: Seq[OfferErrorDTOData] = Nil 

  ) {

  def asJson: String = write(this)

  def validationErrors(path : Seq[Field], failFast : Boolean) : Seq[ValidationError] = {
    val errors = scala.collection.mutable.ListBuffer[ValidationError]()
        // ==================
        // offerId
        // validate against pattern '^[^\\x00-\\x08\\x0A-\\x1f\\x7f]{1,255}$'
        if (errors.isEmpty || !failFast) {
           val regex = """^[^\\x00-\\x08\\x0A-\\x1f\\x7f]{1,255}$"""
           if offerId == null || !regex.r.matches(offerId) then
              errors += ValidationError(path :+ GetCampaignOfferDTO.Fields.offerId, s"value '$offerId' doesn't match pattern $regex")
        }



        // validate min length 1
        if (errors.isEmpty || !failFast)  {
          val len = if offerId == null then 0 else offerId.length
            if (len < 1) {
               errors += ValidationError(path :+ GetCampaignOfferDTO.Fields.offerId, s"length $len is shorter than the min length 1")
            }
        }

        // validate max length 255
        if (errors.isEmpty || !failFast)  {
          val len = if offerId == null then 0 else offerId.length
            if (len < 255) {
               errors += ValidationError(path :+ GetCampaignOfferDTO.Fields.offerId, s"length $len is longer than the max length 255")
            }
        }


        







        

        // ==================
        // quantum






        







        
        // validating quantum
        if (errors.isEmpty || !failFast) {
            if quantum != null then errors ++= quantum.validationErrors(path :+ GetCampaignOfferDTO.Fields.quantum, failFast)
        }

        // ==================
        // available






        







        

        // ==================
        // basicPrice






        







        
        // validating basicPrice
        if (errors.isEmpty || !failFast) {
            if basicPrice != null then errors ++= basicPrice.validationErrors(path :+ GetCampaignOfferDTO.Fields.basicPrice, failFast)
        }

        // ==================
        // campaignPrice






        







        
        // validating campaignPrice
        if (errors.isEmpty || !failFast) {
            if campaignPrice != null then errors ++= campaignPrice.validationErrors(path :+ GetCampaignOfferDTO.Fields.campaignPrice, failFast)
        }

        // ==================
        // status






        







        

        // ==================
        // errors






        







        
        if (errors.isEmpty || !failFast) {
            if (errors != null) {
                errors.zipWithIndex.foreach {
                    case (value, i) if errors.isEmpty || !failFast =>
                      errors ++= value.validationErrors(
                        path :+ GetCampaignOfferDTO.Fields.errors :+ Field(i.toString),
                        failFast)
                    case (value, i) =>
                }
            }
        }
        

        // ==================
        // warnings






        







        
        if (errors.isEmpty || !failFast) {
            if (warnings != null) {
                warnings.zipWithIndex.foreach {
                    case (value, i) if errors.isEmpty || !failFast =>
                      errors ++= value.validationErrors(
                        path :+ GetCampaignOfferDTO.Fields.warnings :+ Field(i.toString),
                        failFast)
                    case (value, i) =>
                }
            }
        }
        

    errors.toSeq
  }

  def validated(failFast : Boolean = false) : scala.util.Try[GetCampaignOfferDTO] = {
    validationErrors(Vector(), failFast) match {
      case Seq() => Success(asModel)
      case first +: theRest => Failure(ValidationErrors(first, theRest))
    }
  }

  /** use 'validated' to check validation */
  def asModel : GetCampaignOfferDTO = {
    GetCampaignOfferDTO(
        offerId = 
        offerId
        
        ,
        quantum = Option(
        quantum
        )
        .map(_.asModel),
        available = Option(
        available
        )
        ,
        basicPrice = Option(
        basicPrice
        )
        .map(_.asModel),
        campaignPrice = Option(
        campaignPrice
        )
        .map(_.asModel),
        status = Option(
        status
        )
        ,
        errors = 
        errors
        
        .map(_.asModel),
        warnings = 
        warnings
        
        .map(_.asModel)
    )
  }
}

object GetCampaignOfferDTOData {

  given readWriter : RW[GetCampaignOfferDTOData] = macroRW

  def fromJsonString(jason : String) : GetCampaignOfferDTOData = try {
        read[GetCampaignOfferDTOData](jason)
     } catch {
          case NonFatal(e) => sys.error(s"Error parsing json '$jason': $e")
     }

  def manyFromJsonString(jason : String) : Seq[GetCampaignOfferDTOData] = try {
        read[List[GetCampaignOfferDTOData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as list: $e")
    }

  def manyFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Seq[GetCampaignOfferDTO]] = {
      Try(manyFromJsonString(jason)).flatMap { list =>
        list.zipWithIndex.foldLeft(Try(Vector[GetCampaignOfferDTO]())) {
          case (Success(list), (next, i)) => 
            next.validated(failFast) match {
              case Success(ok) => Success(list :+ ok)
              case Failure(err) => Failure(new Exception(s"Validation error on element $i: ${err.getMessage}", err))
            }
          case (fail, _)  => fail
        }
      }
    }

  def mapFromJsonString(jason : String) : Map[String, GetCampaignOfferDTOData] = try {
        read[Map[String, GetCampaignOfferDTOData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as map: $e")
    }


  def mapFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Map[String, GetCampaignOfferDTO]] = {
     Try(mapFromJsonString(jason)).flatMap { map =>
       map.foldLeft(Try(Map[String, GetCampaignOfferDTO]())) {
         case (Success(map), (key, next)) =>
           next.validated(failFast) match {
             case Success(ok) => Success(map.updated(key, ok))
             case Failure(err) => Failure(new Exception(s"Validation error on element $key: ${err.getMessage}", err))
           }
         case (fail, _) => fail
       }
     }
  }
}

