/**
 * Партнерский API Маркета
 * API Яндекс Маркета помогает продавцам автоматизировать и упростить работу с маркетплейсом.  В числе возможностей интеграции:  * управление каталогом товаров и витриной,  * обработка заказов,  * изменение настроек магазина,  * получение отчетов. 
 *
 * OpenAPI spec version: LATEST
 *
 * Contact: team@openapitools.org
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 *
 * https://openapi-generator.tech
 */

// this model was generated using modelData.mustache
package ympa_scala_cask_server.model
import java.time.LocalDate
import ympa_scala_cask_server.model.QualityRatingComponentDTO
import scala.util.control.NonFatal
import scala.util.*

// see https://com-lihaoyi.github.io/upickle/
import upickle.default.{ReadWriter => RW, macroRW}
import upickle.default.*

/** QualityRatingDTOData a data transfer object, primarily for simple json serialisation.
  * It has no validation - there may be nulls, values out of range, etc
  */
case class QualityRatingDTOData(
/* Значение индекса качества. */
  rating: Long,

  /* Дата вычисления.  Формат даты: `ГГГГ‑ММ‑ДД`.  */
  calculationDate: LocalDate,

  /* Составляющие индекса качества. */
  components: Seq[QualityRatingComponentDTOData]

  ) {

  def asJson: String = write(this)

  def validationErrors(path : Seq[Field], failFast : Boolean) : Seq[ValidationError] = {
    val errors = scala.collection.mutable.ListBuffer[ValidationError]()
        // ==================
        // rating

        // validate against minimum 0
        if (errors.isEmpty || !failFast) {
            if !(rating >= 0) then
            errors += ValidationError(path :+ QualityRatingDTO.Fields.rating, s"value '$rating' is not greater than the minimum value 0")
        }

        // validate against maximum 100
        if (errors.isEmpty || !failFast) {
            if !(rating <= 100) then
            errors += ValidationError(path :+ QualityRatingDTO.Fields.rating, s"value '$rating' is not greater than the maximum value 100")
        }




        







        

        // ==================
        // calculationDate






        
        if (errors.isEmpty || !failFast) {
            if (calculationDate == null) {
            errors += ValidationError(path :+ QualityRatingDTO.Fields.calculationDate, "calculationDate is a required field and cannot be null")
            }
        }
        







        

        // ==================
        // components






        
        if (errors.isEmpty || !failFast) {
            if (components == null) {
            errors += ValidationError(path :+ QualityRatingDTO.Fields.components, "components is a required field and cannot be null")
            }
        }
        







        
        if (errors.isEmpty || !failFast) {
            if (components != null) {
                components.zipWithIndex.foreach {
                    case (value, i) if errors.isEmpty || !failFast =>
                      errors ++= value.validationErrors(
                        path :+ QualityRatingDTO.Fields.components :+ Field(i.toString),
                        failFast)
                    case (value, i) =>
                }
            }
        }
        

    errors.toSeq
  }

  def validated(failFast : Boolean = false) : scala.util.Try[QualityRatingDTO] = {
    validationErrors(Vector(), failFast) match {
      case Seq() => Success(asModel)
      case first +: theRest => Failure(ValidationErrors(first, theRest))
    }
  }

  /** use 'validated' to check validation */
  def asModel : QualityRatingDTO = {
    QualityRatingDTO(
        rating = 
        rating
        
        ,
        calculationDate = 
        calculationDate
        
        ,
        components = 
        components
        
        .map(_.asModel)
    )
  }
}

object QualityRatingDTOData {

  given readWriter : RW[QualityRatingDTOData] = macroRW

  def fromJsonString(jason : String) : QualityRatingDTOData = try {
        read[QualityRatingDTOData](jason)
     } catch {
          case NonFatal(e) => sys.error(s"Error parsing json '$jason': $e")
     }

  def manyFromJsonString(jason : String) : Seq[QualityRatingDTOData] = try {
        read[List[QualityRatingDTOData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as list: $e")
    }

  def manyFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Seq[QualityRatingDTO]] = {
      Try(manyFromJsonString(jason)).flatMap { list =>
        list.zipWithIndex.foldLeft(Try(Vector[QualityRatingDTO]())) {
          case (Success(list), (next, i)) => 
            next.validated(failFast) match {
              case Success(ok) => Success(list :+ ok)
              case Failure(err) => Failure(new Exception(s"Validation error on element $i: ${err.getMessage}", err))
            }
          case (fail, _)  => fail
        }
      }
    }

  def mapFromJsonString(jason : String) : Map[String, QualityRatingDTOData] = try {
        read[Map[String, QualityRatingDTOData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as map: $e")
    }


  def mapFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Map[String, QualityRatingDTO]] = {
     Try(mapFromJsonString(jason)).flatMap { map =>
       map.foldLeft(Try(Map[String, QualityRatingDTO]())) {
         case (Success(map), (key, next)) =>
           next.validated(failFast) match {
             case Success(ok) => Success(map.updated(key, ok))
             case Failure(err) => Failure(new Exception(s"Validation error on element $key: ${err.getMessage}", err))
           }
         case (fail, _) => fail
       }
     }
  }
}

