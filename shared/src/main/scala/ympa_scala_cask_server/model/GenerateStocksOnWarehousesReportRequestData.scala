/**
 * Партнерский API Маркета
 * API Яндекс Маркета помогает продавцам автоматизировать и упростить работу с маркетплейсом.  В числе возможностей интеграции:  * управление каталогом товаров и витриной,  * обработка заказов,  * изменение настроек магазина,  * получение отчетов. 
 *
 * OpenAPI spec version: LATEST
 *
 * Contact: team@openapitools.org
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 *
 * https://openapi-generator.tech
 */

// this model was generated using modelData.mustache
package ympa_scala_cask_server.model
import java.time.LocalDate
import scala.util.control.NonFatal
import scala.util.*

// see https://com-lihaoyi.github.io/upickle/
import upickle.default.{ReadWriter => RW, macroRW}
import upickle.default.*

/** GenerateStocksOnWarehousesReportRequestData a data transfer object, primarily for simple json serialisation.
  * It has no validation - there may be nulls, values out of range, etc
  */
case class GenerateStocksOnWarehousesReportRequestData(
/* Идентификатор магазина. */
  campaignId: Long,

  /* Фильтр по идентификаторам складов (только модель FBY). Чтобы узнать идентификатор, воспользуйтесь запросом [GET warehouses](../../reference/warehouses/getFulfillmentWarehouses.md). */
  warehouseIds: Seq[Long] = Nil ,

  /* Фильтр по дате (для модели FBY). В отчет попадут данные за **предшествующий** дате день. */
  reportDate: LocalDate = null ,

  /* Фильтр по категориям на Маркете (кроме модели FBY). */
  categoryIds: Seq[Long] = Nil ,

  /* Фильтр по наличию остатков (кроме модели FBY). */
  hasStocks: Boolean = false 

  ) {

  def asJson: String = write(this)

  def validationErrors(path : Seq[Field], failFast : Boolean) : Seq[ValidationError] = {
    val errors = scala.collection.mutable.ListBuffer[ValidationError]()
        // ==================
        // campaignId






        







        

        // ==================
        // warehouseIds






        







        

        // ==================
        // reportDate






        







        

        // ==================
        // categoryIds






        







        

        // ==================
        // hasStocks






        







        

    errors.toSeq
  }

  def validated(failFast : Boolean = false) : scala.util.Try[GenerateStocksOnWarehousesReportRequest] = {
    validationErrors(Vector(), failFast) match {
      case Seq() => Success(asModel)
      case first +: theRest => Failure(ValidationErrors(first, theRest))
    }
  }

  /** use 'validated' to check validation */
  def asModel : GenerateStocksOnWarehousesReportRequest = {
    GenerateStocksOnWarehousesReportRequest(
        campaignId = 
        campaignId
        
        ,
        warehouseIds = 
        warehouseIds
        
        ,
        reportDate = Option(
        reportDate
        )
        ,
        categoryIds = 
        categoryIds
        
        ,
        hasStocks = Option(
        hasStocks
        )
        
    )
  }
}

object GenerateStocksOnWarehousesReportRequestData {

  given readWriter : RW[GenerateStocksOnWarehousesReportRequestData] = macroRW

  def fromJsonString(jason : String) : GenerateStocksOnWarehousesReportRequestData = try {
        read[GenerateStocksOnWarehousesReportRequestData](jason)
     } catch {
          case NonFatal(e) => sys.error(s"Error parsing json '$jason': $e")
     }

  def manyFromJsonString(jason : String) : Seq[GenerateStocksOnWarehousesReportRequestData] = try {
        read[List[GenerateStocksOnWarehousesReportRequestData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as list: $e")
    }

  def manyFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Seq[GenerateStocksOnWarehousesReportRequest]] = {
      Try(manyFromJsonString(jason)).flatMap { list =>
        list.zipWithIndex.foldLeft(Try(Vector[GenerateStocksOnWarehousesReportRequest]())) {
          case (Success(list), (next, i)) => 
            next.validated(failFast) match {
              case Success(ok) => Success(list :+ ok)
              case Failure(err) => Failure(new Exception(s"Validation error on element $i: ${err.getMessage}", err))
            }
          case (fail, _)  => fail
        }
      }
    }

  def mapFromJsonString(jason : String) : Map[String, GenerateStocksOnWarehousesReportRequestData] = try {
        read[Map[String, GenerateStocksOnWarehousesReportRequestData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as map: $e")
    }


  def mapFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Map[String, GenerateStocksOnWarehousesReportRequest]] = {
     Try(mapFromJsonString(jason)).flatMap { map =>
       map.foldLeft(Try(Map[String, GenerateStocksOnWarehousesReportRequest]())) {
         case (Success(map), (key, next)) =>
           next.validated(failFast) match {
             case Success(ok) => Success(map.updated(key, ok))
             case Failure(err) => Failure(new Exception(s"Validation error on element $key: ${err.getMessage}", err))
           }
         case (fail, _) => fail
       }
     }
  }
}

