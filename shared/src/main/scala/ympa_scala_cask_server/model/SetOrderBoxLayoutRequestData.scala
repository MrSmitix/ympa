/**
 * Партнерский API Маркета
 * API Яндекс Маркета помогает продавцам автоматизировать и упростить работу с маркетплейсом.  В числе возможностей интеграции:  * управление каталогом товаров и витриной,  * обработка заказов,  * изменение настроек магазина,  * получение отчетов. 
 *
 * OpenAPI spec version: LATEST
 *
 * Contact: team@openapitools.org
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 *
 * https://openapi-generator.tech
 */

// this model was generated using modelData.mustache
package ympa_scala_cask_server.model
import ympa_scala_cask_server.model.OrderBoxLayoutDTO
import scala.util.control.NonFatal
import scala.util.*

// see https://com-lihaoyi.github.io/upickle/
import upickle.default.{ReadWriter => RW, macroRW}
import upickle.default.*

/** SetOrderBoxLayoutRequestData a data transfer object, primarily for simple json serialisation.
  * It has no validation - there may be nulls, values out of range, etc
  */
case class SetOrderBoxLayoutRequestData(
/* Список коробок. */
  boxes: Seq[OrderBoxLayoutDTOData],

  /* Передайте `true`, если вы собираетесь удалить часть товаров из заказа. */
  allowRemove: Boolean = false 

  ) {

  def asJson: String = write(this)

  def validationErrors(path : Seq[Field], failFast : Boolean) : Seq[ValidationError] = {
    val errors = scala.collection.mutable.ListBuffer[ValidationError]()
        // ==================
        // boxes






        
        if (errors.isEmpty || !failFast) {
            if (boxes == null) {
            errors += ValidationError(path :+ SetOrderBoxLayoutRequest.Fields.boxes, "boxes is a required field and cannot be null")
            }
        }
        



        // validate min items 1
        if (errors.isEmpty || !failFast) {
          val len = if boxes == null then 0 else boxes.size
            if (len < 1) {
               errors += ValidationError(path :+ SetOrderBoxLayoutRequest.Fields.boxes, s"boxes has $len, which is less than the min items 1")
            }
        }




        
        if (errors.isEmpty || !failFast) {
            if (boxes != null) {
                boxes.zipWithIndex.foreach {
                    case (value, i) if errors.isEmpty || !failFast =>
                      errors ++= value.validationErrors(
                        path :+ SetOrderBoxLayoutRequest.Fields.boxes :+ Field(i.toString),
                        failFast)
                    case (value, i) =>
                }
            }
        }
        

        // ==================
        // allowRemove






        







        

    errors.toSeq
  }

  def validated(failFast : Boolean = false) : scala.util.Try[SetOrderBoxLayoutRequest] = {
    validationErrors(Vector(), failFast) match {
      case Seq() => Success(asModel)
      case first +: theRest => Failure(ValidationErrors(first, theRest))
    }
  }

  /** use 'validated' to check validation */
  def asModel : SetOrderBoxLayoutRequest = {
    SetOrderBoxLayoutRequest(
        boxes = 
        boxes
        
        .map(_.asModel),
        allowRemove = Option(
        allowRemove
        )
        
    )
  }
}

object SetOrderBoxLayoutRequestData {

  given readWriter : RW[SetOrderBoxLayoutRequestData] = macroRW

  def fromJsonString(jason : String) : SetOrderBoxLayoutRequestData = try {
        read[SetOrderBoxLayoutRequestData](jason)
     } catch {
          case NonFatal(e) => sys.error(s"Error parsing json '$jason': $e")
     }

  def manyFromJsonString(jason : String) : Seq[SetOrderBoxLayoutRequestData] = try {
        read[List[SetOrderBoxLayoutRequestData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as list: $e")
    }

  def manyFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Seq[SetOrderBoxLayoutRequest]] = {
      Try(manyFromJsonString(jason)).flatMap { list =>
        list.zipWithIndex.foldLeft(Try(Vector[SetOrderBoxLayoutRequest]())) {
          case (Success(list), (next, i)) => 
            next.validated(failFast) match {
              case Success(ok) => Success(list :+ ok)
              case Failure(err) => Failure(new Exception(s"Validation error on element $i: ${err.getMessage}", err))
            }
          case (fail, _)  => fail
        }
      }
    }

  def mapFromJsonString(jason : String) : Map[String, SetOrderBoxLayoutRequestData] = try {
        read[Map[String, SetOrderBoxLayoutRequestData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as map: $e")
    }


  def mapFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Map[String, SetOrderBoxLayoutRequest]] = {
     Try(mapFromJsonString(jason)).flatMap { map =>
       map.foldLeft(Try(Map[String, SetOrderBoxLayoutRequest]())) {
         case (Success(map), (key, next)) =>
           next.validated(failFast) match {
             case Success(ok) => Success(map.updated(key, ok))
             case Failure(err) => Failure(new Exception(s"Validation error on element $key: ${err.getMessage}", err))
           }
         case (fail, _) => fail
       }
     }
  }
}

