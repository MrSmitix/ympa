/**
 * Партнерский API Маркета
 * API Яндекс Маркета помогает продавцам автоматизировать и упростить работу с маркетплейсом.  В числе возможностей интеграции:  * управление каталогом товаров и витриной,  * обработка заказов,  * изменение настроек магазина,  * получение отчетов. 
 *
 * OpenAPI spec version: LATEST
 *
 * Contact: team@openapitools.org
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 *
 * https://openapi-generator.tech
 */

// this model was generated using modelData.mustache
package ympa_scala_cask_server.model
import java.time.OffsetDateTime
import ympa_scala_cask_server.model.ReportStatusType
import ympa_scala_cask_server.model.ReportSubStatusType
import scala.util.control.NonFatal
import scala.util.*

// see https://com-lihaoyi.github.io/upickle/
import upickle.default.{ReadWriter => RW, macroRW}
import upickle.default.*

/** ReportInfoDTOData a data transfer object, primarily for simple json serialisation.
  * It has no validation - there may be nulls, values out of range, etc
  */
case class ReportInfoDTOData(
  status: ReportStatusType,

    subStatus: ReportSubStatusType = null ,

  /* Дата и время запроса на генерацию. */
  generationRequestedAt: OffsetDateTime,

  /* Дата и время завершения генерации. */
  generationFinishedAt: OffsetDateTime = null ,

  /* Ссылка на готовый отчет. */
  file: String = "" ,

  /* Ожидаемая продолжительность генерации в миллисекундах. */
  estimatedGenerationTime: Long = 0 

  ) {

  def asJson: String = write(this)

  def validationErrors(path : Seq[Field], failFast : Boolean) : Seq[ValidationError] = {
    val errors = scala.collection.mutable.ListBuffer[ValidationError]()
        // ==================
        // status






        
        if (errors.isEmpty || !failFast) {
            if (status == null) {
            errors += ValidationError(path :+ ReportInfoDTO.Fields.status, "status is a required field and cannot be null")
            }
        }
        







        

        // ==================
        // subStatus






        







        

        // ==================
        // generationRequestedAt






        
        if (errors.isEmpty || !failFast) {
            if (generationRequestedAt == null) {
            errors += ValidationError(path :+ ReportInfoDTO.Fields.generationRequestedAt, "generationRequestedAt is a required field and cannot be null")
            }
        }
        







        

        // ==================
        // generationFinishedAt






        







        

        // ==================
        // file






        







        

        // ==================
        // estimatedGenerationTime






        







        

    errors.toSeq
  }

  def validated(failFast : Boolean = false) : scala.util.Try[ReportInfoDTO] = {
    validationErrors(Vector(), failFast) match {
      case Seq() => Success(asModel)
      case first +: theRest => Failure(ValidationErrors(first, theRest))
    }
  }

  /** use 'validated' to check validation */
  def asModel : ReportInfoDTO = {
    ReportInfoDTO(
        status = 
        status
        
        ,
        subStatus = Option(
        subStatus
        )
        ,
        generationRequestedAt = 
        generationRequestedAt
        
        ,
        generationFinishedAt = Option(
        generationFinishedAt
        )
        ,
        file = Option(
        file
        )
        ,
        estimatedGenerationTime = Option(
        estimatedGenerationTime
        )
        
    )
  }
}

object ReportInfoDTOData {

  given readWriter : RW[ReportInfoDTOData] = macroRW

  def fromJsonString(jason : String) : ReportInfoDTOData = try {
        read[ReportInfoDTOData](jason)
     } catch {
          case NonFatal(e) => sys.error(s"Error parsing json '$jason': $e")
     }

  def manyFromJsonString(jason : String) : Seq[ReportInfoDTOData] = try {
        read[List[ReportInfoDTOData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as list: $e")
    }

  def manyFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Seq[ReportInfoDTO]] = {
      Try(manyFromJsonString(jason)).flatMap { list =>
        list.zipWithIndex.foldLeft(Try(Vector[ReportInfoDTO]())) {
          case (Success(list), (next, i)) => 
            next.validated(failFast) match {
              case Success(ok) => Success(list :+ ok)
              case Failure(err) => Failure(new Exception(s"Validation error on element $i: ${err.getMessage}", err))
            }
          case (fail, _)  => fail
        }
      }
    }

  def mapFromJsonString(jason : String) : Map[String, ReportInfoDTOData] = try {
        read[Map[String, ReportInfoDTOData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as map: $e")
    }


  def mapFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Map[String, ReportInfoDTO]] = {
     Try(mapFromJsonString(jason)).flatMap { map =>
       map.foldLeft(Try(Map[String, ReportInfoDTO]())) {
         case (Success(map), (key, next)) =>
           next.validated(failFast) match {
             case Success(ok) => Success(map.updated(key, ok))
             case Failure(err) => Failure(new Exception(s"Validation error on element $key: ${err.getMessage}", err))
           }
         case (fail, _) => fail
       }
     }
  }
}

