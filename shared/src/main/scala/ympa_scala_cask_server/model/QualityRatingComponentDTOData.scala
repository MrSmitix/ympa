/**
 * Партнерский API Маркета
 * API Яндекс Маркета помогает продавцам автоматизировать и упростить работу с маркетплейсом.  В числе возможностей интеграции:  * управление каталогом товаров и витриной,  * обработка заказов,  * изменение настроек магазина,  * получение отчетов. 
 *
 * OpenAPI spec version: LATEST
 *
 * Contact: team@openapitools.org
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 *
 * https://openapi-generator.tech
 */

// this model was generated using modelData.mustache
package ympa_scala_cask_server.model
import ympa_scala_cask_server.model.QualityRatingComponentType
import scala.util.control.NonFatal
import scala.util.*

// see https://com-lihaoyi.github.io/upickle/
import upickle.default.{ReadWriter => RW, macroRW}
import upickle.default.*

/** QualityRatingComponentDTOData a data transfer object, primarily for simple json serialisation.
  * It has no validation - there may be nulls, values out of range, etc
  */
case class QualityRatingComponentDTOData(
/* Значение составляющей в процентах. */
  value: Double,

    componentType: QualityRatingComponentType

  ) {

  def asJson: String = write(this)

  def validationErrors(path : Seq[Field], failFast : Boolean) : Seq[ValidationError] = {
    val errors = scala.collection.mutable.ListBuffer[ValidationError]()
        // ==================
        // value

        // validate against minimum 0
        if (errors.isEmpty || !failFast) {
            if !(value >= 0) then
            errors += ValidationError(path :+ QualityRatingComponentDTO.Fields.value, s"value '$value' is not greater than the minimum value 0")
        }

        // validate against maximum 100
        if (errors.isEmpty || !failFast) {
            if !(value <= 100) then
            errors += ValidationError(path :+ QualityRatingComponentDTO.Fields.value, s"value '$value' is not greater than the maximum value 100")
        }




        







        

        // ==================
        // componentType






        
        if (errors.isEmpty || !failFast) {
            if (componentType == null) {
            errors += ValidationError(path :+ QualityRatingComponentDTO.Fields.componentType, "componentType is a required field and cannot be null")
            }
        }
        







        

    errors.toSeq
  }

  def validated(failFast : Boolean = false) : scala.util.Try[QualityRatingComponentDTO] = {
    validationErrors(Vector(), failFast) match {
      case Seq() => Success(asModel)
      case first +: theRest => Failure(ValidationErrors(first, theRest))
    }
  }

  /** use 'validated' to check validation */
  def asModel : QualityRatingComponentDTO = {
    QualityRatingComponentDTO(
        value = 
        value
        
        ,
        componentType = 
        componentType
        
        
    )
  }
}

object QualityRatingComponentDTOData {

  given readWriter : RW[QualityRatingComponentDTOData] = macroRW

  def fromJsonString(jason : String) : QualityRatingComponentDTOData = try {
        read[QualityRatingComponentDTOData](jason)
     } catch {
          case NonFatal(e) => sys.error(s"Error parsing json '$jason': $e")
     }

  def manyFromJsonString(jason : String) : Seq[QualityRatingComponentDTOData] = try {
        read[List[QualityRatingComponentDTOData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as list: $e")
    }

  def manyFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Seq[QualityRatingComponentDTO]] = {
      Try(manyFromJsonString(jason)).flatMap { list =>
        list.zipWithIndex.foldLeft(Try(Vector[QualityRatingComponentDTO]())) {
          case (Success(list), (next, i)) => 
            next.validated(failFast) match {
              case Success(ok) => Success(list :+ ok)
              case Failure(err) => Failure(new Exception(s"Validation error on element $i: ${err.getMessage}", err))
            }
          case (fail, _)  => fail
        }
      }
    }

  def mapFromJsonString(jason : String) : Map[String, QualityRatingComponentDTOData] = try {
        read[Map[String, QualityRatingComponentDTOData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as map: $e")
    }


  def mapFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Map[String, QualityRatingComponentDTO]] = {
     Try(mapFromJsonString(jason)).flatMap { map =>
       map.foldLeft(Try(Map[String, QualityRatingComponentDTO]())) {
         case (Success(map), (key, next)) =>
           next.validated(failFast) match {
             case Success(ok) => Success(map.updated(key, ok))
             case Failure(err) => Failure(new Exception(s"Validation error on element $key: ${err.getMessage}", err))
           }
         case (fail, _) => fail
       }
     }
  }
}

