/**
 * Партнерский API Маркета
 * API Яндекс Маркета помогает продавцам автоматизировать и упростить работу с маркетплейсом.  В числе возможностей интеграции:  * управление каталогом товаров и витриной,  * обработка заказов,  * изменение настроек магазина,  * получение отчетов. 
 *
 * OpenAPI spec version: LATEST
 *
 * Contact: team@openapitools.org
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 *
 * https://openapi-generator.tech
 */

// this model was generated using modelData.mustache
package ympa_scala_cask_server.model
import scala.util.control.NonFatal
import scala.util.*

// see https://com-lihaoyi.github.io/upickle/
import upickle.default.{ReadWriter => RW, macroRW}
import upickle.default.*

/** UpdateGoodsFeedbackCommentDTOData a data transfer object, primarily for simple json serialisation.
  * It has no validation - there may be nulls, values out of range, etc
  */
case class UpdateGoodsFeedbackCommentDTOData(
/* Идентификатор комментария к отзыву.  */
  id: Long = 0 ,

  /* Идентификатор комментария к отзыву.  */
  parentId: Long = 0 ,

  /* Текст комментария. */
  text: String

  ) {

  def asJson: String = write(this)

  def validationErrors(path : Seq[Field], failFast : Boolean) : Seq[ValidationError] = {
    val errors = scala.collection.mutable.ListBuffer[ValidationError]()
        // ==================
        // id






        







        

        // ==================
        // parentId






        







        

        // ==================
        // text



        // validate min length 1
        if (errors.isEmpty || !failFast)  {
          val len = if text == null then 0 else text.length
            if (len < 1) {
               errors += ValidationError(path :+ UpdateGoodsFeedbackCommentDTO.Fields.text, s"length $len is shorter than the min length 1")
            }
        }

        // validate max length 4096
        if (errors.isEmpty || !failFast)  {
          val len = if text == null then 0 else text.length
            if (len < 4096) {
               errors += ValidationError(path :+ UpdateGoodsFeedbackCommentDTO.Fields.text, s"length $len is longer than the max length 4096")
            }
        }


        







        

    errors.toSeq
  }

  def validated(failFast : Boolean = false) : scala.util.Try[UpdateGoodsFeedbackCommentDTO] = {
    validationErrors(Vector(), failFast) match {
      case Seq() => Success(asModel)
      case first +: theRest => Failure(ValidationErrors(first, theRest))
    }
  }

  /** use 'validated' to check validation */
  def asModel : UpdateGoodsFeedbackCommentDTO = {
    UpdateGoodsFeedbackCommentDTO(
        id = Option(
        id
        )
        ,
        parentId = Option(
        parentId
        )
        ,
        text = 
        text
        
        
    )
  }
}

object UpdateGoodsFeedbackCommentDTOData {

  given readWriter : RW[UpdateGoodsFeedbackCommentDTOData] = macroRW

  def fromJsonString(jason : String) : UpdateGoodsFeedbackCommentDTOData = try {
        read[UpdateGoodsFeedbackCommentDTOData](jason)
     } catch {
          case NonFatal(e) => sys.error(s"Error parsing json '$jason': $e")
     }

  def manyFromJsonString(jason : String) : Seq[UpdateGoodsFeedbackCommentDTOData] = try {
        read[List[UpdateGoodsFeedbackCommentDTOData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as list: $e")
    }

  def manyFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Seq[UpdateGoodsFeedbackCommentDTO]] = {
      Try(manyFromJsonString(jason)).flatMap { list =>
        list.zipWithIndex.foldLeft(Try(Vector[UpdateGoodsFeedbackCommentDTO]())) {
          case (Success(list), (next, i)) => 
            next.validated(failFast) match {
              case Success(ok) => Success(list :+ ok)
              case Failure(err) => Failure(new Exception(s"Validation error on element $i: ${err.getMessage}", err))
            }
          case (fail, _)  => fail
        }
      }
    }

  def mapFromJsonString(jason : String) : Map[String, UpdateGoodsFeedbackCommentDTOData] = try {
        read[Map[String, UpdateGoodsFeedbackCommentDTOData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as map: $e")
    }


  def mapFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Map[String, UpdateGoodsFeedbackCommentDTO]] = {
     Try(mapFromJsonString(jason)).flatMap { map =>
       map.foldLeft(Try(Map[String, UpdateGoodsFeedbackCommentDTO]())) {
         case (Success(map), (key, next)) =>
           next.validated(failFast) match {
             case Success(ok) => Success(map.updated(key, ok))
             case Failure(err) => Failure(new Exception(s"Validation error on element $key: ${err.getMessage}", err))
           }
         case (fail, _) => fail
       }
     }
  }
}

