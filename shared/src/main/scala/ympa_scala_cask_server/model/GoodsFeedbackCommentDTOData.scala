/**
 * Партнерский API Маркета
 * API Яндекс Маркета помогает продавцам автоматизировать и упростить работу с маркетплейсом.  В числе возможностей интеграции:  * управление каталогом товаров и витриной,  * обработка заказов,  * изменение настроек магазина,  * получение отчетов. 
 *
 * OpenAPI spec version: LATEST
 *
 * Contact: team@openapitools.org
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 *
 * https://openapi-generator.tech
 */

// this model was generated using modelData.mustache
package ympa_scala_cask_server.model
import ympa_scala_cask_server.model.GoodsFeedbackCommentAuthorDTO
import ympa_scala_cask_server.model.GoodsFeedbackCommentStatusType
import scala.util.control.NonFatal
import scala.util.*

// see https://com-lihaoyi.github.io/upickle/
import upickle.default.{ReadWriter => RW, macroRW}
import upickle.default.*

/** GoodsFeedbackCommentDTOData a data transfer object, primarily for simple json serialisation.
  * It has no validation - there may be nulls, values out of range, etc
  */
case class GoodsFeedbackCommentDTOData(
/* Идентификатор комментария к отзыву.  */
  id: Long,

  /* Текст комментария. */
  text: String,

  /* Может ли продавец изменять комментарий или удалять его. */
  canModify: Boolean = false ,

  /* Идентификатор комментария к отзыву.  */
  parentId: Long = 0 ,

    author: GoodsFeedbackCommentAuthorDTOData,

    status: GoodsFeedbackCommentStatusType

  ) {

  def asJson: String = write(this)

  def validationErrors(path : Seq[Field], failFast : Boolean) : Seq[ValidationError] = {
    val errors = scala.collection.mutable.ListBuffer[ValidationError]()
        // ==================
        // id






        







        

        // ==================
        // text



        // validate min length 1
        if (errors.isEmpty || !failFast)  {
          val len = if text == null then 0 else text.length
            if (len < 1) {
               errors += ValidationError(path :+ GoodsFeedbackCommentDTO.Fields.text, s"length $len is shorter than the min length 1")
            }
        }

        // validate max length 4096
        if (errors.isEmpty || !failFast)  {
          val len = if text == null then 0 else text.length
            if (len < 4096) {
               errors += ValidationError(path :+ GoodsFeedbackCommentDTO.Fields.text, s"length $len is longer than the max length 4096")
            }
        }


        







        

        // ==================
        // canModify






        







        

        // ==================
        // parentId






        







        

        // ==================
        // author






        
        if (errors.isEmpty || !failFast) {
            if (author == null) {
            errors += ValidationError(path :+ GoodsFeedbackCommentDTO.Fields.author, "author is a required field and cannot be null")
            }
        }
        







        
        // validating author
        if (errors.isEmpty || !failFast) {
            if author != null then errors ++= author.validationErrors(path :+ GoodsFeedbackCommentDTO.Fields.author, failFast)
        }

        // ==================
        // status






        
        if (errors.isEmpty || !failFast) {
            if (status == null) {
            errors += ValidationError(path :+ GoodsFeedbackCommentDTO.Fields.status, "status is a required field and cannot be null")
            }
        }
        







        

    errors.toSeq
  }

  def validated(failFast : Boolean = false) : scala.util.Try[GoodsFeedbackCommentDTO] = {
    validationErrors(Vector(), failFast) match {
      case Seq() => Success(asModel)
      case first +: theRest => Failure(ValidationErrors(first, theRest))
    }
  }

  /** use 'validated' to check validation */
  def asModel : GoodsFeedbackCommentDTO = {
    GoodsFeedbackCommentDTO(
        id = 
        id
        
        ,
        text = 
        text
        
        ,
        canModify = Option(
        canModify
        )
        ,
        parentId = Option(
        parentId
        )
        ,
        author = 
        author
        
        ,
        status = 
        status
        
        
    )
  }
}

object GoodsFeedbackCommentDTOData {

  given readWriter : RW[GoodsFeedbackCommentDTOData] = macroRW

  def fromJsonString(jason : String) : GoodsFeedbackCommentDTOData = try {
        read[GoodsFeedbackCommentDTOData](jason)
     } catch {
          case NonFatal(e) => sys.error(s"Error parsing json '$jason': $e")
     }

  def manyFromJsonString(jason : String) : Seq[GoodsFeedbackCommentDTOData] = try {
        read[List[GoodsFeedbackCommentDTOData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as list: $e")
    }

  def manyFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Seq[GoodsFeedbackCommentDTO]] = {
      Try(manyFromJsonString(jason)).flatMap { list =>
        list.zipWithIndex.foldLeft(Try(Vector[GoodsFeedbackCommentDTO]())) {
          case (Success(list), (next, i)) => 
            next.validated(failFast) match {
              case Success(ok) => Success(list :+ ok)
              case Failure(err) => Failure(new Exception(s"Validation error on element $i: ${err.getMessage}", err))
            }
          case (fail, _)  => fail
        }
      }
    }

  def mapFromJsonString(jason : String) : Map[String, GoodsFeedbackCommentDTOData] = try {
        read[Map[String, GoodsFeedbackCommentDTOData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as map: $e")
    }


  def mapFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Map[String, GoodsFeedbackCommentDTO]] = {
     Try(mapFromJsonString(jason)).flatMap { map =>
       map.foldLeft(Try(Map[String, GoodsFeedbackCommentDTO]())) {
         case (Success(map), (key, next)) =>
           next.validated(failFast) match {
             case Success(ok) => Success(map.updated(key, ok))
             case Failure(err) => Failure(new Exception(s"Validation error on element $key: ${err.getMessage}", err))
           }
         case (fail, _) => fail
       }
     }
  }
}

