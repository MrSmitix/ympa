/**
 * Партнерский API Маркета
 * API Яндекс Маркета помогает продавцам автоматизировать и упростить работу с маркетплейсом.  В числе возможностей интеграции:  * управление каталогом товаров и витриной,  * обработка заказов,  * изменение настроек магазина,  * получение отчетов. 
 *
 * OpenAPI spec version: LATEST
 *
 * Contact: team@openapitools.org
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 *
 * https://openapi-generator.tech
 */

// this model was generated using modelData.mustache
package ympa_scala_cask_server.model
import ympa_scala_cask_server.model.OfferCardStatusType
import scala.util.control.NonFatal
import scala.util.*

// see https://com-lihaoyi.github.io/upickle/
import upickle.default.{ReadWriter => RW, macroRW}
import upickle.default.*

/** GetQuarantineOffersRequestData a data transfer object, primarily for simple json serialisation.
  * It has no validation - there may be nulls, values out of range, etc
  */
case class GetQuarantineOffersRequestData(
/* Идентификаторы товаров, информация о которых нужна. <br><br> ⚠️ Не используйте это поле одновременно с фильтрами по статусам карточек, категориям, брендам или тегам. Если вы хотите воспользоваться фильтрами, оставьте поле пустым.  */
  offerIds: Seq[String] = Nil ,

  /* Фильтр по статусам карточек.  [Что такое карточка товара](https://yandex.ru/support/marketplace/assortment/content/index.html)  */
  cardStatuses: Seq[OfferCardStatusType] = Nil ,

  /* Фильтр по категориям на Маркете. */
  categoryIds: Seq[Int] = Nil ,

  /* Фильтр по брендам. */
  vendorNames: Seq[String] = Nil ,

  /* Фильтр по тегам. */
  tags: Seq[String] = Nil 

  ) {

  def asJson: String = write(this)

  def validationErrors(path : Seq[Field], failFast : Boolean) : Seq[ValidationError] = {
    val errors = scala.collection.mutable.ListBuffer[ValidationError]()
        // ==================
        // offerIds






        



        // validate min items 1
        if (errors.isEmpty || !failFast) {
          val len = if offerIds == null then 0 else offerIds.size
            if (len < 1) {
               errors += ValidationError(path :+ GetQuarantineOffersRequest.Fields.offerIds, s"offerIds has $len, which is less than the min items 1")
            }
        }

        // validate min items 500
        if (errors.isEmpty || !failFast) {
          val len = if offerIds == null then 0 else offerIds.size
            if (len > 500) {
               errors += ValidationError(path :+ GetQuarantineOffersRequest.Fields.offerIds, s"offerIds has $len, which is greater than the max items 500")
            }
        }



        

        // ==================
        // cardStatuses






        



        // validate min items 1
        if (errors.isEmpty || !failFast) {
          val len = if cardStatuses == null then 0 else cardStatuses.size
            if (len < 1) {
               errors += ValidationError(path :+ GetQuarantineOffersRequest.Fields.cardStatuses, s"cardStatuses has $len, which is less than the min items 1")
            }
        }




        

        // ==================
        // categoryIds






        



        // validate min items 1
        if (errors.isEmpty || !failFast) {
          val len = if categoryIds == null then 0 else categoryIds.size
            if (len < 1) {
               errors += ValidationError(path :+ GetQuarantineOffersRequest.Fields.categoryIds, s"categoryIds has $len, which is less than the min items 1")
            }
        }




        

        // ==================
        // vendorNames






        



        // validate min items 1
        if (errors.isEmpty || !failFast) {
          val len = if vendorNames == null then 0 else vendorNames.size
            if (len < 1) {
               errors += ValidationError(path :+ GetQuarantineOffersRequest.Fields.vendorNames, s"vendorNames has $len, which is less than the min items 1")
            }
        }




        

        // ==================
        // tags






        



        // validate min items 1
        if (errors.isEmpty || !failFast) {
          val len = if tags == null then 0 else tags.size
            if (len < 1) {
               errors += ValidationError(path :+ GetQuarantineOffersRequest.Fields.tags, s"tags has $len, which is less than the min items 1")
            }
        }




        

    errors.toSeq
  }

  def validated(failFast : Boolean = false) : scala.util.Try[GetQuarantineOffersRequest] = {
    validationErrors(Vector(), failFast) match {
      case Seq() => Success(asModel)
      case first +: theRest => Failure(ValidationErrors(first, theRest))
    }
  }

  /** use 'validated' to check validation */
  def asModel : GetQuarantineOffersRequest = {
    GetQuarantineOffersRequest(
        offerIds = 
        offerIds
        
        ,
        cardStatuses = 
        cardStatuses
        
        ,
        categoryIds = 
        categoryIds
        
        ,
        vendorNames = 
        vendorNames
        
        ,
        tags = 
        tags
        
        
    )
  }
}

object GetQuarantineOffersRequestData {

  given readWriter : RW[GetQuarantineOffersRequestData] = macroRW

  def fromJsonString(jason : String) : GetQuarantineOffersRequestData = try {
        read[GetQuarantineOffersRequestData](jason)
     } catch {
          case NonFatal(e) => sys.error(s"Error parsing json '$jason': $e")
     }

  def manyFromJsonString(jason : String) : Seq[GetQuarantineOffersRequestData] = try {
        read[List[GetQuarantineOffersRequestData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as list: $e")
    }

  def manyFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Seq[GetQuarantineOffersRequest]] = {
      Try(manyFromJsonString(jason)).flatMap { list =>
        list.zipWithIndex.foldLeft(Try(Vector[GetQuarantineOffersRequest]())) {
          case (Success(list), (next, i)) => 
            next.validated(failFast) match {
              case Success(ok) => Success(list :+ ok)
              case Failure(err) => Failure(new Exception(s"Validation error on element $i: ${err.getMessage}", err))
            }
          case (fail, _)  => fail
        }
      }
    }

  def mapFromJsonString(jason : String) : Map[String, GetQuarantineOffersRequestData] = try {
        read[Map[String, GetQuarantineOffersRequestData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as map: $e")
    }


  def mapFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Map[String, GetQuarantineOffersRequest]] = {
     Try(mapFromJsonString(jason)).flatMap { map =>
       map.foldLeft(Try(Map[String, GetQuarantineOffersRequest]())) {
         case (Success(map), (key, next)) =>
           next.validated(failFast) match {
             case Success(ok) => Success(map.updated(key, ok))
             case Failure(err) => Failure(new Exception(s"Validation error on element $key: ${err.getMessage}", err))
           }
         case (fail, _) => fail
       }
     }
  }
}

