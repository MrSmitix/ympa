/**
 * Партнерский API Маркета
 * API Яндекс Маркета помогает продавцам автоматизировать и упростить работу с маркетплейсом.  В числе возможностей интеграции:  * управление каталогом товаров и витриной,  * обработка заказов,  * изменение настроек магазина,  * получение отчетов. 
 *
 * OpenAPI spec version: LATEST
 *
 * Contact: team@openapitools.org
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 *
 * https://openapi-generator.tech
 */

// this model was generated using modelData.mustache
package ympa_scala_cask_server.model
import scala.math.BigDecimal
import scala.util.control.NonFatal
import scala.util.*

// see https://com-lihaoyi.github.io/upickle/
import upickle.default.{ReadWriter => RW, macroRW}
import upickle.default.*

/** OfferWeightDimensionsDTOData a data transfer object, primarily for simple json serialisation.
  * It has no validation - there may be nulls, values out of range, etc
  */
case class OfferWeightDimensionsDTOData(
/* Длина упаковки в см.  */
  length: BigDecimal,

  /* Ширина упаковки в см.  */
  width: BigDecimal,

  /* Высота упаковки в см.  */
  height: BigDecimal,

  /* Вес товара в кг с учетом упаковки (брутто).  */
  weight: BigDecimal

  ) {

  def asJson: String = write(this)

  def validationErrors(path : Seq[Field], failFast : Boolean) : Seq[ValidationError] = {
    val errors = scala.collection.mutable.ListBuffer[ValidationError]()
        // ==================
        // length






        
        if (errors.isEmpty || !failFast) {
            if (length == null) {
            errors += ValidationError(path :+ OfferWeightDimensionsDTO.Fields.length, "length is a required field and cannot be null")
            }
        }
        







        

        // ==================
        // width






        
        if (errors.isEmpty || !failFast) {
            if (width == null) {
            errors += ValidationError(path :+ OfferWeightDimensionsDTO.Fields.width, "width is a required field and cannot be null")
            }
        }
        







        

        // ==================
        // height






        
        if (errors.isEmpty || !failFast) {
            if (height == null) {
            errors += ValidationError(path :+ OfferWeightDimensionsDTO.Fields.height, "height is a required field and cannot be null")
            }
        }
        







        

        // ==================
        // weight






        
        if (errors.isEmpty || !failFast) {
            if (weight == null) {
            errors += ValidationError(path :+ OfferWeightDimensionsDTO.Fields.weight, "weight is a required field and cannot be null")
            }
        }
        







        

    errors.toSeq
  }

  def validated(failFast : Boolean = false) : scala.util.Try[OfferWeightDimensionsDTO] = {
    validationErrors(Vector(), failFast) match {
      case Seq() => Success(asModel)
      case first +: theRest => Failure(ValidationErrors(first, theRest))
    }
  }

  /** use 'validated' to check validation */
  def asModel : OfferWeightDimensionsDTO = {
    OfferWeightDimensionsDTO(
        length = 
        length
        
        ,
        width = 
        width
        
        ,
        height = 
        height
        
        ,
        weight = 
        weight
        
        
    )
  }
}

object OfferWeightDimensionsDTOData {

  given readWriter : RW[OfferWeightDimensionsDTOData] = macroRW

  def fromJsonString(jason : String) : OfferWeightDimensionsDTOData = try {
        read[OfferWeightDimensionsDTOData](jason)
     } catch {
          case NonFatal(e) => sys.error(s"Error parsing json '$jason': $e")
     }

  def manyFromJsonString(jason : String) : Seq[OfferWeightDimensionsDTOData] = try {
        read[List[OfferWeightDimensionsDTOData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as list: $e")
    }

  def manyFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Seq[OfferWeightDimensionsDTO]] = {
      Try(manyFromJsonString(jason)).flatMap { list =>
        list.zipWithIndex.foldLeft(Try(Vector[OfferWeightDimensionsDTO]())) {
          case (Success(list), (next, i)) => 
            next.validated(failFast) match {
              case Success(ok) => Success(list :+ ok)
              case Failure(err) => Failure(new Exception(s"Validation error on element $i: ${err.getMessage}", err))
            }
          case (fail, _)  => fail
        }
      }
    }

  def mapFromJsonString(jason : String) : Map[String, OfferWeightDimensionsDTOData] = try {
        read[Map[String, OfferWeightDimensionsDTOData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as map: $e")
    }


  def mapFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Map[String, OfferWeightDimensionsDTO]] = {
     Try(mapFromJsonString(jason)).flatMap { map =>
       map.foldLeft(Try(Map[String, OfferWeightDimensionsDTO]())) {
         case (Success(map), (key, next)) =>
           next.validated(failFast) match {
             case Success(ok) => Success(map.updated(key, ok))
             case Failure(err) => Failure(new Exception(s"Validation error on element $key: ${err.getMessage}", err))
           }
         case (fail, _) => fail
       }
     }
  }
}

