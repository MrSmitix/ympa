/**
 * Партнерский API Маркета
 * API Яндекс Маркета помогает продавцам автоматизировать и упростить работу с маркетплейсом.  В числе возможностей интеграции:  * управление каталогом товаров и витриной,  * обработка заказов,  * изменение настроек магазина,  * получение отчетов. 
 *
 * The version of the OpenAPI document: LATEST
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

#include <QJsonArray>
#include <QJsonDocument>
#include <QJsonObject>
#include <QVariantMap>


#include "OAIApiRouter.h"
#include "OAIBidsApiRequest.h"
#include "OAIBusinessOfferMappingsApiRequest.h"
#include "OAIBusinessesApiRequest.h"
#include "OAICampaignsApiRequest.h"
#include "OAICategoriesApiRequest.h"
#include "OAIChatsApiRequest.h"
#include "OAIContentApiRequest.h"
#include "OAIDbsApiRequest.h"
#include "OAIDeliveryServicesApiRequest.h"
#include "OAIExpressApiRequest.h"
#include "OAIFbsApiRequest.h"
#include "OAIFbyApiRequest.h"
#include "OAIFeedbacksApiRequest.h"
#include "OAIFeedsApiRequest.h"
#include "OAIGoodsFeedbackApiRequest.h"
#include "OAIGoodsStatsApiRequest.h"
#include "OAIHiddenOffersApiRequest.h"
#include "OAIModelsApiRequest.h"
#include "OAIOfferMappingsApiRequest.h"
#include "OAIOffersApiRequest.h"
#include "OAIOrderBusinessInformationApiRequest.h"
#include "OAIOrderDeliveryApiRequest.h"
#include "OAIOrderLabelsApiRequest.h"
#include "OAIOrdersApiRequest.h"
#include "OAIOrdersStatsApiRequest.h"
#include "OAIOutletLicensesApiRequest.h"
#include "OAIOutletsApiRequest.h"
#include "OAIPriceQuarantineApiRequest.h"
#include "OAIPricesApiRequest.h"
#include "OAIPromosApiRequest.h"
#include "OAIRatingsApiRequest.h"
#include "OAIRegionsApiRequest.h"
#include "OAIReportsApiRequest.h"
#include "OAIReturnsApiRequest.h"
#include "OAIShipmentsApiRequest.h"
#include "OAIStocksApiRequest.h"
#include "OAITariffsApiRequest.h"
#include "OAIWarehousesApiRequest.h"


namespace OpenAPI {

OAIApiRouter::OAIApiRouter() {
    createApiHandlers();
}

OAIApiRouter::~OAIApiRouter(){

}

void OAIApiRouter::createApiHandlers() { 
    mOAIBidsApiHandler = QSharedPointer<OAIBidsApiHandler>::create();
    mOAIBusinessOfferMappingsApiHandler = QSharedPointer<OAIBusinessOfferMappingsApiHandler>::create();
    mOAIBusinessesApiHandler = QSharedPointer<OAIBusinessesApiHandler>::create();
    mOAICampaignsApiHandler = QSharedPointer<OAICampaignsApiHandler>::create();
    mOAICategoriesApiHandler = QSharedPointer<OAICategoriesApiHandler>::create();
    mOAIChatsApiHandler = QSharedPointer<OAIChatsApiHandler>::create();
    mOAIContentApiHandler = QSharedPointer<OAIContentApiHandler>::create();
    mOAIDbsApiHandler = QSharedPointer<OAIDbsApiHandler>::create();
    mOAIDeliveryServicesApiHandler = QSharedPointer<OAIDeliveryServicesApiHandler>::create();
    mOAIExpressApiHandler = QSharedPointer<OAIExpressApiHandler>::create();
    mOAIFbsApiHandler = QSharedPointer<OAIFbsApiHandler>::create();
    mOAIFbyApiHandler = QSharedPointer<OAIFbyApiHandler>::create();
    mOAIFeedbacksApiHandler = QSharedPointer<OAIFeedbacksApiHandler>::create();
    mOAIFeedsApiHandler = QSharedPointer<OAIFeedsApiHandler>::create();
    mOAIGoodsFeedbackApiHandler = QSharedPointer<OAIGoodsFeedbackApiHandler>::create();
    mOAIGoodsStatsApiHandler = QSharedPointer<OAIGoodsStatsApiHandler>::create();
    mOAIHiddenOffersApiHandler = QSharedPointer<OAIHiddenOffersApiHandler>::create();
    mOAIModelsApiHandler = QSharedPointer<OAIModelsApiHandler>::create();
    mOAIOfferMappingsApiHandler = QSharedPointer<OAIOfferMappingsApiHandler>::create();
    mOAIOffersApiHandler = QSharedPointer<OAIOffersApiHandler>::create();
    mOAIOrderBusinessInformationApiHandler = QSharedPointer<OAIOrderBusinessInformationApiHandler>::create();
    mOAIOrderDeliveryApiHandler = QSharedPointer<OAIOrderDeliveryApiHandler>::create();
    mOAIOrderLabelsApiHandler = QSharedPointer<OAIOrderLabelsApiHandler>::create();
    mOAIOrdersApiHandler = QSharedPointer<OAIOrdersApiHandler>::create();
    mOAIOrdersStatsApiHandler = QSharedPointer<OAIOrdersStatsApiHandler>::create();
    mOAIOutletLicensesApiHandler = QSharedPointer<OAIOutletLicensesApiHandler>::create();
    mOAIOutletsApiHandler = QSharedPointer<OAIOutletsApiHandler>::create();
    mOAIPriceQuarantineApiHandler = QSharedPointer<OAIPriceQuarantineApiHandler>::create();
    mOAIPricesApiHandler = QSharedPointer<OAIPricesApiHandler>::create();
    mOAIPromosApiHandler = QSharedPointer<OAIPromosApiHandler>::create();
    mOAIRatingsApiHandler = QSharedPointer<OAIRatingsApiHandler>::create();
    mOAIRegionsApiHandler = QSharedPointer<OAIRegionsApiHandler>::create();
    mOAIReportsApiHandler = QSharedPointer<OAIReportsApiHandler>::create();
    mOAIReturnsApiHandler = QSharedPointer<OAIReturnsApiHandler>::create();
    mOAIShipmentsApiHandler = QSharedPointer<OAIShipmentsApiHandler>::create();
    mOAIStocksApiHandler = QSharedPointer<OAIStocksApiHandler>::create();
    mOAITariffsApiHandler = QSharedPointer<OAITariffsApiHandler>::create();
    mOAIWarehousesApiHandler = QSharedPointer<OAIWarehousesApiHandler>::create();
}


void OAIApiRouter::setOAIBidsApiHandler(QSharedPointer<OAIBidsApiHandler> handler){
    mOAIBidsApiHandler = handler;
}
void OAIApiRouter::setOAIBusinessOfferMappingsApiHandler(QSharedPointer<OAIBusinessOfferMappingsApiHandler> handler){
    mOAIBusinessOfferMappingsApiHandler = handler;
}
void OAIApiRouter::setOAIBusinessesApiHandler(QSharedPointer<OAIBusinessesApiHandler> handler){
    mOAIBusinessesApiHandler = handler;
}
void OAIApiRouter::setOAICampaignsApiHandler(QSharedPointer<OAICampaignsApiHandler> handler){
    mOAICampaignsApiHandler = handler;
}
void OAIApiRouter::setOAICategoriesApiHandler(QSharedPointer<OAICategoriesApiHandler> handler){
    mOAICategoriesApiHandler = handler;
}
void OAIApiRouter::setOAIChatsApiHandler(QSharedPointer<OAIChatsApiHandler> handler){
    mOAIChatsApiHandler = handler;
}
void OAIApiRouter::setOAIContentApiHandler(QSharedPointer<OAIContentApiHandler> handler){
    mOAIContentApiHandler = handler;
}
void OAIApiRouter::setOAIDbsApiHandler(QSharedPointer<OAIDbsApiHandler> handler){
    mOAIDbsApiHandler = handler;
}
void OAIApiRouter::setOAIDeliveryServicesApiHandler(QSharedPointer<OAIDeliveryServicesApiHandler> handler){
    mOAIDeliveryServicesApiHandler = handler;
}
void OAIApiRouter::setOAIExpressApiHandler(QSharedPointer<OAIExpressApiHandler> handler){
    mOAIExpressApiHandler = handler;
}
void OAIApiRouter::setOAIFbsApiHandler(QSharedPointer<OAIFbsApiHandler> handler){
    mOAIFbsApiHandler = handler;
}
void OAIApiRouter::setOAIFbyApiHandler(QSharedPointer<OAIFbyApiHandler> handler){
    mOAIFbyApiHandler = handler;
}
void OAIApiRouter::setOAIFeedbacksApiHandler(QSharedPointer<OAIFeedbacksApiHandler> handler){
    mOAIFeedbacksApiHandler = handler;
}
void OAIApiRouter::setOAIFeedsApiHandler(QSharedPointer<OAIFeedsApiHandler> handler){
    mOAIFeedsApiHandler = handler;
}
void OAIApiRouter::setOAIGoodsFeedbackApiHandler(QSharedPointer<OAIGoodsFeedbackApiHandler> handler){
    mOAIGoodsFeedbackApiHandler = handler;
}
void OAIApiRouter::setOAIGoodsStatsApiHandler(QSharedPointer<OAIGoodsStatsApiHandler> handler){
    mOAIGoodsStatsApiHandler = handler;
}
void OAIApiRouter::setOAIHiddenOffersApiHandler(QSharedPointer<OAIHiddenOffersApiHandler> handler){
    mOAIHiddenOffersApiHandler = handler;
}
void OAIApiRouter::setOAIModelsApiHandler(QSharedPointer<OAIModelsApiHandler> handler){
    mOAIModelsApiHandler = handler;
}
void OAIApiRouter::setOAIOfferMappingsApiHandler(QSharedPointer<OAIOfferMappingsApiHandler> handler){
    mOAIOfferMappingsApiHandler = handler;
}
void OAIApiRouter::setOAIOffersApiHandler(QSharedPointer<OAIOffersApiHandler> handler){
    mOAIOffersApiHandler = handler;
}
void OAIApiRouter::setOAIOrderBusinessInformationApiHandler(QSharedPointer<OAIOrderBusinessInformationApiHandler> handler){
    mOAIOrderBusinessInformationApiHandler = handler;
}
void OAIApiRouter::setOAIOrderDeliveryApiHandler(QSharedPointer<OAIOrderDeliveryApiHandler> handler){
    mOAIOrderDeliveryApiHandler = handler;
}
void OAIApiRouter::setOAIOrderLabelsApiHandler(QSharedPointer<OAIOrderLabelsApiHandler> handler){
    mOAIOrderLabelsApiHandler = handler;
}
void OAIApiRouter::setOAIOrdersApiHandler(QSharedPointer<OAIOrdersApiHandler> handler){
    mOAIOrdersApiHandler = handler;
}
void OAIApiRouter::setOAIOrdersStatsApiHandler(QSharedPointer<OAIOrdersStatsApiHandler> handler){
    mOAIOrdersStatsApiHandler = handler;
}
void OAIApiRouter::setOAIOutletLicensesApiHandler(QSharedPointer<OAIOutletLicensesApiHandler> handler){
    mOAIOutletLicensesApiHandler = handler;
}
void OAIApiRouter::setOAIOutletsApiHandler(QSharedPointer<OAIOutletsApiHandler> handler){
    mOAIOutletsApiHandler = handler;
}
void OAIApiRouter::setOAIPriceQuarantineApiHandler(QSharedPointer<OAIPriceQuarantineApiHandler> handler){
    mOAIPriceQuarantineApiHandler = handler;
}
void OAIApiRouter::setOAIPricesApiHandler(QSharedPointer<OAIPricesApiHandler> handler){
    mOAIPricesApiHandler = handler;
}
void OAIApiRouter::setOAIPromosApiHandler(QSharedPointer<OAIPromosApiHandler> handler){
    mOAIPromosApiHandler = handler;
}
void OAIApiRouter::setOAIRatingsApiHandler(QSharedPointer<OAIRatingsApiHandler> handler){
    mOAIRatingsApiHandler = handler;
}
void OAIApiRouter::setOAIRegionsApiHandler(QSharedPointer<OAIRegionsApiHandler> handler){
    mOAIRegionsApiHandler = handler;
}
void OAIApiRouter::setOAIReportsApiHandler(QSharedPointer<OAIReportsApiHandler> handler){
    mOAIReportsApiHandler = handler;
}
void OAIApiRouter::setOAIReturnsApiHandler(QSharedPointer<OAIReturnsApiHandler> handler){
    mOAIReturnsApiHandler = handler;
}
void OAIApiRouter::setOAIShipmentsApiHandler(QSharedPointer<OAIShipmentsApiHandler> handler){
    mOAIShipmentsApiHandler = handler;
}
void OAIApiRouter::setOAIStocksApiHandler(QSharedPointer<OAIStocksApiHandler> handler){
    mOAIStocksApiHandler = handler;
}
void OAIApiRouter::setOAITariffsApiHandler(QSharedPointer<OAITariffsApiHandler> handler){
    mOAITariffsApiHandler = handler;
}
void OAIApiRouter::setOAIWarehousesApiHandler(QSharedPointer<OAIWarehousesApiHandler> handler){
    mOAIWarehousesApiHandler = handler;
}

void OAIApiRouter::setUpRoutes() {
    
    Routes.insert(QString("%1 %2").arg("GET").arg("/campaigns").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAICampaignsApiRequest(socket, mOAICampaignsApiHandler);
            reqObj->getCampaignsRequest();
    });
    Routes.insert(QString("%1 %2").arg("POST").arg("/categories/max-sale-quantum").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAICategoriesApiRequest(socket, mOAICategoriesApiHandler);
            reqObj->getCategoriesMaxSaleQuantumRequest();
    });
    Routes.insert(QString("%1 %2").arg("POST").arg("/categories/tree").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAICategoriesApiRequest(socket, mOAICategoriesApiHandler);
            reqObj->getCategoriesTreeRequest();
    });
    Routes.insert(QString("%1 %2").arg("POST").arg("/tariffs/calculate").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIDbsApiRequest(socket, mOAIDbsApiHandler);
            reqObj->calculateTariffsRequest();
    });
    Routes.insert(QString("%1 %2").arg("POST").arg("/reports/boost-consolidated/generate").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIDbsApiRequest(socket, mOAIDbsApiHandler);
            reqObj->generateBoostConsolidatedReportRequest();
    });
    Routes.insert(QString("%1 %2").arg("POST").arg("/reports/competitors-position/generate").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIDbsApiRequest(socket, mOAIDbsApiHandler);
            reqObj->generateCompetitorsPositionReportRequest();
    });
    Routes.insert(QString("%1 %2").arg("POST").arg("/reports/goods-feedback/generate").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIDbsApiRequest(socket, mOAIDbsApiHandler);
            reqObj->generateGoodsFeedbackReportRequest();
    });
    Routes.insert(QString("%1 %2").arg("POST").arg("/reports/documents/labels/generate").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIDbsApiRequest(socket, mOAIDbsApiHandler);
            reqObj->generateMassOrderLabelsReportRequest();
    });
    Routes.insert(QString("%1 %2").arg("POST").arg("/reports/prices/generate").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIDbsApiRequest(socket, mOAIDbsApiHandler);
            reqObj->generatePricesReportRequest();
    });
    Routes.insert(QString("%1 %2").arg("POST").arg("/reports/shelf-statistics/generate").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIDbsApiRequest(socket, mOAIDbsApiHandler);
            reqObj->generateShelfsStatisticsReportRequest();
    });
    Routes.insert(QString("%1 %2").arg("POST").arg("/reports/shows-sales/generate").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIDbsApiRequest(socket, mOAIDbsApiHandler);
            reqObj->generateShowsSalesReportRequest();
    });
    Routes.insert(QString("%1 %2").arg("POST").arg("/reports/stocks-on-warehouses/generate").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIDbsApiRequest(socket, mOAIDbsApiHandler);
            reqObj->generateStocksOnWarehousesReportRequest();
    });
    Routes.insert(QString("%1 %2").arg("POST").arg("/reports/united-marketplace-services/generate").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIDbsApiRequest(socket, mOAIDbsApiHandler);
            reqObj->generateUnitedMarketplaceServicesReportRequest();
    });
    Routes.insert(QString("%1 %2").arg("POST").arg("/reports/united-netting/generate").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIDbsApiRequest(socket, mOAIDbsApiHandler);
            reqObj->generateUnitedNettingReportRequest();
    });
    Routes.insert(QString("%1 %2").arg("POST").arg("/reports/united-orders/generate").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIDbsApiRequest(socket, mOAIDbsApiHandler);
            reqObj->generateUnitedOrdersReportRequest();
    });
    Routes.insert(QString("%1 %2").arg("GET").arg("/campaigns").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIDbsApiRequest(socket, mOAIDbsApiHandler);
            reqObj->getCampaignsRequest();
    });
    Routes.insert(QString("%1 %2").arg("POST").arg("/categories/max-sale-quantum").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIDbsApiRequest(socket, mOAIDbsApiHandler);
            reqObj->getCategoriesMaxSaleQuantumRequest();
    });
    Routes.insert(QString("%1 %2").arg("POST").arg("/categories/tree").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIDbsApiRequest(socket, mOAIDbsApiHandler);
            reqObj->getCategoriesTreeRequest();
    });
    Routes.insert(QString("%1 %2").arg("GET").arg("/delivery/services").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIDbsApiRequest(socket, mOAIDbsApiHandler);
            reqObj->getDeliveryServicesRequest();
    });
    Routes.insert(QString("%1 %2").arg("POST").arg("/models").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIDbsApiRequest(socket, mOAIDbsApiHandler);
            reqObj->getModelsRequest();
    });
    Routes.insert(QString("%1 %2").arg("POST").arg("/models/offers").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIDbsApiRequest(socket, mOAIDbsApiHandler);
            reqObj->getModelsOffersRequest();
    });
    Routes.insert(QString("%1 %2").arg("GET").arg("/models").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIDbsApiRequest(socket, mOAIDbsApiHandler);
            reqObj->searchModelsRequest();
    });
    Routes.insert(QString("%1 %2").arg("GET").arg("/regions").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIDbsApiRequest(socket, mOAIDbsApiHandler);
            reqObj->searchRegionsByNameRequest();
    });
    Routes.insert(QString("%1 %2").arg("GET").arg("/delivery/services").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIDeliveryServicesApiRequest(socket, mOAIDeliveryServicesApiHandler);
            reqObj->getDeliveryServicesRequest();
    });
    Routes.insert(QString("%1 %2").arg("POST").arg("/tariffs/calculate").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIExpressApiRequest(socket, mOAIExpressApiHandler);
            reqObj->calculateTariffsRequest();
    });
    Routes.insert(QString("%1 %2").arg("POST").arg("/reports/boost-consolidated/generate").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIExpressApiRequest(socket, mOAIExpressApiHandler);
            reqObj->generateBoostConsolidatedReportRequest();
    });
    Routes.insert(QString("%1 %2").arg("POST").arg("/reports/competitors-position/generate").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIExpressApiRequest(socket, mOAIExpressApiHandler);
            reqObj->generateCompetitorsPositionReportRequest();
    });
    Routes.insert(QString("%1 %2").arg("POST").arg("/reports/goods-feedback/generate").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIExpressApiRequest(socket, mOAIExpressApiHandler);
            reqObj->generateGoodsFeedbackReportRequest();
    });
    Routes.insert(QString("%1 %2").arg("POST").arg("/reports/goods-realization/generate").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIExpressApiRequest(socket, mOAIExpressApiHandler);
            reqObj->generateGoodsRealizationReportRequest();
    });
    Routes.insert(QString("%1 %2").arg("POST").arg("/reports/documents/labels/generate").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIExpressApiRequest(socket, mOAIExpressApiHandler);
            reqObj->generateMassOrderLabelsReportRequest();
    });
    Routes.insert(QString("%1 %2").arg("POST").arg("/reports/prices/generate").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIExpressApiRequest(socket, mOAIExpressApiHandler);
            reqObj->generatePricesReportRequest();
    });
    Routes.insert(QString("%1 %2").arg("POST").arg("/reports/shelf-statistics/generate").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIExpressApiRequest(socket, mOAIExpressApiHandler);
            reqObj->generateShelfsStatisticsReportRequest();
    });
    Routes.insert(QString("%1 %2").arg("POST").arg("/reports/shows-sales/generate").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIExpressApiRequest(socket, mOAIExpressApiHandler);
            reqObj->generateShowsSalesReportRequest();
    });
    Routes.insert(QString("%1 %2").arg("POST").arg("/reports/stocks-on-warehouses/generate").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIExpressApiRequest(socket, mOAIExpressApiHandler);
            reqObj->generateStocksOnWarehousesReportRequest();
    });
    Routes.insert(QString("%1 %2").arg("POST").arg("/reports/united-marketplace-services/generate").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIExpressApiRequest(socket, mOAIExpressApiHandler);
            reqObj->generateUnitedMarketplaceServicesReportRequest();
    });
    Routes.insert(QString("%1 %2").arg("POST").arg("/reports/united-netting/generate").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIExpressApiRequest(socket, mOAIExpressApiHandler);
            reqObj->generateUnitedNettingReportRequest();
    });
    Routes.insert(QString("%1 %2").arg("POST").arg("/reports/united-orders/generate").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIExpressApiRequest(socket, mOAIExpressApiHandler);
            reqObj->generateUnitedOrdersReportRequest();
    });
    Routes.insert(QString("%1 %2").arg("GET").arg("/campaigns").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIExpressApiRequest(socket, mOAIExpressApiHandler);
            reqObj->getCampaignsRequest();
    });
    Routes.insert(QString("%1 %2").arg("POST").arg("/categories/max-sale-quantum").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIExpressApiRequest(socket, mOAIExpressApiHandler);
            reqObj->getCategoriesMaxSaleQuantumRequest();
    });
    Routes.insert(QString("%1 %2").arg("POST").arg("/categories/tree").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIExpressApiRequest(socket, mOAIExpressApiHandler);
            reqObj->getCategoriesTreeRequest();
    });
    Routes.insert(QString("%1 %2").arg("GET").arg("/delivery/services").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIExpressApiRequest(socket, mOAIExpressApiHandler);
            reqObj->getDeliveryServicesRequest();
    });
    Routes.insert(QString("%1 %2").arg("GET").arg("/regions").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIExpressApiRequest(socket, mOAIExpressApiHandler);
            reqObj->searchRegionsByNameRequest();
    });
    Routes.insert(QString("%1 %2").arg("POST").arg("/tariffs/calculate").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIFbsApiRequest(socket, mOAIFbsApiHandler);
            reqObj->calculateTariffsRequest();
    });
    Routes.insert(QString("%1 %2").arg("POST").arg("/reports/boost-consolidated/generate").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIFbsApiRequest(socket, mOAIFbsApiHandler);
            reqObj->generateBoostConsolidatedReportRequest();
    });
    Routes.insert(QString("%1 %2").arg("POST").arg("/reports/competitors-position/generate").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIFbsApiRequest(socket, mOAIFbsApiHandler);
            reqObj->generateCompetitorsPositionReportRequest();
    });
    Routes.insert(QString("%1 %2").arg("POST").arg("/reports/goods-feedback/generate").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIFbsApiRequest(socket, mOAIFbsApiHandler);
            reqObj->generateGoodsFeedbackReportRequest();
    });
    Routes.insert(QString("%1 %2").arg("POST").arg("/reports/goods-realization/generate").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIFbsApiRequest(socket, mOAIFbsApiHandler);
            reqObj->generateGoodsRealizationReportRequest();
    });
    Routes.insert(QString("%1 %2").arg("POST").arg("/reports/documents/labels/generate").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIFbsApiRequest(socket, mOAIFbsApiHandler);
            reqObj->generateMassOrderLabelsReportRequest();
    });
    Routes.insert(QString("%1 %2").arg("POST").arg("/reports/prices/generate").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIFbsApiRequest(socket, mOAIFbsApiHandler);
            reqObj->generatePricesReportRequest();
    });
    Routes.insert(QString("%1 %2").arg("POST").arg("/reports/shelf-statistics/generate").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIFbsApiRequest(socket, mOAIFbsApiHandler);
            reqObj->generateShelfsStatisticsReportRequest();
    });
    Routes.insert(QString("%1 %2").arg("POST").arg("/reports/documents/shipment-list/generate").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIFbsApiRequest(socket, mOAIFbsApiHandler);
            reqObj->generateShipmentListDocumentReportRequest();
    });
    Routes.insert(QString("%1 %2").arg("POST").arg("/reports/shows-sales/generate").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIFbsApiRequest(socket, mOAIFbsApiHandler);
            reqObj->generateShowsSalesReportRequest();
    });
    Routes.insert(QString("%1 %2").arg("POST").arg("/reports/stocks-on-warehouses/generate").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIFbsApiRequest(socket, mOAIFbsApiHandler);
            reqObj->generateStocksOnWarehousesReportRequest();
    });
    Routes.insert(QString("%1 %2").arg("POST").arg("/reports/united-marketplace-services/generate").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIFbsApiRequest(socket, mOAIFbsApiHandler);
            reqObj->generateUnitedMarketplaceServicesReportRequest();
    });
    Routes.insert(QString("%1 %2").arg("POST").arg("/reports/united-netting/generate").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIFbsApiRequest(socket, mOAIFbsApiHandler);
            reqObj->generateUnitedNettingReportRequest();
    });
    Routes.insert(QString("%1 %2").arg("POST").arg("/reports/united-orders/generate").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIFbsApiRequest(socket, mOAIFbsApiHandler);
            reqObj->generateUnitedOrdersReportRequest();
    });
    Routes.insert(QString("%1 %2").arg("GET").arg("/campaigns").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIFbsApiRequest(socket, mOAIFbsApiHandler);
            reqObj->getCampaignsRequest();
    });
    Routes.insert(QString("%1 %2").arg("POST").arg("/categories/max-sale-quantum").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIFbsApiRequest(socket, mOAIFbsApiHandler);
            reqObj->getCategoriesMaxSaleQuantumRequest();
    });
    Routes.insert(QString("%1 %2").arg("POST").arg("/categories/tree").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIFbsApiRequest(socket, mOAIFbsApiHandler);
            reqObj->getCategoriesTreeRequest();
    });
    Routes.insert(QString("%1 %2").arg("GET").arg("/delivery/services").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIFbsApiRequest(socket, mOAIFbsApiHandler);
            reqObj->getDeliveryServicesRequest();
    });
    Routes.insert(QString("%1 %2").arg("GET").arg("/regions").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIFbsApiRequest(socket, mOAIFbsApiHandler);
            reqObj->searchRegionsByNameRequest();
    });
    Routes.insert(QString("%1 %2").arg("POST").arg("/tariffs/calculate").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIFbyApiRequest(socket, mOAIFbyApiHandler);
            reqObj->calculateTariffsRequest();
    });
    Routes.insert(QString("%1 %2").arg("POST").arg("/reports/boost-consolidated/generate").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIFbyApiRequest(socket, mOAIFbyApiHandler);
            reqObj->generateBoostConsolidatedReportRequest();
    });
    Routes.insert(QString("%1 %2").arg("POST").arg("/reports/competitors-position/generate").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIFbyApiRequest(socket, mOAIFbyApiHandler);
            reqObj->generateCompetitorsPositionReportRequest();
    });
    Routes.insert(QString("%1 %2").arg("POST").arg("/reports/goods-feedback/generate").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIFbyApiRequest(socket, mOAIFbyApiHandler);
            reqObj->generateGoodsFeedbackReportRequest();
    });
    Routes.insert(QString("%1 %2").arg("POST").arg("/reports/goods-movement/generate").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIFbyApiRequest(socket, mOAIFbyApiHandler);
            reqObj->generateGoodsMovementReportRequest();
    });
    Routes.insert(QString("%1 %2").arg("POST").arg("/reports/goods-realization/generate").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIFbyApiRequest(socket, mOAIFbyApiHandler);
            reqObj->generateGoodsRealizationReportRequest();
    });
    Routes.insert(QString("%1 %2").arg("POST").arg("/reports/goods-turnover/generate").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIFbyApiRequest(socket, mOAIFbyApiHandler);
            reqObj->generateGoodsTurnoverReportRequest();
    });
    Routes.insert(QString("%1 %2").arg("POST").arg("/reports/prices/generate").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIFbyApiRequest(socket, mOAIFbyApiHandler);
            reqObj->generatePricesReportRequest();
    });
    Routes.insert(QString("%1 %2").arg("POST").arg("/reports/shelf-statistics/generate").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIFbyApiRequest(socket, mOAIFbyApiHandler);
            reqObj->generateShelfsStatisticsReportRequest();
    });
    Routes.insert(QString("%1 %2").arg("POST").arg("/reports/shows-sales/generate").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIFbyApiRequest(socket, mOAIFbyApiHandler);
            reqObj->generateShowsSalesReportRequest();
    });
    Routes.insert(QString("%1 %2").arg("POST").arg("/reports/stocks-on-warehouses/generate").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIFbyApiRequest(socket, mOAIFbyApiHandler);
            reqObj->generateStocksOnWarehousesReportRequest();
    });
    Routes.insert(QString("%1 %2").arg("POST").arg("/reports/united-marketplace-services/generate").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIFbyApiRequest(socket, mOAIFbyApiHandler);
            reqObj->generateUnitedMarketplaceServicesReportRequest();
    });
    Routes.insert(QString("%1 %2").arg("POST").arg("/reports/united-netting/generate").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIFbyApiRequest(socket, mOAIFbyApiHandler);
            reqObj->generateUnitedNettingReportRequest();
    });
    Routes.insert(QString("%1 %2").arg("POST").arg("/reports/united-orders/generate").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIFbyApiRequest(socket, mOAIFbyApiHandler);
            reqObj->generateUnitedOrdersReportRequest();
    });
    Routes.insert(QString("%1 %2").arg("GET").arg("/campaigns").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIFbyApiRequest(socket, mOAIFbyApiHandler);
            reqObj->getCampaignsRequest();
    });
    Routes.insert(QString("%1 %2").arg("POST").arg("/categories/max-sale-quantum").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIFbyApiRequest(socket, mOAIFbyApiHandler);
            reqObj->getCategoriesMaxSaleQuantumRequest();
    });
    Routes.insert(QString("%1 %2").arg("POST").arg("/categories/tree").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIFbyApiRequest(socket, mOAIFbyApiHandler);
            reqObj->getCategoriesTreeRequest();
    });
    Routes.insert(QString("%1 %2").arg("GET").arg("/warehouses").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIFbyApiRequest(socket, mOAIFbyApiHandler);
            reqObj->getFulfillmentWarehousesRequest();
    });
    Routes.insert(QString("%1 %2").arg("GET").arg("/regions").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIFbyApiRequest(socket, mOAIFbyApiHandler);
            reqObj->searchRegionsByNameRequest();
    });
    Routes.insert(QString("%1 %2").arg("POST").arg("/models").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIModelsApiRequest(socket, mOAIModelsApiHandler);
            reqObj->getModelsRequest();
    });
    Routes.insert(QString("%1 %2").arg("POST").arg("/models/offers").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIModelsApiRequest(socket, mOAIModelsApiHandler);
            reqObj->getModelsOffersRequest();
    });
    Routes.insert(QString("%1 %2").arg("GET").arg("/models").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIModelsApiRequest(socket, mOAIModelsApiHandler);
            reqObj->searchModelsRequest();
    });
    Routes.insert(QString("%1 %2").arg("GET").arg("/regions").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIRegionsApiRequest(socket, mOAIRegionsApiHandler);
            reqObj->searchRegionsByNameRequest();
    });
    Routes.insert(QString("%1 %2").arg("POST").arg("/reports/boost-consolidated/generate").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIReportsApiRequest(socket, mOAIReportsApiHandler);
            reqObj->generateBoostConsolidatedReportRequest();
    });
    Routes.insert(QString("%1 %2").arg("POST").arg("/reports/competitors-position/generate").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIReportsApiRequest(socket, mOAIReportsApiHandler);
            reqObj->generateCompetitorsPositionReportRequest();
    });
    Routes.insert(QString("%1 %2").arg("POST").arg("/reports/goods-feedback/generate").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIReportsApiRequest(socket, mOAIReportsApiHandler);
            reqObj->generateGoodsFeedbackReportRequest();
    });
    Routes.insert(QString("%1 %2").arg("POST").arg("/reports/goods-movement/generate").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIReportsApiRequest(socket, mOAIReportsApiHandler);
            reqObj->generateGoodsMovementReportRequest();
    });
    Routes.insert(QString("%1 %2").arg("POST").arg("/reports/goods-realization/generate").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIReportsApiRequest(socket, mOAIReportsApiHandler);
            reqObj->generateGoodsRealizationReportRequest();
    });
    Routes.insert(QString("%1 %2").arg("POST").arg("/reports/goods-turnover/generate").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIReportsApiRequest(socket, mOAIReportsApiHandler);
            reqObj->generateGoodsTurnoverReportRequest();
    });
    Routes.insert(QString("%1 %2").arg("POST").arg("/reports/documents/labels/generate").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIReportsApiRequest(socket, mOAIReportsApiHandler);
            reqObj->generateMassOrderLabelsReportRequest();
    });
    Routes.insert(QString("%1 %2").arg("POST").arg("/reports/prices/generate").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIReportsApiRequest(socket, mOAIReportsApiHandler);
            reqObj->generatePricesReportRequest();
    });
    Routes.insert(QString("%1 %2").arg("POST").arg("/reports/shelf-statistics/generate").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIReportsApiRequest(socket, mOAIReportsApiHandler);
            reqObj->generateShelfsStatisticsReportRequest();
    });
    Routes.insert(QString("%1 %2").arg("POST").arg("/reports/documents/shipment-list/generate").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIReportsApiRequest(socket, mOAIReportsApiHandler);
            reqObj->generateShipmentListDocumentReportRequest();
    });
    Routes.insert(QString("%1 %2").arg("POST").arg("/reports/shows-sales/generate").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIReportsApiRequest(socket, mOAIReportsApiHandler);
            reqObj->generateShowsSalesReportRequest();
    });
    Routes.insert(QString("%1 %2").arg("POST").arg("/reports/stocks-on-warehouses/generate").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIReportsApiRequest(socket, mOAIReportsApiHandler);
            reqObj->generateStocksOnWarehousesReportRequest();
    });
    Routes.insert(QString("%1 %2").arg("POST").arg("/reports/united-marketplace-services/generate").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIReportsApiRequest(socket, mOAIReportsApiHandler);
            reqObj->generateUnitedMarketplaceServicesReportRequest();
    });
    Routes.insert(QString("%1 %2").arg("POST").arg("/reports/united-netting/generate").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIReportsApiRequest(socket, mOAIReportsApiHandler);
            reqObj->generateUnitedNettingReportRequest();
    });
    Routes.insert(QString("%1 %2").arg("POST").arg("/reports/united-orders/generate").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIReportsApiRequest(socket, mOAIReportsApiHandler);
            reqObj->generateUnitedOrdersReportRequest();
    });
    Routes.insert(QString("%1 %2").arg("POST").arg("/tariffs/calculate").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAITariffsApiRequest(socket, mOAITariffsApiHandler);
            reqObj->calculateTariffsRequest();
    });
    Routes.insert(QString("%1 %2").arg("GET").arg("/warehouses").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIWarehousesApiRequest(socket, mOAIWarehousesApiHandler);
            reqObj->getFulfillmentWarehousesRequest();
    });
}

void OAIApiRouter::processRequest(QHttpEngine::Socket *socket){
    if( handleRequest(socket) ){
        return;
    }
    if( handleRequestAndExtractPathParam(socket) ){
        return;
    }
    socket->setStatusCode(QHttpEngine::Socket::NotFound);
    if(socket->isOpen()){
        socket->writeHeaders();
        socket->close();
    }
}

bool OAIApiRouter::handleRequest(QHttpEngine::Socket *socket){
    auto reqPath = QString("%1 %2").arg(fromQHttpEngineMethod(socket->method())).arg(socket->path()).toLower();
    if ( Routes.contains(reqPath) ) {
        Routes.value(reqPath).operator()(socket);
        return true;
    }
    return false;
}

bool OAIApiRouter::handleRequestAndExtractPathParam(QHttpEngine::Socket *socket){
    auto reqPath = QString("%1 %2").arg(fromQHttpEngineMethod(socket->method())).arg(socket->path()).toLower();
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/bids/info").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIBidsApiRequest(socket, mOAIBidsApiHandler);
                reqObj->getBidsInfoForBusinessRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/bids/recommendations").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIBidsApiRequest(socket, mOAIBidsApiHandler);
                reqObj->getBidsRecommendationsRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("PUT").arg("/businesses/{businessId}/bids").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIBidsApiRequest(socket, mOAIBidsApiHandler);
                reqObj->putBidsForBusinessRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("PUT").arg("/campaigns/{campaignId}/bids").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIBidsApiRequest(socket, mOAIBidsApiHandler);
                reqObj->putBidsForCampaignRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/offer-mappings/archive").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIBusinessOfferMappingsApiRequest(socket, mOAIBusinessOfferMappingsApiHandler);
                reqObj->addOffersToArchiveRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/offer-mappings/delete").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIBusinessOfferMappingsApiRequest(socket, mOAIBusinessOfferMappingsApiHandler);
                reqObj->deleteOffersRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/offer-mappings/unarchive").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIBusinessOfferMappingsApiRequest(socket, mOAIBusinessOfferMappingsApiHandler);
                reqObj->deleteOffersFromArchiveRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/offer-mappings").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIBusinessOfferMappingsApiRequest(socket, mOAIBusinessOfferMappingsApiHandler);
                reqObj->getOfferMappingsRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/offer-mappings/suggestions").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIBusinessOfferMappingsApiRequest(socket, mOAIBusinessOfferMappingsApiHandler);
                reqObj->getSuggestedOfferMappingsRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/offer-mappings/update").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIBusinessOfferMappingsApiRequest(socket, mOAIBusinessOfferMappingsApiHandler);
                reqObj->updateOfferMappingsRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/settings").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIBusinessesApiRequest(socket, mOAIBusinessesApiHandler);
                reqObj->getBusinessSettingsRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/campaigns/{campaignId}").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAICampaignsApiRequest(socket, mOAICampaignsApiHandler);
                reqObj->getCampaignRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/campaigns/{campaignId}/logins").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAICampaignsApiRequest(socket, mOAICampaignsApiHandler);
                reqObj->getCampaignLoginsRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/campaigns/{campaignId}/region").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAICampaignsApiRequest(socket, mOAICampaignsApiHandler);
                reqObj->getCampaignRegionRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/campaigns/{campaignId}/settings").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAICampaignsApiRequest(socket, mOAICampaignsApiHandler);
                reqObj->getCampaignSettingsRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/campaigns/by_login/{login}").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString login = match.captured(QString("login").toLower());
                auto reqObj = new OAICampaignsApiRequest(socket, mOAICampaignsApiHandler);
                reqObj->getCampaignsByLoginRequest(login);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/chats/new").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIChatsApiRequest(socket, mOAIChatsApiHandler);
                reqObj->createChatRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/chats/history").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIChatsApiRequest(socket, mOAIChatsApiHandler);
                reqObj->getChatHistoryRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/chats").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIChatsApiRequest(socket, mOAIChatsApiHandler);
                reqObj->getChatsRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/chats/file/send").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIChatsApiRequest(socket, mOAIChatsApiHandler);
                reqObj->sendFileToChatRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/chats/message").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIChatsApiRequest(socket, mOAIChatsApiHandler);
                reqObj->sendMessageToChatRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/category/{categoryId}/parameters").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString categoryId = match.captured(QString("categoryId").toLower());
                auto reqObj = new OAIContentApiRequest(socket, mOAIContentApiHandler);
                reqObj->getCategoryContentParametersRequest(categoryId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/offer-cards").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIContentApiRequest(socket, mOAIContentApiHandler);
                reqObj->getOfferCardsContentStatusRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/offer-cards/update").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIContentApiRequest(socket, mOAIContentApiHandler);
                reqObj->updateOfferContentRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("PUT").arg("/campaigns/{campaignId}/orders/{orderId}/cancellation/accept").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                QString orderId = match.captured(QString("orderId").toLower());
                auto reqObj = new OAIDbsApiRequest(socket, mOAIDbsApiHandler);
                reqObj->acceptOrderCancellationRequest(campaignId, orderId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/campaigns/{campaignId}/hidden-offers").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIDbsApiRequest(socket, mOAIDbsApiHandler);
                reqObj->addHiddenOffersRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/offer-mappings/archive").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIDbsApiRequest(socket, mOAIDbsApiHandler);
                reqObj->addOffersToArchiveRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/price-quarantine/confirm").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIDbsApiRequest(socket, mOAIDbsApiHandler);
                reqObj->confirmBusinessPricesRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/campaigns/{campaignId}/price-quarantine/confirm").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIDbsApiRequest(socket, mOAIDbsApiHandler);
                reqObj->confirmCampaignPricesRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/chats/new").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIDbsApiRequest(socket, mOAIDbsApiHandler);
                reqObj->createChatRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/campaigns/{campaignId}/outlets").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIDbsApiRequest(socket, mOAIDbsApiHandler);
                reqObj->createOutletRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/campaigns/{campaignId}/offers/delete").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIDbsApiRequest(socket, mOAIDbsApiHandler);
                reqObj->deleteCampaignOffersRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/goods-feedback/comments/delete").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIDbsApiRequest(socket, mOAIDbsApiHandler);
                reqObj->deleteGoodsFeedbackCommentRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/campaigns/{campaignId}/hidden-offers/delete").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIDbsApiRequest(socket, mOAIDbsApiHandler);
                reqObj->deleteHiddenOffersRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/offer-mappings/delete").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIDbsApiRequest(socket, mOAIDbsApiHandler);
                reqObj->deleteOffersRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/offer-mappings/unarchive").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIDbsApiRequest(socket, mOAIDbsApiHandler);
                reqObj->deleteOffersFromArchiveRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("DELETE").arg("/campaigns/{campaignId}/outlets/{outletId}").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                QString outletId = match.captured(QString("outletId").toLower());
                auto reqObj = new OAIDbsApiRequest(socket, mOAIDbsApiHandler);
                reqObj->deleteOutletRequest(campaignId, outletId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("DELETE").arg("/campaigns/{campaignId}/outlets/licenses").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIDbsApiRequest(socket, mOAIDbsApiHandler);
                reqObj->deleteOutletLicensesRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/promos/offers/delete").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIDbsApiRequest(socket, mOAIDbsApiHandler);
                reqObj->deletePromoOffersRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/campaigns/{campaignId}/orders/{orderId}/delivery/shipments/{shipmentId}/boxes/{boxId}/label").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                QString orderId = match.captured(QString("orderId").toLower());
                QString shipmentId = match.captured(QString("shipmentId").toLower());
                QString boxId = match.captured(QString("boxId").toLower());
                auto reqObj = new OAIDbsApiRequest(socket, mOAIDbsApiHandler);
                reqObj->generateOrderLabelRequest(campaignId, orderId, shipmentId, boxId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/campaigns/{campaignId}/orders/{orderId}/delivery/labels").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                QString orderId = match.captured(QString("orderId").toLower());
                auto reqObj = new OAIDbsApiRequest(socket, mOAIDbsApiHandler);
                reqObj->generateOrderLabelsRequest(campaignId, orderId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/campaigns/{campaignId}/offers/all").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIDbsApiRequest(socket, mOAIDbsApiHandler);
                reqObj->getAllOffersRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/bids/info").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIDbsApiRequest(socket, mOAIDbsApiHandler);
                reqObj->getBidsInfoForBusinessRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/bids/recommendations").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIDbsApiRequest(socket, mOAIDbsApiHandler);
                reqObj->getBidsRecommendationsRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/price-quarantine").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIDbsApiRequest(socket, mOAIDbsApiHandler);
                reqObj->getBusinessQuarantineOffersRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/settings").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIDbsApiRequest(socket, mOAIDbsApiHandler);
                reqObj->getBusinessSettingsRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/campaigns/{campaignId}").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIDbsApiRequest(socket, mOAIDbsApiHandler);
                reqObj->getCampaignRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/campaigns/{campaignId}/logins").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIDbsApiRequest(socket, mOAIDbsApiHandler);
                reqObj->getCampaignLoginsRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/campaigns/{campaignId}/offers").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIDbsApiRequest(socket, mOAIDbsApiHandler);
                reqObj->getCampaignOffersRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/campaigns/{campaignId}/price-quarantine").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIDbsApiRequest(socket, mOAIDbsApiHandler);
                reqObj->getCampaignQuarantineOffersRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/campaigns/{campaignId}/region").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIDbsApiRequest(socket, mOAIDbsApiHandler);
                reqObj->getCampaignRegionRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/campaigns/{campaignId}/settings").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIDbsApiRequest(socket, mOAIDbsApiHandler);
                reqObj->getCampaignSettingsRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/campaigns/by_login/{login}").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString login = match.captured(QString("login").toLower());
                auto reqObj = new OAIDbsApiRequest(socket, mOAIDbsApiHandler);
                reqObj->getCampaignsByLoginRequest(login);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/category/{categoryId}/parameters").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString categoryId = match.captured(QString("categoryId").toLower());
                auto reqObj = new OAIDbsApiRequest(socket, mOAIDbsApiHandler);
                reqObj->getCategoryContentParametersRequest(categoryId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/chats/history").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIDbsApiRequest(socket, mOAIDbsApiHandler);
                reqObj->getChatHistoryRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/chats").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIDbsApiRequest(socket, mOAIDbsApiHandler);
                reqObj->getChatsRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/campaigns/{campaignId}/feeds/{feedId}").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                QString feedId = match.captured(QString("feedId").toLower());
                auto reqObj = new OAIDbsApiRequest(socket, mOAIDbsApiHandler);
                reqObj->getFeedRequest(campaignId, feedId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/campaigns/{campaignId}/feeds/{feedId}/index-logs").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                QString feedId = match.captured(QString("feedId").toLower());
                auto reqObj = new OAIDbsApiRequest(socket, mOAIDbsApiHandler);
                reqObj->getFeedIndexLogsRequest(campaignId, feedId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/campaigns/{campaignId}/feedback/updates").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIDbsApiRequest(socket, mOAIDbsApiHandler);
                reqObj->getFeedbackAndCommentUpdatesRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/campaigns/{campaignId}/feeds").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIDbsApiRequest(socket, mOAIDbsApiHandler);
                reqObj->getFeedsRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/goods-feedback/comments").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIDbsApiRequest(socket, mOAIDbsApiHandler);
                reqObj->getGoodsFeedbackCommentsRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/goods-feedback").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIDbsApiRequest(socket, mOAIDbsApiHandler);
                reqObj->getGoodsFeedbacksRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/campaigns/{campaignId}/stats/skus").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIDbsApiRequest(socket, mOAIDbsApiHandler);
                reqObj->getGoodsStatsRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/campaigns/{campaignId}/hidden-offers").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIDbsApiRequest(socket, mOAIDbsApiHandler);
                reqObj->getHiddenOffersRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/models/{modelId}").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString modelId = match.captured(QString("modelId").toLower());
                auto reqObj = new OAIDbsApiRequest(socket, mOAIDbsApiHandler);
                reqObj->getModelRequest(modelId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/models/{modelId}/offers").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString modelId = match.captured(QString("modelId").toLower());
                auto reqObj = new OAIDbsApiRequest(socket, mOAIDbsApiHandler);
                reqObj->getModelOffersRequest(modelId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/offer-cards").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIDbsApiRequest(socket, mOAIDbsApiHandler);
                reqObj->getOfferCardsContentStatusRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/campaigns/{campaignId}/offer-mapping-entries").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIDbsApiRequest(socket, mOAIDbsApiHandler);
                reqObj->getOfferMappingEntriesRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/offer-mappings").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIDbsApiRequest(socket, mOAIDbsApiHandler);
                reqObj->getOfferMappingsRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/offers/recommendations").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIDbsApiRequest(socket, mOAIDbsApiHandler);
                reqObj->getOfferRecommendationsRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/campaigns/{campaignId}/offers").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIDbsApiRequest(socket, mOAIDbsApiHandler);
                reqObj->getOffersRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/campaigns/{campaignId}/orders/{orderId}").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                QString orderId = match.captured(QString("orderId").toLower());
                auto reqObj = new OAIDbsApiRequest(socket, mOAIDbsApiHandler);
                reqObj->getOrderRequest(campaignId, orderId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/campaigns/{campaignId}/orders/{orderId}/business-buyer").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                QString orderId = match.captured(QString("orderId").toLower());
                auto reqObj = new OAIDbsApiRequest(socket, mOAIDbsApiHandler);
                reqObj->getOrderBusinessBuyerInfoRequest(campaignId, orderId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/campaigns/{campaignId}/orders/{orderId}/documents").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                QString orderId = match.captured(QString("orderId").toLower());
                auto reqObj = new OAIDbsApiRequest(socket, mOAIDbsApiHandler);
                reqObj->getOrderBusinessDocumentsInfoRequest(campaignId, orderId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/campaigns/{campaignId}/orders/{orderId}/buyer").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                QString orderId = match.captured(QString("orderId").toLower());
                auto reqObj = new OAIDbsApiRequest(socket, mOAIDbsApiHandler);
                reqObj->getOrderBuyerInfoRequest(campaignId, orderId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/campaigns/{campaignId}/orders/{orderId}/delivery/labels/data").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                QString orderId = match.captured(QString("orderId").toLower());
                auto reqObj = new OAIDbsApiRequest(socket, mOAIDbsApiHandler);
                reqObj->getOrderLabelsDataRequest(campaignId, orderId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/campaigns/{campaignId}/orders").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIDbsApiRequest(socket, mOAIDbsApiHandler);
                reqObj->getOrdersRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/campaigns/{campaignId}/stats/orders").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIDbsApiRequest(socket, mOAIDbsApiHandler);
                reqObj->getOrdersStatsRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/campaigns/{campaignId}/outlets/{outletId}").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                QString outletId = match.captured(QString("outletId").toLower());
                auto reqObj = new OAIDbsApiRequest(socket, mOAIDbsApiHandler);
                reqObj->getOutletRequest(campaignId, outletId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/campaigns/{campaignId}/outlets/licenses").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIDbsApiRequest(socket, mOAIDbsApiHandler);
                reqObj->getOutletLicensesRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/campaigns/{campaignId}/outlets").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIDbsApiRequest(socket, mOAIDbsApiHandler);
                reqObj->getOutletsRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/campaigns/{campaignId}/offer-prices").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIDbsApiRequest(socket, mOAIDbsApiHandler);
                reqObj->getPricesRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/campaigns/{campaignId}/offer-prices").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIDbsApiRequest(socket, mOAIDbsApiHandler);
                reqObj->getPricesByOfferIdsRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/promos/offers").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIDbsApiRequest(socket, mOAIDbsApiHandler);
                reqObj->getPromoOffersRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/promos").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIDbsApiRequest(socket, mOAIDbsApiHandler);
                reqObj->getPromosRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/campaigns/{campaignId}/ratings/quality/details").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIDbsApiRequest(socket, mOAIDbsApiHandler);
                reqObj->getQualityRatingDetailsRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/ratings/quality").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIDbsApiRequest(socket, mOAIDbsApiHandler);
                reqObj->getQualityRatingsRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/reports/info/{reportId}").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString reportId = match.captured(QString("reportId").toLower());
                auto reqObj = new OAIDbsApiRequest(socket, mOAIDbsApiHandler);
                reqObj->getReportInfoRequest(reportId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/campaigns/{campaignId}/orders/{orderId}/returns/{returnId}").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                QString orderId = match.captured(QString("orderId").toLower());
                QString returnId = match.captured(QString("returnId").toLower());
                auto reqObj = new OAIDbsApiRequest(socket, mOAIDbsApiHandler);
                reqObj->getReturnRequest(campaignId, orderId, returnId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/campaigns/{campaignId}/orders/{orderId}/returns/{returnId}/application").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                QString orderId = match.captured(QString("orderId").toLower());
                QString returnId = match.captured(QString("returnId").toLower());
                auto reqObj = new OAIDbsApiRequest(socket, mOAIDbsApiHandler);
                reqObj->getReturnApplicationRequest(campaignId, orderId, returnId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/campaigns/{campaignId}/orders/{orderId}/returns/{returnId}/decision/{itemId}/image/{imageHash}").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                QString orderId = match.captured(QString("orderId").toLower());
                QString returnId = match.captured(QString("returnId").toLower());
                QString itemId = match.captured(QString("itemId").toLower());
                QString imageHash = match.captured(QString("imageHash").toLower());
                auto reqObj = new OAIDbsApiRequest(socket, mOAIDbsApiHandler);
                reqObj->getReturnPhotoRequest(campaignId, orderId, returnId, itemId, imageHash);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/campaigns/{campaignId}/returns").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIDbsApiRequest(socket, mOAIDbsApiHandler);
                reqObj->getReturnsRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/campaigns/{campaignId}/offers/stocks").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIDbsApiRequest(socket, mOAIDbsApiHandler);
                reqObj->getStocksRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/campaigns/{campaignId}/offer-mapping-entries/suggestions").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIDbsApiRequest(socket, mOAIDbsApiHandler);
                reqObj->getSuggestedOfferMappingEntriesRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/offer-mappings/suggestions").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIDbsApiRequest(socket, mOAIDbsApiHandler);
                reqObj->getSuggestedOfferMappingsRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/campaigns/{campaignId}/offer-prices/suggestions").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIDbsApiRequest(socket, mOAIDbsApiHandler);
                reqObj->getSuggestedPricesRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/businesses/{businessId}/warehouses").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIDbsApiRequest(socket, mOAIDbsApiHandler);
                reqObj->getWarehousesRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/campaigns/{campaignId}/orders/{orderId}/deliverDigitalGoods").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                QString orderId = match.captured(QString("orderId").toLower());
                auto reqObj = new OAIDbsApiRequest(socket, mOAIDbsApiHandler);
                reqObj->provideOrderDigitalCodesRequest(campaignId, orderId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("PUT").arg("/campaigns/{campaignId}/orders/{orderId}/identifiers").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                QString orderId = match.captured(QString("orderId").toLower());
                auto reqObj = new OAIDbsApiRequest(socket, mOAIDbsApiHandler);
                reqObj->provideOrderItemIdentifiersRequest(campaignId, orderId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("PUT").arg("/businesses/{businessId}/bids").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIDbsApiRequest(socket, mOAIDbsApiHandler);
                reqObj->putBidsForBusinessRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("PUT").arg("/campaigns/{campaignId}/bids").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIDbsApiRequest(socket, mOAIDbsApiHandler);
                reqObj->putBidsForCampaignRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/campaigns/{campaignId}/feeds/{feedId}/refresh").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                QString feedId = match.captured(QString("feedId").toLower());
                auto reqObj = new OAIDbsApiRequest(socket, mOAIDbsApiHandler);
                reqObj->refreshFeedRequest(campaignId, feedId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/regions/{regionId}/children").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString regionId = match.captured(QString("regionId").toLower());
                auto reqObj = new OAIDbsApiRequest(socket, mOAIDbsApiHandler);
                reqObj->searchRegionChildrenRequest(regionId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/regions/{regionId}").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString regionId = match.captured(QString("regionId").toLower());
                auto reqObj = new OAIDbsApiRequest(socket, mOAIDbsApiHandler);
                reqObj->searchRegionsByIdRequest(regionId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/chats/file/send").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIDbsApiRequest(socket, mOAIDbsApiHandler);
                reqObj->sendFileToChatRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/chats/message").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIDbsApiRequest(socket, mOAIDbsApiHandler);
                reqObj->sendMessageToChatRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/campaigns/{campaignId}/feeds/{feedId}/params").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                QString feedId = match.captured(QString("feedId").toLower());
                auto reqObj = new OAIDbsApiRequest(socket, mOAIDbsApiHandler);
                reqObj->setFeedParamsRequest(campaignId, feedId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("PUT").arg("/campaigns/{campaignId}/orders/{orderId}/boxes").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                QString orderId = match.captured(QString("orderId").toLower());
                auto reqObj = new OAIDbsApiRequest(socket, mOAIDbsApiHandler);
                reqObj->setOrderBoxLayoutRequest(campaignId, orderId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("PUT").arg("/campaigns/{campaignId}/orders/{orderId}/delivery/date").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                QString orderId = match.captured(QString("orderId").toLower());
                auto reqObj = new OAIDbsApiRequest(socket, mOAIDbsApiHandler);
                reqObj->setOrderDeliveryDateRequest(campaignId, orderId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/campaigns/{campaignId}/orders/{orderId}/delivery/track").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                QString orderId = match.captured(QString("orderId").toLower());
                auto reqObj = new OAIDbsApiRequest(socket, mOAIDbsApiHandler);
                reqObj->setOrderDeliveryTrackCodeRequest(campaignId, orderId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("PUT").arg("/campaigns/{campaignId}/orders/{orderId}/delivery/shipments/{shipmentId}/boxes").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                QString orderId = match.captured(QString("orderId").toLower());
                QString shipmentId = match.captured(QString("shipmentId").toLower());
                auto reqObj = new OAIDbsApiRequest(socket, mOAIDbsApiHandler);
                reqObj->setOrderShipmentBoxesRequest(campaignId, orderId, shipmentId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/campaigns/{campaignId}/orders/{orderId}/returns/{returnId}/decision").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                QString orderId = match.captured(QString("orderId").toLower());
                QString returnId = match.captured(QString("returnId").toLower());
                auto reqObj = new OAIDbsApiRequest(socket, mOAIDbsApiHandler);
                reqObj->setReturnDecisionRequest(campaignId, orderId, returnId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/goods-feedback/skip-reaction").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIDbsApiRequest(socket, mOAIDbsApiHandler);
                reqObj->skipGoodsFeedbacksReactionRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/campaigns/{campaignId}/orders/{orderId}/returns/{returnId}/decision/submit").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                QString orderId = match.captured(QString("orderId").toLower());
                QString returnId = match.captured(QString("returnId").toLower());
                auto reqObj = new OAIDbsApiRequest(socket, mOAIDbsApiHandler);
                reqObj->submitReturnDecisionRequest(campaignId, orderId, returnId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/offer-prices/updates").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIDbsApiRequest(socket, mOAIDbsApiHandler);
                reqObj->updateBusinessPricesRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/campaigns/{campaignId}/offers/update").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIDbsApiRequest(socket, mOAIDbsApiHandler);
                reqObj->updateCampaignOffersRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/goods-feedback/comments/update").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIDbsApiRequest(socket, mOAIDbsApiHandler);
                reqObj->updateGoodsFeedbackCommentRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/offer-cards/update").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIDbsApiRequest(socket, mOAIDbsApiHandler);
                reqObj->updateOfferContentRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/campaigns/{campaignId}/offer-mapping-entries/updates").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIDbsApiRequest(socket, mOAIDbsApiHandler);
                reqObj->updateOfferMappingEntriesRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/offer-mappings/update").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIDbsApiRequest(socket, mOAIDbsApiHandler);
                reqObj->updateOfferMappingsRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("PUT").arg("/campaigns/{campaignId}/orders/{orderId}/items").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                QString orderId = match.captured(QString("orderId").toLower());
                auto reqObj = new OAIDbsApiRequest(socket, mOAIDbsApiHandler);
                reqObj->updateOrderItemsRequest(campaignId, orderId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("PUT").arg("/campaigns/{campaignId}/orders/{orderId}/status").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                QString orderId = match.captured(QString("orderId").toLower());
                auto reqObj = new OAIDbsApiRequest(socket, mOAIDbsApiHandler);
                reqObj->updateOrderStatusRequest(campaignId, orderId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/campaigns/{campaignId}/orders/status-update").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIDbsApiRequest(socket, mOAIDbsApiHandler);
                reqObj->updateOrderStatusesRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("PUT").arg("/campaigns/{campaignId}/orders/{orderId}/delivery/storage-limit").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                QString orderId = match.captured(QString("orderId").toLower());
                auto reqObj = new OAIDbsApiRequest(socket, mOAIDbsApiHandler);
                reqObj->updateOrderStorageLimitRequest(campaignId, orderId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("PUT").arg("/campaigns/{campaignId}/outlets/{outletId}").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                QString outletId = match.captured(QString("outletId").toLower());
                auto reqObj = new OAIDbsApiRequest(socket, mOAIDbsApiHandler);
                reqObj->updateOutletRequest(campaignId, outletId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/campaigns/{campaignId}/outlets/licenses").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIDbsApiRequest(socket, mOAIDbsApiHandler);
                reqObj->updateOutletLicensesRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/campaigns/{campaignId}/offer-prices/updates").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIDbsApiRequest(socket, mOAIDbsApiHandler);
                reqObj->updatePricesRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/promos/offers/update").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIDbsApiRequest(socket, mOAIDbsApiHandler);
                reqObj->updatePromoOffersRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("PUT").arg("/campaigns/{campaignId}/offers/stocks").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIDbsApiRequest(socket, mOAIDbsApiHandler);
                reqObj->updateStocksRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/campaigns/{campaignId}/hidden-offers").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIExpressApiRequest(socket, mOAIExpressApiHandler);
                reqObj->addHiddenOffersRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/offer-mappings/archive").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIExpressApiRequest(socket, mOAIExpressApiHandler);
                reqObj->addOffersToArchiveRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/price-quarantine/confirm").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIExpressApiRequest(socket, mOAIExpressApiHandler);
                reqObj->confirmBusinessPricesRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/campaigns/{campaignId}/price-quarantine/confirm").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIExpressApiRequest(socket, mOAIExpressApiHandler);
                reqObj->confirmCampaignPricesRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/chats/new").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIExpressApiRequest(socket, mOAIExpressApiHandler);
                reqObj->createChatRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/campaigns/{campaignId}/offers/delete").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIExpressApiRequest(socket, mOAIExpressApiHandler);
                reqObj->deleteCampaignOffersRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/goods-feedback/comments/delete").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIExpressApiRequest(socket, mOAIExpressApiHandler);
                reqObj->deleteGoodsFeedbackCommentRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/campaigns/{campaignId}/hidden-offers/delete").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIExpressApiRequest(socket, mOAIExpressApiHandler);
                reqObj->deleteHiddenOffersRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/offer-mappings/delete").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIExpressApiRequest(socket, mOAIExpressApiHandler);
                reqObj->deleteOffersRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/offer-mappings/unarchive").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIExpressApiRequest(socket, mOAIExpressApiHandler);
                reqObj->deleteOffersFromArchiveRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/promos/offers/delete").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIExpressApiRequest(socket, mOAIExpressApiHandler);
                reqObj->deletePromoOffersRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/campaigns/{campaignId}/orders/{orderId}/delivery/shipments/{shipmentId}/boxes/{boxId}/label").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                QString orderId = match.captured(QString("orderId").toLower());
                QString shipmentId = match.captured(QString("shipmentId").toLower());
                QString boxId = match.captured(QString("boxId").toLower());
                auto reqObj = new OAIExpressApiRequest(socket, mOAIExpressApiHandler);
                reqObj->generateOrderLabelRequest(campaignId, orderId, shipmentId, boxId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/campaigns/{campaignId}/orders/{orderId}/delivery/labels").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                QString orderId = match.captured(QString("orderId").toLower());
                auto reqObj = new OAIExpressApiRequest(socket, mOAIExpressApiHandler);
                reqObj->generateOrderLabelsRequest(campaignId, orderId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/campaigns/{campaignId}/offers/all").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIExpressApiRequest(socket, mOAIExpressApiHandler);
                reqObj->getAllOffersRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/bids/info").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIExpressApiRequest(socket, mOAIExpressApiHandler);
                reqObj->getBidsInfoForBusinessRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/bids/recommendations").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIExpressApiRequest(socket, mOAIExpressApiHandler);
                reqObj->getBidsRecommendationsRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/price-quarantine").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIExpressApiRequest(socket, mOAIExpressApiHandler);
                reqObj->getBusinessQuarantineOffersRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/settings").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIExpressApiRequest(socket, mOAIExpressApiHandler);
                reqObj->getBusinessSettingsRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/campaigns/{campaignId}").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIExpressApiRequest(socket, mOAIExpressApiHandler);
                reqObj->getCampaignRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/campaigns/{campaignId}/logins").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIExpressApiRequest(socket, mOAIExpressApiHandler);
                reqObj->getCampaignLoginsRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/campaigns/{campaignId}/offers").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIExpressApiRequest(socket, mOAIExpressApiHandler);
                reqObj->getCampaignOffersRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/campaigns/{campaignId}/price-quarantine").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIExpressApiRequest(socket, mOAIExpressApiHandler);
                reqObj->getCampaignQuarantineOffersRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/campaigns/{campaignId}/region").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIExpressApiRequest(socket, mOAIExpressApiHandler);
                reqObj->getCampaignRegionRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/campaigns/{campaignId}/settings").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIExpressApiRequest(socket, mOAIExpressApiHandler);
                reqObj->getCampaignSettingsRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/campaigns/by_login/{login}").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString login = match.captured(QString("login").toLower());
                auto reqObj = new OAIExpressApiRequest(socket, mOAIExpressApiHandler);
                reqObj->getCampaignsByLoginRequest(login);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/category/{categoryId}/parameters").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString categoryId = match.captured(QString("categoryId").toLower());
                auto reqObj = new OAIExpressApiRequest(socket, mOAIExpressApiHandler);
                reqObj->getCategoryContentParametersRequest(categoryId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/chats/history").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIExpressApiRequest(socket, mOAIExpressApiHandler);
                reqObj->getChatHistoryRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/chats").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIExpressApiRequest(socket, mOAIExpressApiHandler);
                reqObj->getChatsRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/campaigns/{campaignId}/feeds/{feedId}").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                QString feedId = match.captured(QString("feedId").toLower());
                auto reqObj = new OAIExpressApiRequest(socket, mOAIExpressApiHandler);
                reqObj->getFeedRequest(campaignId, feedId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/campaigns/{campaignId}/feeds/{feedId}/index-logs").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                QString feedId = match.captured(QString("feedId").toLower());
                auto reqObj = new OAIExpressApiRequest(socket, mOAIExpressApiHandler);
                reqObj->getFeedIndexLogsRequest(campaignId, feedId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/campaigns/{campaignId}/feedback/updates").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIExpressApiRequest(socket, mOAIExpressApiHandler);
                reqObj->getFeedbackAndCommentUpdatesRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/campaigns/{campaignId}/feeds").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIExpressApiRequest(socket, mOAIExpressApiHandler);
                reqObj->getFeedsRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/goods-feedback/comments").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIExpressApiRequest(socket, mOAIExpressApiHandler);
                reqObj->getGoodsFeedbackCommentsRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/goods-feedback").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIExpressApiRequest(socket, mOAIExpressApiHandler);
                reqObj->getGoodsFeedbacksRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/campaigns/{campaignId}/stats/skus").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIExpressApiRequest(socket, mOAIExpressApiHandler);
                reqObj->getGoodsStatsRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/campaigns/{campaignId}/hidden-offers").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIExpressApiRequest(socket, mOAIExpressApiHandler);
                reqObj->getHiddenOffersRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/offer-cards").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIExpressApiRequest(socket, mOAIExpressApiHandler);
                reqObj->getOfferCardsContentStatusRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/campaigns/{campaignId}/offer-mapping-entries").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIExpressApiRequest(socket, mOAIExpressApiHandler);
                reqObj->getOfferMappingEntriesRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/offer-mappings").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIExpressApiRequest(socket, mOAIExpressApiHandler);
                reqObj->getOfferMappingsRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/offers/recommendations").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIExpressApiRequest(socket, mOAIExpressApiHandler);
                reqObj->getOfferRecommendationsRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/campaigns/{campaignId}/offers").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIExpressApiRequest(socket, mOAIExpressApiHandler);
                reqObj->getOffersRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/campaigns/{campaignId}/orders/{orderId}").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                QString orderId = match.captured(QString("orderId").toLower());
                auto reqObj = new OAIExpressApiRequest(socket, mOAIExpressApiHandler);
                reqObj->getOrderRequest(campaignId, orderId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/campaigns/{campaignId}/orders/{orderId}/business-buyer").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                QString orderId = match.captured(QString("orderId").toLower());
                auto reqObj = new OAIExpressApiRequest(socket, mOAIExpressApiHandler);
                reqObj->getOrderBusinessBuyerInfoRequest(campaignId, orderId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/campaigns/{campaignId}/orders/{orderId}/documents").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                QString orderId = match.captured(QString("orderId").toLower());
                auto reqObj = new OAIExpressApiRequest(socket, mOAIExpressApiHandler);
                reqObj->getOrderBusinessDocumentsInfoRequest(campaignId, orderId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/campaigns/{campaignId}/orders/{orderId}/delivery/labels/data").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                QString orderId = match.captured(QString("orderId").toLower());
                auto reqObj = new OAIExpressApiRequest(socket, mOAIExpressApiHandler);
                reqObj->getOrderLabelsDataRequest(campaignId, orderId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/campaigns/{campaignId}/orders").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIExpressApiRequest(socket, mOAIExpressApiHandler);
                reqObj->getOrdersRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/campaigns/{campaignId}/stats/orders").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIExpressApiRequest(socket, mOAIExpressApiHandler);
                reqObj->getOrdersStatsRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/campaigns/{campaignId}/offer-prices").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIExpressApiRequest(socket, mOAIExpressApiHandler);
                reqObj->getPricesRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/campaigns/{campaignId}/offer-prices").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIExpressApiRequest(socket, mOAIExpressApiHandler);
                reqObj->getPricesByOfferIdsRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/promos/offers").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIExpressApiRequest(socket, mOAIExpressApiHandler);
                reqObj->getPromoOffersRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/promos").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIExpressApiRequest(socket, mOAIExpressApiHandler);
                reqObj->getPromosRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/campaigns/{campaignId}/ratings/quality/details").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIExpressApiRequest(socket, mOAIExpressApiHandler);
                reqObj->getQualityRatingDetailsRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/ratings/quality").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIExpressApiRequest(socket, mOAIExpressApiHandler);
                reqObj->getQualityRatingsRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/reports/info/{reportId}").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString reportId = match.captured(QString("reportId").toLower());
                auto reqObj = new OAIExpressApiRequest(socket, mOAIExpressApiHandler);
                reqObj->getReportInfoRequest(reportId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/campaigns/{campaignId}/orders/{orderId}/returns/{returnId}").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                QString orderId = match.captured(QString("orderId").toLower());
                QString returnId = match.captured(QString("returnId").toLower());
                auto reqObj = new OAIExpressApiRequest(socket, mOAIExpressApiHandler);
                reqObj->getReturnRequest(campaignId, orderId, returnId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/campaigns/{campaignId}/orders/{orderId}/returns/{returnId}/application").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                QString orderId = match.captured(QString("orderId").toLower());
                QString returnId = match.captured(QString("returnId").toLower());
                auto reqObj = new OAIExpressApiRequest(socket, mOAIExpressApiHandler);
                reqObj->getReturnApplicationRequest(campaignId, orderId, returnId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/campaigns/{campaignId}/orders/{orderId}/returns/{returnId}/decision/{itemId}/image/{imageHash}").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                QString orderId = match.captured(QString("orderId").toLower());
                QString returnId = match.captured(QString("returnId").toLower());
                QString itemId = match.captured(QString("itemId").toLower());
                QString imageHash = match.captured(QString("imageHash").toLower());
                auto reqObj = new OAIExpressApiRequest(socket, mOAIExpressApiHandler);
                reqObj->getReturnPhotoRequest(campaignId, orderId, returnId, itemId, imageHash);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/campaigns/{campaignId}/returns").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIExpressApiRequest(socket, mOAIExpressApiHandler);
                reqObj->getReturnsRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/campaigns/{campaignId}/offers/stocks").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIExpressApiRequest(socket, mOAIExpressApiHandler);
                reqObj->getStocksRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/campaigns/{campaignId}/offer-mapping-entries/suggestions").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIExpressApiRequest(socket, mOAIExpressApiHandler);
                reqObj->getSuggestedOfferMappingEntriesRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/offer-mappings/suggestions").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIExpressApiRequest(socket, mOAIExpressApiHandler);
                reqObj->getSuggestedOfferMappingsRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/campaigns/{campaignId}/offer-prices/suggestions").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIExpressApiRequest(socket, mOAIExpressApiHandler);
                reqObj->getSuggestedPricesRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/businesses/{businessId}/warehouses").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIExpressApiRequest(socket, mOAIExpressApiHandler);
                reqObj->getWarehousesRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("PUT").arg("/campaigns/{campaignId}/orders/{orderId}/identifiers").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                QString orderId = match.captured(QString("orderId").toLower());
                auto reqObj = new OAIExpressApiRequest(socket, mOAIExpressApiHandler);
                reqObj->provideOrderItemIdentifiersRequest(campaignId, orderId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("PUT").arg("/businesses/{businessId}/bids").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIExpressApiRequest(socket, mOAIExpressApiHandler);
                reqObj->putBidsForBusinessRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("PUT").arg("/campaigns/{campaignId}/bids").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIExpressApiRequest(socket, mOAIExpressApiHandler);
                reqObj->putBidsForCampaignRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/campaigns/{campaignId}/feeds/{feedId}/refresh").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                QString feedId = match.captured(QString("feedId").toLower());
                auto reqObj = new OAIExpressApiRequest(socket, mOAIExpressApiHandler);
                reqObj->refreshFeedRequest(campaignId, feedId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/regions/{regionId}/children").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString regionId = match.captured(QString("regionId").toLower());
                auto reqObj = new OAIExpressApiRequest(socket, mOAIExpressApiHandler);
                reqObj->searchRegionChildrenRequest(regionId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/regions/{regionId}").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString regionId = match.captured(QString("regionId").toLower());
                auto reqObj = new OAIExpressApiRequest(socket, mOAIExpressApiHandler);
                reqObj->searchRegionsByIdRequest(regionId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/chats/file/send").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIExpressApiRequest(socket, mOAIExpressApiHandler);
                reqObj->sendFileToChatRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/chats/message").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIExpressApiRequest(socket, mOAIExpressApiHandler);
                reqObj->sendMessageToChatRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/campaigns/{campaignId}/feeds/{feedId}/params").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                QString feedId = match.captured(QString("feedId").toLower());
                auto reqObj = new OAIExpressApiRequest(socket, mOAIExpressApiHandler);
                reqObj->setFeedParamsRequest(campaignId, feedId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("PUT").arg("/campaigns/{campaignId}/orders/{orderId}/boxes").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                QString orderId = match.captured(QString("orderId").toLower());
                auto reqObj = new OAIExpressApiRequest(socket, mOAIExpressApiHandler);
                reqObj->setOrderBoxLayoutRequest(campaignId, orderId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("PUT").arg("/campaigns/{campaignId}/orders/{orderId}/delivery/shipments/{shipmentId}/boxes").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                QString orderId = match.captured(QString("orderId").toLower());
                QString shipmentId = match.captured(QString("shipmentId").toLower());
                auto reqObj = new OAIExpressApiRequest(socket, mOAIExpressApiHandler);
                reqObj->setOrderShipmentBoxesRequest(campaignId, orderId, shipmentId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/goods-feedback/skip-reaction").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIExpressApiRequest(socket, mOAIExpressApiHandler);
                reqObj->skipGoodsFeedbacksReactionRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/offer-prices/updates").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIExpressApiRequest(socket, mOAIExpressApiHandler);
                reqObj->updateBusinessPricesRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/campaigns/{campaignId}/offers/update").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIExpressApiRequest(socket, mOAIExpressApiHandler);
                reqObj->updateCampaignOffersRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/goods-feedback/comments/update").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIExpressApiRequest(socket, mOAIExpressApiHandler);
                reqObj->updateGoodsFeedbackCommentRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/offer-cards/update").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIExpressApiRequest(socket, mOAIExpressApiHandler);
                reqObj->updateOfferContentRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/campaigns/{campaignId}/offer-mapping-entries/updates").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIExpressApiRequest(socket, mOAIExpressApiHandler);
                reqObj->updateOfferMappingEntriesRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/offer-mappings/update").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIExpressApiRequest(socket, mOAIExpressApiHandler);
                reqObj->updateOfferMappingsRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("PUT").arg("/campaigns/{campaignId}/orders/{orderId}/items").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                QString orderId = match.captured(QString("orderId").toLower());
                auto reqObj = new OAIExpressApiRequest(socket, mOAIExpressApiHandler);
                reqObj->updateOrderItemsRequest(campaignId, orderId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("PUT").arg("/campaigns/{campaignId}/orders/{orderId}/status").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                QString orderId = match.captured(QString("orderId").toLower());
                auto reqObj = new OAIExpressApiRequest(socket, mOAIExpressApiHandler);
                reqObj->updateOrderStatusRequest(campaignId, orderId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/campaigns/{campaignId}/orders/status-update").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIExpressApiRequest(socket, mOAIExpressApiHandler);
                reqObj->updateOrderStatusesRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/campaigns/{campaignId}/offer-prices/updates").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIExpressApiRequest(socket, mOAIExpressApiHandler);
                reqObj->updatePricesRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/promos/offers/update").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIExpressApiRequest(socket, mOAIExpressApiHandler);
                reqObj->updatePromoOffersRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("PUT").arg("/campaigns/{campaignId}/offers/stocks").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIExpressApiRequest(socket, mOAIExpressApiHandler);
                reqObj->updateStocksRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("PUT").arg("/campaigns/{campaignId}/orders/{orderId}/verifyEac").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                QString orderId = match.captured(QString("orderId").toLower());
                auto reqObj = new OAIExpressApiRequest(socket, mOAIExpressApiHandler);
                reqObj->verifyOrderEacRequest(campaignId, orderId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/campaigns/{campaignId}/hidden-offers").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIFbsApiRequest(socket, mOAIFbsApiHandler);
                reqObj->addHiddenOffersRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/offer-mappings/archive").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIFbsApiRequest(socket, mOAIFbsApiHandler);
                reqObj->addOffersToArchiveRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/price-quarantine/confirm").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIFbsApiRequest(socket, mOAIFbsApiHandler);
                reqObj->confirmBusinessPricesRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/campaigns/{campaignId}/price-quarantine/confirm").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIFbsApiRequest(socket, mOAIFbsApiHandler);
                reqObj->confirmCampaignPricesRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/campaigns/{campaignId}/first-mile/shipments/{shipmentId}/confirm").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                QString shipmentId = match.captured(QString("shipmentId").toLower());
                auto reqObj = new OAIFbsApiRequest(socket, mOAIFbsApiHandler);
                reqObj->confirmShipmentRequest(campaignId, shipmentId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/chats/new").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIFbsApiRequest(socket, mOAIFbsApiHandler);
                reqObj->createChatRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/campaigns/{campaignId}/offers/delete").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIFbsApiRequest(socket, mOAIFbsApiHandler);
                reqObj->deleteCampaignOffersRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/goods-feedback/comments/delete").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIFbsApiRequest(socket, mOAIFbsApiHandler);
                reqObj->deleteGoodsFeedbackCommentRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/campaigns/{campaignId}/hidden-offers/delete").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIFbsApiRequest(socket, mOAIFbsApiHandler);
                reqObj->deleteHiddenOffersRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/offer-mappings/delete").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIFbsApiRequest(socket, mOAIFbsApiHandler);
                reqObj->deleteOffersRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/offer-mappings/unarchive").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIFbsApiRequest(socket, mOAIFbsApiHandler);
                reqObj->deleteOffersFromArchiveRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/promos/offers/delete").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIFbsApiRequest(socket, mOAIFbsApiHandler);
                reqObj->deletePromoOffersRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/campaigns/{campaignId}/first-mile/shipments/{shipmentId}/act").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                QString shipmentId = match.captured(QString("shipmentId").toLower());
                auto reqObj = new OAIFbsApiRequest(socket, mOAIFbsApiHandler);
                reqObj->downloadShipmentActRequest(campaignId, shipmentId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/campaigns/{campaignId}/first-mile/shipments/{shipmentId}/discrepancy-act").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                QString shipmentId = match.captured(QString("shipmentId").toLower());
                auto reqObj = new OAIFbsApiRequest(socket, mOAIFbsApiHandler);
                reqObj->downloadShipmentDiscrepancyActRequest(campaignId, shipmentId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/campaigns/{campaignId}/first-mile/shipments/{shipmentId}/inbound-act").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                QString shipmentId = match.captured(QString("shipmentId").toLower());
                auto reqObj = new OAIFbsApiRequest(socket, mOAIFbsApiHandler);
                reqObj->downloadShipmentInboundActRequest(campaignId, shipmentId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/campaigns/{campaignId}/first-mile/shipments/{shipmentId}/pallet/labels").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                QString shipmentId = match.captured(QString("shipmentId").toLower());
                auto reqObj = new OAIFbsApiRequest(socket, mOAIFbsApiHandler);
                reqObj->downloadShipmentPalletLabelsRequest(campaignId, shipmentId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/campaigns/{campaignId}/shipments/reception-transfer-act").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIFbsApiRequest(socket, mOAIFbsApiHandler);
                reqObj->downloadShipmentReceptionTransferActRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/campaigns/{campaignId}/first-mile/shipments/{shipmentId}/transportation-waybill").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                QString shipmentId = match.captured(QString("shipmentId").toLower());
                auto reqObj = new OAIFbsApiRequest(socket, mOAIFbsApiHandler);
                reqObj->downloadShipmentTransportationWaybillRequest(campaignId, shipmentId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/campaigns/{campaignId}/orders/{orderId}/delivery/shipments/{shipmentId}/boxes/{boxId}/label").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                QString orderId = match.captured(QString("orderId").toLower());
                QString shipmentId = match.captured(QString("shipmentId").toLower());
                QString boxId = match.captured(QString("boxId").toLower());
                auto reqObj = new OAIFbsApiRequest(socket, mOAIFbsApiHandler);
                reqObj->generateOrderLabelRequest(campaignId, orderId, shipmentId, boxId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/campaigns/{campaignId}/orders/{orderId}/delivery/labels").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                QString orderId = match.captured(QString("orderId").toLower());
                auto reqObj = new OAIFbsApiRequest(socket, mOAIFbsApiHandler);
                reqObj->generateOrderLabelsRequest(campaignId, orderId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/campaigns/{campaignId}/offers/all").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIFbsApiRequest(socket, mOAIFbsApiHandler);
                reqObj->getAllOffersRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/bids/info").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIFbsApiRequest(socket, mOAIFbsApiHandler);
                reqObj->getBidsInfoForBusinessRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/bids/recommendations").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIFbsApiRequest(socket, mOAIFbsApiHandler);
                reqObj->getBidsRecommendationsRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/price-quarantine").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIFbsApiRequest(socket, mOAIFbsApiHandler);
                reqObj->getBusinessQuarantineOffersRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/settings").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIFbsApiRequest(socket, mOAIFbsApiHandler);
                reqObj->getBusinessSettingsRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/campaigns/{campaignId}").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIFbsApiRequest(socket, mOAIFbsApiHandler);
                reqObj->getCampaignRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/campaigns/{campaignId}/logins").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIFbsApiRequest(socket, mOAIFbsApiHandler);
                reqObj->getCampaignLoginsRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/campaigns/{campaignId}/offers").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIFbsApiRequest(socket, mOAIFbsApiHandler);
                reqObj->getCampaignOffersRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/campaigns/{campaignId}/price-quarantine").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIFbsApiRequest(socket, mOAIFbsApiHandler);
                reqObj->getCampaignQuarantineOffersRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/campaigns/{campaignId}/region").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIFbsApiRequest(socket, mOAIFbsApiHandler);
                reqObj->getCampaignRegionRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/campaigns/{campaignId}/settings").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIFbsApiRequest(socket, mOAIFbsApiHandler);
                reqObj->getCampaignSettingsRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/campaigns/by_login/{login}").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString login = match.captured(QString("login").toLower());
                auto reqObj = new OAIFbsApiRequest(socket, mOAIFbsApiHandler);
                reqObj->getCampaignsByLoginRequest(login);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/category/{categoryId}/parameters").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString categoryId = match.captured(QString("categoryId").toLower());
                auto reqObj = new OAIFbsApiRequest(socket, mOAIFbsApiHandler);
                reqObj->getCategoryContentParametersRequest(categoryId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/chats/history").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIFbsApiRequest(socket, mOAIFbsApiHandler);
                reqObj->getChatHistoryRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/chats").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIFbsApiRequest(socket, mOAIFbsApiHandler);
                reqObj->getChatsRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/campaigns/{campaignId}/feeds/{feedId}").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                QString feedId = match.captured(QString("feedId").toLower());
                auto reqObj = new OAIFbsApiRequest(socket, mOAIFbsApiHandler);
                reqObj->getFeedRequest(campaignId, feedId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/campaigns/{campaignId}/feeds/{feedId}/index-logs").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                QString feedId = match.captured(QString("feedId").toLower());
                auto reqObj = new OAIFbsApiRequest(socket, mOAIFbsApiHandler);
                reqObj->getFeedIndexLogsRequest(campaignId, feedId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/campaigns/{campaignId}/feedback/updates").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIFbsApiRequest(socket, mOAIFbsApiHandler);
                reqObj->getFeedbackAndCommentUpdatesRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/campaigns/{campaignId}/feeds").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIFbsApiRequest(socket, mOAIFbsApiHandler);
                reqObj->getFeedsRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/goods-feedback/comments").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIFbsApiRequest(socket, mOAIFbsApiHandler);
                reqObj->getGoodsFeedbackCommentsRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/goods-feedback").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIFbsApiRequest(socket, mOAIFbsApiHandler);
                reqObj->getGoodsFeedbacksRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/campaigns/{campaignId}/stats/skus").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIFbsApiRequest(socket, mOAIFbsApiHandler);
                reqObj->getGoodsStatsRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/campaigns/{campaignId}/hidden-offers").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIFbsApiRequest(socket, mOAIFbsApiHandler);
                reqObj->getHiddenOffersRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/offer-cards").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIFbsApiRequest(socket, mOAIFbsApiHandler);
                reqObj->getOfferCardsContentStatusRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/campaigns/{campaignId}/offer-mapping-entries").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIFbsApiRequest(socket, mOAIFbsApiHandler);
                reqObj->getOfferMappingEntriesRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/offer-mappings").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIFbsApiRequest(socket, mOAIFbsApiHandler);
                reqObj->getOfferMappingsRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/offers/recommendations").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIFbsApiRequest(socket, mOAIFbsApiHandler);
                reqObj->getOfferRecommendationsRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/campaigns/{campaignId}/offers").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIFbsApiRequest(socket, mOAIFbsApiHandler);
                reqObj->getOffersRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/campaigns/{campaignId}/orders/{orderId}").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                QString orderId = match.captured(QString("orderId").toLower());
                auto reqObj = new OAIFbsApiRequest(socket, mOAIFbsApiHandler);
                reqObj->getOrderRequest(campaignId, orderId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/campaigns/{campaignId}/orders/{orderId}/business-buyer").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                QString orderId = match.captured(QString("orderId").toLower());
                auto reqObj = new OAIFbsApiRequest(socket, mOAIFbsApiHandler);
                reqObj->getOrderBusinessBuyerInfoRequest(campaignId, orderId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/campaigns/{campaignId}/orders/{orderId}/documents").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                QString orderId = match.captured(QString("orderId").toLower());
                auto reqObj = new OAIFbsApiRequest(socket, mOAIFbsApiHandler);
                reqObj->getOrderBusinessDocumentsInfoRequest(campaignId, orderId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/campaigns/{campaignId}/orders/{orderId}/delivery/labels/data").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                QString orderId = match.captured(QString("orderId").toLower());
                auto reqObj = new OAIFbsApiRequest(socket, mOAIFbsApiHandler);
                reqObj->getOrderLabelsDataRequest(campaignId, orderId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/campaigns/{campaignId}/orders").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIFbsApiRequest(socket, mOAIFbsApiHandler);
                reqObj->getOrdersRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/campaigns/{campaignId}/stats/orders").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIFbsApiRequest(socket, mOAIFbsApiHandler);
                reqObj->getOrdersStatsRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/campaigns/{campaignId}/offer-prices").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIFbsApiRequest(socket, mOAIFbsApiHandler);
                reqObj->getPricesRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/campaigns/{campaignId}/offer-prices").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIFbsApiRequest(socket, mOAIFbsApiHandler);
                reqObj->getPricesByOfferIdsRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/promos/offers").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIFbsApiRequest(socket, mOAIFbsApiHandler);
                reqObj->getPromoOffersRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/promos").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIFbsApiRequest(socket, mOAIFbsApiHandler);
                reqObj->getPromosRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/campaigns/{campaignId}/ratings/quality/details").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIFbsApiRequest(socket, mOAIFbsApiHandler);
                reqObj->getQualityRatingDetailsRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/ratings/quality").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIFbsApiRequest(socket, mOAIFbsApiHandler);
                reqObj->getQualityRatingsRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/reports/info/{reportId}").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString reportId = match.captured(QString("reportId").toLower());
                auto reqObj = new OAIFbsApiRequest(socket, mOAIFbsApiHandler);
                reqObj->getReportInfoRequest(reportId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/campaigns/{campaignId}/orders/{orderId}/returns/{returnId}").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                QString orderId = match.captured(QString("orderId").toLower());
                QString returnId = match.captured(QString("returnId").toLower());
                auto reqObj = new OAIFbsApiRequest(socket, mOAIFbsApiHandler);
                reqObj->getReturnRequest(campaignId, orderId, returnId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/campaigns/{campaignId}/orders/{orderId}/returns/{returnId}/application").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                QString orderId = match.captured(QString("orderId").toLower());
                QString returnId = match.captured(QString("returnId").toLower());
                auto reqObj = new OAIFbsApiRequest(socket, mOAIFbsApiHandler);
                reqObj->getReturnApplicationRequest(campaignId, orderId, returnId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/campaigns/{campaignId}/orders/{orderId}/returns/{returnId}/decision/{itemId}/image/{imageHash}").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                QString orderId = match.captured(QString("orderId").toLower());
                QString returnId = match.captured(QString("returnId").toLower());
                QString itemId = match.captured(QString("itemId").toLower());
                QString imageHash = match.captured(QString("imageHash").toLower());
                auto reqObj = new OAIFbsApiRequest(socket, mOAIFbsApiHandler);
                reqObj->getReturnPhotoRequest(campaignId, orderId, returnId, itemId, imageHash);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/campaigns/{campaignId}/returns").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIFbsApiRequest(socket, mOAIFbsApiHandler);
                reqObj->getReturnsRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/campaigns/{campaignId}/first-mile/shipments/{shipmentId}").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                QString shipmentId = match.captured(QString("shipmentId").toLower());
                auto reqObj = new OAIFbsApiRequest(socket, mOAIFbsApiHandler);
                reqObj->getShipmentRequest(campaignId, shipmentId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/campaigns/{campaignId}/first-mile/shipments/{shipmentId}/orders/info").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                QString shipmentId = match.captured(QString("shipmentId").toLower());
                auto reqObj = new OAIFbsApiRequest(socket, mOAIFbsApiHandler);
                reqObj->getShipmentOrdersInfoRequest(campaignId, shipmentId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/campaigns/{campaignId}/offers/stocks").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIFbsApiRequest(socket, mOAIFbsApiHandler);
                reqObj->getStocksRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/campaigns/{campaignId}/offer-mapping-entries/suggestions").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIFbsApiRequest(socket, mOAIFbsApiHandler);
                reqObj->getSuggestedOfferMappingEntriesRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/offer-mappings/suggestions").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIFbsApiRequest(socket, mOAIFbsApiHandler);
                reqObj->getSuggestedOfferMappingsRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/campaigns/{campaignId}/offer-prices/suggestions").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIFbsApiRequest(socket, mOAIFbsApiHandler);
                reqObj->getSuggestedPricesRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/businesses/{businessId}/warehouses").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIFbsApiRequest(socket, mOAIFbsApiHandler);
                reqObj->getWarehousesRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("PUT").arg("/campaigns/{campaignId}/orders/{orderId}/identifiers").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                QString orderId = match.captured(QString("orderId").toLower());
                auto reqObj = new OAIFbsApiRequest(socket, mOAIFbsApiHandler);
                reqObj->provideOrderItemIdentifiersRequest(campaignId, orderId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("PUT").arg("/businesses/{businessId}/bids").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIFbsApiRequest(socket, mOAIFbsApiHandler);
                reqObj->putBidsForBusinessRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("PUT").arg("/campaigns/{campaignId}/bids").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIFbsApiRequest(socket, mOAIFbsApiHandler);
                reqObj->putBidsForCampaignRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/campaigns/{campaignId}/feeds/{feedId}/refresh").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                QString feedId = match.captured(QString("feedId").toLower());
                auto reqObj = new OAIFbsApiRequest(socket, mOAIFbsApiHandler);
                reqObj->refreshFeedRequest(campaignId, feedId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/regions/{regionId}/children").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString regionId = match.captured(QString("regionId").toLower());
                auto reqObj = new OAIFbsApiRequest(socket, mOAIFbsApiHandler);
                reqObj->searchRegionChildrenRequest(regionId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/regions/{regionId}").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString regionId = match.captured(QString("regionId").toLower());
                auto reqObj = new OAIFbsApiRequest(socket, mOAIFbsApiHandler);
                reqObj->searchRegionsByIdRequest(regionId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("PUT").arg("/campaigns/{campaignId}/first-mile/shipments").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIFbsApiRequest(socket, mOAIFbsApiHandler);
                reqObj->searchShipmentsRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/chats/file/send").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIFbsApiRequest(socket, mOAIFbsApiHandler);
                reqObj->sendFileToChatRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/chats/message").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIFbsApiRequest(socket, mOAIFbsApiHandler);
                reqObj->sendMessageToChatRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/campaigns/{campaignId}/feeds/{feedId}/params").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                QString feedId = match.captured(QString("feedId").toLower());
                auto reqObj = new OAIFbsApiRequest(socket, mOAIFbsApiHandler);
                reqObj->setFeedParamsRequest(campaignId, feedId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("PUT").arg("/campaigns/{campaignId}/orders/{orderId}/boxes").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                QString orderId = match.captured(QString("orderId").toLower());
                auto reqObj = new OAIFbsApiRequest(socket, mOAIFbsApiHandler);
                reqObj->setOrderBoxLayoutRequest(campaignId, orderId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("PUT").arg("/campaigns/{campaignId}/orders/{orderId}/delivery/shipments/{shipmentId}/boxes").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                QString orderId = match.captured(QString("orderId").toLower());
                QString shipmentId = match.captured(QString("shipmentId").toLower());
                auto reqObj = new OAIFbsApiRequest(socket, mOAIFbsApiHandler);
                reqObj->setOrderShipmentBoxesRequest(campaignId, orderId, shipmentId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("PUT").arg("/campaigns/{campaignId}/first-mile/shipments/{shipmentId}/pallets").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                QString shipmentId = match.captured(QString("shipmentId").toLower());
                auto reqObj = new OAIFbsApiRequest(socket, mOAIFbsApiHandler);
                reqObj->setShipmentPalletsCountRequest(campaignId, shipmentId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/goods-feedback/skip-reaction").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIFbsApiRequest(socket, mOAIFbsApiHandler);
                reqObj->skipGoodsFeedbacksReactionRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/campaigns/{campaignId}/first-mile/shipments/{shipmentId}/orders/transfer").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                QString shipmentId = match.captured(QString("shipmentId").toLower());
                auto reqObj = new OAIFbsApiRequest(socket, mOAIFbsApiHandler);
                reqObj->transferOrdersFromShipmentRequest(campaignId, shipmentId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/offer-prices/updates").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIFbsApiRequest(socket, mOAIFbsApiHandler);
                reqObj->updateBusinessPricesRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/campaigns/{campaignId}/offers/update").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIFbsApiRequest(socket, mOAIFbsApiHandler);
                reqObj->updateCampaignOffersRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/goods-feedback/comments/update").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIFbsApiRequest(socket, mOAIFbsApiHandler);
                reqObj->updateGoodsFeedbackCommentRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/offer-cards/update").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIFbsApiRequest(socket, mOAIFbsApiHandler);
                reqObj->updateOfferContentRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/campaigns/{campaignId}/offer-mapping-entries/updates").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIFbsApiRequest(socket, mOAIFbsApiHandler);
                reqObj->updateOfferMappingEntriesRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/offer-mappings/update").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIFbsApiRequest(socket, mOAIFbsApiHandler);
                reqObj->updateOfferMappingsRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("PUT").arg("/campaigns/{campaignId}/orders/{orderId}/items").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                QString orderId = match.captured(QString("orderId").toLower());
                auto reqObj = new OAIFbsApiRequest(socket, mOAIFbsApiHandler);
                reqObj->updateOrderItemsRequest(campaignId, orderId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("PUT").arg("/campaigns/{campaignId}/orders/{orderId}/status").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                QString orderId = match.captured(QString("orderId").toLower());
                auto reqObj = new OAIFbsApiRequest(socket, mOAIFbsApiHandler);
                reqObj->updateOrderStatusRequest(campaignId, orderId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/campaigns/{campaignId}/orders/status-update").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIFbsApiRequest(socket, mOAIFbsApiHandler);
                reqObj->updateOrderStatusesRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/campaigns/{campaignId}/offer-prices/updates").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIFbsApiRequest(socket, mOAIFbsApiHandler);
                reqObj->updatePricesRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/promos/offers/update").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIFbsApiRequest(socket, mOAIFbsApiHandler);
                reqObj->updatePromoOffersRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("PUT").arg("/campaigns/{campaignId}/offers/stocks").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIFbsApiRequest(socket, mOAIFbsApiHandler);
                reqObj->updateStocksRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/campaigns/{campaignId}/hidden-offers").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIFbyApiRequest(socket, mOAIFbyApiHandler);
                reqObj->addHiddenOffersRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/offer-mappings/archive").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIFbyApiRequest(socket, mOAIFbyApiHandler);
                reqObj->addOffersToArchiveRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/price-quarantine/confirm").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIFbyApiRequest(socket, mOAIFbyApiHandler);
                reqObj->confirmBusinessPricesRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/campaigns/{campaignId}/price-quarantine/confirm").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIFbyApiRequest(socket, mOAIFbyApiHandler);
                reqObj->confirmCampaignPricesRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/chats/new").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIFbyApiRequest(socket, mOAIFbyApiHandler);
                reqObj->createChatRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/campaigns/{campaignId}/offers/delete").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIFbyApiRequest(socket, mOAIFbyApiHandler);
                reqObj->deleteCampaignOffersRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/goods-feedback/comments/delete").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIFbyApiRequest(socket, mOAIFbyApiHandler);
                reqObj->deleteGoodsFeedbackCommentRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/campaigns/{campaignId}/hidden-offers/delete").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIFbyApiRequest(socket, mOAIFbyApiHandler);
                reqObj->deleteHiddenOffersRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/offer-mappings/delete").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIFbyApiRequest(socket, mOAIFbyApiHandler);
                reqObj->deleteOffersRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/offer-mappings/unarchive").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIFbyApiRequest(socket, mOAIFbyApiHandler);
                reqObj->deleteOffersFromArchiveRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/promos/offers/delete").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIFbyApiRequest(socket, mOAIFbyApiHandler);
                reqObj->deletePromoOffersRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/campaigns/{campaignId}/offers/all").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIFbyApiRequest(socket, mOAIFbyApiHandler);
                reqObj->getAllOffersRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/bids/info").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIFbyApiRequest(socket, mOAIFbyApiHandler);
                reqObj->getBidsInfoForBusinessRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/bids/recommendations").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIFbyApiRequest(socket, mOAIFbyApiHandler);
                reqObj->getBidsRecommendationsRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/price-quarantine").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIFbyApiRequest(socket, mOAIFbyApiHandler);
                reqObj->getBusinessQuarantineOffersRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/settings").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIFbyApiRequest(socket, mOAIFbyApiHandler);
                reqObj->getBusinessSettingsRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/campaigns/{campaignId}").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIFbyApiRequest(socket, mOAIFbyApiHandler);
                reqObj->getCampaignRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/campaigns/{campaignId}/logins").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIFbyApiRequest(socket, mOAIFbyApiHandler);
                reqObj->getCampaignLoginsRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/campaigns/{campaignId}/offers").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIFbyApiRequest(socket, mOAIFbyApiHandler);
                reqObj->getCampaignOffersRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/campaigns/{campaignId}/price-quarantine").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIFbyApiRequest(socket, mOAIFbyApiHandler);
                reqObj->getCampaignQuarantineOffersRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/campaigns/{campaignId}/region").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIFbyApiRequest(socket, mOAIFbyApiHandler);
                reqObj->getCampaignRegionRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/campaigns/{campaignId}/settings").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIFbyApiRequest(socket, mOAIFbyApiHandler);
                reqObj->getCampaignSettingsRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/campaigns/by_login/{login}").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString login = match.captured(QString("login").toLower());
                auto reqObj = new OAIFbyApiRequest(socket, mOAIFbyApiHandler);
                reqObj->getCampaignsByLoginRequest(login);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/category/{categoryId}/parameters").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString categoryId = match.captured(QString("categoryId").toLower());
                auto reqObj = new OAIFbyApiRequest(socket, mOAIFbyApiHandler);
                reqObj->getCategoryContentParametersRequest(categoryId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/chats/history").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIFbyApiRequest(socket, mOAIFbyApiHandler);
                reqObj->getChatHistoryRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/chats").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIFbyApiRequest(socket, mOAIFbyApiHandler);
                reqObj->getChatsRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/campaigns/{campaignId}/feeds/{feedId}").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                QString feedId = match.captured(QString("feedId").toLower());
                auto reqObj = new OAIFbyApiRequest(socket, mOAIFbyApiHandler);
                reqObj->getFeedRequest(campaignId, feedId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/campaigns/{campaignId}/feeds/{feedId}/index-logs").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                QString feedId = match.captured(QString("feedId").toLower());
                auto reqObj = new OAIFbyApiRequest(socket, mOAIFbyApiHandler);
                reqObj->getFeedIndexLogsRequest(campaignId, feedId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/campaigns/{campaignId}/feedback/updates").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIFbyApiRequest(socket, mOAIFbyApiHandler);
                reqObj->getFeedbackAndCommentUpdatesRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/campaigns/{campaignId}/feeds").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIFbyApiRequest(socket, mOAIFbyApiHandler);
                reqObj->getFeedsRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/goods-feedback/comments").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIFbyApiRequest(socket, mOAIFbyApiHandler);
                reqObj->getGoodsFeedbackCommentsRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/goods-feedback").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIFbyApiRequest(socket, mOAIFbyApiHandler);
                reqObj->getGoodsFeedbacksRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/campaigns/{campaignId}/stats/skus").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIFbyApiRequest(socket, mOAIFbyApiHandler);
                reqObj->getGoodsStatsRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/campaigns/{campaignId}/hidden-offers").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIFbyApiRequest(socket, mOAIFbyApiHandler);
                reqObj->getHiddenOffersRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/offer-cards").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIFbyApiRequest(socket, mOAIFbyApiHandler);
                reqObj->getOfferCardsContentStatusRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/campaigns/{campaignId}/offer-mapping-entries").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIFbyApiRequest(socket, mOAIFbyApiHandler);
                reqObj->getOfferMappingEntriesRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/offer-mappings").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIFbyApiRequest(socket, mOAIFbyApiHandler);
                reqObj->getOfferMappingsRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/offers/recommendations").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIFbyApiRequest(socket, mOAIFbyApiHandler);
                reqObj->getOfferRecommendationsRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/campaigns/{campaignId}/offers").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIFbyApiRequest(socket, mOAIFbyApiHandler);
                reqObj->getOffersRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/campaigns/{campaignId}/orders/{orderId}").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                QString orderId = match.captured(QString("orderId").toLower());
                auto reqObj = new OAIFbyApiRequest(socket, mOAIFbyApiHandler);
                reqObj->getOrderRequest(campaignId, orderId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/campaigns/{campaignId}/orders/{orderId}/business-buyer").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                QString orderId = match.captured(QString("orderId").toLower());
                auto reqObj = new OAIFbyApiRequest(socket, mOAIFbyApiHandler);
                reqObj->getOrderBusinessBuyerInfoRequest(campaignId, orderId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/campaigns/{campaignId}/orders/{orderId}/documents").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                QString orderId = match.captured(QString("orderId").toLower());
                auto reqObj = new OAIFbyApiRequest(socket, mOAIFbyApiHandler);
                reqObj->getOrderBusinessDocumentsInfoRequest(campaignId, orderId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/campaigns/{campaignId}/orders").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIFbyApiRequest(socket, mOAIFbyApiHandler);
                reqObj->getOrdersRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/campaigns/{campaignId}/stats/orders").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIFbyApiRequest(socket, mOAIFbyApiHandler);
                reqObj->getOrdersStatsRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/campaigns/{campaignId}/offer-prices").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIFbyApiRequest(socket, mOAIFbyApiHandler);
                reqObj->getPricesRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/campaigns/{campaignId}/offer-prices").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIFbyApiRequest(socket, mOAIFbyApiHandler);
                reqObj->getPricesByOfferIdsRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/promos/offers").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIFbyApiRequest(socket, mOAIFbyApiHandler);
                reqObj->getPromoOffersRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/promos").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIFbyApiRequest(socket, mOAIFbyApiHandler);
                reqObj->getPromosRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/ratings/quality").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIFbyApiRequest(socket, mOAIFbyApiHandler);
                reqObj->getQualityRatingsRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/reports/info/{reportId}").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString reportId = match.captured(QString("reportId").toLower());
                auto reqObj = new OAIFbyApiRequest(socket, mOAIFbyApiHandler);
                reqObj->getReportInfoRequest(reportId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/campaigns/{campaignId}/orders/{orderId}/returns/{returnId}").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                QString orderId = match.captured(QString("orderId").toLower());
                QString returnId = match.captured(QString("returnId").toLower());
                auto reqObj = new OAIFbyApiRequest(socket, mOAIFbyApiHandler);
                reqObj->getReturnRequest(campaignId, orderId, returnId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/campaigns/{campaignId}/orders/{orderId}/returns/{returnId}/decision/{itemId}/image/{imageHash}").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                QString orderId = match.captured(QString("orderId").toLower());
                QString returnId = match.captured(QString("returnId").toLower());
                QString itemId = match.captured(QString("itemId").toLower());
                QString imageHash = match.captured(QString("imageHash").toLower());
                auto reqObj = new OAIFbyApiRequest(socket, mOAIFbyApiHandler);
                reqObj->getReturnPhotoRequest(campaignId, orderId, returnId, itemId, imageHash);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/campaigns/{campaignId}/returns").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIFbyApiRequest(socket, mOAIFbyApiHandler);
                reqObj->getReturnsRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/campaigns/{campaignId}/offers/stocks").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIFbyApiRequest(socket, mOAIFbyApiHandler);
                reqObj->getStocksRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/campaigns/{campaignId}/offer-mapping-entries/suggestions").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIFbyApiRequest(socket, mOAIFbyApiHandler);
                reqObj->getSuggestedOfferMappingEntriesRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/offer-mappings/suggestions").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIFbyApiRequest(socket, mOAIFbyApiHandler);
                reqObj->getSuggestedOfferMappingsRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/campaigns/{campaignId}/offer-prices/suggestions").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIFbyApiRequest(socket, mOAIFbyApiHandler);
                reqObj->getSuggestedPricesRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("PUT").arg("/businesses/{businessId}/bids").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIFbyApiRequest(socket, mOAIFbyApiHandler);
                reqObj->putBidsForBusinessRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("PUT").arg("/campaigns/{campaignId}/bids").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIFbyApiRequest(socket, mOAIFbyApiHandler);
                reqObj->putBidsForCampaignRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/campaigns/{campaignId}/feeds/{feedId}/refresh").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                QString feedId = match.captured(QString("feedId").toLower());
                auto reqObj = new OAIFbyApiRequest(socket, mOAIFbyApiHandler);
                reqObj->refreshFeedRequest(campaignId, feedId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/regions/{regionId}/children").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString regionId = match.captured(QString("regionId").toLower());
                auto reqObj = new OAIFbyApiRequest(socket, mOAIFbyApiHandler);
                reqObj->searchRegionChildrenRequest(regionId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/regions/{regionId}").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString regionId = match.captured(QString("regionId").toLower());
                auto reqObj = new OAIFbyApiRequest(socket, mOAIFbyApiHandler);
                reqObj->searchRegionsByIdRequest(regionId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/chats/file/send").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIFbyApiRequest(socket, mOAIFbyApiHandler);
                reqObj->sendFileToChatRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/chats/message").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIFbyApiRequest(socket, mOAIFbyApiHandler);
                reqObj->sendMessageToChatRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/campaigns/{campaignId}/feeds/{feedId}/params").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                QString feedId = match.captured(QString("feedId").toLower());
                auto reqObj = new OAIFbyApiRequest(socket, mOAIFbyApiHandler);
                reqObj->setFeedParamsRequest(campaignId, feedId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/goods-feedback/skip-reaction").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIFbyApiRequest(socket, mOAIFbyApiHandler);
                reqObj->skipGoodsFeedbacksReactionRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/offer-prices/updates").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIFbyApiRequest(socket, mOAIFbyApiHandler);
                reqObj->updateBusinessPricesRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/campaigns/{campaignId}/offers/update").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIFbyApiRequest(socket, mOAIFbyApiHandler);
                reqObj->updateCampaignOffersRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/goods-feedback/comments/update").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIFbyApiRequest(socket, mOAIFbyApiHandler);
                reqObj->updateGoodsFeedbackCommentRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/offer-cards/update").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIFbyApiRequest(socket, mOAIFbyApiHandler);
                reqObj->updateOfferContentRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/campaigns/{campaignId}/offer-mapping-entries/updates").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIFbyApiRequest(socket, mOAIFbyApiHandler);
                reqObj->updateOfferMappingEntriesRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/offer-mappings/update").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIFbyApiRequest(socket, mOAIFbyApiHandler);
                reqObj->updateOfferMappingsRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/campaigns/{campaignId}/offer-prices/updates").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIFbyApiRequest(socket, mOAIFbyApiHandler);
                reqObj->updatePricesRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/promos/offers/update").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIFbyApiRequest(socket, mOAIFbyApiHandler);
                reqObj->updatePromoOffersRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/campaigns/{campaignId}/feedback/updates").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIFeedbacksApiRequest(socket, mOAIFeedbacksApiHandler);
                reqObj->getFeedbackAndCommentUpdatesRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/campaigns/{campaignId}/feeds/{feedId}").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                QString feedId = match.captured(QString("feedId").toLower());
                auto reqObj = new OAIFeedsApiRequest(socket, mOAIFeedsApiHandler);
                reqObj->getFeedRequest(campaignId, feedId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/campaigns/{campaignId}/feeds/{feedId}/index-logs").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                QString feedId = match.captured(QString("feedId").toLower());
                auto reqObj = new OAIFeedsApiRequest(socket, mOAIFeedsApiHandler);
                reqObj->getFeedIndexLogsRequest(campaignId, feedId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/campaigns/{campaignId}/feeds").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIFeedsApiRequest(socket, mOAIFeedsApiHandler);
                reqObj->getFeedsRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/campaigns/{campaignId}/feeds/{feedId}/refresh").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                QString feedId = match.captured(QString("feedId").toLower());
                auto reqObj = new OAIFeedsApiRequest(socket, mOAIFeedsApiHandler);
                reqObj->refreshFeedRequest(campaignId, feedId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/campaigns/{campaignId}/feeds/{feedId}/params").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                QString feedId = match.captured(QString("feedId").toLower());
                auto reqObj = new OAIFeedsApiRequest(socket, mOAIFeedsApiHandler);
                reqObj->setFeedParamsRequest(campaignId, feedId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/goods-feedback/comments/delete").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIGoodsFeedbackApiRequest(socket, mOAIGoodsFeedbackApiHandler);
                reqObj->deleteGoodsFeedbackCommentRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/goods-feedback/comments").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIGoodsFeedbackApiRequest(socket, mOAIGoodsFeedbackApiHandler);
                reqObj->getGoodsFeedbackCommentsRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/goods-feedback").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIGoodsFeedbackApiRequest(socket, mOAIGoodsFeedbackApiHandler);
                reqObj->getGoodsFeedbacksRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/goods-feedback/skip-reaction").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIGoodsFeedbackApiRequest(socket, mOAIGoodsFeedbackApiHandler);
                reqObj->skipGoodsFeedbacksReactionRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/goods-feedback/comments/update").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIGoodsFeedbackApiRequest(socket, mOAIGoodsFeedbackApiHandler);
                reqObj->updateGoodsFeedbackCommentRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/campaigns/{campaignId}/stats/skus").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIGoodsStatsApiRequest(socket, mOAIGoodsStatsApiHandler);
                reqObj->getGoodsStatsRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/campaigns/{campaignId}/hidden-offers").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIHiddenOffersApiRequest(socket, mOAIHiddenOffersApiHandler);
                reqObj->addHiddenOffersRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/campaigns/{campaignId}/hidden-offers/delete").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIHiddenOffersApiRequest(socket, mOAIHiddenOffersApiHandler);
                reqObj->deleteHiddenOffersRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/campaigns/{campaignId}/hidden-offers").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIHiddenOffersApiRequest(socket, mOAIHiddenOffersApiHandler);
                reqObj->getHiddenOffersRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/models/{modelId}").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString modelId = match.captured(QString("modelId").toLower());
                auto reqObj = new OAIModelsApiRequest(socket, mOAIModelsApiHandler);
                reqObj->getModelRequest(modelId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/models/{modelId}/offers").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString modelId = match.captured(QString("modelId").toLower());
                auto reqObj = new OAIModelsApiRequest(socket, mOAIModelsApiHandler);
                reqObj->getModelOffersRequest(modelId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/campaigns/{campaignId}/offer-mapping-entries").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIOfferMappingsApiRequest(socket, mOAIOfferMappingsApiHandler);
                reqObj->getOfferMappingEntriesRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/campaigns/{campaignId}/offer-mapping-entries/suggestions").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIOfferMappingsApiRequest(socket, mOAIOfferMappingsApiHandler);
                reqObj->getSuggestedOfferMappingEntriesRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/campaigns/{campaignId}/offer-mapping-entries/updates").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIOfferMappingsApiRequest(socket, mOAIOfferMappingsApiHandler);
                reqObj->updateOfferMappingEntriesRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/campaigns/{campaignId}/offers/delete").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIOffersApiRequest(socket, mOAIOffersApiHandler);
                reqObj->deleteCampaignOffersRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/campaigns/{campaignId}/offers/all").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIOffersApiRequest(socket, mOAIOffersApiHandler);
                reqObj->getAllOffersRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/campaigns/{campaignId}/offers").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIOffersApiRequest(socket, mOAIOffersApiHandler);
                reqObj->getCampaignOffersRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/offers/recommendations").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIOffersApiRequest(socket, mOAIOffersApiHandler);
                reqObj->getOfferRecommendationsRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/campaigns/{campaignId}/offers").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIOffersApiRequest(socket, mOAIOffersApiHandler);
                reqObj->getOffersRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/campaigns/{campaignId}/offers/update").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIOffersApiRequest(socket, mOAIOffersApiHandler);
                reqObj->updateCampaignOffersRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/campaigns/{campaignId}/orders/{orderId}/business-buyer").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                QString orderId = match.captured(QString("orderId").toLower());
                auto reqObj = new OAIOrderBusinessInformationApiRequest(socket, mOAIOrderBusinessInformationApiHandler);
                reqObj->getOrderBusinessBuyerInfoRequest(campaignId, orderId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/campaigns/{campaignId}/orders/{orderId}/documents").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                QString orderId = match.captured(QString("orderId").toLower());
                auto reqObj = new OAIOrderBusinessInformationApiRequest(socket, mOAIOrderBusinessInformationApiHandler);
                reqObj->getOrderBusinessDocumentsInfoRequest(campaignId, orderId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/campaigns/{campaignId}/orders/{orderId}/buyer").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                QString orderId = match.captured(QString("orderId").toLower());
                auto reqObj = new OAIOrderDeliveryApiRequest(socket, mOAIOrderDeliveryApiHandler);
                reqObj->getOrderBuyerInfoRequest(campaignId, orderId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("PUT").arg("/campaigns/{campaignId}/orders/{orderId}/delivery/date").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                QString orderId = match.captured(QString("orderId").toLower());
                auto reqObj = new OAIOrderDeliveryApiRequest(socket, mOAIOrderDeliveryApiHandler);
                reqObj->setOrderDeliveryDateRequest(campaignId, orderId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/campaigns/{campaignId}/orders/{orderId}/delivery/track").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                QString orderId = match.captured(QString("orderId").toLower());
                auto reqObj = new OAIOrderDeliveryApiRequest(socket, mOAIOrderDeliveryApiHandler);
                reqObj->setOrderDeliveryTrackCodeRequest(campaignId, orderId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("PUT").arg("/campaigns/{campaignId}/orders/{orderId}/delivery/storage-limit").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                QString orderId = match.captured(QString("orderId").toLower());
                auto reqObj = new OAIOrderDeliveryApiRequest(socket, mOAIOrderDeliveryApiHandler);
                reqObj->updateOrderStorageLimitRequest(campaignId, orderId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("PUT").arg("/campaigns/{campaignId}/orders/{orderId}/verifyEac").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                QString orderId = match.captured(QString("orderId").toLower());
                auto reqObj = new OAIOrderDeliveryApiRequest(socket, mOAIOrderDeliveryApiHandler);
                reqObj->verifyOrderEacRequest(campaignId, orderId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/campaigns/{campaignId}/orders/{orderId}/delivery/shipments/{shipmentId}/boxes/{boxId}/label").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                QString orderId = match.captured(QString("orderId").toLower());
                QString shipmentId = match.captured(QString("shipmentId").toLower());
                QString boxId = match.captured(QString("boxId").toLower());
                auto reqObj = new OAIOrderLabelsApiRequest(socket, mOAIOrderLabelsApiHandler);
                reqObj->generateOrderLabelRequest(campaignId, orderId, shipmentId, boxId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/campaigns/{campaignId}/orders/{orderId}/delivery/labels").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                QString orderId = match.captured(QString("orderId").toLower());
                auto reqObj = new OAIOrderLabelsApiRequest(socket, mOAIOrderLabelsApiHandler);
                reqObj->generateOrderLabelsRequest(campaignId, orderId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/campaigns/{campaignId}/orders/{orderId}/delivery/labels/data").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                QString orderId = match.captured(QString("orderId").toLower());
                auto reqObj = new OAIOrderLabelsApiRequest(socket, mOAIOrderLabelsApiHandler);
                reqObj->getOrderLabelsDataRequest(campaignId, orderId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("PUT").arg("/campaigns/{campaignId}/orders/{orderId}/cancellation/accept").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                QString orderId = match.captured(QString("orderId").toLower());
                auto reqObj = new OAIOrdersApiRequest(socket, mOAIOrdersApiHandler);
                reqObj->acceptOrderCancellationRequest(campaignId, orderId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/campaigns/{campaignId}/orders/{orderId}").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                QString orderId = match.captured(QString("orderId").toLower());
                auto reqObj = new OAIOrdersApiRequest(socket, mOAIOrdersApiHandler);
                reqObj->getOrderRequest(campaignId, orderId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/campaigns/{campaignId}/orders").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIOrdersApiRequest(socket, mOAIOrdersApiHandler);
                reqObj->getOrdersRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/campaigns/{campaignId}/orders/{orderId}/deliverDigitalGoods").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                QString orderId = match.captured(QString("orderId").toLower());
                auto reqObj = new OAIOrdersApiRequest(socket, mOAIOrdersApiHandler);
                reqObj->provideOrderDigitalCodesRequest(campaignId, orderId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("PUT").arg("/campaigns/{campaignId}/orders/{orderId}/identifiers").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                QString orderId = match.captured(QString("orderId").toLower());
                auto reqObj = new OAIOrdersApiRequest(socket, mOAIOrdersApiHandler);
                reqObj->provideOrderItemIdentifiersRequest(campaignId, orderId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("PUT").arg("/campaigns/{campaignId}/orders/{orderId}/boxes").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                QString orderId = match.captured(QString("orderId").toLower());
                auto reqObj = new OAIOrdersApiRequest(socket, mOAIOrdersApiHandler);
                reqObj->setOrderBoxLayoutRequest(campaignId, orderId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("PUT").arg("/campaigns/{campaignId}/orders/{orderId}/delivery/shipments/{shipmentId}/boxes").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                QString orderId = match.captured(QString("orderId").toLower());
                QString shipmentId = match.captured(QString("shipmentId").toLower());
                auto reqObj = new OAIOrdersApiRequest(socket, mOAIOrdersApiHandler);
                reqObj->setOrderShipmentBoxesRequest(campaignId, orderId, shipmentId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("PUT").arg("/campaigns/{campaignId}/orders/{orderId}/items").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                QString orderId = match.captured(QString("orderId").toLower());
                auto reqObj = new OAIOrdersApiRequest(socket, mOAIOrdersApiHandler);
                reqObj->updateOrderItemsRequest(campaignId, orderId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("PUT").arg("/campaigns/{campaignId}/orders/{orderId}/status").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                QString orderId = match.captured(QString("orderId").toLower());
                auto reqObj = new OAIOrdersApiRequest(socket, mOAIOrdersApiHandler);
                reqObj->updateOrderStatusRequest(campaignId, orderId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/campaigns/{campaignId}/orders/status-update").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIOrdersApiRequest(socket, mOAIOrdersApiHandler);
                reqObj->updateOrderStatusesRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/campaigns/{campaignId}/stats/orders").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIOrdersStatsApiRequest(socket, mOAIOrdersStatsApiHandler);
                reqObj->getOrdersStatsRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("DELETE").arg("/campaigns/{campaignId}/outlets/licenses").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIOutletLicensesApiRequest(socket, mOAIOutletLicensesApiHandler);
                reqObj->deleteOutletLicensesRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/campaigns/{campaignId}/outlets/licenses").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIOutletLicensesApiRequest(socket, mOAIOutletLicensesApiHandler);
                reqObj->getOutletLicensesRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/campaigns/{campaignId}/outlets/licenses").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIOutletLicensesApiRequest(socket, mOAIOutletLicensesApiHandler);
                reqObj->updateOutletLicensesRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/campaigns/{campaignId}/outlets").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIOutletsApiRequest(socket, mOAIOutletsApiHandler);
                reqObj->createOutletRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("DELETE").arg("/campaigns/{campaignId}/outlets/{outletId}").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                QString outletId = match.captured(QString("outletId").toLower());
                auto reqObj = new OAIOutletsApiRequest(socket, mOAIOutletsApiHandler);
                reqObj->deleteOutletRequest(campaignId, outletId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/campaigns/{campaignId}/outlets/{outletId}").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                QString outletId = match.captured(QString("outletId").toLower());
                auto reqObj = new OAIOutletsApiRequest(socket, mOAIOutletsApiHandler);
                reqObj->getOutletRequest(campaignId, outletId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/campaigns/{campaignId}/outlets").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIOutletsApiRequest(socket, mOAIOutletsApiHandler);
                reqObj->getOutletsRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("PUT").arg("/campaigns/{campaignId}/outlets/{outletId}").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                QString outletId = match.captured(QString("outletId").toLower());
                auto reqObj = new OAIOutletsApiRequest(socket, mOAIOutletsApiHandler);
                reqObj->updateOutletRequest(campaignId, outletId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/price-quarantine/confirm").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIPriceQuarantineApiRequest(socket, mOAIPriceQuarantineApiHandler);
                reqObj->confirmBusinessPricesRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/campaigns/{campaignId}/price-quarantine/confirm").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIPriceQuarantineApiRequest(socket, mOAIPriceQuarantineApiHandler);
                reqObj->confirmCampaignPricesRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/price-quarantine").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIPriceQuarantineApiRequest(socket, mOAIPriceQuarantineApiHandler);
                reqObj->getBusinessQuarantineOffersRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/campaigns/{campaignId}/price-quarantine").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIPriceQuarantineApiRequest(socket, mOAIPriceQuarantineApiHandler);
                reqObj->getCampaignQuarantineOffersRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/campaigns/{campaignId}/offer-prices").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIPricesApiRequest(socket, mOAIPricesApiHandler);
                reqObj->getPricesRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/campaigns/{campaignId}/offer-prices").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIPricesApiRequest(socket, mOAIPricesApiHandler);
                reqObj->getPricesByOfferIdsRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/campaigns/{campaignId}/offer-prices/suggestions").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIPricesApiRequest(socket, mOAIPricesApiHandler);
                reqObj->getSuggestedPricesRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/offer-prices/updates").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIPricesApiRequest(socket, mOAIPricesApiHandler);
                reqObj->updateBusinessPricesRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/campaigns/{campaignId}/offer-prices/updates").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIPricesApiRequest(socket, mOAIPricesApiHandler);
                reqObj->updatePricesRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/promos/offers/delete").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIPromosApiRequest(socket, mOAIPromosApiHandler);
                reqObj->deletePromoOffersRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/promos/offers").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIPromosApiRequest(socket, mOAIPromosApiHandler);
                reqObj->getPromoOffersRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/promos").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIPromosApiRequest(socket, mOAIPromosApiHandler);
                reqObj->getPromosRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/promos/offers/update").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIPromosApiRequest(socket, mOAIPromosApiHandler);
                reqObj->updatePromoOffersRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/campaigns/{campaignId}/ratings/quality/details").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIRatingsApiRequest(socket, mOAIRatingsApiHandler);
                reqObj->getQualityRatingDetailsRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/businesses/{businessId}/ratings/quality").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIRatingsApiRequest(socket, mOAIRatingsApiHandler);
                reqObj->getQualityRatingsRequest(businessId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/regions/{regionId}/children").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString regionId = match.captured(QString("regionId").toLower());
                auto reqObj = new OAIRegionsApiRequest(socket, mOAIRegionsApiHandler);
                reqObj->searchRegionChildrenRequest(regionId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/regions/{regionId}").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString regionId = match.captured(QString("regionId").toLower());
                auto reqObj = new OAIRegionsApiRequest(socket, mOAIRegionsApiHandler);
                reqObj->searchRegionsByIdRequest(regionId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/reports/info/{reportId}").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString reportId = match.captured(QString("reportId").toLower());
                auto reqObj = new OAIReportsApiRequest(socket, mOAIReportsApiHandler);
                reqObj->getReportInfoRequest(reportId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/campaigns/{campaignId}/orders/{orderId}/returns/{returnId}").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                QString orderId = match.captured(QString("orderId").toLower());
                QString returnId = match.captured(QString("returnId").toLower());
                auto reqObj = new OAIReturnsApiRequest(socket, mOAIReturnsApiHandler);
                reqObj->getReturnRequest(campaignId, orderId, returnId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/campaigns/{campaignId}/orders/{orderId}/returns/{returnId}/application").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                QString orderId = match.captured(QString("orderId").toLower());
                QString returnId = match.captured(QString("returnId").toLower());
                auto reqObj = new OAIReturnsApiRequest(socket, mOAIReturnsApiHandler);
                reqObj->getReturnApplicationRequest(campaignId, orderId, returnId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/campaigns/{campaignId}/orders/{orderId}/returns/{returnId}/decision/{itemId}/image/{imageHash}").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                QString orderId = match.captured(QString("orderId").toLower());
                QString returnId = match.captured(QString("returnId").toLower());
                QString itemId = match.captured(QString("itemId").toLower());
                QString imageHash = match.captured(QString("imageHash").toLower());
                auto reqObj = new OAIReturnsApiRequest(socket, mOAIReturnsApiHandler);
                reqObj->getReturnPhotoRequest(campaignId, orderId, returnId, itemId, imageHash);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/campaigns/{campaignId}/returns").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIReturnsApiRequest(socket, mOAIReturnsApiHandler);
                reqObj->getReturnsRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/campaigns/{campaignId}/orders/{orderId}/returns/{returnId}/decision").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                QString orderId = match.captured(QString("orderId").toLower());
                QString returnId = match.captured(QString("returnId").toLower());
                auto reqObj = new OAIReturnsApiRequest(socket, mOAIReturnsApiHandler);
                reqObj->setReturnDecisionRequest(campaignId, orderId, returnId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/campaigns/{campaignId}/orders/{orderId}/returns/{returnId}/decision/submit").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                QString orderId = match.captured(QString("orderId").toLower());
                QString returnId = match.captured(QString("returnId").toLower());
                auto reqObj = new OAIReturnsApiRequest(socket, mOAIReturnsApiHandler);
                reqObj->submitReturnDecisionRequest(campaignId, orderId, returnId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/campaigns/{campaignId}/first-mile/shipments/{shipmentId}/confirm").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                QString shipmentId = match.captured(QString("shipmentId").toLower());
                auto reqObj = new OAIShipmentsApiRequest(socket, mOAIShipmentsApiHandler);
                reqObj->confirmShipmentRequest(campaignId, shipmentId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/campaigns/{campaignId}/first-mile/shipments/{shipmentId}/act").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                QString shipmentId = match.captured(QString("shipmentId").toLower());
                auto reqObj = new OAIShipmentsApiRequest(socket, mOAIShipmentsApiHandler);
                reqObj->downloadShipmentActRequest(campaignId, shipmentId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/campaigns/{campaignId}/first-mile/shipments/{shipmentId}/discrepancy-act").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                QString shipmentId = match.captured(QString("shipmentId").toLower());
                auto reqObj = new OAIShipmentsApiRequest(socket, mOAIShipmentsApiHandler);
                reqObj->downloadShipmentDiscrepancyActRequest(campaignId, shipmentId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/campaigns/{campaignId}/first-mile/shipments/{shipmentId}/inbound-act").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                QString shipmentId = match.captured(QString("shipmentId").toLower());
                auto reqObj = new OAIShipmentsApiRequest(socket, mOAIShipmentsApiHandler);
                reqObj->downloadShipmentInboundActRequest(campaignId, shipmentId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/campaigns/{campaignId}/first-mile/shipments/{shipmentId}/pallet/labels").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                QString shipmentId = match.captured(QString("shipmentId").toLower());
                auto reqObj = new OAIShipmentsApiRequest(socket, mOAIShipmentsApiHandler);
                reqObj->downloadShipmentPalletLabelsRequest(campaignId, shipmentId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/campaigns/{campaignId}/shipments/reception-transfer-act").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIShipmentsApiRequest(socket, mOAIShipmentsApiHandler);
                reqObj->downloadShipmentReceptionTransferActRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/campaigns/{campaignId}/first-mile/shipments/{shipmentId}/transportation-waybill").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                QString shipmentId = match.captured(QString("shipmentId").toLower());
                auto reqObj = new OAIShipmentsApiRequest(socket, mOAIShipmentsApiHandler);
                reqObj->downloadShipmentTransportationWaybillRequest(campaignId, shipmentId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/campaigns/{campaignId}/first-mile/shipments/{shipmentId}").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                QString shipmentId = match.captured(QString("shipmentId").toLower());
                auto reqObj = new OAIShipmentsApiRequest(socket, mOAIShipmentsApiHandler);
                reqObj->getShipmentRequest(campaignId, shipmentId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/campaigns/{campaignId}/first-mile/shipments/{shipmentId}/orders/info").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                QString shipmentId = match.captured(QString("shipmentId").toLower());
                auto reqObj = new OAIShipmentsApiRequest(socket, mOAIShipmentsApiHandler);
                reqObj->getShipmentOrdersInfoRequest(campaignId, shipmentId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("PUT").arg("/campaigns/{campaignId}/first-mile/shipments").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIShipmentsApiRequest(socket, mOAIShipmentsApiHandler);
                reqObj->searchShipmentsRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("PUT").arg("/campaigns/{campaignId}/first-mile/shipments/{shipmentId}/pallets").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                QString shipmentId = match.captured(QString("shipmentId").toLower());
                auto reqObj = new OAIShipmentsApiRequest(socket, mOAIShipmentsApiHandler);
                reqObj->setShipmentPalletsCountRequest(campaignId, shipmentId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/campaigns/{campaignId}/first-mile/shipments/{shipmentId}/orders/transfer").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                QString shipmentId = match.captured(QString("shipmentId").toLower());
                auto reqObj = new OAIShipmentsApiRequest(socket, mOAIShipmentsApiHandler);
                reqObj->transferOrdersFromShipmentRequest(campaignId, shipmentId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/campaigns/{campaignId}/offers/stocks").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIStocksApiRequest(socket, mOAIStocksApiHandler);
                reqObj->getStocksRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("PUT").arg("/campaigns/{campaignId}/offers/stocks").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString campaignId = match.captured(QString("campaignId").toLower());
                auto reqObj = new OAIStocksApiRequest(socket, mOAIStocksApiHandler);
                reqObj->updateStocksRequest(campaignId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/businesses/{businessId}/warehouses").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString businessId = match.captured(QString("businessId").toLower());
                auto reqObj = new OAIWarehousesApiRequest(socket, mOAIWarehousesApiHandler);
                reqObj->getWarehousesRequest(businessId);
                return true;
            }
        }
    }
    return false;
}

}
