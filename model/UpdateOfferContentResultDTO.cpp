/**
 * Партнерский API Маркета
 * API Яндекс Маркета помогает продавцам автоматизировать и упростить работу с маркетплейсом.  В числе возможностей интеграции:  * управление каталогом товаров и витриной,  * обработка заказов,  * изменение настроек магазина,  * получение отчетов. 
 *
 * The version of the OpenAPI document: LATEST
 * 
 *
 * NOTE: This class is auto generated by OpenAPI-Generator 7.8.0.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */



#include "UpdateOfferContentResultDTO.h"

#include <string>
#include <vector>
#include <map>
#include <sstream>
#include <stdexcept>
#include <regex>
#include <boost/lexical_cast.hpp>
#include <boost/property_tree/ptree.hpp>
#include <boost/property_tree/json_parser.hpp>
#include "helpers.h"

using boost::property_tree::ptree;
using boost::property_tree::read_json;
using boost::property_tree::write_json;

namespace org {
namespace openapitools {
namespace server {
namespace model {

UpdateOfferContentResultDTO::UpdateOfferContentResultDTO(boost::property_tree::ptree const& pt)
{
        fromPropertyTree(pt);
}


std::string UpdateOfferContentResultDTO::toJsonString(bool prettyJson /* = false */) const
{
	std::stringstream ss;
	write_json(ss, this->toPropertyTree(), prettyJson);
    // workaround inspired by: https://stackoverflow.com/a/56395440
    std::regex reg("\\\"([0-9]+\\.{0,1}[0-9]*)\\\"");
    std::string result = std::regex_replace(ss.str(), reg, "$1");
    return result;
}

void UpdateOfferContentResultDTO::fromJsonString(std::string const& jsonString)
{
	std::stringstream ss(jsonString);
	ptree pt;
	read_json(ss,pt);
	this->fromPropertyTree(pt);
}

ptree UpdateOfferContentResultDTO::toPropertyTree() const
{
	ptree pt;
	ptree tmp_node;
	pt.put("offerId", m_OfferId);
	// generate tree for Errors
    tmp_node.clear();
	if (!m_Errors.empty()) {
        tmp_node = toPt(m_Errors);
		pt.add_child("errors", tmp_node);
		tmp_node.clear();
	}
	// generate tree for Warnings
    tmp_node.clear();
	if (!m_Warnings.empty()) {
        tmp_node = toPt(m_Warnings);
		pt.add_child("warnings", tmp_node);
		tmp_node.clear();
	}
	return pt;
}

void UpdateOfferContentResultDTO::fromPropertyTree(ptree const &pt)
{
	ptree tmp_node;
	m_OfferId = pt.get("offerId", "");
	// push all items of Errors into member
	if (pt.get_child_optional("errors")) {
        m_Errors = fromPt<std::vector<OfferContentErrorDTO>>(pt.get_child("errors"));
	}
	// push all items of Warnings into member
	if (pt.get_child_optional("warnings")) {
        m_Warnings = fromPt<std::vector<OfferContentErrorDTO>>(pt.get_child("warnings"));
	}
}

std::string UpdateOfferContentResultDTO::getOfferId() const
{
    return m_OfferId;
}

void UpdateOfferContentResultDTO::setOfferId(std::string value)
{
    m_OfferId = value;
}


std::vector<OfferContentErrorDTO> UpdateOfferContentResultDTO::getErrors() const
{
    return m_Errors;
}

void UpdateOfferContentResultDTO::setErrors(std::vector<OfferContentErrorDTO> value)
{
    m_Errors = value;
}


std::vector<OfferContentErrorDTO> UpdateOfferContentResultDTO::getWarnings() const
{
    return m_Warnings;
}

void UpdateOfferContentResultDTO::setWarnings(std::vector<OfferContentErrorDTO> value)
{
    m_Warnings = value;
}



std::vector<UpdateOfferContentResultDTO> createUpdateOfferContentResultDTOVectorFromJsonString(const std::string& json)
{
    std::stringstream sstream(json);
    boost::property_tree::ptree pt;
    boost::property_tree::json_parser::read_json(sstream,pt);

    auto vec = std::vector<UpdateOfferContentResultDTO>();
    for (const auto& child: pt) {
        vec.emplace_back(UpdateOfferContentResultDTO(child.second));
    }

    return vec;
}

}
}
}
}

