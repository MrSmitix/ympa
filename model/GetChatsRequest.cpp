/**
 * Партнерский API Маркета
 * API Яндекс Маркета помогает продавцам автоматизировать и упростить работу с маркетплейсом.  В числе возможностей интеграции:  * управление каталогом товаров и витриной,  * обработка заказов,  * изменение настроек магазина,  * получение отчетов. 
 *
 * The version of the OpenAPI document: LATEST
 * 
 *
 * NOTE: This class is auto generated by OpenAPI-Generator 7.8.0.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */



#include "GetChatsRequest.h"

#include <string>
#include <vector>
#include <map>
#include <sstream>
#include <stdexcept>
#include <regex>
#include <boost/lexical_cast.hpp>
#include <boost/property_tree/ptree.hpp>
#include <boost/property_tree/json_parser.hpp>
#include "helpers.h"

using boost::property_tree::ptree;
using boost::property_tree::read_json;
using boost::property_tree::write_json;

namespace org {
namespace openapitools {
namespace server {
namespace model {

GetChatsRequest::GetChatsRequest(boost::property_tree::ptree const& pt)
{
        fromPropertyTree(pt);
}


std::string GetChatsRequest::toJsonString(bool prettyJson /* = false */) const
{
	std::stringstream ss;
	write_json(ss, this->toPropertyTree(), prettyJson);
    // workaround inspired by: https://stackoverflow.com/a/56395440
    std::regex reg("\\\"([0-9]+\\.{0,1}[0-9]*)\\\"");
    std::string result = std::regex_replace(ss.str(), reg, "$1");
    return result;
}

void GetChatsRequest::fromJsonString(std::string const& jsonString)
{
	std::stringstream ss(jsonString);
	ptree pt;
	read_json(ss,pt);
	this->fromPropertyTree(pt);
}

ptree GetChatsRequest::toPropertyTree() const
{
	ptree pt;
	ptree tmp_node;
	// generate tree for OrderIds
    tmp_node.clear();
	if (!m_OrderIds.empty()) {
        tmp_node = toPt(m_OrderIds);
		pt.add_child("orderIds", tmp_node);
		tmp_node.clear();
	}
	// generate tree for Types
    tmp_node.clear();
	if (!m_Types.empty()) {
        tmp_node = toPt(m_Types);
		pt.add_child("types", tmp_node);
		tmp_node.clear();
	}
	// generate tree for Statuses
    tmp_node.clear();
	if (!m_Statuses.empty()) {
        tmp_node = toPt(m_Statuses);
		pt.add_child("statuses", tmp_node);
		tmp_node.clear();
	}
	return pt;
}

void GetChatsRequest::fromPropertyTree(ptree const &pt)
{
	ptree tmp_node;
	// push all items of OrderIds into member
	if (pt.get_child_optional("orderIds")) {
        m_OrderIds = fromPt<std::vector<int64_t>>(pt.get_child("orderIds"));
	}
	// push all items of Types into member
	if (pt.get_child_optional("types")) {
        m_Types = fromPt<std::vector<ChatType>>(pt.get_child("types"));
	}
	// push all items of Statuses into member
	if (pt.get_child_optional("statuses")) {
        m_Statuses = fromPt<std::vector<ChatStatusType>>(pt.get_child("statuses"));
	}
}

std::vector<int64_t> GetChatsRequest::getOrderIds() const
{
    return m_OrderIds;
}

void GetChatsRequest::setOrderIds(std::vector<int64_t> value)
{
    m_OrderIds = value;
}


std::vector<ChatType> GetChatsRequest::getTypes() const
{
    return m_Types;
}

void GetChatsRequest::setTypes(std::vector<ChatType> value)
{
    m_Types = value;
}


std::vector<ChatStatusType> GetChatsRequest::getStatuses() const
{
    return m_Statuses;
}

void GetChatsRequest::setStatuses(std::vector<ChatStatusType> value)
{
    m_Statuses = value;
}



std::vector<GetChatsRequest> createGetChatsRequestVectorFromJsonString(const std::string& json)
{
    std::stringstream sstream(json);
    boost::property_tree::ptree pt;
    boost::property_tree::json_parser::read_json(sstream,pt);

    auto vec = std::vector<GetChatsRequest>();
    for (const auto& child: pt) {
        vec.emplace_back(GetChatsRequest(child.second));
    }

    return vec;
}

}
}
}
}

