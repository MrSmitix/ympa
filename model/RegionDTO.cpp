/**
 * Партнерский API Маркета
 * API Яндекс Маркета помогает продавцам автоматизировать и упростить работу с маркетплейсом.  В числе возможностей интеграции:  * управление каталогом товаров и витриной,  * обработка заказов,  * изменение настроек магазина,  * получение отчетов. 
 *
 * The version of the OpenAPI document: LATEST
 * 
 *
 * NOTE: This class is auto generated by OpenAPI-Generator 7.8.0.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */



#include "RegionDTO.h"

#include <string>
#include <vector>
#include <sstream>
#include <stdexcept>
#include <algorithm>
#include <boost/property_tree/ptree.hpp>
#include <boost/property_tree/json_parser.hpp>

using boost::property_tree::ptree;
using boost::property_tree::read_json;
using boost::property_tree::write_json;

namespace org {
namespace openapitools {
namespace server {
namespace model {

RegionDTO::RegionDTO(boost::property_tree::ptree const& pt)
{
        fromPropertyTree(pt);
}

std::string RegionDTO::toJsonString(bool prettyJson /* = false */)
{
    return toJsonString_internal(prettyJson);
}

void RegionDTO::fromJsonString(std::string const& jsonString)
{
    fromJsonString_internal(jsonString);
}

boost::property_tree::ptree RegionDTO::toPropertyTree()
{
    return toPropertyTree_internal();
}

void RegionDTO::fromPropertyTree(boost::property_tree::ptree const& pt)
{
    fromPropertyTree_internal(pt);
}

std::string RegionDTO::toJsonString_internal(bool prettyJson)
{
	std::stringstream ss;
	write_json(ss, this->toPropertyTree(), prettyJson);
	return ss.str();
}

void RegionDTO::fromJsonString_internal(std::string const& jsonString)
{
	std::stringstream ss(jsonString);
	ptree pt;
	read_json(ss,pt);
	this->fromPropertyTree(pt);
}

ptree RegionDTO::toPropertyTree_internal()
{
	ptree pt;
	ptree tmp_node;
	pt.put("id", m_Id);
	pt.put("name", m_Name);
	if (m_Parent != nullptr) {
		pt.add_child("parent", m_Parent->toPropertyTree());
	}
	// generate tree for Children
	if (!m_Children.empty()) {
		for (const auto &childEntry : m_Children) {
            tmp_node.push_back(std::make_pair("", childEntry->toPropertyTree()));
		}
		pt.add_child("children", tmp_node);
		tmp_node.clear();
	}
	return pt;
}

void RegionDTO::fromPropertyTree_internal(ptree const &pt)
{
	ptree tmp_node;
	m_Id = pt.get("id", 0L);
	m_Name = pt.get("name", "");
	if (pt.get_child_optional("parent")) {
		m_Parent = std::make_shared<RegionDTO>();
		m_Parent->fromPropertyTree(pt.get_child("parent"));
	}
	// push all items of Children into member vector
	if (pt.get_child_optional("children")) {
		for (const auto &childTree : pt.get_child("children")) {
            std::shared_ptr<RegionDTO> val =
                std::make_shared<RegionDTO>(childTree.second);
            m_Children.emplace_back(std::move(val));
		}
	}
}

int64_t RegionDTO::getId() const
{
    return m_Id;
}

void RegionDTO::setId(int64_t value)
{
	m_Id = value;
}
std::string RegionDTO::getName() const
{
    return m_Name;
}

void RegionDTO::setName(std::string value)
{
	m_Name = value;
}
std::shared_ptr<RegionType> RegionDTO::getType() const
{
    return m_Type;
}

void RegionDTO::setType(std::shared_ptr<RegionType> value)
{
	m_Type = value;
}
std::shared_ptr<RegionDTO> RegionDTO::getParent() const
{
    return m_Parent;
}

void RegionDTO::setParent(std::shared_ptr<RegionDTO> value)
{
	m_Parent = value;
}
std::vector<std::shared_ptr<RegionDTO>> RegionDTO::getChildren() const
{
    return m_Children;
}

void RegionDTO::setChildren(std::vector<std::shared_ptr<RegionDTO>> value)
{
	m_Children = value;
}

std::vector<RegionDTO> createRegionDTOVectorFromJsonString(const std::string& json)
{
    std::stringstream sstream(json);
    boost::property_tree::ptree pt;
    boost::property_tree::json_parser::read_json(sstream,pt);

    auto vec = std::vector<RegionDTO>();
    for (const auto& child: pt) {
        vec.emplace_back(RegionDTO(child.second));
    }

    return vec;
}

}
}
}
}

