/**
 * Партнерский API Маркета
 * API Яндекс Маркета помогает продавцам автоматизировать и упростить работу с маркетплейсом.  В числе возможностей интеграции:  * управление каталогом товаров и витриной,  * обработка заказов,  * изменение настроек магазина,  * получение отчетов. 
 *
 * The version of the OpenAPI document: LATEST
 * 
 *
 * NOTE: This class is auto generated by OpenAPI-Generator 7.8.0.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */



#include "QualityRatingDTO.h"

#include <string>
#include <vector>
#include <map>
#include <sstream>
#include <stdexcept>
#include <regex>
#include <boost/lexical_cast.hpp>
#include <boost/property_tree/ptree.hpp>
#include <boost/property_tree/json_parser.hpp>
#include "helpers.h"

using boost::property_tree::ptree;
using boost::property_tree::read_json;
using boost::property_tree::write_json;

namespace org {
namespace openapitools {
namespace server {
namespace model {

QualityRatingDTO::QualityRatingDTO(boost::property_tree::ptree const& pt)
{
        fromPropertyTree(pt);
}


std::string QualityRatingDTO::toJsonString(bool prettyJson /* = false */) const
{
	std::stringstream ss;
	write_json(ss, this->toPropertyTree(), prettyJson);
    // workaround inspired by: https://stackoverflow.com/a/56395440
    std::regex reg("\\\"([0-9]+\\.{0,1}[0-9]*)\\\"");
    std::string result = std::regex_replace(ss.str(), reg, "$1");
    return result;
}

void QualityRatingDTO::fromJsonString(std::string const& jsonString)
{
	std::stringstream ss(jsonString);
	ptree pt;
	read_json(ss,pt);
	this->fromPropertyTree(pt);
}

ptree QualityRatingDTO::toPropertyTree() const
{
	ptree pt;
	ptree tmp_node;
	pt.put("rating", m_Rating);
	pt.put("calculationDate", m_CalculationDate);
	// generate tree for Components
    tmp_node.clear();
	if (!m_Components.empty()) {
        tmp_node = toPt(m_Components);
		pt.add_child("components", tmp_node);
		tmp_node.clear();
	}
	return pt;
}

void QualityRatingDTO::fromPropertyTree(ptree const &pt)
{
	ptree tmp_node;
	m_Rating = pt.get("rating", 0L);
	m_CalculationDate = pt.get("calculationDate", "");
	// push all items of Components into member
	if (pt.get_child_optional("components")) {
        m_Components = fromPt<std::vector<QualityRatingComponentDTO>>(pt.get_child("components"));
	}
}

int64_t QualityRatingDTO::getRating() const
{
    return m_Rating;
}

void QualityRatingDTO::setRating(int64_t value)
{
    m_Rating = value;
}


std::string QualityRatingDTO::getCalculationDate() const
{
    return m_CalculationDate;
}

void QualityRatingDTO::setCalculationDate(std::string value)
{
    m_CalculationDate = value;
}


std::vector<QualityRatingComponentDTO> QualityRatingDTO::getComponents() const
{
    return m_Components;
}

void QualityRatingDTO::setComponents(std::vector<QualityRatingComponentDTO> value)
{
    m_Components = value;
}



std::vector<QualityRatingDTO> createQualityRatingDTOVectorFromJsonString(const std::string& json)
{
    std::stringstream sstream(json);
    boost::property_tree::ptree pt;
    boost::property_tree::json_parser::read_json(sstream,pt);

    auto vec = std::vector<QualityRatingDTO>();
    for (const auto& child: pt) {
        vec.emplace_back(QualityRatingDTO(child.second));
    }

    return vec;
}

}
}
}
}

