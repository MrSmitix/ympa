/**
 * Партнерский API Маркета
 * API Яндекс Маркета помогает продавцам автоматизировать и упростить работу с маркетплейсом.  В числе возможностей интеграции:  * управление каталогом товаров и витриной,  * обработка заказов,  * изменение настроек магазина,  * получение отчетов. 
 *
 * The version of the OpenAPI document: LATEST
 * 
 *
 * NOTE: This class is auto generated by OpenAPI-Generator 7.8.0.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */



#include "WarehousesDTO.h"

#include <string>
#include <vector>
#include <map>
#include <sstream>
#include <stdexcept>
#include <regex>
#include <boost/lexical_cast.hpp>
#include <boost/property_tree/ptree.hpp>
#include <boost/property_tree/json_parser.hpp>
#include "helpers.h"

using boost::property_tree::ptree;
using boost::property_tree::read_json;
using boost::property_tree::write_json;

namespace org {
namespace openapitools {
namespace server {
namespace model {

WarehousesDTO::WarehousesDTO(boost::property_tree::ptree const& pt)
{
        fromPropertyTree(pt);
}


std::string WarehousesDTO::toJsonString(bool prettyJson /* = false */) const
{
	std::stringstream ss;
	write_json(ss, this->toPropertyTree(), prettyJson);
    // workaround inspired by: https://stackoverflow.com/a/56395440
    std::regex reg("\\\"([0-9]+\\.{0,1}[0-9]*)\\\"");
    std::string result = std::regex_replace(ss.str(), reg, "$1");
    return result;
}

void WarehousesDTO::fromJsonString(std::string const& jsonString)
{
	std::stringstream ss(jsonString);
	ptree pt;
	read_json(ss,pt);
	this->fromPropertyTree(pt);
}

ptree WarehousesDTO::toPropertyTree() const
{
	ptree pt;
	ptree tmp_node;
	// generate tree for Warehouses
    tmp_node.clear();
	if (!m_Warehouses.empty()) {
        tmp_node = toPt(m_Warehouses);
		pt.add_child("warehouses", tmp_node);
		tmp_node.clear();
	}
	// generate tree for WarehouseGroups
    tmp_node.clear();
	if (!m_WarehouseGroups.empty()) {
        tmp_node = toPt(m_WarehouseGroups);
		pt.add_child("warehouseGroups", tmp_node);
		tmp_node.clear();
	}
	return pt;
}

void WarehousesDTO::fromPropertyTree(ptree const &pt)
{
	ptree tmp_node;
	// push all items of Warehouses into member
	if (pt.get_child_optional("warehouses")) {
        m_Warehouses = fromPt<std::vector<WarehouseDTO>>(pt.get_child("warehouses"));
	}
	// push all items of WarehouseGroups into member
	if (pt.get_child_optional("warehouseGroups")) {
        m_WarehouseGroups = fromPt<std::vector<WarehouseGroupDTO>>(pt.get_child("warehouseGroups"));
	}
}

std::vector<WarehouseDTO> WarehousesDTO::getWarehouses() const
{
    return m_Warehouses;
}

void WarehousesDTO::setWarehouses(std::vector<WarehouseDTO> value)
{
    m_Warehouses = value;
}


std::vector<WarehouseGroupDTO> WarehousesDTO::getWarehouseGroups() const
{
    return m_WarehouseGroups;
}

void WarehousesDTO::setWarehouseGroups(std::vector<WarehouseGroupDTO> value)
{
    m_WarehouseGroups = value;
}



std::vector<WarehousesDTO> createWarehousesDTOVectorFromJsonString(const std::string& json)
{
    std::stringstream sstream(json);
    boost::property_tree::ptree pt;
    boost::property_tree::json_parser::read_json(sstream,pt);

    auto vec = std::vector<WarehousesDTO>();
    for (const auto& child: pt) {
        vec.emplace_back(WarehousesDTO(child.second));
    }

    return vec;
}

}
}
}
}

