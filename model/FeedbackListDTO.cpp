/**
 * Партнерский API Маркета
 * API Яндекс Маркета помогает продавцам автоматизировать и упростить работу с маркетплейсом.  В числе возможностей интеграции:  * управление каталогом товаров и витриной,  * обработка заказов,  * изменение настроек магазина,  * получение отчетов. 
 *
 * The version of the OpenAPI document: LATEST
 * 
 *
 * NOTE: This class is auto generated by OpenAPI-Generator 7.8.0.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */



#include "FeedbackListDTO.h"

#include <string>
#include <vector>
#include <map>
#include <sstream>
#include <stdexcept>
#include <regex>
#include <boost/lexical_cast.hpp>
#include <boost/property_tree/ptree.hpp>
#include <boost/property_tree/json_parser.hpp>
#include "helpers.h"

using boost::property_tree::ptree;
using boost::property_tree::read_json;
using boost::property_tree::write_json;

namespace org {
namespace openapitools {
namespace server {
namespace model {

FeedbackListDTO::FeedbackListDTO(boost::property_tree::ptree const& pt)
{
        fromPropertyTree(pt);
}


std::string FeedbackListDTO::toJsonString(bool prettyJson /* = false */) const
{
	std::stringstream ss;
	write_json(ss, this->toPropertyTree(), prettyJson);
    // workaround inspired by: https://stackoverflow.com/a/56395440
    std::regex reg("\\\"([0-9]+\\.{0,1}[0-9]*)\\\"");
    std::string result = std::regex_replace(ss.str(), reg, "$1");
    return result;
}

void FeedbackListDTO::fromJsonString(std::string const& jsonString)
{
	std::stringstream ss(jsonString);
	ptree pt;
	read_json(ss,pt);
	this->fromPropertyTree(pt);
}

ptree FeedbackListDTO::toPropertyTree() const
{
	ptree pt;
	ptree tmp_node;
	pt.add_child("paging", m_Paging.toPropertyTree());
	// generate tree for FeedbackList
    tmp_node.clear();
	if (!m_FeedbackList.empty()) {
        tmp_node = toPt(m_FeedbackList);
		pt.add_child("feedbackList", tmp_node);
		tmp_node.clear();
	}
	return pt;
}

void FeedbackListDTO::fromPropertyTree(ptree const &pt)
{
	ptree tmp_node;
	if (pt.get_child_optional("paging")) {
        m_Paging = fromPt<ScrollingPagerDTO>(pt.get_child("paging"));
	}
	// push all items of FeedbackList into member
	if (pt.get_child_optional("feedbackList")) {
        m_FeedbackList = fromPt<std::vector<FeedbackDTO>>(pt.get_child("feedbackList"));
	}
}

ScrollingPagerDTO FeedbackListDTO::getPaging() const
{
    return m_Paging;
}

void FeedbackListDTO::setPaging(ScrollingPagerDTO value)
{
    m_Paging = value;
}


std::vector<FeedbackDTO> FeedbackListDTO::getFeedbackList() const
{
    return m_FeedbackList;
}

void FeedbackListDTO::setFeedbackList(std::vector<FeedbackDTO> value)
{
    m_FeedbackList = value;
}



std::vector<FeedbackListDTO> createFeedbackListDTOVectorFromJsonString(const std::string& json)
{
    std::stringstream sstream(json);
    boost::property_tree::ptree pt;
    boost::property_tree::json_parser::read_json(sstream,pt);

    auto vec = std::vector<FeedbackListDTO>();
    for (const auto& child: pt) {
        vec.emplace_back(FeedbackListDTO(child.second));
    }

    return vec;
}

}
}
}
}

