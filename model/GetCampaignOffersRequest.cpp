/**
 * Партнерский API Маркета
 * API Яндекс Маркета помогает продавцам автоматизировать и упростить работу с маркетплейсом.  В числе возможностей интеграции:  * управление каталогом товаров и витриной,  * обработка заказов,  * изменение настроек магазина,  * получение отчетов. 
 *
 * The version of the OpenAPI document: LATEST
 * 
 *
 * NOTE: This class is auto generated by OpenAPI-Generator 7.8.0.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */



#include "GetCampaignOffersRequest.h"

#include <string>
#include <vector>
#include <map>
#include <sstream>
#include <stdexcept>
#include <regex>
#include <boost/lexical_cast.hpp>
#include <boost/property_tree/ptree.hpp>
#include <boost/property_tree/json_parser.hpp>
#include "helpers.h"

using boost::property_tree::ptree;
using boost::property_tree::read_json;
using boost::property_tree::write_json;

namespace org {
namespace openapitools {
namespace server {
namespace model {

GetCampaignOffersRequest::GetCampaignOffersRequest(boost::property_tree::ptree const& pt)
{
        fromPropertyTree(pt);
}


std::string GetCampaignOffersRequest::toJsonString(bool prettyJson /* = false */) const
{
	std::stringstream ss;
	write_json(ss, this->toPropertyTree(), prettyJson);
    // workaround inspired by: https://stackoverflow.com/a/56395440
    std::regex reg("\\\"([0-9]+\\.{0,1}[0-9]*)\\\"");
    std::string result = std::regex_replace(ss.str(), reg, "$1");
    return result;
}

void GetCampaignOffersRequest::fromJsonString(std::string const& jsonString)
{
	std::stringstream ss(jsonString);
	ptree pt;
	read_json(ss,pt);
	this->fromPropertyTree(pt);
}

ptree GetCampaignOffersRequest::toPropertyTree() const
{
	ptree pt;
	ptree tmp_node;
	// generate tree for OfferIds
    tmp_node.clear();
	if (!m_OfferIds.empty()) {
        tmp_node = toPt(m_OfferIds);
		pt.add_child("offerIds", tmp_node);
		tmp_node.clear();
	}
	// generate tree for Statuses
    tmp_node.clear();
	if (!m_Statuses.empty()) {
        tmp_node = toPt(m_Statuses);
		pt.add_child("statuses", tmp_node);
		tmp_node.clear();
	}
	// generate tree for CategoryIds
    tmp_node.clear();
	if (!m_CategoryIds.empty()) {
        tmp_node = toPt(m_CategoryIds);
		pt.add_child("categoryIds", tmp_node);
		tmp_node.clear();
	}
	// generate tree for VendorNames
    tmp_node.clear();
	if (!m_VendorNames.empty()) {
        tmp_node = toPt(m_VendorNames);
		pt.add_child("vendorNames", tmp_node);
		tmp_node.clear();
	}
	// generate tree for Tags
    tmp_node.clear();
	if (!m_Tags.empty()) {
        tmp_node = toPt(m_Tags);
		pt.add_child("tags", tmp_node);
		tmp_node.clear();
	}
	return pt;
}

void GetCampaignOffersRequest::fromPropertyTree(ptree const &pt)
{
	ptree tmp_node;
	// push all items of OfferIds into member
	if (pt.get_child_optional("offerIds")) {
        m_OfferIds = fromPt<std::vector<std::string>>(pt.get_child("offerIds"));
	}
	// push all items of Statuses into member
	if (pt.get_child_optional("statuses")) {
        m_Statuses = fromPt<std::vector<OfferCampaignStatusType>>(pt.get_child("statuses"));
	}
	// push all items of CategoryIds into member
	if (pt.get_child_optional("categoryIds")) {
        m_CategoryIds = fromPt<std::vector<int32_t>>(pt.get_child("categoryIds"));
	}
	// push all items of VendorNames into member
	if (pt.get_child_optional("vendorNames")) {
        m_VendorNames = fromPt<std::vector<std::string>>(pt.get_child("vendorNames"));
	}
	// push all items of Tags into member
	if (pt.get_child_optional("tags")) {
        m_Tags = fromPt<std::vector<std::string>>(pt.get_child("tags"));
	}
}

std::vector<std::string> GetCampaignOffersRequest::getOfferIds() const
{
    return m_OfferIds;
}

void GetCampaignOffersRequest::setOfferIds(std::vector<std::string> value)
{
    m_OfferIds = value;
}


std::vector<OfferCampaignStatusType> GetCampaignOffersRequest::getStatuses() const
{
    return m_Statuses;
}

void GetCampaignOffersRequest::setStatuses(std::vector<OfferCampaignStatusType> value)
{
    m_Statuses = value;
}


std::vector<int32_t> GetCampaignOffersRequest::getCategoryIds() const
{
    return m_CategoryIds;
}

void GetCampaignOffersRequest::setCategoryIds(std::vector<int32_t> value)
{
    m_CategoryIds = value;
}


std::vector<std::string> GetCampaignOffersRequest::getVendorNames() const
{
    return m_VendorNames;
}

void GetCampaignOffersRequest::setVendorNames(std::vector<std::string> value)
{
    m_VendorNames = value;
}


std::vector<std::string> GetCampaignOffersRequest::getTags() const
{
    return m_Tags;
}

void GetCampaignOffersRequest::setTags(std::vector<std::string> value)
{
    m_Tags = value;
}



std::vector<GetCampaignOffersRequest> createGetCampaignOffersRequestVectorFromJsonString(const std::string& json)
{
    std::stringstream sstream(json);
    boost::property_tree::ptree pt;
    boost::property_tree::json_parser::read_json(sstream,pt);

    auto vec = std::vector<GetCampaignOffersRequest>();
    for (const auto& child: pt) {
        vec.emplace_back(GetCampaignOffersRequest(child.second));
    }

    return vec;
}

}
}
}
}

