/**
 * Партнерский API Маркета
 * API Яндекс Маркета помогает продавцам автоматизировать и упростить работу с маркетплейсом.  В числе возможностей интеграции:  * управление каталогом товаров и витриной,  * обработка заказов,  * изменение настроек магазина,  * получение отчетов. 
 *
 * The version of the OpenAPI document: LATEST
 * 
 *
 * NOTE: This class is auto generated by OpenAPI-Generator 7.8.0.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */



#include "GetCategoriesMaxSaleQuantumDTO.h"

#include <string>
#include <vector>
#include <map>
#include <sstream>
#include <stdexcept>
#include <regex>
#include <boost/lexical_cast.hpp>
#include <boost/property_tree/ptree.hpp>
#include <boost/property_tree/json_parser.hpp>
#include "helpers.h"

using boost::property_tree::ptree;
using boost::property_tree::read_json;
using boost::property_tree::write_json;

namespace org {
namespace openapitools {
namespace server {
namespace model {

GetCategoriesMaxSaleQuantumDTO::GetCategoriesMaxSaleQuantumDTO(boost::property_tree::ptree const& pt)
{
        fromPropertyTree(pt);
}


std::string GetCategoriesMaxSaleQuantumDTO::toJsonString(bool prettyJson /* = false */) const
{
	std::stringstream ss;
	write_json(ss, this->toPropertyTree(), prettyJson);
    // workaround inspired by: https://stackoverflow.com/a/56395440
    std::regex reg("\\\"([0-9]+\\.{0,1}[0-9]*)\\\"");
    std::string result = std::regex_replace(ss.str(), reg, "$1");
    return result;
}

void GetCategoriesMaxSaleQuantumDTO::fromJsonString(std::string const& jsonString)
{
	std::stringstream ss(jsonString);
	ptree pt;
	read_json(ss,pt);
	this->fromPropertyTree(pt);
}

ptree GetCategoriesMaxSaleQuantumDTO::toPropertyTree() const
{
	ptree pt;
	ptree tmp_node;
	// generate tree for Results
    tmp_node.clear();
	if (!m_Results.empty()) {
        tmp_node = toPt(m_Results);
		pt.add_child("results", tmp_node);
		tmp_node.clear();
	}
	// generate tree for Errors
    tmp_node.clear();
	if (!m_Errors.empty()) {
        tmp_node = toPt(m_Errors);
		pt.add_child("errors", tmp_node);
		tmp_node.clear();
	}
	return pt;
}

void GetCategoriesMaxSaleQuantumDTO::fromPropertyTree(ptree const &pt)
{
	ptree tmp_node;
	// push all items of Results into member
	if (pt.get_child_optional("results")) {
        m_Results = fromPt<std::vector<MaxSaleQuantumDTO>>(pt.get_child("results"));
	}
	// push all items of Errors into member
	if (pt.get_child_optional("errors")) {
        m_Errors = fromPt<std::vector<CategoryErrorDTO>>(pt.get_child("errors"));
	}
}

std::vector<MaxSaleQuantumDTO> GetCategoriesMaxSaleQuantumDTO::getResults() const
{
    return m_Results;
}

void GetCategoriesMaxSaleQuantumDTO::setResults(std::vector<MaxSaleQuantumDTO> value)
{
    m_Results = value;
}


std::vector<CategoryErrorDTO> GetCategoriesMaxSaleQuantumDTO::getErrors() const
{
    return m_Errors;
}

void GetCategoriesMaxSaleQuantumDTO::setErrors(std::vector<CategoryErrorDTO> value)
{
    m_Errors = value;
}



std::vector<GetCategoriesMaxSaleQuantumDTO> createGetCategoriesMaxSaleQuantumDTOVectorFromJsonString(const std::string& json)
{
    std::stringstream sstream(json);
    boost::property_tree::ptree pt;
    boost::property_tree::json_parser::read_json(sstream,pt);

    auto vec = std::vector<GetCategoriesMaxSaleQuantumDTO>();
    for (const auto& child: pt) {
        vec.emplace_back(GetCategoriesMaxSaleQuantumDTO(child.second));
    }

    return vec;
}

}
}
}
}

